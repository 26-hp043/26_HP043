# AGENTS.md — 본 저장소 작업 규칙

본 파일은 AI 에이전트가 이 저장소에서 작업할 때 반드시 따라야 할 규칙을 정의한다.

| 항목 | 내용 |
|---|---|
| 문서명 | AGENTS.md |
| 버전 | v1.8 |
| 상태 | 운영 중 (전 저장소 적용) — v1.5에서 §3.2.3에 MVP 범위 판정 행 추가 (#269) |
| 최종 수정일 | 2026-08-20 |

---

## 1. 언어 정책

- **커밋 메시지**: 한국어로 작성한다. 단, 기술 용어(PR, API, SQL 등)는 영어를 그대로 사용한다.
- **문서 수정**: 본문은 한국어, 기술 용어는 영어를 병용한다.
- **코드 주석**: 한국어로 작성한다.

---

## 2. AI 보조 리뷰 교차 검증 규칙 (필수)

> **교훈**: AI 에이전트 보조 리뷰에서 규제값·외부 팩트에 대한 finding이 나오면, 반드시 권위 있는 원문으로 교차 검증한다. AI 에이전트가 두 번이나 잘못된 "정정"을 제안했다.

### 2.1 검증 대상

AI 에이전트(리뷰·분석 에이전트 등)가 다음 유형의 finding을 제공할 때, 원문 교차 검증 없이 적용하지 않는다:

- IMO 결의안 인용 값 (MEPC.xxx)
- 공식 규제 표의 수치
- 외부 라이브러리/프레임워크의 공식 스펙
- 타 문서와의 정합성 주장 (한 쪽이 오타라고 단정하는 경우)
- **검증된 값·계산에서 끌어낸 결론** — 값이 원문과 일치해도, 그 값에서 끌어낸 결론은 별개 명제다

> **마지막 항목이 따로 필요한 이유** — 앞의 네 항목은 **출처**를 검사한다. 출처가 맞아도 거기서 끌어낸 결론은 검사되지 않는데, **검증된 숫자가 바로 옆에 있으면 결론까지 검증된 것처럼 읽힌다.** #87 유형(2차 출처를 1차로 착각)은 출처 확인 단계에서 걸리지만, 이 유형은 출처를 통과한 뒤에 생긴다.
>
> 두 형태로 나타난다.
>
> - **검증된 값에 미검증 설명을 붙이는 것** — PR #145에서 `RO_RO_PASSENGER_HSC`의 등급 경계 상속 근거로 `4196 / 2023 = 2.074147`이 capacity와 무관하게 일정함을 각주에 적었다. 계산은 맞았다. 그러나 이어 붙인 *"`c`가 달랐다면 같은 벡터를 쓸 수 없다"* 는 거짓이다 — dd 벡터는 각 선종 **자신의** `required_CII`에 곱해지므로 `c`가 달라도 그대로 적용된다. `#166`에서도 같은 일이 있었다. 값 6개는 독립 재계산으로 대조했으면서, **그 값이 왜 어긋났는지에 대한 설명은 검증 없이 승인**했고 실제 원인은 달랐다.
> - **결론은 맞는데 근거가 틀린 것** — 이쪽이 더 잡기 어렵다. **결론이 맞으니 검토를 통과한다.** `#166`에서 한 선택지를 기각하며 든 근거 하나가 성립하지 않았고, 결론만 같아 그대로 넘어갈 뻔했다.
>
> **실무 지침** — 근거를 적을 때 **강도를 구분해 표기한다**(확정 근거 / `(정황)`). PR #145의 각주에서 `인지된 부재`와 `(정황)` 2건을 구분한 것이 적용례다.

### 2.2 권위 소스 우선순위

| 주제 | 권위 소스 | 링크 |
|---|---|---|
| CII Reference Line (G2) | MEPC.353(78) Table 1 (`MEPC 78/17/Add.1` Annex 15, 인쇄면 4쪽) | [PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.353%2878%29.pdf) |
| Capacity 축 (G1 분모) | MEPC.352(78) §4.2 Transport work (`MEPC 78/17/Add.1` Annex 14, 인쇄면 5쪽) | [PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.352%2878%29.pdf) |
| CII Reduction Factor (G3) | MEPC.400(83) | [PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.400%2883%29.pdf) |
| CII Rating (G4) | MEPC.354(78) | [PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.354%2878%29.pdf) |
| CII Guidelines (G1) | MEPC.352(78), as amended by MEPC.412(84) | [PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.352%2878%29.pdf) · [개정 PDF](https://wwwcdn.imo.org/localresources/en/OurWork/Environment/Documents/Annex%2014.pdf) |
| 연료 CF 값 | MEPC.364(79) §2.2.1 (G1 §4.1이 참조 지정) | [PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.364%2879%29.pdf) |
| 등급 하락의 귀결 (시정조치계획) | MARPOL Annex VI Reg 6.8·26.3.2·28.7~28.9 (MEPC.328(76)) · SEEMP Part III는 MEPC.395(82) §9.4·§15.4.1 | [MARPOL PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.328%2876%29.pdf) · [SEEMP PDF](https://wwwcdn.imo.org/localresources/en/KnowledgeCentre/IndexofIMOResolutions/MEPCDocuments/MEPC.395%2882%29.pdf) |

> **[#148] Capacity 축 · G2 Table 1 로케이터의 원문 대조 확인: sky01170851 (2026-07-30).** G1 `§4.2`가 `W_s = C × D_t`의 `C`를 **DWT 8종 / GT 4종**으로 규정하며, 이 12종 구분이 G2 Table 1의 최상위 선종 12종과 일치한다는 판정은 IMO 원문을 직접 대조해 확인한 결과다. `RO_RO_PASSENGER_HSC`가 별도 코드인 것은 G2 Table 1이 `Ro-ro passenger ship` 칸 아래 하위 2행을 두기 때문이다(#126).
>
> **[#163] G1은 `MEPC.412(84)`로 개정됐다 — 다만 `§4.2`(Transport work) 한 절만이다.** 다른 절은 원문 그대로 유효하며, 지표 명칭을 정하는 `§2.5`는 개정되지 않았다. 결의 자체를 대체한 문서는 없으므로 인용은 **`MEPC.352(78), as amended by MEPC.412(84)`** 형태로 적는다. 같은 형태를 IMO 문서가 쓴 선례가 `MEPC.364(79)`에 있다.
>
> **원문 대조 확인: 신하늘(`sky01170851`), 2026-08-06.** `§2.5`(MEPC 78/17/Add.1 Annex 14, 3쪽) · 개정 `§4.2`(같은 Annex 14, 5쪽) · `MEPC.412(84)`의 *"Section 4.2 is replaced with the following"* 문언과 DWT 8종·GT 4종 목록을 IMO 원문과 직접 대조해 확인했다.

> **MEPC.364(79)는 EEDI 계산 지침이며, CII G2 reference line과 무관하다.** G2 인용 시 MEPC.353(78)을 사용한다.
>
> **[#87 추가] 단, 연료 CF 값은 예외다.** 위 문장은 **G2 기준선 계수(`a` · `c`)** 에 대한 규칙이며 CF 값과는 별개 사안이다. G1(MEPC.352(78)) §4.1이 CF를 EEDI 계산 지침 계열에 참조 지정하므로 **CF 값의 출처는 MEPC.364(79) §2.2.1**이다. G1에는 CF 표가 없다. 두 축을 혼동하지 않는다 — **G2 기준선 = 353(78)**, **CF 값 = 364(79)**.

### 2.3 사례: LNG_CARRIER reference line `a` 값

MEPC.353(78) Table 1 기준 (권위값):

| 선종 | 조건 | capacity_rule | a_raw | c |
|---|---|---|---|---|
| LNG_CARRIER | DWT ≥ 100,000 | DWT | 9.827 | 0.000 |
| LNG_CARRIER | 65,000 ≤ DWT < 100,000 | DWT | **14479E10** | 2.673 |
| LNG_CARRIER | DWT < 65,000 | fixed 65000 | **14779E10** | 2.673 |

- `14479E10`과 `14779E10`은 **서로 다른 DWT 구간의 서로 다른 값**이다.
- AI 에이전트가 한때 `14779E10`을 `14479E10`의 오타라고 "정정"했으나, 실제로는 두 값 모두 유효한 서로 다른 구간의 값이었다.
- 이 교훈을 되새워, 수치 정정 시 반드시 원문 표의 전체 행을 확인한다.

---

## 3. 문서 우선순위

### 3.1 일반 우선순위

문서 간 충돌 시 다음 우선순위를 따른다:

```
1. IMO 원문 (MEPC 결의안)     — 규제값의 최종 권위
2. PRD.md                     — 제품 요구사항의 최종 권위
3. TECH_SPEC.md               — 기술 구현의 최종 권위
4. API_SPEC.md                — API 인터페이스의 최종 권위
5. DB_SCHEMA.md               — DB 구조의 최종 권위
6. TEST_PLAN.md               — 테스트의 최종 권위
```

상위 문서가 하위 문서와 충돌하면, 하위 문서를 상위 문서에 맞춘다. 단, 규제값은 항상 IMO 원문이 최우선이다.

### 3.2 소관 한정 정본 — `UIFLOW.md` · `DESIGN_SYSTEM.md`

두 문서는 §3.1의 번호 목록에 넣지 않는다. **문서 전체가 아니라 아래에 적힌 소관의 정본**이며, 그 소관에서는 §3.1의 목록 대신 이 절을 적용한다. **소관 밖의 사항에서는 §3.1을 그대로 따른다.** 규제값은 §3.1대로 **항상 IMO 원문이 최우선**이다.

번호를 붙이지 않는 것은 §3.1이 문서 전체에 적용되는 **선형 순위**이기 때문이다. 소관 한정 정본은 그 순위의 위나 아래가 아니라 **별도 축**에 있다.

#### 3.2.1 `UIFLOW.md` — 화면 구조

**화면 목록 · 화면 계층 · 화면 간 흐름 · 진입 조건**에 한해 `UIFLOW.md`가 `PRD.md`보다 앞선다.

소관을 이 네 항목으로 닫아 두는 것은, 그 밖의 UI 관련 요구사항까지 `UIFLOW.md`가 앞선다는 해석을 막기 위함이다. 화면에 담기는 **제품 요구사항과 문구 원문**은 `PRD.md`, 화면 **안의 시각 표현**은 `DESIGN_SYSTEM.md`가 소유한다.

#### 3.2.2 `DESIGN_SYSTEM.md` — 디자인 토큰 · 화면 표현

소관은 **디자인 토큰, 화면 내 레이아웃, 수치·추정값의 표시 형식, UI 표현 규칙, `DESIGN_SYSTEM.md` §14가 정의한 UI 접근성 규칙**이다. 접근성 전반을 소유하지 않는다 — 구현 방법은 `TECH_SPEC.md`, 검증 방법은 `TEST_PLAN.md`가 소관이다. 토큰 **값**(hex·radius·그림자 수치)의 소유는 Figma이며, `DESIGN_SYSTEM.md` §0.2의 제약을 위반할 수 없다.

#### 3.2.3 소관 경계

두 문서의 소관은 §3.1의 문서들과 접점이 있다. **충돌 시 아래 기준으로 소관을 가른다.**

| 사안 | 소관 | 가르는 기준 |
|---|---|---|
| **MVP 범위 포함 여부** | `PRD.md` §5 | `UIFLOW.md`는 **범위에 든 화면의 흐름**을 정한다. 화면이 그려져 있다는 사실이 곧 MVP 포함을 뜻하지 않는다 |
| 계산값의 정의·정밀도·판정 | IMO 원문 · `PRD.md` · `TECH_SPEC.md` | `DESIGN_SYSTEM.md`는 **그 값을 화면에 어떻게 보이는가**만 정한다. 표시 자릿수는 등급 판정·경계 비교에 쓰는 정밀도가 아니다 |
| 고지·면책의 **여부와 문구 원문** | `PRD.md` §6.3 | 고지의 **시각적 표현**(노출 위치, 점선 밑줄, `ⓘ`, 툴팁)은 `DESIGN_SYSTEM.md`. `DESIGN_SYSTEM.md` §11·§13이 이미 이 경계를 따른다 |
| 화면 목록·계층·흐름·진입 조건 | `UIFLOW.md` | 예: 사이드바에서 **메뉴의 존재·순서·그룹·이동 경로**는 `UIFLOW.md`, **폭·간격·색·활성 상태 표현**은 `DESIGN_SYSTEM.md` |
| API 필드·타입·직렬화 | `API_SPEC.md` | 표시 형식은 `DESIGN_SYSTEM.md`가 정하되 **전송 타입을 바꾸지 못한다** |

> **이 행(「MVP 범위 포함 여부」)이 따로 필요한 이유** — `§3.2.1`은 화면 목록의 정본을 `UIFLOW.md`로 정했다. 그런데 **"목록에 있다"와 "MVP에 넣는다"는 별개 명제다.** 구분이 없으면 디자인 산출물에 그려진 화면이 자동으로 범위에 들어온다.
>
> 실제로 어긋나 있던 것이 세 건이다. `UIFLOW §0`의 로그인·회원가입 화면 4개(#269), `UIFLOW §2-4` 선대 모니터링, `UIFLOW §2-5` 보고서(대관 제출용)가 각각 `PRD §2.4`·`§5.2`의 OUT OF SCOPE 항목과 정면으로 부딪힌다. **특히 "대관 제출용 보고서"는 `PRD §0.3`의 제품 기본 원칙(규제 제출용 공식 계산 시스템이 아님)과 어긋난다.**
>
> **⚠️ 위 세 예시는 이 행이 신설되던 2026-08-13 당시의 기록이다.** 이후 #343(PR #364)·#344(PR #365)의 관리 중심 전환으로 판정이 바뀌었다 — 로그인은 `#270`에서 구글 OIDC 인증만 MVP로 확정됐다가 **`#413`(2026-08-16)에서 자체 이메일·비밀번호 인증으로 전환**됐고(`PRD O-14` — 회원가입·비밀번호 재설정이 MVP에 포함된다), 2-4는 SCR-001(대시보드)로 MVP 중심 화면이 됐으며, 2-5 보고서도 MVP로 승격됐다. **「대관 제출용」은 `PRD §5.2`·`§0.3` 원칙에 따라 지금도 제외다 — 이것이 현재도 유효한 적용례다.** 규칙 자체(범위 판정의 소관은 `PRD §5`)는 변하지 않는다.
>
> `TECH_SPEC §16.2`가 이미 *"`PRD §6.1 · §6.2`와 `UIFLOW §2`는 아직 어긋나 있다"* 고 기록해 두었으나, 그 각주는 **구현 판단이 막히지 않는다**는 점만 확인했고 범위 판정은 다루지 않았다. 이 행이 그 빈자리를 채운다.

---

## 4. 문서 버전 관리

- 각 문서는 헤더에 버전, 상태, 최종 수정일을 명시한다. 정본은 본문 끝에 **변경 이력 표**를 둔다.
- AI 보조 리뷰 또는 외부 리뷰 반영 시 전용 섹션(예: §12, §14)에 리뷰 결과를 기록한다.
- `README.md`는 각 문서의 최신 버전과 상태를 항상 반영한다. **정본을 새로 추가하면 `README.md`의 문서 구조 표에 행을 함께 추가한다.** 추가를 빠뜨려도 CI가 잡아주지 않으므로 같은 PR 안에서 처리한다.

### 4.1 변경 이력 기록

- **변경 시점에 직접 기록한다.** 정본을 수정하는 PR은 그 문서의 변경 이력에 행을 추가한다. 나중에 git 로그를 훑어 일괄 복원하는 방식을 쓰지 않는다.
  > 사후 복원은 누락을 낳는다. 실제로 `2026-07-23`까지 5개 문서의 이력이 통째로 비어 있어 복원해야 했다.
- **커밋 열에는 PR 번호를 적는다** (예: `` `#142` ``).
  > 본 저장소는 squash merge를 쓴다(§7). 브랜치 커밋 해시는 머지 시 재작성되므로(`db070ea` → `3fc32d2`) 작업 중 로컬 해시를 적으면 **머지 후 존재하지 않는 값**이 된다. PR 번호는 사전에 확정되고 squash 커밋 제목에도 남아 `git log --grep`으로 추적된다.
  > `2026-07-23` 이전 행은 사후 복원분이라 해시로 남아 있다. 그 경계는 각 문서 변경 이력 서두에 명시되어 있다.

### 4.2 최종 수정일

- **본문이 마지막으로 바뀐 날**을 적는다.
- **헤더·변경 이력 표만 손댄 메타 작업은 날짜를 올리지 않는다.**
  > `be0dc23`이 변경 이력 표를 추가하면서 최종 수정일을 자기 커밋일(07-21)이 아니라 마지막 본문 변경일(07-14)로 넣은 것이 이 규칙의 선례다.

### 4.3 버전을 올리는 기준

| 올린다 | 올리지 않는다 |
|---|---|
| 리뷰 반영, 절 신설, 구조 변경 | 오기·값 정정, 소규모 행 추가, 각주 보강 |

> 정정 계열에서 버전을 올리지 않은 선례: #78 · #84 · #115 · #140.

### 4.4 「상위 문서」가 가리키는 것 [#460]

헤더의 **「상위 문서」는 이 문서가 마지막으로 대조를 마친 판본**이다. 최신판을 자동으로 따라가지 않는다.

**숫자만 올리면 하지 않은 확인을 했다고 적는 것**이 된다. 예를 들어 `TECH_SPEC`이 `PRD v4.3`이 신설한 절을 아직 반영하지 않았는데 헤더만 v4.3으로 올리면, 읽는 사람은 두 문서가 맞춰져 있다고 믿는다. 그 믿음이 틀렸다는 것은 **한참 뒤에 값이 어긋날 때** 드러난다.

| 올린다 | 올리지 않는다 |
|---|---|
| 상위 문서의 변경을 실제로 반영·대조했을 때 | 상위 문서의 버전만 올라갔을 때 |

> **최신판 강제를 택하지 않은 이유.** `#445`가 `TEST_PLAN`의 참조를 현행으로 맞췄더니 **같은 날 안에 다시 낡았다**(`TECH_SPEC` v1.6→v1.7 · `API_SPEC` v1.16→v1.18). 상위 문서 하나를 올릴 때마다 하위 5~6개를 함께 고쳐야 하는데, 그 수정은 **대조했다는 사실과 무관하게** 숫자만 맞추는 작업이 된다.
>
> `README` 문서 표는 다르다 — 그쪽은 「최신 버전을 반영한다」가 §4의 규정이고 `tests/test_doc_version_sync.py`가 CI에서 강제한다. **두 필드는 다른 것을 말한다.**

### 4.5 제품명 [#460]

정식 명칭은 **BlueLog**다 (2026-08-04 디자인 담당 확인 · 2026-08-17 확정).

- 문서 제목은 `문서명 — BlueLog` 형식으로 적는다.
- 「중소선사를 위한 CII 예측 및 운항 의사결정 보조 플랫폼」은 **부제·설명구**로 남긴다 — 제품명이 아니다.
- 표기는 **붙여 쓴다.** `Blue Log`·`bluelog`가 아니다.

### 4.6 화면 문구의 소관 — 정본 문구와 표시 문구 [#468]

화면에 나오는 한국어는 **두 종류**이고, 바꿀 수 있는 사람이 다르다.

| 구분 | 무엇인가 | 바꾸려면 |
|---|---|---|
| **정본 문구** | `PRD`·`API_SPEC`·`DESIGN_SYSTEM`이 **원문을 확정**한 것. 면책 문구(`PRD §6.3`) · 경고 배너 · 규제 용어 · warning 메시지(`API_SPEC §1.6`) | **정본 개정이 먼저**다. 화면만 고치면 문서와 어긋난다 |
| **표시 문구** | 레이블·안내문·버튼 등 나머지 전부 | 디자인 담당이 바꾼다. 문서 개정이 필요 없다 |

#### 테스트가 깨졌을 때 누가 판단하는가

**깨진 단언에 정본 인용이 붙어 있는지만 본다.** 검색 한 번으로 갈린다.

```ts
// 정본 문구 — 이 표시가 있으면 문구를 바꾸면 안 된다
// 정본 문구 (PRD §6.3) — 바꾸려면 PRD 개정이 먼저다.
expect(warningBannerText(2)).toBe('시정조치계획 대상 위험 선박 2척')
```

| 표시가 | 뜻 | 조치 |
|---|---|---|
| 있다 | 문구를 바꾸면 안 되는 것을 바꿨다 | 되돌리고, 정말 바꿔야 하면 정본 개정 이슈를 연다 |
| 없다 | **테스트가 낡았다** | 새 문구로 갱신한다. 같은 PR에 넣는다 |

**이 판단을 매번 사람에게 묻지 않는다.** 물어야 하는 상태 자체가 결함이다.

#### 표시 문구를 검사하는 방법

표시 문구는 **리터럴로 단언하지 않는다.** 문구가 아니라 **성질**을 단언한다 — 그래야 문구를
바꿔도 테스트가 지키려던 것이 그대로 남는다.

```ts
// ✗ 문구가 바뀌면 깨진다. 그런데 이 테스트가 지키려던 것은 문구가 아니었다
expect(underwayStateText(unknown)).toBe('상태 미기록')

// ✓ 원래 의도(「상태 미기록을 정박으로 적지 않는다」)를 직접 단언한다
expect(underwayStateText(unknown)).not.toBe(underwayStateText(notUnderWay))
```

약화가 아니다. **문구와 무관하게 더 강한 단언**이다.

---

## 5. 검색·분석 원칙

- "결과가 나오면 멈추지 말고 최소 2개 이상의 독립적 소스를 교차 확인한다."
- 수치 검증: 정수 연산으로 직접 계산하여 확인한다 (예: `attained_CII` — `249,120,000 ÷ 50,000,000 = 4.9824`).
  > **입력으로 반올림·절단된 값을 쓰지 않는다** (`TECH_SPEC §1.2.1`). 기존 예시 `5045066331 × 94 = 474236235114`는 **소수 9자리로 절단한 `required_CII`** 를 곱한 것이라 교체했다. 정수 연산으로 검산하면 그 자릿수 안에서는 항상 맞아떨어지므로, **절단된 입력에서 시작하면 검산이 오히려 틀린 값을 확증한다** — `#166`이 그 경로였다.
- 외부 라이브러리: 공식 문서 + 실제 OSS 구현 예를 함께 확인한다.

---

## 6. 이슈 생성 규칙

- **제목에 분류 태그를 넣지 않는다.** 우선순위(P0/P1/P2)와 영역(infra/db/calc/api/test/quality)은 GitHub 라벨로 표시한다.
  - ❌ `[P0][calc] attained_cii 계산`
  - ✅ `attained_cii 계산 (Layer 1: Decimal 정본값 30자리)`
- **이슈 하나의 크기는 하루(6~8시간) 작업량을 기준으로 한다.** 한 이슈가 여러 파일·여러 기능을 다루면 더 작게 쪼갠다.
- 이슈 본문에는 반드시 포함한다:
  - **목표**: 이 이슈가 해결하는 문제 (1~2문장)
  - **작업 체크리스트**: 구체적인 구현 항목들 (`- [ ]` 마크다운 체크박스)
  - **참조**: PRD/TECH_SPEC/API_SPEC/DB_SCHEMA/TEST_PLAN의 관련 섹션 번호
  - **완료 기준**: 어떤 상태가 되어야 이슈를 닫을 수 있는지 (테스트 이름, 동작 등)
- 수치·공식·규제값은 반드시 상위 문서(PRD/TECH_SPEC/DB_SCHEMA)에서 복사하고, 해당 섹션 번호를 참조한다. 임의로 값을 재작성하지 않는다.
- 본 문서의 §3 우선순위에 따라, 이슈 본문이 하위 문서(API_SPEC/TEST_PLAN)와 충돌하면 상위 문서(PRD/TECH_SPEC) 기준으로 맞춘다.

### 6.1 닫을 때의 유예 처리 [#571]

**이슈를 닫기 전에 본문·코멘트에 조건부 유예 항목이 남아 있는지 확인한다.**

조건부 유예란 「지금은 하지 않고, 어떤 조건이 갖춰지면 한다」로 미뤄 둔 항목이다. 대개 이런 형태로 적힌다.

> 서버에 목록 엔드포인트가 없다 — **없다면 이 이슈에서 만들지 말고 후속 이슈로 분리하고, 그때까지 고정표를 유지한다**

- **유예가 있으면 후속 이슈를 등록하고, 그 번호를 닫는 이슈에 남긴다.** 등록은 닫는 시점에 한다 — 「나중에 만들자」는 남지 않는다.
- **해제 조건이 이미 충족됐으면 그 사실을 후속 이슈에 적는다.** 충족시킨 이슈 번호를 함께 남긴다.
- 유예를 그대로 두기로 판단했다면 **왜 후속 이슈를 만들지 않는지**를 닫는 코멘트에 적는다. 조용히 닫지 않는다.

#### 왜 규칙으로 두는가

유예는 **이슈 본문 안에만** 적히고, 그 이슈가 닫히면 아무도 보지 않는다. 해제 조건은 **다른 이슈에서 충족되는데**, 충족시킨 쪽은 그런 유예가 있었다는 사실을 모른다.

결과는 「기능이 없다」가 아니라 **「있는데 반쪽만 동작한다」**로 나타나 발견이 늦다.

#### 사례 — 한 유예가 결함 두 건이 됐다 (#236)

`#236`(실API 모드에서도 선박·연도·연료 선택지가 고정표에서 온다)이 세 축 중 **선박만 처리하고 연도·연료를 유예**했다. 판단 자체는 타당했다 — 서버에 엔드포인트가 없었다.

| | 유예 해제 조건 | 충족 시점 | 후속 이슈 | 결과 |
|---|---|---|---|---|
| 연도 | 규정연도 목록 API | `#444` (08-18) | **등록 안 됨** | 다음 날 증상 쪽에서 발견 → `#534` |
| 연료 | 연료 목록 API | `#444` (08-18) | **등록 안 됨** | 뒤늦게 등재 → `#558`, 범위도 1곳으로 잘못 적힘(실제 4곳) |

`#534`는 벌크선에서는 정상이고 나머지 3척에서만 규제연도가 조회되지 않는 형태였다. **화면이 깨지지 않아** 코드만 읽어서는 드러나지 않았다.

---

## 7. PR 생성 규칙

- **PR 제목**: 한국어로 작성. 커밋 메시지와 동일한 형식.
- **커밋 메시지**: 한국어. 기술 용어(PR, API, SQL 등)는 영어 그대로 사용. (§1 언어 정책 준수)
- **이슈 연결**: PR 본문에 대상 이슈를 명시한다. **완료 범위에 따라 키워드를 나눈다** — 전부 완료면 `Closes`·`Fixes`, 일부만 완료면 `Refs`·`Part of`, **분할된 이슈의 마지막 PR**이면 `Closes`.
  > 부분 완료에 `Closes`를 달면 **미완료 상태로 이슈가 닫힌다.** 실제로 #159는 처음부터 두 PR로 나뉘도록 설계됐고(`AGENTS §3` 정리는 즉시 가능, `TECH_SPEC §16.2` 정리는 해당 절이 PR #158로 들어온 뒤 가능), PR #162가 완료 기준 4개 중 3개만 충족했다. 그때는 규칙이 없어 `Refs`를 쓰고 사유를 본문에 밝히는 형태가 됐다.
  > **분할된 이슈는 마지막 PR에서만 자동 종료한다.**
- **계층 분리 준수**: 코드 변경 PR은 서비스 레이어 규칙(TECH_SPEC §16)을 지키는지 확인한다. 상세 체크리스트는 TECH_SPEC §16.5 참조.
- **단위**: 1 PR = **하나의 주 작업 이슈**. 단, 밀접하게 관련된 이슈 2~3개는 하나의 PR에 포함할 수 있다.
  > 「주 작업 이슈」로 적는 것은 **부분 완료 PR과 충돌하지 않게** 하기 위해서다. 한 이슈를 여러 PR로 나눠도 각 PR의 작업 범위는 여전히 하나의 이슈에 속한다. **관련 이슈·선행 PR을 본문에서 참조하는 것은 이 원칙에 어긋나지 않는다.**
- **PR 본문 템플릿**: 아래 4절이 **최소 요건**이다. 작은 PR은 이것만으로 충분하고, 필요하면 선택 절을 덧붙인다.

  ```markdown
  ## Summary
  - 변경 내용 요약 (2~3 bullet)

  ## 관련 이슈
  Closes #이슈번호

  ## 변경 사항
  - 주요 변경 파일 및 내용

  ## 테스트
  - [ ] 단위 테스트 통과
  - [ ] lint 통과 (ruff check)
  ```

  **필수 4절** — `Summary` · `관련 이슈` · `변경 사항` · `테스트`
  **선택 절** — `구현 결정 (리뷰 필요)` · `검증 결과` · `#___ 완료 기준` · `후속 대응 필요` · `참조 문서`

  > 템플릿을 9절로 확대하지 않는 것은 **정본 오탈자 수정 같은 작은 PR까지 같은 구조를 강제하면 본문만 길어지기** 때문이다. 판단이 갈린 지점이 있거나 검증 수치를 남겨야 하면 선택 절을 쓴다.
- **머지 조건**: CI가 전부 통과해야 머지할 수 있다.

  **어느 잡이 실제로 머지를 막는지는 저장소 설정(브랜치 보호)이 정한다.** 아래 표가 그 설정의 사본이며, 설정을 바꾸면 이 표도 함께 고친다.

  | CI 잡 | required check | 근거 |
  |---|---|---|
  | `lint` | ✅ | `#215` |
  | `test` | ✅ | `#215` |
  | `frontend` | ✅ | `#215` |
  | `docker` | ✅ | `#402`(2026-08-20 적용) — `#393`이 잡을 신설한 뒤 required로 올라가지 못한 채 남아 있었다. 브랜치 보호는 PR로 바꿀 수 없어 저장소 관리 권한으로 별도 적용했다 |

  > ✅ **2026-08-20 기준 CI 4종이 모두 required다.** 아래 조회 명령으로 확인할 수 있다.

  > ⚠️ **잡이 돌지만 아무것도 막지 않는 상태가 가장 나쁘다.** 빨간불이 떠도 머지가 되면 다음 사람은 그 불을 무시하거나 뒤늦게 발견한다. **CI 잡을 새로 만들면 required로 올릴지 그 자리에서 정하고 이 표에 남긴다** — `#393`은 후속 대응으로 적어 두었으나 그 사실이 저장소 문서 어디에도 없어 확인할 방법이 없었다.

  설정 조회·변경은 다음과 같다.

  ```bash
  # 현재 설정 확인
  gh api repos/26-hp043/26_HP043/branches/main/protection \
    --jq '.required_status_checks | {strict, contexts}'

  # required check 변경 (저장소 관리 권한 필요)
  gh api -X PATCH repos/26-hp043/26_HP043/branches/main/protection/required_status_checks \
    -f strict=true -f 'contexts[]=lint' -f 'contexts[]=test' \
    -f 'contexts[]=frontend' -f 'contexts[]=docker'
  ```

  `strict: true`(base 최신화 요구)는 유지한다. base가 앞서면 리베이스 후 잡이 다시 도는데, `#240` 머지 때 실제로 그 상황에서 재검증이 의미가 있었다.
- **코드 PR 테스트 게이트**: `src/` 변경 PR(코드·스키마·마이그레이션)은 테스트를 포함해야 머지할 수 있다. 테스트 0건 PR은 리뷰 단계에서 보류한다 — #302(상태 머신 223줄, 테스트 0건)가 그 사례다.
- **Squash merge 원칙**: 여러 커밋이 있더라도 머지 시 1개 커밋으로 squash한다.

### 7.1 정본 변경 PR

정본을 수정하는 PR은 본문 **최상단**에 아래 인용블록을 단다.

```markdown
> ⚠️ **정본 변경 포함 — 명시적 승인 필요**
> 이 PR은 정본 문서 `___`(§___)를 수정합니다.
```

**적용 대상**

| 구분 | 문서 |
|---|---|
| 본 문서 자신 | `AGENTS.md` — §3.1 목록에는 없으나 저장소 운영 규칙의 정본이다 |
| §3.1 정본 | `PRD.md` · `TECH_SPEC.md` · `API_SPEC.md` · `DB_SCHEMA.md` · `TEST_PLAN.md` |
| §3.2 소관 한정 정본 | `UIFLOW.md` · `DESIGN_SYSTEM.md` |

§3.1의 1번인 **IMO 원문은 외부 문서**이므로 저장소 PR로 수정하는 대상이 아니다. 인용블록에는 **변경한 문서와 절**을 함께 적는다.

> 이 규칙은 PR #152 · #158 · #162가 문구·형식까지 동일하게 써 온 관례를 정본에 올린 것이다. 규칙이 없던 동안 #159 본문이 *"PR 본문에 「정본 변경 포함」을 명시한다. **AGENTS.md §7 참조**"* 라고 적었으나 §7에 그런 조항이 없었다 — 관례와 정본이 어긋난 상태였다.

### 7.2 PR 본문 승인 시점

| 대상 | 처리 |
|---|---|
| 최초 승인 | **PR 생성 전** 본문 초안을 승인받는다 |
| 승인된 본문의 **실질 내용 변경** (범위 · 결정 · 설명) | **재승인** |
| **확인된 사실 반영** (CI 결과 · 커밋 SHA · PR 번호·URL 기입 · 검증 완료 체크박스) | 승인 없이 직접 처리한다 |

> 재승인 대상은 「정본 관련 수정」보다 넓다. **승인받은 본문의 실질이 바뀌면** 문서 종류와 무관하게 재승인이다. 반대로 변경 이력 커밋 열에 PR 번호를 기입하는 것처럼(§4.1) **이미 확인된 사실을 반영하는 것**은 직접 처리한다.

### 7.3 팀원 기여 기록

- 팀원이 조사·작성한 내용을 반영할 때는 **두 곳 모두**에 남긴다.
  1. **문서 본문** — 헤더 `작성` 행 또는 해당 절의 각주에 이름을 적는다.
  2. **커밋** — `Co-Authored-By: <핸들> <ID+핸들@users.noreply.github.com>`
  > 커밋 트레일러만 달면 GitHub 기여자 집계에는 잡히지만 **문서를 읽는 사람에게는 드러나지 않는다.** 반대로 문서에만 적으면 기여자 집계에서 빠진다. 둘은 서로를 대체하지 않는다.
  > 적용례: `DESIGN_SYSTEM.md` · `UIFLOW.md` 헤더의 `작성` 행, `DB_SCHEMA.md` §3.2 각주의 원문 대조 확인자.
- **머지된 커밋은 재작성하지 않는다.** 크레딧이 빠졌더라도 `main`에 강제 푸시하지 않고 **새 커밋으로 보완**한다.
  > 이미 받아 간 사람의 로컬 `main`이 어긋난다. `0d938c6`(#87)의 크레딧 누락을 PR #142에서 새 커밋으로 처리한 것이 선례다.

---

## 변경 이력

> git 커밋 기록에서 복원했다(날짜는 커밋 기준).
>
> **2026-07-23까지가 사후 복원분이다.** 이후 항목은 변경 시점에 직접 기록하며, squash merge로 브랜치 커밋 해시가 재작성되므로 커밋 열에는 **PR 번호**를 적는다.

| 날짜 | 커밋 | 변경 요약 |
|---|---|---|
| 2026-07-04 | `0f59999` | v1.0 최초 작성 (외부 리뷰 반영 커밋에 포함) |
| 2026-07-04 | `547eeed` | Oracle → AI 에이전트 용어 일반화 |
| 2026-07-05 | `7bc248b` | v1.1: 이슈·PR 생성 규칙 추가 (§6, §7) |
| 2026-07-21 | `be0dc23` | 변경이력 표 추가 및 최종 수정일 갱신 |
| 2026-07-23 | `9febca6` | §7에 계층 분리 준수 항목 추가 (#100) |
| 2026-07-29 | `#140` | §2.2에 연료 CF 값 권위 소스 행 추가 (#87) |
| 2026-07-29 | `#141` | §3에 디자인 토큰 권위 문단 추가 |
| 2026-07-29 | `#142` | 최종 수정일 정정 (07-05 → 07-29) |
| 2026-07-29 | `#144` | v1.2: §4에 문서 이력 규칙(4.1~4.3) 신설, §7.1에 팀원 기여 기록 규칙 추가 (#143) |
| 2026-08-02 | `#162` | v1.3: §3을 3.1(일반 우선순위)·3.2(소관 한정 정본)로 분리, `UIFLOW.md` 편입 및 `DESIGN_SYSTEM.md` 소관 확장·경계 명시. #141의 디자인 토큰 문단을 §3.2.2로 흡수 (#159) |
| 2026-08-04 | `#178` | §7 머지 조건에서 job 이름 열거 제거 — `CI(lint + test)` → `CI` (#160) |
| 2026-08-05 | `#181` | §2.1에 '검증된 값·계산에서 끌어낸 결론'을 검증 대상으로 추가 — 두 형태와 근거 강도 표기 지침 포함. §5 수치 검증 예시를 절단값 기반에서 교체 (#146) |
| 2026-08-06 | `#190` | §2.2에 Capacity 축(G1 분모) 행 신설 + G2 reference line 행에 절·페이지 로케이터 보강 (#148) |
| 2026-08-06 | `#191` | v1.4: §7 이슈 연결에 `Closes`/`Refs`/`Part of` 구분 신설, 「1 PR = 하나의 주 작업 이슈」로 정확화, PR 본문 템플릿을 최소 요건 4절 + 선택 절로 규정, §7.1 정본 변경 PR·§7.2 승인 시점 신설. **종전 §7.1(팀원 기여 기록)은 §7.3으로 이동** (#165) |
| 2026-08-06 | `#163` | §2.2 CII Guidelines (G1) 행을 현행판 `MEPC.352(78), as amended by MEPC.412(84)`로 갱신하고 개정 범위·확인자 각주 추가 (#163) |
| 2026-08-07 | `#194` | §6 이슈 제목 예시의 `prec=30`을 「정본값 30자리」로 정정 — `TECH_SPEC §1.2.1`이 `prec`에 넣는 값을 작업 정밀도(50)로 규정해 예시가 정본과 반대가 됨 (#179) |
| 2026-08-13 | `#305` | §7에 「코드 PR 테스트 게이트」 추가 — `src/` 변경 PR은 테스트 포함 필수, 테스트 0건 PR은 리뷰 단계 보류 (#304) |
| 2026-08-15 | `#371` | §3.2.3 근거 예시에 방향 전환 각주 추가 — 세 예시 중 판정이 #343·#344로 뒤집힌 사실과 현재 유효 적용례(대관 제출용 계속 제외) 명시 (#367) |
| 2026-08-15 | `#386` | §2.2에 등급 하락의 귀결 권위 소스 행 추가 — MARPOL Annex VI Reg 6.8·28.7~28.9(MEPC.328(76))·MEPC.395(82) §9.4·§15.4.1 (#352) |
| 2026-08-17 | `#460` | **v1.6 — §4.4 「상위 문서」 의미 정의 · §4.5 제품명 신설.** 「상위 문서」가 최신판인지 대조 시점인지 정해져 있지 않아 **낡았는지 판단할 근거 자체가 없었다.** 「마지막으로 대조를 마친 판본」으로 정한다 — 최신판 강제는 `#445`에서 실제로 실패했다(맞춘 참조가 같은 날 다시 낡았다). `README` 문서 표와는 다른 필드임을 함께 명시했다. §4.5는 정식 명칭 **BlueLog**를 문서 규칙으로 확정한다 — 화면(`AppShell.tsx`)과 `MAIL_FROM`은 쓰고 있었는데 문서 9종에는 없었다 (#460) |
| 2026-08-20 | PR #NNN | **v1.8 — §6.1 닫을 때의 유예 처리 신설.** 조건부 유예는 이슈 본문 안에만 적히고 그 이슈가 닫히면 아무도 보지 않는데, **해제 조건은 다른 이슈에서 충족된다.** `#236`의 유예 2건이 그렇게 잊혀 `#534`(벌크선 외 3척에서 규제연도 조회 불가)와 `#558`로 이어졌고, 발견은 증상 쪽에서 뒤늦게 이뤄졌다. 닫는 시점에 후속 이슈를 등록하고 번호를 남기게 한다 — 그대로 두기로 했다면 그 사유를 코멘트에 적는다 (#571) |
| 2026-08-17 | PR #NNN | **v1.7 — §4.6 화면 문구의 소관 신설.** 디자인 담당의 작업 범위를 `frontend/src/**` 전역으로 정하면서, **문구를 바꿀 때마다 「테스트가 낡은 것인지 바꾸면 안 되는 문구인지」를 개발에게 물어야 하는** 상태가 드러났다. 정본이 원문을 확정한 문구와 화면 표시 문구를 나누고, **깨진 단언에 정본 인용이 붙어 있는지**로 판단이 갈리게 했다 — 검색 한 번이면 된다. 표시 문구는 리터럴이 아니라 성질로 단언한다는 규칙도 함께 넣었다 (#468) |
