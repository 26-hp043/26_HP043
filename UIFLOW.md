# UIFLOW — 화면 구조 및 흐름

| 항목 | 내용 |
|---|---|
| 버전 | v3.0 |
| 상태 | **3계층 재작성.** `PRD §6.2` 확정에 따른 전면 개편(`AGENTS §3.2.3`). 신규 화면 2종(SCR-012·013) 반영 |
| 최종 수정일 | 2026-08-16 |
| 소유 범위 | 화면 목록 · 계층 · 라우트 · 흐름 · 진입 조건 · 화면 상태 (`AGENTS §3.2.1`) |
| 상위 문서 | `PRD.md` |

---

## 0. 이 문서를 읽는 법

### 0.1 소유 범위

본 문서가 **소유**하는 것과 **참조만** 하는 것을 구분한다. 참조 항목을 본 문서에서 재정의하면 드리프트가 발생한다.

| 항목 | 소유 | 비고 |
|---|---|---|
| 화면 목록 · SCR ID | **UIFLOW** | `PRD §6.2`는 기능 요구사항의 화면 정의 목록이며 구조의 정본이 아니다 |
| 계층 · 흐름 · 진입 조건 | **UIFLOW** | |
| 라우트 · 쿼리 파라미터 | **UIFLOW** | |
| 화면 상태 매트릭스 | **UIFLOW** | |
| 기능 요구사항 · 계산식 | PRD | 본 문서는 참조만 |
| UX 문구 | PRD `§6.3` | **본 문서에서 문구를 새로 쓰지 않는다** |
| 색 · 타이포 · 컴포넌트 | DESIGN_SYSTEM | |
| API 스펙 | API_SPEC | |

### 0.2 요구사항 강도

`PRD §0.1`을 따른다 — MUST · SHOULD · MAY · OUT OF SCOPE.

### 0.3 마커

| 마커 | 의미 |
|---|---|
| 🔒 | 변경 시 본 문서 개정 필요 |
| ⚠️ | 미확정. `§9`에 등재 |
| 🆕 | v3.0 신설 |

---

## 1. 진입과 계층

### 1.1 계층 구조 🔒

`PRD §6.1`의 3계층을 따른다.

```text
[선대]  대시보드 · 감축 계획 · 데이터 점검
   │
   └─[선박]  선박 상세 · 연간 시뮬레이터
        │
        └─[항차]  실시간 CII · 항차 CII 추정 · 시나리오 비교

(계층 밖)  인증 · 선박 관리 · 리포트 · 파라미터 · 데이터 입출력 · 설정
```

**계층은 데이터 범위를 뜻한다.** 선대는 보유 선박 전체, 선박은 1척, 항차는 1항차다. 화면이 어느 계층에 속하는지는 「무엇을 집계하는가」로 판정한다.

### 1.2 로그인 직후 진입 🔒

**기본 진입 경로는 대시보드다** (`PRD §6.1`).

| 역할 | 랜딩 | 근거 |
|---|---|---|
| 선주 · 경영진 · 운항관리자 | `SCR-001` 대시보드 | 함대 전체 조망이 과업 |
| 선장 · 항해사 | `SCR-009` 실시간 CII (배정 선박) | 자기 선박이 과업 |

- 선장에게 배정 선박이 **여러 척**이면 대시보드로 보낸다
- 선장에게 배정 선박이 **없으면** 대시보드로 보내고 안내를 표시한다
- 역할 분기는 **랜딩만 다르게 하는 것이며 접근 제한이 아니다.** 권한 가드는 `§3.5`

### 1.3 선박 0척 유도 🔒

`PRD §6.1` 각주가 참조하는 조항이다.

등록된 선박이 없으면 대시보드는 **KPI·지도·목록을 모두 숨기고** 빈 상태만 노출한다. 빈 껍데기를 보여주지 않는다.

| 조건 | 처리 |
|---|---|
| 계정 생성 직후 | 온보딩 문구 + `선박 등록`(주) + `샘플 선박으로 둘러보기`(보조) ⚠️ |
| 이전에 선박이 있었음 | 온보딩 문구 없이 `선박 등록`만 |
| 1척 이상 등록됨 | 즉시 `SCR-001` 기본 상태로 전환. 별도 확인 단계 없음 |

---

## 2. 화면 목록 🔒

**SCR ID는 재번호하지 않는다** (`PRD §6.2`). 신설 화면은 `SCR-012` 이후를 쓴다.

| SCR ID | 계층 | 화면명 | 라우트 | 강도 |
|---|---|---|---|---|
| SCR-011 | 계층 밖 | Authentication | `/login` 외 | MUST |
| **SCR-001** | **선대** | Fleet Dashboard | `/dashboard` | MUST |
| **SCR-012** 🆕 | **선대** | Fleet Reduction Planning | `/fleet/planning` | SHOULD |
| **SCR-013** 🆕 | **선대** | Fleet Data Health | `/fleet/data-health` | MUST |
| **SCR-008** | **선박** | Vessel Detail | `/vessels/{vessel_id}` | MUST |
| SCR-005 | 선박 | Annual CII Simulator | `/vessels/{vessel_id}/annual` | MUST |
| **SCR-009** | **항차** | Realtime CII | `/vessels/{vessel_id}/voyages/{voyage_id}` | MUST |
| SCR-003 | 항차 | Voyage CII Estimator | `/vessels/{vessel_id}/voyages/new` | MUST |
| SCR-004 | 항차 | Scenario Comparison | `…/voyages/{voyage_id}/scenarios` | MUST |
| **SCR-010** | 산출물 | Reports | `/reports` | MUST |
| SCR-002 | 계층 밖 | Vessel Management | `/vessels` | MUST |
| SCR-006 | 계층 밖 | Parameter Management | `/parameters` | SHOULD |
| SCR-007 | 계층 밖 | Data Import/Export | `/data` | SHOULD |
| SCR-014 🆕 | 계층 밖 | Settings | `/settings` | SHOULD ⚠️ |

### 2.1 신규 화면 2종의 성격 🆕

**두 화면은 신규 기능이 아니라 기존 기능의 함대 단위 집계 뷰다.**

| SCR | 집계 대상 | 근거 |
|---|---|---|
| SCR-012 감축 계획 | 기능③의 개선 시나리오(`PRD §12.7`)를 보유 선박 전체에 동시 적용 | `PRD §12.3` · `§11.4.1` |
| SCR-013 데이터 점검 | `PRD §17.3` 데이터 부족 경고와 `§8.4` 전이 실패를 함대 단위로 집계 | `PRD §8.3` · `§8.4` · `§17.3` |

새 계산 로직도 새 데이터 모델도 필요하지 않다. 상세 명세는 별도 문서를 따른다.

### 2.2 사이드바 순서 🔒

`DESIGN_SYSTEM §7.2`가 참조하는 순서다. 미구현 항목은 숨기지 않고 **비활성 상태로 노출**한다.

| 순서 | 항목 | 대상 |
|---|---|---|
| 1 | 대시보드 | SCR-001 |
| 2 | 선박 | SCR-002 |
| 3 | 감축 계획 | SCR-012 |
| 4 | 데이터 점검 | SCR-013 |
| 5 | 리포트 | SCR-010 |
| 6 | 파라미터 | SCR-006 |
| 7 | 설정 | SCR-014 |

---

## 3. 라우트 규약 🔒

### 3.1 선박 식별자

라우트는 **`vessel_id`(UUID)** 를 쓴다. `PRD §12.2`·`§14`의 API 경로와 일치시켜 매핑 계층을 만들지 않기 위함이다.

**IMO 번호는 대체 진입 경로로 허용한다.**

```text
/vessels/imo/{imo}  →  302  /vessels/{vessel_id}
```

외부 시스템(AIS·DCS 보고)이 IMO를 조회 키로 쓰므로, 그쪽에서 오는 딥링크를 받기 위한 경로다. **정규 URL은 항상 UUID이며 리다이렉트 후 주소창에 UUID가 남는다.**

> IMO를 정규 식별자로 전환할지는 미확정이다 (`§9-1`).

### 3.2 탭과 경로

탭은 **path segment**로 표현한다. 쿼리(`?tab=`)를 쓰지 않는다 — 탭마다 내용이 완전히 다르고, 뒤로가기가 탭 이동으로 동작해야 한다.

```text
/vessels/{vessel_id}              선박 상세 — 기본
/vessels/{vessel_id}/history      연도별 CII 이력
/vessels/{vessel_id}/annual       연간 시뮬레이터 (SCR-005)
```

### 3.3 쿼리 파라미터

판단 기준은 **「링크를 복사해 동료에게 보냈을 때 상대가 같은 화면을 봐야 하는가」**다.

| 대상 | 쿼리 | 비고 |
|---|---|---|
| 대시보드 필터 (등급·상태) | `?rating=` `?status=` | 공유 가치 있음. 뒤로가기 복원의 실질적 수단 |
| 대시보드 정렬 | `?sort=` | 기본 `risk` |
| 지도 선택 선박 | `?selected={vessel_id}` | 사이드 패널 상태 복원 |
| 이력 연도 범위 | `?from=` `?to=` | |
| 데이터 점검 필터 | `?vessel_id=` `?severity=` | |
| 알림 유입 표시 | `?from=alert&at={ISO8601}` | `§8.2` |
| 지도 줌·중심 좌표 | **쓰지 않음** | URL 노이즈. 선박 선택으로 충분히 복원됨 |
| 감축 계획 조정안 | **쓰지 않음** | 선박 N척 × 감속률이라 담기 어렵고 읽을 수 없음. `§3.4` |

```text
/dashboard?rating=d,e&status=sailing&sort=risk&selected={vessel_id}
```

**필터를 컴포넌트 state로만 들고 있으면 뒤로가기 복원 로직을 직접 짜야 하고, 그 로직은 반드시 어딘가에서 어긋난다.**

### 3.4 감축 계획의 저장 모델

```text
/fleet/planning              미저장 draft (세션 보관)
/fleet/planning/{plan_id}    저장본
```

담당자가 만든 안을 경영진에 보고해야 하므로 휘발되면 안 된다. 저장 시 `plan_id`를 부여하며, **새로고침으로 draft가 사라지는 문제도 함께 해결된다.**

### 3.5 권한 가드 🔒

| 환경 | 정책 |
|---|---|
| 데모 | 막지 않는다. 역할 토글이 시연 도구다 |
| 실사용 | 선장·항해사는 **배정된 선박만.** `/vessels/{vessel_id}` 진입 시 소속 검증 |

**이 차이를 명시하지 않으면 데모 코드가 그대로 넘어가면서 권한 검증이 영영 들어가지 않는다.** 실사용 전환 시 반드시 닫아야 하는 항목이다.

### 3.6 인증 리다이렉트 🔒

알림 링크는 **미로그인 상태에서 클릭된다.**

```text
/login?redirect=%2Fvessels%2F{vessel_id}
```

로그인 후 원래 목적지로 복귀시킨다. 빠지면 사용자가 대시보드에서 선박을 다시 찾아 들어가야 하고 알림 클릭률이 죽는다.

`redirect` 값은 **자기 오리진 내부 경로만 허용**한다. 외부 URL은 무시하고 대시보드로 보낸다.

---

## 4. 화면 상세

각 화면은 **라우트 · 진입 · 목적 · 구성 · 상태 분기** 순으로 기술한다. 문구는 `PRD §6.3`을 참조하며 여기서 새로 쓰지 않는다.

---

### 4.1 SCR-011 · Authentication

```text
/login                로그인
/signup               회원가입
/password/reset       재설정 요청
/password/reset/{token}  재설정 입력
/verify-email/{token} 이메일 인증
```

| 상태 | 조건 | 처리 |
|---|---|---|
| 로그인 실패 | 인증 실패 | `PRD §6.3` 문구. **계정 존재 여부를 노출하지 않는다** |
| 이메일 중복 | 회원가입 | 알린다. 비대칭은 의도된 것 (`PRD §6.3` 각주) |
| 이메일 미인증 | 로그인 성공 + 미인증 | 진입은 허용하고 상단 배너 표시 |
| 토큰 만료·사용됨 | 재설정·인증 | `PRD §6.3` 문구 + 재요청 링크 |
| 재설정 요청 | 가입 여부 무관 | **같은 문구·같은 소요시간**으로 응답 |

---

### 4.2 SCR-001 · Fleet Dashboard 〔선대〕

```text
/dashboard
```

**진입** — 로그인 성공(운항관리자) · GNB · 일일 요약 알림 · 하위 화면 뒤로가기

**목적** — 보유 선박 전체 현황 조망과 위험 선박 경고. 서비스의 중심 화면.

**구성** (`DESIGN_SYSTEM §7.1` 12컬럼 · 7:5)

| 영역 | 컬럼 | 내용 |
|---|---|---|
| 경고 배너 | 12 | **조건부.** 위험 선박 존재 시에만 |
| KPI | 12 (3×4) | 운항/정박 · 등급 분포 · 시정조치 대상 · 연말 하락 예상 |
| 지도 | 7 | AIS 위치 · 등급 색 마커 · 항적/계획항로 |
| 선박 목록 | 5 | 카드형. 위험도순 기본 정렬 |
| 조치 큐 | 12 | 개별 조치 항목 |

**지도 ↔ 목록은 양방향 하이라이트**한다. 같은 데이터의 두 가지 뷰다.

**상태 분기**

| 상태 | 조건 | 처리 |
|---|---|---|
| 빈 상태 | 선박 0척 | `§1.3` |
| 기본 | 위험 선박 0척 | 배너를 **DOM에서 제거.** 숨김이 아니다 |
| 경고 | `PRD §3.3.7` 트리거 충족 | 배너 표시. 문구는 `PRD §6.3` |
| 지도 로딩 | AIS 응답 대기 | 지도만 스켈레톤. 목록은 먼저 표시 |
| AIS 지연 | 임계 초과 ⚠️ | 신선도 배지 Warning. 마커 투명도 저하 |
| AIS 두절 | 임계 초과 ⚠️ | 신선도 배지 Danger. 마커 점선 테두리 |
| 필터 적용 | 쿼리 존재 | 필터 칩 활성. 지도·목록 동시 축소 |

> **위험 선박 판정은 `PRD §3.3.7`이 소유한다** — YTD 등급 E, 또는 직전 2년 연속 D + 올해 YTD D. 본 문서는 표시 조건만 정의한다.

> **정보 중복 방지 🔒** — 배너·KPI·조치 큐가 같은 정보를 세 번 보여주지 않도록 역할을 나눈다. **배너는 요약과 필터 액션**, **KPI는 집계 수치**, **조치 큐는 선박별 개별 항목**이다.

**신선도 표기 🔒** — 위치(분 단위)와 CII(항차·일 단위)는 **갱신 주기가 다르다.** 두 신선도를 분리 표기하지 않으면 사용자가 CII도 실시간인 줄 착각한다.

---

### 4.3 SCR-012 · Fleet Reduction Planning 〔선대〕 🆕

```text
/fleet/planning
/fleet/planning/{plan_id}
```

**진입** — GNB · SCR-001 경고 배너 액션 · SCR-005 「함대 단위로 보기」

**목적** — 기능③의 개선 시나리오를 함대 전체에 동시 적용하고, 목표 달성 여부와 손익을 함께 본다.

**구성** (7:5)

| 영역 | 컬럼 | 내용 |
|---|---|---|
| 헤더 | 12 | 제목 · 기준연도 · 초기화 · 저장 · **결정론 안내** |
| 감속 조정 | 7 | 선박별 행 — 등급 전이 · 슬라이더 · 속력 · 추가일 · 연료 |
| 요약 | 5 | 목표 선택 → 달성 여부 → 비용 |
| 등급 분포 | 12 | 현재 / 적용 2행 비교 |

**결정론 전용 🔒**

본 화면은 `PRD §12.3` 결정론 계산만 사용하며 **`§12.4` Monte Carlo를 호출하지 않는다.**

| 근거 | 내용 |
|---|---|
| 성능 | 기본 `simulation_runs = 5000`. 함대 10척이면 5만 회. 슬라이더 조작마다 실행 불가 |
| DoS 가드 | `PRD §12.8` — 잔여 항차 200개 초과 시 계산 거부 |
| 목적 | 분포가 아니라 배분 방향·크기 비교가 필요 |

따라서 `DESIGN_SYSTEM §10` 불확실성 표현(팬 차트·확률 스택·확률 플래그)을 **적용하지 않으며**, 위험도는 `§2.5` 결정론 계열을 쓴다.

**SCR-005와 값이 달라 보일 수 있으므로 안내를 상시 노출한다** ⚠️ — 문구는 `PRD §6.3`에 신설이 필요하다 (`§9-4`).

**상태 분기**

| 상태 | 조건 | 처리 |
|---|---|---|
| 초기 | 조정 없음 | 요약에 안내. 분포 2행 동일 |
| 목표 미달 | 조정했으나 미충족 | Warning + **부족분을 숫자로** |
| 목표 달성 | 충족 | Success. 저장 활성 |
| 조정 불가 | 잔여 항차 없음 · 연료 모델 없음 · 속도 하한 ⚠️ | 슬라이더 비활성 + 사유 표기 |
| 단가 미입력 | 용선료 단가 없음 | 손익란 `—` + 설정 링크 |
| 미저장 이탈 | 조정 후 라우트 이동 | 확인 다이얼로그 |

**조정 없는 선박도 목록에 남긴다.** 빼면 사용자가 전체를 검토했는지 확신하지 못한다.

**슬라이더 채움은 시맨틱 Primary 단색**이다 (`DESIGN_SYSTEM §8`). 채움 길이가 이미 조정량을 뜻하므로 색에 등급까지 실으면 한 요소가 두 의미를 진다.

---

### 4.4 SCR-013 · Fleet Data Health 〔선대〕 🆕

```text
/fleet/data-health?vessel_id=&severity=
```

**진입** — GNB · SCR-001 조치 큐 · SCR-008 신뢰도 배지 · 알림 딥링크

**목적** — CII 계산에 **실측이 아닌 값이 쓰인 항차**를 함대 단위로 드러낸다.

> `PRD §12.3`은 결측 항차를 제외하지 않고 **`§8.3` 우선순위에 따라 계획값으로 대체**한다. 따라서 본 화면은 「빠진 항차 목록」이 아니라 **「실측이 아닌 값으로 계산된 항차 목록」**이다. 화면에서 사라진 것보다 실측인 척하는 추정값이 더 위험하다.

**구성** — 12컬럼 전폭. **7:5를 쓰지 않는다.** 목록이 전부라 좌우 분할은 한 행 정보만 줄인다.

| 영역 | 내용 |
|---|---|
| 헤더 | 제목 · 마지막 검사 시각 · 재검사 |
| 요약 4칸 | 대체 계산 · 계산 불가 · 이상치 · **데이터 완결성** |
| 이슈 목록 | 심각도 그룹. 선박 · 항차 · 문제 · **CII 영향** · 이동 |

**심각도**

| 심각도 | 의미 | 근거 |
|---|---|---|
| 대체 계산 | 실적 대신 계획값으로 계산됨 | `PRD §8.3` |
| 계산 불가 | 대체할 계획값조차 없음 | `PRD §17.3` |
| 이상치 | 계산됐으나 신뢰도 낮음 | 신설 ⚠️ |
| 실적 미입력 | `COMPLETED`에서 `CONFIRMED`로 미전이 | `PRD §8.4` |

> 심각도 색은 `DESIGN_SYSTEM §2.3` 시맨틱만 차용한다. **등급 램프에서 가져오지 않는다** (`§2.5` 동일 취지).

**그룹은 접히지 않는다.** 아코디언은 반드시 봐야 할 항목을 닫힌 채 지나치게 한다.

**`CII 영향` 열이 핵심이다.** "연료 실적 미입력"만으로는 심각성을 알 수 없고, "계획값으로 대체되어 계산됨"까지 있어야 조치 여부가 결정된다.

**상태 분기**

| 상태 | 조건 | 처리 |
|---|---|---|
| 정상 | 이슈 0건 | 「전체 항차 실측 기준」 + 마지막 검사 시각 |
| 이슈 있음 | 1건 이상 | 심각도순. 대체 계산이 최상단 |
| 필터 진입 | `?vessel_id=` | 해당 선박만 + 해제 링크 |
| 재검사 중 | 실행 중 | 진행 표시. **이전 결과 유지** |

**읽기 전용이다.** 수정은 `PRD §17.2` 실적 보정 절차를 따른다. 자동 보정·보간은 `PRD §17.1`(계획값과 실측값을 절대 덮어쓰지 않는다) 위반이다.

---

### 4.5 SCR-008 · Vessel Detail 〔선박〕

```text
/vessels/{vessel_id}
/vessels/{vessel_id}/history
```

**진입** — SCR-001 카드·마커 클릭 · 알림 딥링크 · 직접 URL · SCR-002

**목적** — 연도별 CII 이력, 올해 누적(YTD) 등급, 현재 위치·운항 상태.

**신뢰도 배지 🆕 🔒** — SCR-013과 **함께 들어가야 의미가 있다.** 이 배지가 없으면 사용자는 등급이 대체값 기반인지 모른 채 본다.

| 데이터 완결성 | 표시 |
|---|---|
| 100% | 미표시 |
| 90 ~ 99.9% | 「일부 계획값으로 계산됨」 + SCR-013 링크 |
| < 90% | 실측 데이터 부족 경고 + SCR-013 링크 |

**상태 분기**

| 상태 | 조건 | 처리 |
|---|---|---|
| 기본 | 항차 이력 있음 | 이력 차트 + YTD + 현재 상태 |
| 산출 불가 | 항차 0건 또는 필수값 결측 | 등급 `—`(`DESIGN_SYSTEM §2.4.3`) + 필요 데이터 안내 + `항차 등록` CTA |
| 운항 중 | 진행 항차 있음 | SCR-009 진입 링크 강조 |
| 정박 중 | 진행 항차 없음 + 정박 | 정박 정보 표시. SCR-009는 정박 상태로 진입 |

> **빈 상태 처리가 `§1.3`과 다르다.** 여기는 **골격을 유지한 채 값만 `—`** 로 둔다. 선박은 존재하므로 화면 구조를 없애면 안 된다. 의도적 차이다.

---

### 4.6 SCR-005 · Annual CII Simulator 〔선박〕

```text
/vessels/{vessel_id}/annual
```

**진입** — SCR-008 · GNB · 리포트 생성 흐름

**목적** — 연말 예상 등급, 목표 달성 확률, 개선 시나리오, 민감도 (`PRD §12`).

**SCR-012와의 관계 🔒** — 본 화면은 **선박 1척**, 확률 시뮬레이션 포함. SCR-012는 **함대 전체**, 결정론 전용. 「함대 단위로 보기」로 SCR-012에 진입한다.

**상태 분기**

| 상태 | 조건 | 처리 |
|---|---|---|
| 계산 중 | Monte Carlo 실행 | 진행 표시. 영역 자리 유지 |
| 누적 실적 없음 | — | `PRD §12.8` 문구. 잔여 계획 기반만 수행 |
| 잔여 항차 없음 | — | 확정 실적만으로 연말 등급 산출 |
| 목표 E | 입력 | 입력 거부. `PRD §12.8` |
| 목표 D | 입력 | `TARGET_RATING_D` warning 후 진행 |
| 항차 과다 | 200개 초과 | 계산 거부 (`PRD §12.8`) |

---

### 4.7 SCR-009 · Realtime CII 〔항차〕

```text
/vessels/{vessel_id}/voyages/{voyage_id}
```

**진입** — 선장 로그인 랜딩 · SCR-008 · SCR-001 마커 · 알림 딥링크

**목적** — 항해 중 누적값, 연말 예상 등급, **정박 반영**.

**운항 상태별 구성 🔒** — 같은 화면이되 **주인공이 바뀐다.**

| | 운항 중 | 정박 중 |
|---|---|---|
| 결론 카드 주인공 | 현재 → 연말 예측 등급 | **하락까지 남은 일수** |
| 우측 패널 | 항로 · 진척률 · ETA | 정박지 · 경과 · 다음 항차 |
| 지표 타일 | 대지 속력 · 연료 소모율 | 보조기관 연료 · 일일 악화율 |
| 차트 | 실적선 + 예측 점선 | **정박 구간 음영 + 하락 지점 마커** |
| 주 액션 | 시나리오 비교 (SCR-004) | 다음 항차 배정 |

> **정박 중 CII 악화 🔒** — 정박 중에는 항해거리가 늘지 않고 보조기관 연료만 누적되어 CII가 계속 악화된다. 등급이 확정값이 아니라 **매일 나빠지는 값**이므로 결론 카드의 주인공이 등급에서 카운트다운으로 이동한다. 정박 중에는 속력 조정이 불가능하므로 유일한 개선 수단은 출항이다.

**상태 분기**

| 상태 | 조건 | 처리 |
|---|---|---|
| 운항 중 | `IN_PROGRESS` + 항행 | 기본 |
| 정박 중 | `IN_PROGRESS` + 정박 | 위 표 |
| 산출 불가 | 필수값 결측 | 등급 `—`. 위치·속력만 |
| 데이터 대체 | 계획값으로 계산됨 | 신뢰도 배지 + SCR-013 링크 |
| AIS 두절 | 임계 초과 ⚠️ | 마지막 수신 시각 명시. 위치를 추정으로 표기 |

---

### 4.8 SCR-003 · Voyage CII Estimator 〔항차〕

```text
/vessels/{vessel_id}/voyages/new
```

운항 전 항차 CII 추정 (`PRD §10`). 저장 시 `Voyage: PLANNED`가 생성되며, 이 계획 항차가 **SCR-005·SCR-012의 잔여 항차 입력**이 된다 (`PRD §8.3`).

---

### 4.9 SCR-004 · Scenario Comparison 〔항차〕

```text
/vessels/{vessel_id}/voyages/{voyage_id}/scenarios
```

직항·우회·감속 비교 (`PRD §11`). 계열 색은 한색 계열로 한정한다 (`DESIGN_SYSTEM §9.3`).

**SCR-012는 이 화면의 연료 모델(`PRD §11.4.1` cubic speed model)을 그대로 호출한다.** 두 벌로 구현하면 같은 선박에 대해 화면마다 다른 연료값이 나온다.

---

### 4.10 SCR-010 · Reports 〔산출물〕

```text
/reports
/reports/schedule    규제 일정 ⚠️
```

항차 완료 리포트 · 연간 실적 리포트. 생성 진입점은 **각 화면 컨텍스트에 분산**한다 — 항차 완료는 SCR-009, 연간 실적은 SCR-001·SCR-005, 감축 계획 첨부는 SCR-012.

---

### 4.11 SCR-002 · Vessel Management 〔계층 밖〕

```text
/vessels
/vessels/new
```

**IMO 번호는 7자리 + 체크섬을 여기서 검증한다.** 라우트 대체 진입 경로(`§3.1`)로 쓰이므로 입력 단계에서 막아야 한다.

| 상태 | 조건 | 처리 |
|---|---|---|
| 초기 | 진입 | 필수값 미입력 → 저장 비활성 |
| IMO 오류 | 체크섬 불일치 | 인라인 에러 |
| 중복 | 동일 IMO 존재 | 안내 + 해당 선박 이동 링크 |
| AIS 미매칭 | IMO로 조회 실패 | 등록은 허용. 「위치 정보 없음」 경고만 |
| 저장 완료 | — | SCR-008로 이동 (`운항 대기 + 산출 불가` 상태) |

---

## 5. 선박 상태 매트릭스 🔒

### 5.1 생명주기 상태

**AIS 두절과 데이터 품질은 상태가 아니라 플래그다.** 신호가 끊겼다고 선박이 멈춘 것이 아니라 우리가 모르는 것뿐이므로, 운항/정박과 같은 층에 두지 않는다.

```text
운항 대기 ──항차 시작──▶ 운항 중 ──입항──▶ 정박 중
    ▲                        ◀──출항──
    └──────────항차 완료──────────┘
```

| 상태 | 조건 | 대응 항차 상태 |
|---|---|---|
| 운항 대기 | 진행 항차 없음 | — |
| 운항 중 | 진행 항차 + 항행 | `IN_PROGRESS` |
| 정박 중 | 진행 항차 + 정박, 또는 배선 대기 | `IN_PROGRESS` / — |

### 5.2 겹치는 플래그

| 플래그 | 값 | 판정 |
|---|---|---|
| AIS 신선도 | 정상 / 지연 / 두절 | 마지막 수신 경과 ⚠️ |
| 데이터 품질 | 실측 / 대체 / 계산 불가 | `PRD §8.3` 우선순위 |

두 플래그는 3개 상태 모두에 **독립적으로** 붙는다.

### 5.3 커버리지

| 운항 상태 | 데이터 | 빈도 | 화면 | 정의 |
|---|---|---|---|---|
| 운항 중 | 실측 | 높음 | SCR-009 기본 | ✅ |
| 정박 중 | 실측 | 높음 | SCR-009 정박 | ✅ |
| 운항 대기 | 계산 불가 | 신규 선박 필수 | SCR-008 빈 상태 | ✅ |
| 운항 중 | 대체 | **높음** | SCR-009 + 신뢰도 배지 | ✅ 🆕 |
| 정박 중 | 대체 | 높음 | 위 둘 조합 | ✅ 🆕 |
| 운항 대기 | 실측 | 중간 | SCR-008 (항해 패널 비표시) | ⚠️ |
| 운항/정박 | 계산 불가 | 낮음 | 등급 `—` | ⚠️ |
| AIS 두절 | 전 상태 | 중간 | 마커·배지 처리 | ⚠️ |

> **`운항 대기 + 실측`은 항차를 완료하고 다음 배정을 기다리는 정상 상태다.** 데모 시연에 「항차 완료」가 포함되면 즉시 마주친다 (`§9-6`).

---

## 6. 화면 전이

### 6.1 주요 경로

```text
로그인 ─┬─(운항관리자)─▶ SCR-001 대시보드
        └─(선장·1척)───▶ SCR-009 실시간 CII

SCR-001 ─선박 선택─▶ SCR-008 선박 상세 ─항차 선택─▶ SCR-009
   │                      │
   │                      ├─▶ SCR-008/history  이력
   │                      └─▶ SCR-005 연간 시뮬레이터 ─함대 단위─┐
   │                                                              │
   ├─경고 배너 액션──────────────────────────▶ SCR-012 감축 계획 ◀┘
   ├─조치 큐───────────────────────────────▶ SCR-013 데이터 점검
   └─연간 실적 리포트──────────────────────▶ SCR-010

SCR-009 ─시나리오 비교─▶ SCR-004 ─채택─▶ SCR-009
SCR-009 ─항차 완료 리포트─▶ SCR-010
SCR-013 ─이슈 항목──────▶ SCR-008/history (해당 항차) ⚠️
```

### 6.2 뒤로가기

| 출발 | 뒤로가기 도착 |
|---|---|
| SCR-008 | SCR-001 (**필터 유지**, `§3.3`) |
| SCR-009 | SCR-008 |
| SCR-004 | SCR-009 |
| SCR-012 (미저장) | 확인 다이얼로그 후 이전 화면 |

---

## 7. 알림 → 화면 매핑 🔒

원칙은 하나다 — **개별 선박 사건이면 선박·항차 계층, 함대 집계면 대시보드 + 필터.**

| 알림 | 도착 |
|---|---|
| 시정조치계획 대상 진입 | `/vessels/{vessel_id}` |
| 정박 지속으로 등급 하락 임박 | `/vessels/{vessel_id}/voyages/{voyage_id}` |
| 위험 선박 일일 요약 | `/dashboard?rating=d,e` |
| 실적 미입력 · 데이터 문제 | `/fleet/data-health?vessel_id={vessel_id}` |
| 규제 제출 마감 | `/reports/schedule` ⚠️ |

### 7.1 만료된 딥링크

알림 발생 시점과 클릭 시점 사이에 상태가 바뀌었을 수 있다. 그대로 두면 사용자는 알림이 잘못됐다고 판단한다.

```text
/vessels/{vessel_id}?from=alert&at=2026-03-10T00:00:00Z
```

도착 화면 상단에 **얇은 컨텍스트 바**를 표시한다. 문구는 `PRD §6.3` 신설이 필요하다 (`§9-4`).

---

## 8. 데이터 입력 경로 🔒

**항차·연료 데이터는 사용자 입력으로 들어온다.** 별도 입력 화면을 신설하지 않는다.

| 단계 | 화면 | 산출 |
|---|---|---|
| 계획 | SCR-003 | `Voyage: PLANNED` |
| 실적 | SCR-009 | `actual_*` 입력 → `COMPLETED` |
| 확정 | SCR-009 | `PRD §17.2` 보정 절차 → `CONFIRMED` |
| 일괄 | SCR-007 | CSV ⚠️ |

**입력 검증은 `PRD §8.4` 상태 전이 조건이 이미 강제한다.** UIFLOW가 별도 검증을 정의하지 않는다.

> **데모 범위 표기 🔒** — 데모에서 어느 값이 실연동이고 어느 값이 시드인지 각 화면 스펙에 명시한다. 명시하지 않으면 데모 이후 인수인계에서 전부 다시 파악해야 한다.

---

## 9. 미확정 목록

| # | 항목 | 결정권 | 비고 |
|---|---|---|---|
| 1 | 선박 정규 식별자를 IMO로 전환할지 | 개발 + PO | 현재 UUID 정규 + IMO 리다이렉트 (`§3.1`) |
| 2 | AIS 지연·두절 임계값 | 개발 | 실제 API 갱신 주기 확인 후 |
| 3 | 이상치 판정 임계값 | 도메인 | SCR-013. `PRD §17` 소관 여부도 함께 |
| 4 | 신설 UX 문구 3종 | PO | ⓐ SCR-012 결정론 안내 ⓑ 만료 딥링크 컨텍스트 ⓒ 신뢰도 배지. **문구는 `PRD §6.3`이 소유하므로 PRD 개정 필요** |
| 5 | SCR-014 설정 화면 범위 | PO | `PRD §20 O-13` 어드민 범위와 연동 |
| 6 | 데모 시연에 「항차 완료」 포함 여부 | PO | 포함 시 `운항 대기 + 실측` 상태가 P1으로 상승 (`§5.3`) |
| 7 | SCR-013 이슈 항목의 도착지 | 기획 | 항차 상세 화면 신설 vs 이력 탭 앵커 |
| 8 | 경고 배너 닫기 허용 여부 | PO | 규제 리스크라 닫기를 막는 선택지도 있음 |
| 9 | 샘플 선박으로 둘러보기 | PO | `§1.3`. 심사 시연이 쉬워짐 |
| 10 | `/reports/schedule` 규제 일정 | PO | DCS·MRV 제출 마감 관리. 구현 비용 낮고 겸직 담당자 체감 가치 높음 |
| 11 | SCR-012 속도 하한 | 도메인 | `PRD §11.2`의 `−1kn` 하한을 함대 화면에 그대로 쓸지 |
| 12 | 「실적 미입력」을 경고로 띄울지 | PO | `COMPLETED` 체류는 정상 업무 흐름일 수 있다. 경과 일수 기준 필요 |

---

## 변경 이력

| 날짜 | PR | 내용 |
|---|---|---|
| 2026-08-16 | `#462` | **v3.0 — 3계층 전면 재작성** (`PRD §6.2` 확정, `AGENTS §3.2.3`). ① 평면 메뉴(`§2` 2-1~2-6)를 선대·선박·항차 3계층으로 재편 ② 「선대 모니터링」을 SCR-001로 승격 (`PRD §6.2 [COR-9]`) ③ 「실시간 CII 산출」·「연도별 운항 기록」을 독립 항목에서 계층 내 화면으로 흡수 ④ **SCR-012 감축 계획 · SCR-013 데이터 점검 신설** — 기존 기능의 함대 단위 집계 뷰로 정의 ⑤ SCR-014 설정 신설 ⑥ 라우트 규약(`§3`) 신설 — 식별자·쿼리·권한 가드·인증 리다이렉트 ⑦ 선박 상태 매트릭스(`§5`) 신설 — AIS·데이터 품질을 상태가 아닌 플래그로 정의 ⑧ 알림 매핑(`§7`)·데이터 입력 경로(`§8`) 신설 ⑨ 경고 배너 문구를 `PRD §6.3` 정본으로 교체 (종전 「운항 제한 위험」은 원문 근거 없음으로 폐기) |
