# UIFLOW — BlueLog

| 항목 | 내용 |
|---|---|
| 문서명 | UIFLOW.md |
| 버전 | v2.15 |
| 작성 | **rlatnals4114** (디자인) |
| 상태 | 초안 — 화면 흐름·진입 조건 정의 / **3계층 재편 (제16차 회의)** / **자체 ID/PW 인증 전환 (#413)** / **역할 3종 — 관리자 반영 (#1301)** / **규제 기준값 절 — `2-6` 설정 범위 확정 (`#1239` · `#1516`)** |
| 최종 수정일 | 2026-09-21 |
| 상위 문서 | `PRD.md` v4.4 — `AGENTS §4.4` 「마지막으로 대조를 마친 판본」 |
| 후속 문서 | `DESIGN_SYSTEM.md`, 프론트엔드 구현 (#133 · #136) |
| 문서 목적 | 화면 단위의 흐름과 진입 조건을 정의한다. 화면의 시각 표현은 `DESIGN_SYSTEM.md`가 소유한다 |

본 문서는 서비스의 화면 흐름, 주요 기능 및 진입 조건을 정의합니다.

---

## 🔐 0. 인증 및 초기 진입 단계

**제품이 이메일과 비밀번호를 직접 관리한다**(`PRD §20 O-14` · `COR-10`). 외부 인증 위임은 없다.

### 0.1 화면별 상세 명세

| 화면 ID | 화면명 | 주요 기능 및 구성 요소 | 이동 조건 및 후속 흐름 |
| :--- | :--- | :--- | :--- |
| **0** | **로그인 화면** | • 서비스 소개 및 면책 문구<br>• 이메일 · 비밀번호 입력<br>• `로그인` 버튼<br>• `회원가입` · `비밀번호 찾기` 링크<br>• `둘러보기` 버튼 — **링크에 코드가 실려 있거나**(`/login?tour=<코드>`) **배포가 공개 둘러보기를 켰을 때 보인다**(인터뷰·시연용 관리자 **열람** 세션 · `#1486`). 둘 다 아니면 버튼 자체가 없다 | • 로그인 성공 ➔ `1-1`<br>• 실패 ➔ 같은 화면에 오류 표시(`PRD §6.3` 문구)<br>• 회원가입 ➔ `0-1`<br>• 비밀번호 찾기 ➔ `0-3`<br>• 둘러보기 성공 ➔ `1-1`(관리자 **열람** 세션 — 쓰기는 막힌다 · `API_SPEC §1.2` 「둘러보기」)<br>• 둘러보기 거절 ➔ 같은 화면에 오류 표시(`PRD §6.3` 「둘러보기 — 거절」 — 코드 꺼짐·불일치를 가르지 않는다) |
| **0-1** | **회원가입** | • 이메일 · 비밀번호 · 비밀번호 확인<br>• 비밀번호 규칙 안내(입력 중 표시)<br>• 면책 문구 | • 가입 성공 ➔ `0-4`(인증 메일 안내)<br>• 이메일 중복 ➔ 같은 화면에 오류 표시 |
| **0-2** | **로그인 실패** | • 실패 사유 안내 (`PRD §6.3` 문구 사용)<br>• 재시도 버튼 | • 재시도 클릭 ➔ `0` |
| **0-3** | **비밀번호 찾기** | • ⑴ 요청: 이메일 입력 → 재설정 메일 발송<br>• ⑵ 재설정: 메일 링크로 진입, 새 비밀번호 입력 | • 요청 완료 ➔ 안내 문구(가입 여부와 무관하게 동일)<br>• 재설정 완료 ➔ `0` |
| **0-4** | **이메일 인증** | • ⑴ 안내: 발송 완료 + 재발송 버튼<br>• ⑵ 결과: 메일 링크로 진입한 검증 결과 | • 인증 성공 ➔ `1-1`<br>• 토큰 만료·사용됨 ➔ 재발송 안내 |

### 0.2 진입 조건

- 유효한 세션이 없는 상태에서 어떤 화면에 접근하든 `0. 로그인 화면`으로 이동한다.
- 세션이 만료되면 진행 중이던 화면에서 `0`으로 이동하며, **복귀 경로를 보존한다** — 원래 경로를 `/login?next=…` 쿼리로 넘기고, 로그인 뒤 같은 사이트 안의 경로인지 확인해 돌아온다(`frontend/src/auth/session.ts` `redirectToLogin` · `features/auth/authRules.ts` `safeNext`).
- `0-1` · `0-3` · `0-4`는 **비로그인 상태에서 접근 가능**하다(공개 경로).

### 0.3 이메일 미인증 상태

**인증 전에도 로그인과 화면 이용을 허용한다**(`PRD §7.10`). 대신 셸 상단에 미인증 배너와 재발송 버튼을 노출한다.

> 인증을 강제하면 메일이 도착하지 않을 때 **사용자가 아무것도 하지 못하는 상태**가 된다. 시연 중 메일 지연으로 진행이 막히는 것도 실질적 위험이다.

> **[v2.1] `0-1 회원가입` · `0-3 비밀번호 찾기`를 되살렸다.** 두 화면은 v1.0에서 *"구글 OIDC 도입으로 제품이 비밀번호를 보관하지 않게 되어"* 삭제됐으나, **2026-08-16 결정으로 그 전제가 사라졌다**(`PRD O-14`). 종전 `0-1 로그인 실패`는 **`0-2`로 되돌린다** — v1.0에서 회원가입이 빠지며 당겨졌던 번호다. `0-4 이메일 인증`은 신설이다.

---

## 🗂️ 1. 온보딩 및 대시보드 진입

### 1.1 화면별 상세 명세

*   **1-1. 대시보드 (선박 미등록 상태)**
    *   **조건:** 로그인 성공 후, 등록된 선박 데이터가 없는 최초 진입 상태
    *   **화면 처리:** 서비스 이용을 유도하기 위해 `1-2. 선박 등록` 안내 팝업 혹은 화면으로 자동 이동
*   **1-2. 선박 등록**
    *   **기능:** 사용자의 선박 기본 정보 입력 및 시스템 등록
    *   **샘플 선박 선택:** 샘플 선박을 고르면 선종·제원을 채운다. IMO 번호·선명은 사용자가 입력한다 (`PRD §5.1` · `API_SPEC §2.15` · #982)
    *   **후속 흐름:** 등록 완료 시 `1-3. 대시보드(등록 완료)` 상태로 전환
*   **1-3. 대시보드 — 선대 계층 (정보 등록 완료 상태)**
    *   **기능:** **보유 선박 전체의 현황을 한 화면에서 조망하는 관제 화면.** 서비스의 기본 진입 경로이자 중심 화면이다
    *   **상세 명세:** 2-4 참조 — 종전 「선대 모니터링」이 이 화면으로 통합됐다

> **[v2.0] 「메인보드」를 「대시보드」로 통일했다.** 종전에는 `1-3 메인보드`와 `2-4 선대 모니터링`이 별도 화면이었으나, 관리 중심 전환으로 **둘이 같은 화면**이 됐다(`PRD §6.2 SCR-001`). 화면 ID는 참조 보존을 위해 그대로 둔다.

---

## 📊 2. 계층별 상세 화면

### 2.1 계층 구조도

> 💡 **내비게이션 구조 — v2.0에서 평면 나열을 3계층으로 교체했다.**
> `1-3. 대시보드`가 기본 진입 경로이며, 상위 계층에서 하위 계층으로 **드릴다운**한다.
> 좌측 사이드바는 계층 이동의 보조 수단이다.

```text
[선대]  2-4  대시보드 ────────────── 내 배 전체 · 위험 선박 경고
        2-10 함대 감축 계획 ───────── 함대 단위 감속 배분 · 손익 비교
        2-11 데이터 점검 ─────────── 실측 아닌 값으로 계산된 항차 목록
   │
   └─[선박]  2-8 선박 상세 ────────── 연도별 CII 이력 · 올해 누적(YTD) · 현재 위치·상태
        │        └ 2-3 연간 등급 관리
        │
        └─[항차]  2-9 실시간 CII ──── 항해 중 누적값 · 연말 예상 등급 · 정박 반영
                     ├ 2-1 CII 예측 (항해 전 계획 단계)
                     └ 2-2 항로 비교 (사후 설명·보고 근거)

[산출물]  2-5 보고서 ──────────────── 항차 완료 리포트 · 연간 실적 리포트
[계층 밖] 2-6 설정 · 2-7 AI 어시스턴트
```

### 2.2 화면 ↔ 계층 매핑

| 화면 | 계층 | MVP | `PRD §6.2` 대응 | 사무직 | 현장직 | 관리자 |
|---|---|---|---|---|---|---|
| **2-4 대시보드** | 선대 | ✅ **중심 화면** | SCR-001 | 읽기 | 읽기 | 읽기 |
| **2-8 선박 상세** | 선박 | ✅ 신설 | SCR-008 | 읽기 · **제원 수정** | 읽기 · 위치 갱신 | 읽기 · 제원 수정 |
| 2-3 연간 등급 관리 | 선박 | ✅ | SCR-005 | 읽기 · **실행** | 읽기 | 읽기 · 실행 |
| **2-9 실시간 CII** | 항차 | ✅ 신설 | SCR-009 | 읽기 | 읽기 — **현장직의 주 화면** | 읽기 |
| 2-1 CII 예측 | 항차 | ✅ | SCR-003 | 읽기 · 실행 | 읽기 · 실행 | 읽기 · 실행 |
| 2-2 항로 비교 | 항차 | ✅ | SCR-004 | 읽기 · 실행 · **채택** | 읽기 · 실행 | 읽기 · 실행 · 채택 |
| **2-5 보고서** | 산출물 | ✅ **승격** | SCR-010 | 읽기 · **생성** | — (사이드바에 비활성으로 남는다) | 읽기 · 생성 |
| 2-6 설정 | 계층 밖 | ✅ 계정 관리 (`#506`) · **계정 목록 · 역할 지정** (`#1301` — **관리자 전용**) · **규제 기준값 절**(`#1239` · `#1516`·`#1517`) | SCR-006 | 본인만 · **규제 기준값 조회·개정 적재** | 본인만 · **규제 기준값 조회** | 본인 + **타인 역할 변경** · **규제 기준값 조회·개정 적재**(사무직 상위집합) |
| 2-7 AI 어시스턴트 | 계층 밖 | MAY (실험) | — | 사용 | 사용 | 사용 |
| **2-10 함대 감축 계획** | 선대 | ✅ **신설** (`PRD §5.1` · `#513` · 2026-09-13) | SCR-013 | 읽기 · **실행 · 저장** | — (사이드바에 비활성으로 남는다) | 읽기 · 실행 · 저장 |
| **2-11 데이터 점검** | 선대 | ✅ **신설** (`PRD §5.1` · `#513` · 2026-09-13) | SCR-012 | 읽기 | 읽기 | 읽기 |
| SCR-002 선박 관리 · 1-2 선박 등록 | 계층 밖 | ✅ | SCR-002 | 등록 · 수정 · 삭제 | 읽기 | 등록 · 수정 · 삭제 |

> **[#672 · #1301] 「사무직」·「현장직」·「관리자」 세 열은 역할별 접근이다** (`PRD §20 O-14` · 2026-09-19). **현장직은 「넣고 본다」, 사무직은 「정하고 낸다」, 관리자는 사무직의 업무 전부에 계정 관리(계정 목록·역할 지정)를 더한다** — 항차 실적·정박 구간·위치는 현장직의 주 업무라 세 역할 모두 쓴다. 서버가 막는 경로는 `API_SPEC §1.2` 「사무직 전용 경로」·「관리자 전용 경로」 두 표가 정하고, 화면은 그 표를 따라 **현장직에게 사무직 전용 화면을 「사무직 전용」 뱃지의 비활성 항목으로 보이고, 전용 조작은 비활성으로 둔다** — 항목을 숨기면 사용자가 제품 범위를 좁게 인식한다(`DESIGN_SYSTEM §7.2`가 미구현 항목에 대해 정한 것과 같은 판단). **2-6 설정의 「계정 목록·역할 지정」은 `#1301`로 사무직에서 관리자 전용으로 옮겨갔다** — 사무직 열은 이제 본인만이고, 관리자 열만 타인 역할 변경을 가진다. **2-6의 「규제 기준값」 절은 이 갈래의 예외다** — 값은 IMO 공개 규제 상수라(`API_SPEC §1.2` 「조회는 두 역할 모두」) 계정 관리와 달리 **조회는 세 역할 모두**이고, **개정 적재·개정 이력만 사무직 이상**으로 막힌다(`#1239` 결정 D).

> **화면 번호를 재번호하지 않는다.** `frontend/src/screens.ts`의 `uiflowRef`와 `PRD §6.2` 각주가 이 번호를 참조하고 있어, 바꾸면 함께 깨진다. 신설 화면은 `2-8` 이후를 쓴다.

#### 2.2.1 사이드바 순서

`DESIGN_SYSTEM §7.2`가 사이드바 항목과 순서를 본 절에 위임한다. **위 표는 계층 매핑이지 사이드바 순서가 아니므로** 여기서 따로 정한다.

| # | 화면 | 계층 | 근거 |
|---|---|---|---|
| 1 | **2-4 대시보드** | 선대 | 기본 진입 경로. 선대 전체를 먼저 본다 |
| 2 | SCR-002 선박 관리 | 선박 | **제원이 없으면 뒤의 모든 계산이 실행되지 않는다** |
| 3 | 2-1 CII 예측 | 항차 | 항해 전 조건을 넣어 본다 |
| 4 | 2-2 항로 비교 | 항차 | 그 조건의 대안을 견준다 |
| 5 | 2-3 연간 등급 관리 | 선박 | 항차들이 쌓인 결과를 연 단위로 본다 |
| 6 | **2-10 함대 감축 계획** | 선대 | 연 단위 결과를 **선대 단위 조치**로 옮긴다 (`#513` · 개발 임시 배치) |
| 7 | **2-11 데이터 점검** | 선대 | 내보내기 전에 그 결과가 **무엇으로 계산됐는지** 본다 (`#513` · 개발 임시 배치) |
| 8 | **2-5 보고서** | 산출물 | 위 결과를 내보낸다 |
| 9 | 2-6 설정 | 계층 밖 | 상시 사용이 아니므로 끝에 둔다 |

**계층 순서가 아니라 작업 흐름 순서다.** 종전에는 위 표의 행 순서(선대 → 선박 → 항차 → 산출물)를 그대로 썼는데, **계층은 데이터의 구조이지 사용자가 밟는 순서가 아니다.** 실제 경로는 「배를 등록·관리한다 → 항차를 예측한다 → 항로를 견준다 → 연간 등급을 본다 → 보고서를 낸다」이고, 메뉴가 그 길과 어긋나면 매번 위아래로 오간다.

**선박 관리를 둘째로 올린 것이 이 개정의 핵심이다.** 제원(GT·DWT·기준 일일 연료소모량)이 비어 있으면 CII 계산도 항로 비교도 실행되지 않는다 — 그 화면이 사실상 시작점인데 종전에는 목록 여섯째였다.

> **사이드바에 두지 않는 화면.** `1-2 선박 등록`은 온보딩 흐름(`1-1 → 1-2 → 1-3`)이며 기능 메뉴가 아니다. `2-8 선박 상세`·`2-9 실시간 CII`는 상위 계층에서 드릴다운으로만 진입한다. `2-7`은 실험 기능이다(`2-10`·`2-11`은 2026-09-13 범위 안으로 옮겨 6·7번에 두었다).

**계층 매핑은 바뀌지 않았다.** 어느 화면이 어느 계층인지는 위 표 그대로이며, 이 절은 **표시 순서만** 정한다.

---

### 2-4. 대시보드 — 선대 관제 【선대 계층 · 중심 화면】

> **[v2.0] OUT OF SCOPE 각주를 삭제했다.** 종전 각주는 `PRD §5.2`·`§2.4`의 「선대 통합 모니터링」 제외 항목을 근거로 삼았으나, **그 항목이 `PRD v4.0`에서 삭제**됐다(제16차 회의 결정). 이 화면은 이제 서비스의 중심이다.
> 종전 `1-3 메인보드`와 통합된다 — 진입 조건은 1-3, 화면 명세는 본 절이다.

*   **핵심 기능:** 선주가 보유한 선박 **전체**의 현황을 한 화면에서 조망하고, 위험 선박을 즉시 식별한다
*   **주요 UI:**
    *   **상단 경고 배너** — 위험 선박 척수(`PRD §6.3` 원문 한 줄). **등급 하락까지 남은 일수는 배너가 아니라 선대 요약 행의 「D등급 진입 임박」 칸**이다 (#1569) — 위험 선박은 이미 D · E라 「D등급까지」가 없으므로 배너 부제로 두면 늘 배너가 말하는 배가 아닌 배를 가리켰고, 위험 0척이라 배너가 없는 날에는 함께 사라졌다. 칸은 서버가 고른 한 척(`API_SPEC §2.8` `soonest_d_entry`)의 일수와 선박 상세 링크, 후보가 없으면 「해당 선박 없음」
    *   **이미 D 이하인 선박 카드에는 「D등급까지」 칸을 두지 않는다** (#1569) — 배 마크가 등급을 이미 말한다. 규제 플래그(`DESIGN_SYSTEM §8`)는 별개 축이라 남는다
    *   **적용 기준 판본 한 줄** — 예: 「기준 — MEPC.400(83) · 2026 감축률 11%」. `2-6` 규제 기준값 절로 가는 링크를 겸한다(`#1239` · `#1516`)
    *   **선박 카드 그리드** — 선박별 현 등급(연간 누적 기준) · 운항 상태 · 현재 위치
        *   **기준값 없는 선박**(`NO_PARAMETERS` — 「해당 선종·연도의 규정 기준값이 없습니다. 운영자에게 문의해 주세요.」)의 안내도 `2-6` 규제 기준값 절로 링크한다 — 그 「운영자」가 갈 화면이 이 절이다
        *   **정렬(위험도·등급·이름)은 서버가 하고 목록을 페이지로 나눠 준다** (`API_SPEC §2.8` · #772). 배너·KPI·조치는 선대 전체 기준이다. 받은 목록 뒤에 서버가 더 가지고 있으면 「다음 선박 불러오기」로 이어 받는다
    *   카드 선택 시 `2-8 선박 상세`로 드릴다운
    *   **선박 위치 지도** (`#763` · 2026-09-12 신설) — 확대·축소로 위치를 확인한다. 진행 중 항차는 **대권선**(최단 경로)으로 잇는다
        *   타일은 **우리 오리진의 파일 하나**다(PMTiles). 키도 런타임 외부 요청도 없고 오프라인에서 그대로 뜬다 — `PRD §5.2` 개정 근거가 이것이다
        *   **자산이 없으면 개략도로 떨어진다.** 위치 관계와 등급은 그대로 읽힌다
        *   ⚠️ **확대 상한은 `MAX_ZOOM`**이다(`frontend/src/features/fleet/basemap.ts`가 정본 · 현재 `6`, `#985`). 지금 위치는 사람이 찍은 한 점이라, 더 확대하면 **없는 정확도를 있는 것처럼** 보인다(`PRD §21` `[#763]` 각주)
        *   마커는 개략도와 **같은 배 모양·등급색**을 쓰고, 등급 문자를 함께 적는다(`DESIGN_SYSTEM §14` 색 단독 금지)
*   **선박 상태 표기 — `detail_status` 7값**

    | 값 | 표기 | 계산상 구분 |
    |---|---|---|
    | `SAILING` | 항해 중 | **UNDER_WAY** |
    | `IN_PORT` | 접안 | NOT_UNDER_WAY |
    | `AT_ANCHOR` | 묘박 | NOT_UNDER_WAY |
    | `DRIFTING` | 표류 | NOT_UNDER_WAY |
    | `STS` | 선박 간 이적 | NOT_UNDER_WAY |
    | `CANAL_TRANSIT` | 운하 통과 | NOT_UNDER_WAY |
    | `DRYDOCK` | 드라이독 | NOT_UNDER_WAY |

    > 계산은 `underway_state`(2값)만 보고, 화면은 `detail_status`(7값)를 표시한다. 두 축을 나눈 이유는 `#346` 참조.

*   **⚠️ 경고 배너 문구는 `#352`에서 확정한다.** 종전 명세의 「운항 제한 위험」 표현이 규제 사실과 어긋날 가능성이 있어, 원문 대조 전까지 문구를 확정하지 않는다. **본 화면 구현(`#351`)은 `#352` 이후다.**

### 2-10. 함대 감축 계획 【선대 계층】

> ## ✅ **범위 안으로 옮겼다** (`PRD §5.1` · `#513` · 2026-09-13)
>
> 막던 둘이 모두 섰다 — 감축 산식(`PRD §12.3.1` · `#433`)과 **비용 단가 입력 경로**(`PRD §12.3.2` — 단가는 **계획의 가정값**으로 화면에서 입력하고 계획과 함께 저장한다 · 2026-09-13 사용자 결정). 서버는 `API_SPEC §2.17`이다.
>
> ⚠️ 화면의 배치·치수·문구는 **개발 임시안**이다 — 슬라이더 규격(`DESIGN_SYSTEM §8` 확정분)과 등급 전이(`§8.3`)만 확정이다. 목표 판정 한 줄은 등급 칸 아래에 두었다(7:5에서 표가 넘치지 않게).

> **신규 기능이 아니라 `2-3 연간 등급 관리`의 함대 단위 뷰다.** `PRD §12.6` 민감도 분석이 이미 「잔여 항차 평균 속도 −1kn」을, `§12.7` 출력이 「개선 시나리오 — 속도·연료 변경 효과」를 요구한다. 두 조항은 **선박 한 척**을 대상으로 하며, 본 화면은 같은 계산을 보유 선박 전체에 동시 적용한다.

*   **핵심 기능:** 함대 전체에 감속을 배분해 목표 달성 여부와 손익을 함께 본다
*   **진입 조건:** `2-4 대시보드` 경고 배너 · `2-3 연간 등급 관리`의 「함대 단위로 보기」
*   **주요 UI:**
    *   **선박별 감속 조정 행** — 등급 전이 · 감속률 · 추가 항해일 · 연료 절감
    *   **목표 선택** — 위험 선박 0척 / 전 선박 C 이상
    *   **비용 요약** — 추가 항해일 · 용선료 손실 · 연료비 절감 · 순손익
    *   **연말 등급 분포** — 조정 전/후 2행 비교
    *   **목표 판정 한 줄 — 상태 4종** 〔**확정** 2026-09-18 · `#1052` ⓸〕: **선박 없음**(계산할 선박이 없다 · 빈 상태로 죽인다) · **초기**(아직 조정 전) · **목표 달성** · **목표 미달**. 종전에는 앞에서 둘이 같은 톤을 써 「조작할 대상이 없다」와 「아직 조작하지 않았다」가 가려지지 않았다 — 앞에것은 슬라이더를 움직여도 바뀔 것이 없다. 위험색은 넣지 않는다 — 둘 다 실패가 아니다(`DESIGN_SYSTEM §0.2` 제약 2·3)
*   **결정론 계산만 쓴다** — `PRD §12.3`. `§12.4` Monte Carlo를 호출하지 않는다
*   **범위 밖:** 자동 최적 배분 · 확률 시뮬레이션 · 민감도 분석(`2-3`이 담당)

> **⚠️ 확률 시뮬레이션을 부르지 않는 이유.** `PRD §12.4`의 기본 `simulation_runs`는 5000이고 `§12.8`은 잔여 항차 200개 초과 시 계산을 거부한다. 함대 단위로 곱하면 슬라이더를 움직일 때마다 이 규모를 다시 도는 셈이 된다. `§12.3`은 난수를 쓰지 않는 단순 합산이라 즉시 계산된다.
>
> 대신 **`2-3`과 값이 달라 보인다.** 같은 선박의 연말 등급이 한쪽은 분포의 중앙값, 한쪽은 결정론 단일값이다. 화면에 그 사실을 적는다 — 문구 원문은 `PRD §6.3`이 소유하므로 **정본 개정이 선행**된다.

*   **감속 시 연료는 `PRD §11.4.1` cubic speed model을 그대로 호출한다.** 두 벌로 구현하면 같은 선박에 대해 `2-2 항로 비교`와 다른 값이 나온다
*   **선행 조건:** 선박별 일일 용선료 단가 입력 경로 → **해소**(`PRD §12.3.2`). 화면이 선박별 일일 용선료·유종별 연료 단가를 받는다. **단가가 비면 비용 칸은 「단가 입력 필요」**이고 0으로 채우지 않는다
*   **저장** — 계획 이름을 붙여 저장하고(서버가 다시 계산해 결과까지 남긴다) 저장한 계획을 불러온다. 가장 최근 계획의 단가가 새 계획의 기본값이다

### 2-11. 데이터 점검 【선대 계층】

> ## ✅ **범위 안으로 옮겼다** (`PRD §5.1` · `#513` · 2026-09-13)
>
> 막고 있던 결정 셋이 **2026-09-13 사용자 결정**으로 정해졌다 — 이상치 기준 · `CII 영향` 산출법 · 데이터 완결성 정의. 판정 규칙은 `PRD §17.4`에 있고, 서버는 `API_SPEC §2.16`이다.
>
> **네 심각도를 모두 낸다** — 종전 판정 블록이 「둘만으로 열지 않는다」고 한 조건이 채워졌다. **판정하지 못한 이상치는 0건과 따로 센다**(제원·운항 시각이 없는 항차).
>
> ⚠️ 화면의 치수·배치·문구는 **개발 임시안**이다 — 심각도 명칭·색(`DESIGN_SYSTEM §2.3.1`)과 등급 전이 표기(`§8.3`)만 확정이다.

> **「빠진 항차 목록」이 아니라 「실측이 아닌 값으로 계산된 항차 목록」이다.** `PRD §12.3`은 실제값이 없을 때 계산에서 제외하지 않고 **`§8.3` 우선순위에 따라 계획값을 쓴다.** 화면에서 사라진 것보다 실측인 척하는 추정값이 더 위험하다.

*   **핵심 기능:** CII 계산에 어떤 소스가 쓰였는지를 함대 단위로 드러낸다
*   **진입 조건:** `2-4 대시보드`의 조치 항목 · `2-8 선박 상세`의 신뢰도 표시
*   **주요 UI:**
    *   **요약** — 대체 계산 · 계산 불가 · 이상치 · **데이터 완결성**
    *   **이슈 목록** — 선박 · 항차 · 문제 · **CII 영향** · 해당 항차로 이동
    *   **CII 영향** = 그 항차를 뺀 누적 CII와의 차이(`PRD §17.4.2`). 부호의 뜻(+는 그 항차가 누적을 나쁘게 한다)을 화면에 적는다
    *   **데이터 완결성** = 누적 CO₂ 중 실측 비율(`PRD §17.4.3`) — 무엇의 비율인지 칸에 함께 적는다
    *   **「이 항차로」는 `2-8 선박 상세`의 그 항차 카드로 간다** (`?actuals=<항차 id>` — `2-9`와 같은 진입 · #1549). 실적을 넣을 수 있는 항차(진행 중 · 완료 — 「실적 미입력」)면 입력이 열린 채 도착한다. **선박 단위 행**(계산 불가)은 가리킬 항차가 없어 선박 상세로 간다. 종전에는 모든 행이 선박 상세 맨 위로 갔다(개발 임시안이었다)
*   **심각도**

    | 심각도 | 의미 | 근거 |
    |---|---|---|
    | 대체 계산 | 실적 대신 계획값으로 계산됨 | `PRD §8.3` |
    | 계산 불가 | 대체할 계획값조차 없음 | `PRD §17.3` |
    | 이상치 | 계산됐으나 신뢰도 낮음 | `PRD §17.4.1` — 모델 대비 연료 ±40% · 속력 상한 |
    | 실적 미입력 | `COMPLETED`에서 `CONFIRMED`로 미전이 | `PRD §8.4` |

*   **읽기 전용이다.** 수정은 `PRD §17.2` 실적 보정 절차와 `#370` 정박 구간 입력 경로를 따른다
*   **범위 밖:** 화면 내 데이터 수정 · 자동 보정·보간(`PRD §17.1` — 계획값과 실측값을 덮어쓰지 않는다) · 이슈 임의 해제

> **`#370`과의 관계.** `#370`이 정박 구간을 **넣는 경로**라면 본 화면은 **넣은 것이 계산에 어떻게 반영됐는지 보는 경로**다. `2-9`의 「정박 연료 기록이 없으면 값이 그대로다」 상태가 함대 단위로 모이는 곳이 여기다.

> **선행 조건:** CII 계산이 항차별로 `§8.3` 우선순위 중 **어느 소스를 썼는지 기록**해야 한다. → **`#449`(`API_SPEC §2.14` `ytd.substitutions`)가 그 기록이다** — 실적 확정 항차의 소스는 「실적」 또는 「계획값 대체」 둘뿐이고 대체는 항차·축별로 남는다. `#513`이 **실적도 계획도 없어 더하지 못한 연료 행**(종전에는 조용히 건너뛰었다)의 기록을 더했다(2026-09-13).

### 2-8. 선박 상세 【선박 계층 · 신설】

*   **핵심 기능:** 선박 한 척의 연도별 이력과 현재 상태를 확인한다. 계층 구조의 허리
*   **진입 조건:** `2-4 대시보드`의 선박 카드 선택
*   **주요 UI:**
    *   **올해 누적(YTD) CII + 등급** — 주 표시
    *   **연도별 CII 이력** — 시계열 차트 + 표 요약(`PRD §16.4` 차트 대체 설명 의무). **올해 값은 「올해 누적」 카드 한 곳에서** 말한다 (#1571) — 올해 막대 라벨은 등급 + 「진행 중」이고, 연도별 표는 「표로 보기」 접기 안에 둔다(차트가 없으면 표가 유일한 자리라 펼친다). 연료별 내역 표는 차트에 없는 정보라 접지 않는다
    *   현재 상태 — `detail_status` · 현재 좌표 · 위치 갱신 시각
    *   진행 중 항차가 있으면 `2-9 실시간 CII`로 이동
    *   **항차 카드마다 다음에 누를 것 하나만 주 버튼이다** (#1551) — 정방향 다음 상태(계획 확정 → 항해 중 → 항해 완료 → 실적 확정)로 갈 수 있으면 그 전환, **실적이 모자라 막히면 「실적 입력」**이다. 막힌 전환은 보조로 남고 사유가 「실적 입력」을 가리킨다. 실적 확정은 서버 가드(`API_SPEC §3.5` — 모든 연료 실적 · 실제 거리)를 **누르기 전에** 사유로 보인다. 다음 단계가 없는 실적 확정 카드에는 주 버튼이 없다. 「이 항차 취소」는 텍스트 버튼이다(`DESIGN_SYSTEM §8`)
    *   **`2-9`의 「이 항차 실적 입력」·`2-11`의 「이 항차로」로 오면(`?actuals=<항차 id>`) 그 항차 카드로 스크롤하고 초점을 두며, 카드에 표시 띠를 단다** — 진행 중 · 완료 항차면 실적 입력을 열어 둔 채 첫 칸에 초점을 두고, 아니면 입력은 열지 않고 카드에 초점을 둔다 (#1540 · #1549). **그 항차가 받은 목록에 없으면 말한다** — 다음 페이지가 있으면 「더 보기」로 불러오라고, 없으면 기록에 없다고. 「더 보기」로 불러와 그려지면 그 카드로 간다 (#1549)
    *   **항차 추가 — 출발·도착항** — 샘플 항만 목록에서 고르거나 자유 입력한다. 고르면 좌표가 함께 저장되고, 두 항 모두 좌표가 있으면 **「좌표 기반 추정 거리로 채우기」**로 계획 거리를 채울 수 있다 — 채운 값에는 `좌표 기반 추정 거리`를 표시한다(`PRD §15.1` · `§15.2` · #760)
    *   **계산 이력** — 이 선박으로 실행한 계산(CII 예측 · 항로 비교 · 연간 시뮬레이션) 최신순. 선박 제원이나 항차 계획이 바뀌어 결과가 낡은 계산에 **「재계산 필요」**를 표시한다(`PRD §8.4` · `API_SPEC §1.9` · #992). 다시 계산은 각 화면에서 한다
*   **단위 표기:** 선종의 capacity 축에서 파생한다 — `gCO₂/(DWT·nm)` ↔ `gCO₂/(GT·nm)`. **고정 문자열 금지**(`DESIGN_SYSTEM §4.1` 🔒)

### 2-3. 연간 등급 관리 【선박 계층】
*   **핵심 기능:** 선박별 연간 누적 CII 등급 및 목표 달성 현황 모니터링
*   **주요 UI:** 연간 목표 등급 대비 현재 진척률 바(Bar), 월별/분기별 추세 차트
*   **대상과 조건을 화면에 적는다** (#1553) — 대상 선박은 상단바 전역 선택을 따르므로 실행 조건 머리에 **읽기 전용**으로 선박명과 「상단에서 바꿉니다」를 적는다(미선택 · 목록 불러오는 중 · 이름을 모름을 가른다). 결과 맨 위에는 **이 결과의 조건** 한 줄 — 선박 · 기준연도 · 목표 등급 — 을 **실행 시점의 값**으로 적는다. 목표 등급은 실행 뒤에 바꿔도 결과를 지우지 않으므로(`#1094`) 지금 고른 값을 적으면 결과와 어긋난다
*   **재현 정보:** seed · 데이터 스냅샷 · 계산 이력과 **「이 seed로 다시 실행」 버튼** — `PRD §12.4.3` 「결과 재현 버튼」. 누르면 원본 실행을 같은 스냅샷·seed로 다시 계산해 **같은 결과인지 확인**하고 그 사실만 알린다(새 실행을 만들지 않는다 · `API_SPEC §6.4`). 재현하지 못하면 서버가 준 사유를 그대로 보인다 (`#776`)
    *   **성공과 실패를 같은 무게로 보인다** 〔**확정** 2026-09-17 · `#1053` 40번〕 — 성공도 실패와 같은 모양의 블록(아이콘 + 중립 면 + 테두리)이다. 종전에는 성공이 작은 회색 한 줄이고 실패만 블록이라 **무게가 반대**였다: 재현 확인은 「같은 결과가 나왔다」가 **곧 결론**인 검증 행위인데 그 결론이 더 약하게 보였다. ⚠️ **색은 쓰지 않는다** — 라이트 `--color-success`가 그 면 위에서 `2.89`로 `1.4.11`에 미달한다(별건). 값이 정해지면 아이콘·문구에 입힌다
*   **이 실행에 쓴 항차** — 재현 정보 아래 펼침. 실행 시점 스냅샷의 항차(연간 반영 구분 · 당시 상태 · 거리 · 속력 · 연료)를 **펼칠 때** 불러온다. 사본이라 그 뒤에 항차를 고쳐도 바뀌지 않는다는 것을 함께 적는다(`API_SPEC §6.3` · `TECH_SPEC §11` · #992)

### 2-9. 실시간 CII 【항차 계층 · 신설】

*   **핵심 기능:** 항해 중 CII가 변하는 것을 보여준다
*   **진입 조건:** `2-8 선박 상세`에서 진행 중 항차 선택
*   **주요 UI:**
    *   **연간 누적(YTD) 값을 주 표시** — 등급이 붙는 값은 이것 하나다
    *   보조 표시 — 해당 항차 구간 값 · 남은 거리 기반 연말 예상 등급
    *   **연말 예상은 연말 예상 카드 한 곳에서, 등급과 값의 방향을 한 문장으로 말한다** (#1555) — 예: 「등급은 E 유지 · 값은 현재 누적보다 나빠짐 ▲ +0.752」. YTD 카드는 **현재 누적 등급만** 보인다(종전 `#725`의 「현재 누적 → 연말 예상」 전이를 걷었다 — 같은 등급이 두 카드에 있었고, 등급의 방향과 값의 방향이 떨어진 두 자리에서 「유지」와 「나빠진다」로 어긋나 읽혔다). 값의 차이는 표시 자릿수(`DESIGN_SYSTEM §4.1`)에서 빼고 `§4.3` 화살표·부호를 붙인다. YTD 카드의 「기준 대비」 비율은 스케일 바를 그릴 때 **바 마커 한 곳**에만 적는다
    *   **기준(required) 값 옆에 `2-6` 규제 기준값 절로 가는 링크** — 「왜 이 등급인가」 사슬의 시작점이다(`a`·`c` × Z → d, `#1239` · `#1516`)
    *   **not under way 상태(정박·묘박·운하 통과 등)가 지속되면 등급이 악화되는 것이 보여야 한다**
    *   **항차 구간 값 아래에 「이 항차 실적 입력」** — `2-8`의 그 항차 실적 입력으로 간다. 입력을 이 화면에 복제하지 않는다. **남은 거리가 0이면 채움 버튼, 아직 가는 중이면 보조 링크**다 — 계획 거리를 다 채웠으면 이 화면에서 할 다음 일이 그것뿐이다 (#1540)
*   **「시뮬레이션 데이터」 배지 필수** — MVP는 AIS를 연동하지 않는다(`PRD §1 COR-5` · `R-5`)
*   **범위 밖:** 지도·항로 렌더링 — **이 화면에 한해 그대로다.** 선대 대시보드(`2-1`)는 2026-09-12부터 지도를 그리지만(`PRD §5.2` 개정 · `#763`), 실시간 CII는 **한 척의 값 화면**이라 지도가 답하는 질문(「배들이 어디에 흩어져 있나」)이 없다
*   표시 값의 정의(3종 값 분리)는 `#358`이 `PRD`에 확정한다

> **[v2.2 · `#357` 구현] 갱신 주기는 60초다.**
>
> 값은 시계에 따라 연속적으로 변하지만, 사람이 보는 화면에서 의미 있게 달라지는 단위는 시간이다 — 14 kn · 30 t/day인 배가 1분에 만드는 것은 0.23 nm와 0.02 t이고, 소수 6자리 CII의 끝자리에서나 보인다. 더 짧게 잡으면 서버 부하만 늘고 화면은 같아 보인다. 반대로 더 길게 잡으면 「정박을 시작했는데 화면이 한참 그대로」가 되고, 정박은 등급을 실제로 움직이는 사건이라 그 지연이 이 화면의 목적을 깎는다.
>
> **탭이 보이지 않으면 요청하지 않는다.** 배경 탭을 켜 둔 사용자가 하루 1440번을 요청하는데 사람이 보는 것은 돌아온 순간의 한 번뿐이다.
>
> **`as_of` 불일치 처리는 필요 없어졌다.** 종전 계획은 두 엔드포인트를 호출하는 것을 전제로 「기준 시점이 어긋나면 재조회」를 요구했으나, `#354`가 **3종 값을 한 응답에 담도록** 설계돼 어긋날 수 없다. 재조회 로직을 넣는 대신 상황 자체를 없앤 쪽이다.
>
> **「정박 중」과 「정박이 등급을 밀고 있다」를 구분해 적는다.** 정박 연료 기록이 없으면 분자 `M`이 늘지 않아 값이 그대로다. 그 상태를 「악화 중」이라 적으면 화면이 사실과 다른 말을 한다 — 기록을 넣으라고 안내한다(`#370`이 그 입력 경로다).

### 2-1. CII 예측 【항차 계층】
*   **핵심 기능:** 항해 전 항차 조건으로 CII 추정값을 계산·시각화한다. **실시간 산출(2-9)의 계획 단계**
*   **UI/UX 가이드:**
    *   **추정값 상시 고지:** 계산된 CII 등급 예측 데이터 노출
    *   **최종 판단 권한 명시:** 시스템 예측값임을 안내하고, 최종 판단 권한에 대한 안내 문구 필수 표기
*   **화면 처리:** IMO 타겟 대비 등급(A~E) 인디케이터 표시
*   **계산 근거 패널에 `2-6` 규제 기준값 절로 가는 링크** — 패널이 인용하는 선종·기준선 계수·감축계수·연료 CF의 원출처를 확인하는 자리다(`#1239` · `#1516`)
*   **결과로 할 수 있는 일** (`PRD §10.5` · #891) — 결과 아래에 **계획 저장 · 연간 시뮬레이터에서 보기 · CSV 다운로드**. 계획 저장은 출발·도착·출항 시각을 묻는 작은 입력 단계를 거치고, 연간 반영 여부를 고른다(기본 반영). 입력이 바뀐 뒤에는 다시 계산할 때까지 막힌다
    *   계획 저장의 **출발·도착항**도 샘플 항만에서 고른다 — 고른 항은 저장 이름(`BUSAN`)과 좌표로 저장된다. 항차 추가(`2-8`)와 같은 선택지다 (#1005)

### 2-2. 항로 비교 【항차 계층】
*   **핵심 기능:** 항로별 예상 소모량 및 탄소 배출량 비교
*   **⚠️ 쓰임새가 바뀌었다** — 「실행 지시」가 아니라 **「사후 설명·보고 근거」**다. 중소선사 현장은 시간 준수를 우선해 항로 변경·속도 조정을 실제로 하지 않는 경우가 많다(제16차 회의). 비교 결과는 `2-5 보고서`에 *"이 항차에서 감속했다면 등급이 어떻게 달라졌는가"* 형태로 수록한다
*   **주요 UI:** 출발지/목적지 입력 필드, 경제성·친환경성 비교 테이블
    *   **현재 위치 · 목적항** (#1005) — 샘플 항만에서 고르면 좌표가 채워진다(현재 위치는 위도·경도 칸을 채우는 입력 보조 · 목적항은 이름과 좌표). 목록에 없는 항은 자유 입력이고 좌표가 붙지 않는다
    *   **직항 거리를 비울 수 있다** — 현재 위치와 목적항 좌표가 모두 있으면 서버가 대권거리로 계산한다(`PRD §11.2`). 비우는 순간 폼이 그 사실을 알리고, 결과에 **「직항 거리는 좌표 기반 추정 거리입니다」**를 표시한다(`PRD §15.2`)
*   **계획에 반영** (`#580` · `PRD §5.1` SHOULD) — 비교 결과 아래에서 시나리오 하나를 **계획 단계 항차**(작성 중 · 계획 확정)의 계획값에 반영한다. 흐름은 **`rlatnals4114`의 2026-08-23 판정**이다
    *   **계획만 바꾼다** — 실적 갈래는 서버가 닫았다. 출항 뒤 계획을 갈아 끼우면 계획 대비 실적 비교의 기준선이 사라진다
    *   **같은 화면에 남는다** — 반영 뒤 바뀐 값 · 재계산이 필요해졌다는 사실 · 그 항차로 가는 링크를 그 자리에 낸다. 자동으로 이동하지 않는다
    *   **되돌리기는 없다** — 이전 계획값을 보관하지 않는다. 대신 **반영 전에 확인**을 받고, 다른 시나리오를 다시 반영하면 그 값으로 바뀐다는 것을 함께 알린다
    *   **기존 항차만** — 새 항차 만들기(`CREATE_NEW_VOYAGE`)는 열지 않는다
    *   재계산 **건수**는 보이지 않는다 — `#817`이 닫히기 전에는 참값이 아니다
*   **범위 밖:** 지도 기반 항로 시각화 — **이 화면에 한해 그대로다.** 항로 비교는 **두 안의 수치 차이**를 읽는 자리이고, 지도를 넣으면 「어느 쪽이 유리한가」가 아니라 「어디를 지나는가」로 시선이 옮겨 간다. 선대 지도는 `2-1`에 있다(`#763`)

### 2-5. 보고서 【산출물 · MVP 승격】

> **[v2.0] MVP 포함으로 승격했다.** 종전에는 「내부 보고용 CSV 내보내기」만 `PRD §5.1` SHOULD였으나, `PRD v4.0`에서 **보고서가 MUST**가 됐다.
> **「대관 제출용(규제 제출용 공식 보고서)」은 여전히 OUT OF SCOPE**다 — `PRD §0.3`의 제품 기본 원칙(규제 제출용 공식 계산 시스템이 아님)은 바뀌지 않았다.

*   **핵심 기능:** 선대·선박·항차 계층의 결과를 조합해 **하나의 문서**로 생성한다
*   **산출물 2종:**
    *   **항차 완료 리포트** — 항차 요약 · CII 기여도 · 연료 · **시나리오 사후 비교**(2-2 흡수)
    *   **연간 실적 리포트** — YTD/확정 등급 · 연도별 추이 · not under way 기여 · 목표 달성 현황
*   **포맷:** PDF · CSV
*   **면책 문구 필수 노출** (`PRD §6.3`)
*   **리포트 구성·생성 시점의 정의는 `PRD §25`가 소유한다** (#360) — 본 절은 화면 흐름(진입·미리보기·다운로드)만 기술한다

> **[v2.3 · `#362` 구현] 미리보기와 다운로드가 같은 문서다.**
>
> 서버가 `format=html`로 **PDF와 같은 소스**를 낸다(`API_SPEC §8.3`). 화면이 따로 그리면 미리보기와 받은 파일이 달라지고, 그 차이는 사용자가 파일을 연 뒤에야 드러난다 — 이슈의 완료 기준이 「미리보기와 다운로드 결과가 일치함」인 이유다. 미리보기는 `sandbox`를 건 `iframe`에 격리한다: 문서 전용 스타일(`@page`·표 테두리)이 화면 스타일과 섞이면 양쪽이 깨지고, 문서에 스크립트가 있을 이유가 없다.
>
> **진행 중 항차를 목록에서 감추지 않는다.** 리포트 대상이 아니지만(`PRD §25.2`) 비활성으로 두고 「완료 후 생성 가능」을 붙인다 — 감추면 사용자가 「내 항차가 왜 없지」를 묻게 되고 답이 화면 어디에도 없다.
>
> **연도 선택지에 미래 연도를 넣지 않는다.** `API_SPEC §8.4`는 2019~2100을 받지만, 실적이 있을 수 없는 해의 리포트는 언제나 빈 문서이고 사용자는 그것을 고장으로 읽는다. 선택지는 **규제연도 목록 가운데 올해 이하 전부**다(`#635` — 지금 2023~2026의 넷이고, 2030년에는 여덟이 된다). 종전 「올해부터 과거 5년」은 구현과 달랐다(`#759` 정정).
>
> **다운로드를 `<a href>`가 아니라 fetch로 받는다.** 링크로 걸면 실패가 화면 밖에서 일어난다 — 401이면 로그인 HTML이 `.pdf`로, 500이면 오류 JSON이 저장되고 사용자는 파일을 열고 나서야 잘못을 안다. 성공했을 때만 blob을 만들어 저장하고, 실패는 화면에서 서버 문구 그대로 말한다.

### 2-6. 설정 【계층 밖 · 부분 MVP】

> **[#359] 판정이 끝났다 (2026-08-23).** v2.0이 「`#359`의 결정에 따른다」로 보류해 둔 것을 여기서 닫는다.
>
> **어드민 계정은 1차 시연 범위 밖이다**(`PRD §20 O-14` 각주). 따라서 최소안(`role` 컬럼 + 파라미터 화면 접근 제어)은 **채택되지 않았고**, 이 화면에서 MVP에 들어오는 것은 **계정 관리 하나**다.
>
> **그 하나는 이미 들어와 있다** — `#506`(PR `#612`)이 `PRD §5` 계정 관리 MUST를 근거로 표시 이름·비밀번호 변경을 넣고 사이드바의 「준비 중」 뱃지를 없앴다. **권한과 무관한 부분만 떼어 먼저 넣은 것**이며, 그 판단이 코드 주석에 남아 있다(`pages/SettingsPage.tsx`).
> 
> ⚠️ **[#754 정정] 종전 이 문단은 탈퇴도 함께 들어왔다고 적었으나 사실이 아니었다.** `#506`의 「범위」에 탈퇴는 없었고 **「결정이 필요한 것」 2번**이었다 — 탈퇴한 사용자의 `calculation_run`·`audit_log`를 어떻게 할지가 미정이었기 때문이다. 그 결정은 이후 `PRD §6.3` 탈퇴 확인 문구와 `§5.1` MUST 행으로 내려졌고 서버(`DELETE /auth/me`)도 그에 맞춰 구현됐으나, **화면만 따라오지 않았다.** 이 문서가 그 사이 「따라온 것」으로 적고 있었다. 화면은 `#754`(PR `#862`)가 넣었다.
>
> 재개 지점은 **`#672`**다.
>
> **[#1239 · #1516] 「규제 기준값」 절을 신설한다 (2026-09-21).** 위 재개 지점을 여기서 잇는다. `UIFLOW.md:338`이 「post-MVP · `#673`과 함께 판정」으로 미뤄 둔 `SCR-006`(규제 파라미터 조회)은 **`#673`이 09-18에 닫히며 유예 조건을 채웠고**(`AGENTS §6.1`), `PRD §5.1`(`PRD.md:560`)은 줄곧 MUST였다 — 화면이 없어 사용자가 볼 수 없었을 뿐이다.
>
> **자리는 사이드바 탭이 아니라 이 절 안이다** — 네 표(연도 감축률 · 선종별 기준선 `a`·`c` · 등급 경계 d1~d4 · 연료 탄소계수) 합쳐 50행이고 IMO 개정 때만(연 1~2회) 바뀐다. **접근은 조회 세 역할 모두, 개정 적재·개정 이력은 사무직 이상**(§2.2 각주) — 값이 IMO 공개 상수이기 때문이다(`#1239` 결정 D). **이력은 기본 활성분, 「이전 판본 포함」 전환으로 연다**(`#1239` 결정 C) — 개정 다음 날 옛 판본·새 판본의 계산이 나란히 있을 수 있다(`PRD §8.4`). **연료 탄소계수는 제자리 갱신이라 이전 값이 남지 않는다** — 절 안에 그 사실을 적는다. 숫자는 **서버 문자열 그대로** 표시한다(`2-1`의 「계산 근거」 패널과 같은 원칙 — 반올림하면 대조하려는 사람에게 근거가 아닌 것을 보여주게 된다).
>
> **진입 경로 3곳** — ⑴ `2-9 실시간 CII`의 「기준(required)」 값 옆 ⑵ `2-1 CII 예측`의 「계산 근거」 패널 ⑶ `2-4 대시보드`가 기준값 없는 선박에 내는 안내(`NO_PARAMETERS` — 「해당 선종·연도의 규정 기준값이 없습니다. 운영자에게 문의해 주세요.」) — 셋 모두 이 절로 링크한다. **대시보드에는 별도로 「적용 기준 판본 한 줄」**을 둔다(예: 「기준 — MEPC.400(83) · 2026 감축률 11%」).
>
> ⚠️ **개정 적재·개정 이력 화면 자체는 `#1517`(별도 이슈)이다.** 이 절이 여는 것은 **조회**뿐이다.
>
> ⚠️ **화면 배치(설정 안 절 · 대시보드 한 줄)는 사용자 방향으로 정했으나 `UIFLOW` 소관이라 디자인 담당(`rlatnals4114`)의 확인이 아직이다.** 확정 전까지 표·전환·링크의 배치·치수는 개발 임시안이다.

| 구성 | MVP | 근거 |
|---|---|---|
| 프로필 · 계정 관리 | ✅ **포함** | `PRD §5` 계정 관리 MUST · `#506` |
| 규제 파라미터 조회 (`SCR-006`) | ✅ **범위 안** (`#1239` · `#1516` · 2026-09-21 — 종전 post-MVP였다) | 유예 조건이던 개정 적재 경로(`#673`)가 09-18에 닫혔고, `PRD §5.1`(`PRD.md:560`) MUST가 그대로 살아 있었다(`AGENTS §6.1`). **`2-6` 설정의 「규제 기준값」 절**로 들어온다 — 사이드바 탭을 새로 두지 않는다(§2.2.1) |
| **계정 목록 · 역할 지정** | ✅ **포함** (`#1301` · 2026-09-19 — 종전 사무직 전용이었다 `#672`) | **관리자만 본다.** 계정별 사무직/현장직/관리자 셀렉트 · 마지막 관리자는 강등 불가(서버 `409`) · 본인 역할 표시는 세 역할 모두 |
| 조직 · 세부 권한 설정 | ❌ 두지 않는다 | `PRD §5.2` — 역할 3종 밖의 권한 관리는 계속 제외 |
| 선박 양식 · 알림 임계값 | ❌ post-MVP | `PRD §5`에 근거 조항이 없다 · 임계값은 `#513`이 닫히며 근거 조항 없이 남았다 — 조항이 생기면 별도로 판정한다 |

*   **핵심 기능:** 로그인한 사용자의 자기 계정 관리 · (관리자) 계정 역할 지정 · **규제 기준값 조회**
*   **주요 UI:** 계정 정보(이메일 · **역할**) · 표시 이름 변경 · 비밀번호 변경 · 탈퇴 · **계정 목록·역할 지정**(관리자 전용 절 — `GET /auth/users` · `PATCH /auth/users/{id}/role`) · **규제 기준값 절**(조회는 세 역할 모두 · 표 4종 — 연도 감축률 · 선종별 기준선 · 등급 경계 · 연료 탄소계수, **표마다 접는다** · 「이전 판본 포함」 전환 · 개정 적재·개정 이력은 사무직 이상 — `#1239` · `#1516` · 개정 적재 화면은 `#1517`)

### 2-7. AI 어시스턴트 (챗봇 오버레이)
*   **성격:** LLM 챗봇 — 실험 기능 (`PRD §20 O-12`, MAY 수준)
*   **진입 조건·흐름:** 대시보드 화면 위 **오버레이**로 열리며, 별도 라우트/화면 전환이 없다. 핵심 계산·보고 흐름에 개입하지 않는다.
*   **화면 수준 제약 (3대 봉쇄 원칙 — `PRD §20 O-12` 참조):**
    *   **No-Compute:** IMO 규제값을 재계산하지 않으며, 기존 계산 결과값만 인용한다.
    *   **No-Advice:** 시나리오 순위화·비교 표현("더 낫다/최적")·행동 제안 금지. 값·델타·이미 계산된 what-if 결과 인용만 허용.
    *   **No-Recall:** 선사 기밀 운항 데이터를 외부 LLM으로 전송하지 않는다 (`PRD §16.3` 외부 LLM 전송 통제 MUST).
*   **격리:** 챗봇 장애 시 기존 화면은 정상 동작한다 — 계산·보고 기능에 영향 0 (`PRD §16.2` 챗봇 장애 격리).
*   **주요 UI:** 우하단 열기 버튼 · 대화 목록 · 질문 입력(2000자) · **답변마다 면책** · **버린 답 표시**
    *   **「답이 아닌 것」의 표시만 확정이다** 〔2026-09-17 · `DESIGN_SYSTEM §8.5`〕 — 버린 답은 **줄무늬 + 접두 문구** 두 채널, 면책은 **줄무늬를 쓰지 않는다**, 호출 실패는 줄무늬만 공유한다. 셋이 같은 모양이면 사용자가 「답을 받았다」로 읽는데 받은 것은 「답을 못 드린 이유」다(`API_SPEC §15.2`).
    *   **⚠️ 나머지는 여전히 개발 임시안이다**(`#121` — 열기 버튼·대화 목록·입력칸의 치수와 배치). `PRD §20 O-12`가 MAY·실험이라 `DESIGN_SYSTEM`이 치수를 정하지 않았고, 그 자리는 `§16` 항목 16~18로 남아 있다. **새 색·반경·그림자를 만들지 않았다** — 만들면 확정 뒤 두 벌이 남는다.
    *   **셸 안·본문 밖**에 둔다 — *"별도 라우트/화면 전환이 없다"*를 지키려면 화면이 바뀌어도 대화가 살아 있어야 하고, 본문 에러 경계 안에 두면 화면 하나가 깨질 때 함께 사라진다.
    *   **스트리밍을 쓰지 않는다** — 일반 응답 + 로딩 표시다. 근거는 `API_SPEC §15.5`.

---

## 변경 이력

> git 커밋 기록에서 복원했다(날짜는 커밋 기준).
>
> **2026-07-23까지가 사후 복원분이다.** 이후 항목은 변경 시점에 직접 기록하며, squash merge로 브랜치 커밋 해시가 재작성되므로 커밋 열에는 **PR 번호**를 적는다.

| 날짜 | 커밋 | 변경 요약 |
|---|---|---|
| 2026-07-19 | `c5c22b7` | v1.0 최초 작성 (rlatnals4114 작성분) |
| 2026-07-29 | `#142` | ULFLOW.md에서 파일명 정정 + 헤더·변경이력 표 신설 |
| 2026-08-07 | `#196` | 헤더 「상위 문서」의 `PRD` 버전 참조 갱신 (v3.1 → v3.2) (#163) |
| 2026-08-11 | `#219` | 2-4(선대 모니터링)에 OUT OF SCOPE 표기, 2-5(보고서)를 내부 보고용으로 한정 — `PRD §5.2`·§2.4와 정합 (#212) |
| 2026-08-11 | `#221` | 2-7 AI 어시스턴트(챗봇 오버레이) 추가 — 진입 조건·3대 봉쇄 원칙·장애 격리 각주 명시 (#211) |
| 2026-08-14 | `#337` | §2 헤더의 MVP 화면 목록에서 2-6(설정) 제외 + 2-6에 post-MVP 범위 각주 신설 — `PRD §5`에 설정 화면이 없어 `AGENTS §3.2.3` 범위 판정행 적용 (#280) |
| 2026-08-15 | `#365` | **v2.0 — 선대·선박·항차 3계층 재작성.** §1 「메인보드」를 「대시보드」로 통일하고 2-4를 1-3에 통합 · §2에 계층 구조도와 화면↔계층 매핑표 신설 · **2-4 선대 모니터링의 OUT OF SCOPE 각주 삭제**(PRD v4.0에서 근거 조항 삭제) · **2-8 선박 상세 · 2-9 실시간 CII 신설** · 2-5 보고서를 MVP 승격(대관 제출용은 계속 제외) · 2-6 설정을 #359 결정 대기로 · 2-2 항로 비교의 쓰임새를 사후 보고 근거로 재기술 · `detail_status` 7값 표기 규정 (#344) |
| 2026-08-17 | `#415` | §0 화면 5종 구현 완료 — 로그인(이메일·비밀번호) · `0-1` 회원가입 · `0-2` 로그인 실패 · `0-3` 비밀번호 찾기(요청·재설정 2단) · `0-4` 이메일 인증. **비밀번호 찾기와 이메일 인증은 메일 링크가 곧 경로**라 별도 경로를 두지 않고 `?token=` 유무로 갈랐다 — 나누면 링크를 잘못 만들었을 때 사용자가 404를 만난다. 미인증 배너를 셸 상단에 상시 노출 (#415) |
| 2026-08-16 | `#413` | **v2.1 — 자체 ID/PW 인증 전환.** §0의 「구글 OIDC 위임」 서술 삭제 · 로그인 화면을 이메일·비밀번호 입력으로 재작성 · **v1.0에서 삭제됐던 `0-1 회원가입`·`0-3 비밀번호 찾기` 복원**(삭제 근거였던 구글 위임 전제가 `PRD O-14`로 사라짐) · **`0-4 이메일 인증` 신설** · 종전 `0-1 로그인 실패`를 `0-2`로 되돌림 · 「이메일 미인증 상태」 절 신설(인증 전에도 로그인 허용) (#413) |
| 2026-08-15 | `#406` | 2-5에 리포트 구성·생성 시점의 소관이 `PRD §25`임을 연결하는 상호 참조 추가 (#360) |
| 2026-08-17 | PR #425 | **v2.2 — `2-9 실시간 CII` 구현 확정 사항 기재 (`#357`).** 갱신 주기를 **60초**로 정하고 근거를 남겼다(1분이 만드는 변화는 소수 6자리 끝자리에서나 보이고, 더 길면 정박 시작이 늦게 보인다) · 탭이 숨으면 요청하지 않는다 · **`as_of` 불일치 처리는 필요 없어졌다** — `#354`가 3종 값을 한 응답에 담아 어긋날 수 없다 · 「정박 중」과 「정박이 등급을 밀고 있다」를 구분해 적는다(정박 연료 기록이 없으면 값이 그대로다) |
| 2026-08-17 | PR #427 | **v2.3 — `2-5 보고서` 구현 확정 사항 기재 (`#362`).** 미리보기는 서버 `format=html`을 sandbox iframe에 격리해 **PDF와 같은 소스**를 쓴다(완료 기준 「미리보기와 다운로드 결과 일치」) · 진행 중 항차를 **감추지 않고 비활성**으로 둔다 · 연도 선택지에 미래 연도를 넣지 않는다 · 다운로드를 링크가 아니라 **fetch**로 받아 실패를 화면에서 말한다(링크면 오류 JSON이 `.pdf`로 저장된다) |
| 2026-08-18 | PR #505 | **v2.4 — 선대 계층에 `2-10 함대 감축 계획` · `2-11 데이터 점검` 신설.** 둘 다 신규 기능이 아니라 기존 기능의 함대 단위 집계 뷰다 — `2-10`은 `PRD §12.6`·`§12.7`을 함대 전체에, `2-11`은 `PRD §8.3`·`§17.3`을 함대 전체에 적용한다. `2-10`은 `§12.4` Monte Carlo 대신 `§12.3` 결정론만 쓴다(함대 곱셈이 `§12.8` 한도에 닿는다) · `2-11`은 「빠진 항차」가 아니라 「계획값으로 대체된 항차」를 다룬다(`§12.3`은 결측을 제외하지 않는다) · MVP 범위는 `PRD §5` 판정 대기(`AGENTS §3.2.3`) |
| 2026-08-21 | PR #604 | **v2.5 — §0·§1·§2 하위 절에 번호를 붙이고 §2.1 계층 구조도 헤딩을 신설했다.** `DESIGN_SYSTEM`과 `frontend/src/screens.ts`가 **`UIFLOW §2.2`를 3곳에서 참조하는데 그 번호가 없었다** — 가리킨 것은 「화면 ↔ 계층 매핑」이고 번호가 붙어 있지 않았을 뿐이다. 본 문서는 전 이력에서 하위 절에 번호를 쓰지 않아 **`§N.M` 형태로 참조하면 무엇을 봐도 없었다.** 번호를 붙여 참조 3곳이 그대로 유효해졌다. 화면 헤딩(`2-4.` 등)은 재번호하지 않았다 — `screens.ts`의 `uiflowRef`와 `PRD §6.2` 각주가 그 번호를 참조한다. 문서 상단에 PR 본문용 「정본 변경 포함」 경고블록이 새어 들어 있던 것도 함께 걷어냈다(PR #505 유입) · v2.4 행의 미기입 PR 번호를 `#505`로 채웠다 (#583) |
| 2026-08-23 | `#359` | **2-6 설정의 판정 대기를 닫았다 — 부분 MVP로 확정.** v2.0이 「`#359`의 결정에 따른다」로 보류해 둔 것이다. 어드민 계정이 **1차 시연 범위 밖**으로 정해져(`PRD §20 O-14` 각주) 최소안은 채택되지 않았고, MVP에 들어오는 것은 **계정 관리 하나**다 — 그것은 `#506`(PR `#612`)이 이미 넣었다. 구성 4종의 포함 여부와 각각의 근거를 표로 남겼다. 재개 지점은 `#672`다 (#359) |
| 2026-09-09 | `#754` | **2-6의 「탈퇴가 이미 들어와 있다」 서술을 정정.** `#506`(PR `#612`)이 넣은 것은 표시 이름·비밀번호 변경 둘이고 **탈퇴는 그 이슈의 「결정이 필요한 것」 2번**이었다 — 탈퇴한 사용자의 `calculation_run`·`audit_log` 처리가 미정이었기 때문이다. 그 결정이 `PRD §6.3` 탈퇴 확인 문구와 `§5.1` MUST로 내려진 뒤 서버(`DELETE /auth/me`)는 따라왔으나 **화면이 따라오지 않았고**, 이 문서는 따라온 것으로 적고 있었다. 그 사이 **이메일을 바꿀 유일한 경로가 막혀 있었다** — 같은 화면이 *「다른 주소를 쓰려면 탈퇴 후 다시 가입해 주세요」*라고 안내하는데 탈퇴할 수가 없었다. 화면을 넣으면서 서술을 사실에 맞췄다 (#754) |
| 2026-08-23 | `#589` | **헤더 「상위 문서」를 `PRD` v4.0 → v4.4로 갱신 · `AGENTS §4.4` 각주 신설 · 제목을 `UIFLOW — BlueLog`로 (`§4.5`).** **v4.0 → v4.4를 실제로 대조했다** — v4.1이 신설한 `PRD §25`(보고서)를 본 문서가 **3곳에서 인용**하고, v4.2가 신설한 실시간 CII는 **`2-9` 절로 들어와 있으며**(8곳), v4.3의 `§7.10` User 재정의는 `§0` 인증 흐름이 인용한다. **본문은 이미 v4.3까지 대조된 상태였고 헤더만 낡아 있었다.** v4.4는 제품명 정정이며, 제목이 `🚢 스마트해운물류 [Blue Log] …`로 **옛 이름 + 띄어쓴 표기**여서 `§4.5`(「붙여 쓴다. `Blue Log`가 아니다」)를 두 가지로 어기고 있었다. `§4.3`상 헤더·각주 정정이라 버전은 올리지 않는다 (#589) |
| 2026-08-23 | `#513` | **`2-10`·`2-11`의 판정 대기를 닫았다 — 1차 심사 범위 밖으로 확정.** v2.4가 두 화면을 신설하며 MVP 열을 「`PRD §5` 판정 대기」로 둔 것이다. `PRD §5.2` 제외 범위에 등재했고 매핑 표 두 행을 갱신했다. **명세 본문은 폐기하지 않고 그대로 둔다** — 결정이 서면 판정 블록만 지우고 착수한다. ⚠️ `2-11`은 심각도 4종 중 **둘을 서버가 이미 내고 있어**(`ytd.substitutions` · `vessels[].unavailable_reason` — 2026-08-23 실측) 그 둘만으로 화면이 서지만, **나머지 둘이 빈 채로 나가면 「해당 없음 0건」으로 읽힌다.** 데이터 품질 화면이 자기 불완전을 감추게 되므로 부분 구현을 택하지 않았다. 재개 지점은 `#513`이다 (#513) |
| 2026-08-24 | `#712` | **v2.7 — §2.2.1 사이드바 순서를 신설했다.** `DESIGN_SYSTEM §7.2`가 「사이드바 항목과 순서는 `UIFLOW §2.2`가 소유한다」로 위임하는데, **§2.2에는 순서를 정한 문장이 없었다** — 계층 매핑 표의 행 순서에 암묵적으로 얹혀 있었을 뿐이다. 그래서 한 표가 「어느 화면이 어느 계층인가」와 「메뉴를 어떤 차례로 놓는가」 둘을 겸했고, 둘은 같은 것이 아니다. 순서를 **작업 흐름 기준**으로 다시 정하고 표로 명시한다 — 계층은 데이터의 구조이지 사용자가 밟는 순서가 아니다. **`SCR-002 선박 관리`를 둘째로 올린 것이 핵심이다**: 제원이 비어 있으면 CII 계산도 항로 비교도 실행되지 않는데(`PRD §6.1`) 종전에는 목록 여섯째였다. 그 화면은 계층 매핑 표에 아예 없어(`PRD §6.1` 소관) 자리가 임의로 정해져 있었다. **계층 매핑 자체와 화면 번호는 바꾸지 않았다** (#712) |
| 2026-09-11 | `#776` | **`2-3 연간 등급 관리`에 「재현 정보」 행을 추가했다.** `PRD §12.4.3`이 「`이 seed로 다시 실행` 버튼을 제공한다」로 화면 요구를 명시하는데 이 화면 명세에 대응이 없었고, `#556`이 `§5.1`·`§6.2`·본 문서 셋만 대조해 `reproduce`를 「범위 밖」으로 판정한 원인이 그 공백이었다. 버튼은 새 실행이 아니라 **검증**이라 결과 값을 바꾸지 않고 확인 사실만 알린다. `AGENTS §4.3` 「소규모 행 추가」라 버전은 올리지 않는다 (#776) |
| 2026-09-11 | `#758` | **§0 세션 만료 복귀 경로의 끊긴 참조 정정** — `API_SPEC`의 **12.1절**과 `redirect_to`를 가리키고 있었으나 그 절은 존재한 적이 없고(12절은 하위 절 없는 요약 표), `redirect_to`는 구글 OIDC를 걷어낸 `#413`과 함께 사라졌다. 실제 메커니즘(`?next=` 쿼리 · `safeNext`)으로 고쳤다. 참조 가드를 정본 8종으로 넓히자 드러난 **유일한 끊긴 참조**다. 동작은 원래 정상이었다 (#758) |
| 2026-09-11 | `#759` | **정본 드리프트 정정** — ⑴ §2.2 매핑 표 `2-10`·`2-11`의 `PRD §6.2` 대응 열이 `PRD`에 없는 SCR-012·SCR-013을 가리켜 「미등재」로 정정(범위 판정은 그대로) ⑵ `2-5` 연도 선택지 「올해부터 과거 5년」 → 구현(`#635` — 규제연도 목록 중 올해 이하 전부) ⑶ 헤더 최종 수정일을 본문 변경일로(`AGENTS §4.2`). `AGENTS §4.3` 「오기·값 정정·각주 보강」이라 버전은 올리지 않는다 (#759) |
| 2026-09-11 | `#580` | **2-2 항로 비교에 「계획에 반영」 흐름을 기재했다.** ⚠️ **흐름은 `rlatnals4114`의 2026-08-23 판정(`#580` 코멘트)이며 개발이 정한 것이 아니다**(`AGENTS §7.3`) — 계획만 · 같은 화면 · 되돌리기 없음(반영 전 확인) · 기존 항차만. 판정이 이슈 코멘트에만 있어 **화면이 생긴 뒤에는 흐름의 근거를 찾을 자리가 없었다.** 재계산 건수를 보이지 않는 것은 2026-09-08 착수 판정(`#817`)이다. 소규모 추가라 `AGENTS §4.3`에 따라 버전은 올리지 않는다 (#580) |
| 2026-09-11 | `#982` | `1-2 선박 등록`에 **「샘플 선박 선택」** 한 줄 — 고르면 선종·제원을 채우고 IMO·선명은 사용자가 입력한다(`PRD §5.1` MUST 복원 · `API_SPEC §2.15`). 흐름·화면 목록은 바뀌지 않는다 (#982) |
| 2026-09-11 | `#891` | `2-1 CII 예측`에 **「결과로 할 수 있는 일」** 한 줄 — 계획 저장 · 연간 시뮬레이터에서 보기 · CSV 다운로드(`PRD §10.5`). 배치는 개발 임시안이며 디자인 확정 요청에 올린다 (#891) |
| 2026-09-11 | `#772` | `2-4 대시보드` 선박 카드 그리드에 **정렬·페이지는 서버 소관** 한 줄 — 배너·KPI·조치는 선대 전체 기준, 목록은 서버가 정렬해 나눠 주고 「다음 선박 불러오기」로 이어 받는다(`API_SPEC §2.8`). 버튼 모양은 개발 임시안 (#772) |
| 2026-09-11 | `#992` | `2-8 선박 상세`에 **계산 이력(재계산 필요 표시)** · `2-3 연간 등급 관리`에 **이 실행에 쓴 항차** 펼침 — 결정 3-② 「범위 밖은 없다」로 `#776`의 「화면 미연결 유지」 판정이 뒤집혔다. 모양은 개발 임시안이며 디자인 확정 요청에 올린다 (#992) |
| 2026-09-12 | `#760` | `2-8 선박 상세`의 항차 추가에 **출발·도착항 샘플 목록 선택 · 좌표 기반 추정 거리 채우기** 항목 — `PRD §15.1` 「샘플 항만 테이블」 MUST 구현. 모양은 개발 임시안이며 디자인 확정 요청에 올린다 (#760) |
| 2026-09-12 | `#1005` | `2-2 항로 비교`에 **현재 위치·목적항 샘플 항만 선택**과 **직항 거리를 비우면 좌표 기반 추정 거리** 항목 · `2-1 CII 예측`의 계획 저장 출발·도착항 선택 — `#760`이 항차 추가에 붙인 선택지를 나머지 두 자리에 둔다. 모양은 개발 임시안이며 디자인 확정 요청에 올린다 (#1005) |
| 2026-09-12 | `#763` | **v2.8 — `2-4 대시보드`에 선박 위치 지도 신설** · `2-9`·`2-2`의 「범위 밖」 표기를 **화면 한정**으로 고쳤다. 타일은 우리 오리진의 PMTiles 파일 하나라 키도 런타임 외부 요청도 없고 오프라인에서 뜬다 — `PRD §5.2`가 지도를 제외하며 든 사유 넷이 이 방식에서는 하나도 걸리지 않는다. 자산이 없으면 **개략도로 떨어진다.** ⚠️ 확대 상한 z10은 **위치 데이터가 그 이상을 받치지 못하기** 때문이다(사람이 찍은 한 점). `2-9`(실시간 CII)와 `2-2`(항로 비교)는 지도를 넣지 않는다 — 전자는 한 척의 값 화면이고, 후자는 두 안의 수치 차이를 읽는 자리다 (#763) |
| 2026-09-13 | `#121` | **v2.9 — `2-7 AI 어시스턴트`에 「주요 UI」 신설.** 종전에는 성격·제약·격리만 있고 **화면에 무엇이 있는지**가 없었다 — 오버레이를 만들 때 참조할 것이 없다는 뜻이다. ⚠️ **모양은 여전히 미확정**이고, 적은 것은 개발 임시안이다(`DESIGN_SYSTEM`에 챗봇 항목이 없다). 새 토큰을 만들지 않고 기존 값만 조합했다 — 만들면 확정 뒤 두 벌이 남는다. 항목 중 **「버린 답 표시」가 새로운 요구**다: 서버가 수학 검증에 걸린 응답을 폐기하는데(`API_SPEC §15.2`), 같은 모양으로 두면 사용자가 「답을 받았다」로 읽는다. **셸 안·본문 밖** 배치도 함께 적었다 — `2-7`의 「별도 라우트/화면 전환이 없다」를 지키려면 화면이 바뀌어도 대화가 살아 있어야 하고, 본문 에러 경계 안에 두면 화면 하나가 깨질 때 어시스턴트도 함께 사라져 `PRD §16.2` 격리가 한쪽 방향으로만 성립한다. `AGENTS §4.3`상 항목 신설이라 버전을 올리고 README 문서 구조 표를 함께 갱신했다 (#121) |
| 2026-09-13 | `#513` | **v2.10 — `2-11 데이터 점검`을 범위 안으로 옮겼다.** ⚠️ 막던 결정 셋(이상치 기준 · `CII 영향` 산출법 · 데이터 완결성 정의)은 **2026-09-13 사용자 결정**이며 규칙은 `PRD §17.4`가 소유한다. 판정 블록을 해소 블록으로 바꾸고 §2.2 매핑 표(SCR-012) · **§2.2.1 사이드바**(보고서 앞 — 내보내기 전에 무엇으로 계산됐는지 본다. 신설 당시 6번이었고, 같은 날 `2-10`이 들어오며 **7번이 됐다** — 표가 정본이다)을 고쳤다. 선행 조건 「소스 기록」은 `#449` `ytd.substitutions`가 이미 그 기록임을 적었다. 사이드바 자리·화면 치수·「해당 항차로 이동」이 선박 상세로 가는 것은 **개발 임시안**이다. `2-10`은 그대로 범위 밖(구현 전). 화면 신설·순서 개정이라 `AGENTS §4.3`에 따라 버전을 올린다 (#513) |
| 2026-09-13 | `#513` | **v2.11 — `2-10 함대 감축 계획`을 범위 안으로 옮겼다.** ⚠️ 마지막 조건이던 **비용 단가 입력 경로**는 2026-09-13 사용자 결정(단가는 계획의 가정값 · `PRD §12.3.2`)이다. 판정 블록을 해소 블록으로 · §2.2 매핑 표(SCR-013) · **§2.2.1 사이드바 6번**(연간 등급 관리 다음 — 연 단위 결과를 선대 단위 조치로) · 선행 조건 해소 · 저장/불러오기 한 줄. 배치·치수·문구는 개발 임시안. 화면 신설·순서 개정이라 `AGENTS §4.3`에 따라 버전을 올린다 (#513) |
| 2026-09-15 | `#672` | **v2.12 — §2.2 화면 표에 「사무직」·「현장직」 두 열 추가 · 2-6 설정에 「계정 목록·역할 지정」 포함.** 역할 2종 도입(`PRD §20 O-14` · 2026-09-15 사용자 결정)의 화면별 배정이다 — 현장직은 「넣고 본다」, 사무직은 「정하고 낸다」. 2-5 보고서·2-10 함대 감축 계획은 현장직에게 **비활성 항목**으로 남긴다(숨기지 않는다 — `DESIGN_SYSTEM §7.2`와 같은 판단). §2.2 표에 SCR-002 선박 관리 행을 함께 넣었다(종전에는 이 표에 없어 `screens.ts`가 `PRD §6.2`를 근거로 들고 있었다). 2-6의 「조직·권한 설정 post-MVP」는 「역할 2종 밖은 두지 않는다」로 고쳤다. 열 추가라 버전을 올린다 (#672) |
| 2026-09-19 | `#1308` | **v2.14 — §2.2 화면 표에 「관리자」 열 추가.** 관리자는 사무직의 상위집합이라 계층 매핑 표의 모든 행에서 사무직과 같은 권한을 갖되, **2-6 설정 행만 갈린다** — 「계정 목록·역할 지정」이 사무직에서 관리자로 넘어가 사무직 열은 「본인만」이 되고 관리자 열만 「본인 + 타인 역할 변경」을 갖는다. §2.2 각주를 세 역할 기준으로 다시 쓰고, 2-6 절의 표·핵심 기능·주요 UI 설명을 관리자 전용으로 고쳤다(마지막 관리자 강등 불가 · 계정별 사무직/현장직/관리자 셀렉트). 열 추가라 `AGENTS §4.3`에 따라 버전을 올린다 (#1301) |
| 2026-09-17 | `#1051` | `2-7 AI 어시스턴트`의 「개발 임시안 — 디자인 확정 요청 대상」 표기를 **갈랐다.** 「답이 아닌 것」의 표시(버린 답 · 면책 · 호출 실패)는 `rlatnals4114`의 2026-09-17 확정으로 `DESIGN_SYSTEM §8.5`가 됐고, **치수·배치는 여전히 임시안**이다(`§16` 항목 16~18). 한 줄로 뭉쳐 두면 확정된 것까지 임시안으로 읽혀 다시 열린다 (#1051) |
| 2026-09-17 | `#1053` | **`2-3 연간 등급 관리`의 재현 확인에 「성공과 실패를 같은 무게로」 한 줄.** ⚠️ **`rlatnals4114`의 2026-09-17 확정 ⓑ이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 「디자인 확정 요청」 40번의 답이다 — 성공이 `--text-muted` 작은 한 줄이고 실패만 아이콘 달린 블록이라 **무게가 반대**였다. 재현 확인은 성공이 곧 결론인 검증 행위다. 색은 쓰지 않는다: 라이트 `--color-success`가 `--color-surface-2` 위에서 `2.89`로 비텍스트 `3:1`조차 넘지 못해, 값이 정해질 때까지 구조만 맞춘다(별건 이슈). 함께 재현 정보의 스냅샷 UUID와 설명이 **붙어 나오던 표시 결함**을 고쳤다 — `.annual-sim__hint`가 인라인 span이라 값 뒤에 이어 붙는 이 자리에서만 났다. `AGENTS §4.3` 「소규모 행 추가」라 버전은 올리지 않는다 (#1053) |
| 2026-09-18 | `#1081` ⑻ | **v2.13 — 2-6 설정 표의 「임계값은 `#513`이 별도로 대기 중」 정정.** `#513`은 2026-09-13 닫혔고 임계값 근거 조항은 만들지 않은 채 남았다 — 「대기 중」은 닫힌 이슈를 열린 것처럼 읽게 한다. 조항이 생기면 별도 판정한다는 문구로 바꿨다. 소규모 문구 정정이나 `#513` 참조의 방향을 바꾸는 행이라 버전을 올린다 (#1081) |
| 2026-09-17 | `#1052` | **`§2.2.1` 사이드바 자리 서술 정정 — 두 줄이 모두 「6번」이라 적고 있었다.** `#513`이 `2-11 데이터 점검`과 `2-10 함대 감축 계획`을 같은 날 각각 범위 안으로 옮기면서 변경 이력 두 줄에 **둘 다 「사이드바 6번」**이라 적었다. 나중에 들어온 `2-10`이 앞의 것을 7번으로 밀었으나 **그 서술이 갱신되지 않았다.** `§2.2.1` 표 · `screens.ts`의 `NAV_ORDER` · `screens.test.ts` 가드 **셋은 처음부터 일치**했으므로(6 = 감축 · 7 = 점검) 고친 것은 서술뿐이다. `#1052` 결정 초안이 이 두 줄을 읽고 「사이드바 6번이 겹친다 — 먼저 정하셔야 할 칸」으로 올렸는데, **정할 것이 없었다.** 변경 이력이 정본 표와 어긋나면 그것을 읽는 다음 사람이 없는 결정을 만든다. `AGENTS §4.3` 「오기 정정」이라 버전은 올리지 않는다 (#1052) |
| 2026-09-18 | `#1052` ⓸ | **`2-10` 목표 판정 한 줄의 상태 4종 확정.** ⚠️ **`rlatnals4114`의 2026-09-18 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 구현의 주석은 「상태 분기 3종」이라 적혀 있었는데 분기는 넷이었고, **「선박 없음」과 「아직 조정 안 함」이 같은 톤**을 써 화면에서 가려지지 않았다 — 게다가 그 톤의 CSS 규칙이 **없어** 둘 다 기본 띠로 그려졌다. 둘은 성질이 다르다(「아직」은 조작 이전이라 움직이면 풀리고, 「선박 없음」은 조작할 대상이 없다). 행 추가라 버전은 올리지 않는다(`AGENTS §4.3`) (#1052) |
| 2026-09-20 | `#1360` | **`2-4` 선박 위치 지도 각주의 확대 상한 정정 — 사실 정정이며 디자인 판단이 아니다.** `§2-4`가 「확대 상한은 z10」이라 적고 있었는데 `#985`(2026-09-18)로 자산 구성이 바뀌어 실제 값은 `6`이다(`frontend/src/features/fleet/basemap.ts` `MAX_ZOOM`) — 위치 데이터 정확도가 상한을 받치지 못한다는 디자인 근거(`rlatnals4114` · `#763`)는 그대로 두고, **숫자만 코드 상수를 가리키도록** 고쳐 값이 다시 벌어지지 않게 했다. `399`행(`#763` 변경 이력)은 2026-09-12 당시 기록이라 그대로 둔다. `AGENTS §4.3` 「오기·값 정정」이라 버전은 올리지 않는다 (#1340) |
| 2026-09-21 | `#1491` | **`0 로그인 화면` 행에 `둘러보기` 버튼과 흐름 추가** (`#1486`). 인터뷰·설문 대상자가 `/login?tour=<코드>`로 들어와 가입·로그인 없이 관리자 세션으로 `1-1`에 들어간다. **버튼은 링크에 코드가 있을 때만 보인다** — 버튼의 존재 자체가 기능을 알리므로, 코드 없이 들어온 사람에게는 일반 로그인 화면과 같다. 거절은 같은 화면에 `PRD §6.3` 「둘러보기 — 거절」 문구(꺼짐·불일치를 가르지 않는다). 화면·흐름·진입 조건은 이 문서 소관(`AGENTS §3.2.1`)이고 잠금·응답 형태는 `API_SPEC §1.2`, 시각 표현은 `DESIGN_SYSTEM` 소관이라 적지 않았다. 기존 행의 항목 추가라 버전은 올리지 않는다 (#1486) |
| 2026-09-21 | `#1511` | **`0 로그인 화면` 행의 둘러보기 버튼 노출 조건·진입 결과 정정** (`#1486`). `#1491`이 적은 *「**링크에 코드가 실려 있을 때만 보인다** … 코드 없이 들어오면 버튼 자체가 없다」*가 `#1505` 이후 거짓이 됐다 — 공개 스위치(`TOUR_PUBLIC`)가 켜지면 코드 없이도 상시 버튼이 보인다(`LoginPage.tsx:61` `showTour = tourIsPublic || tourCode !== null`). 진입 결과도 관리자 세션이 아니라 관리자 **열람** 세션이다(쓰기는 `auth/tour_policy.py`가 막는다). **화면 구성·진입 조건은 이 문서 소관**(`AGENTS §3.2.1`)이라 여기서 고친다 — 잠금·응답 형태는 `API_SPEC §1.2`가 같은 PR에서 함께 고쳤다. `AGENTS §4.3`상 오기 정정이라 버전은 올리지 않는다 (#1486) |
| 2026-09-21 | `#1519` | **v2.15 — `UIFLOW.md:338` 「규제 파라미터 조회(`SCR-006`)」를 post-MVP에서 범위 안으로 뒤집고 `2-6 설정`에 「규제 기준값」 절 신설.** `#1239` 결정 A·B·C·D를 정본에 반영했다 — 유예 조건이던 `#673`이 09-18에 닫혔고 `PRD §5.1`(MUST)이 줄곧 살아 있었다(`AGENTS §6.1`). 자리는 사이드바 탭이 아니라 설정 안 절(50행·연 1~2회 개정이라 탭을 정당화하지 않는다) · 접근은 조회 세 역할 모두·개정 적재는 사무직 이상(§2.2 각주 보강) · 이력은 기본 활성분 + 「이전 판본 포함」 전환 · 숫자는 서버 문자열 그대로. 진입 경로 3곳(`2-9` 기준값 옆 · `2-1` 계산 근거 패널 · `2-4`의 `NO_PARAMETERS` 안내)과 `2-4`의 「적용 기준 판본 한 줄」을 각 절에 추가했다. 개정 적재·개정 이력 화면 자체는 `#1517`로 별도다. ⚠️ **화면 배치는 사용자 방향(2026-09-21)이나 디자인 담당(`rlatnals4114`)의 확인이 아직**이라 배치·치수는 개발 임시안이다. `PRD.md:665`·`:668`·`:2350`도 같은 결정으로 함께 정리했다(`PRD` 자체는 각주·값 정정이라 버전 유지). 범위 판정 뒤집기 + 절 신설이라 `AGENTS §4.3`에 따라 버전을 올린다. **PR 번호 확정 전** — 병합 시 이 행의 커밋 열을 실제 PR 번호로 교체한다(`AGENTS §4.1`) (#1239 · #1516) |
| 2026-09-21 | `#1545` | **`2-9 실시간 CII`에 「이 항차 실적 입력」, `2-8 선박 상세`에 `?actuals=` 진입 추가** (`#1540`). 실시간 CII는 현장직의 주 화면이고 연말 예상이 「도착 실적을 입력하면 확정됩니다」라고 말하는데, 입력은 `2-8` 항차 카드 사이에만 있었다. 입력을 `2-9`에 복제하지 않고 `2-8`이 그 항차의 실적 입력을 **열어 둔 채** 카드로 데려간다. 남은 거리가 0이면 채움 버튼, 가는 중이면 보조 링크다. 진행 중 · 완료 항차가 아니면 열지 않는다. `AGENTS §4.3` 「소규모 행 추가」라 버전은 올리지 않는다 (#1540) |
| 2026-09-21 | `#1550` | **`2-11 데이터 점검`의 「이동」을 개발 임시안(선박 상세 맨 위)에서 「이 항차로」로, `2-8`의 `?actuals=` 진입을 모든 상태로 넓혔다** (`#1549`). 항차 행은 `2-8`의 그 항차 카드로 가고 실적 미입력이면 입력이 열린 채 도착한다. 선박 단위 행(계산 불가)은 선박 상세 그대로다. `2-8`은 입력을 열 수 없는 항차도 카드로 데려가 초점·표시 띠를 주고, 그 항차가 받은 목록에 없으면 말한다. `AGENTS §4.3` 「소규모 행 추가 · 정정」이라 버전은 올리지 않는다 (#1549) |
| 2026-09-21 | `#1552` | **`2-8 선박 상세` 항차 카드에 「다음에 누를 것 하나」 규칙 추가** (`#1551`). 카드의 전환 · 실적 입력 · 취소가 모두 같은 외곽선이라, 막힌 전환의 사유가 옆의 「실적 입력」과 이어지지 않았고 완료 카드는 확정과 입력 중 무엇이 먼저인지 말하지 않았다. 정방향 다음 상태로 갈 수 있으면 그 전환, 실적이 모자라 막히면 「실적 입력」이 주 버튼이고, 실적 확정 가드(`API_SPEC §3.5`)는 누르기 전에 사유로 보인다. 취소는 텍스트 버튼이다. `AGENTS §4.3` 「소규모 행 추가」라 버전은 올리지 않는다 (#1551) |
| 2026-09-21 | `#1554` | **`2-3 연간 등급 관리`에 「대상과 조건을 화면에 적는다」 추가** (`#1553`). 대상 선박은 상단바 전역 선택을 따르는데 선박명이 입력에도 결과에도 없었다. 실행 조건 머리에 읽기 전용 대상 선박과 「상단에서 바꿉니다」, 결과 맨 위에 선박 · 기준연도 · 목표 등급 한 줄을 **실행 시점의 값**으로 적는다. `AGENTS §4.3` 「소규모 행 추가」라 버전은 올리지 않는다 (#1553) |
| 2026-09-21 | `#1567` | **`2-9 실시간 CII`에 「연말 예상은 연말 예상 카드 한 곳에서, 등급과 값의 방향을 한 문장으로」 추가** (`#1555`). YTD 카드와 연말 예상 카드가 같은 연말 예상 등급을 두 번 그렸고, 등급의 방향(「등급 유지 예상」)과 값의 방향(「나빠지는 추세」)이 떨어진 두 자리에서 어긋나 읽혔다. `PRD §3.3.8`이 연말 예상을 보조 표시로 정하므로 YTD 카드는 현재 누적 등급만 보인다(`#725`의 전이를 걷었다). YTD 「기준 대비」는 스케일 바 마커 한 곳에만 둔다. `AGENTS §4.3` 「소규모 행 추가」라 버전은 올리지 않는다 (#1555) |
| 2026-09-21 | `#1570` | **`2-4 대시보드` 「상단 경고 배너」 줄 정정 · 카드 규칙 한 줄 추가** (`#1569`). 종전 「위험 선박 척수 · 등급 하락까지 남은 일수」는 두 주제를 한 배너에 두었다 — 남은 일수의 대상은 위험 선박이 아닌 배였다. 배너는 `PRD §6.3` 원문 한 줄, 남은 일수는 선대 요약 행의 「D등급 진입 임박」 칸이다(위험 0척인 날에도 보인다). 이미 D 이하인 카드에는 「D등급까지」 칸을 두지 않고 규제 플래그는 남긴다. `AGENTS §4.3` 「소규모 행 추가 · 정정」이라 버전은 올리지 않는다 (#1569) |
