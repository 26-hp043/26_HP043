"""서비스 레이어 — 비즈니스 로직.

계산 엔진(``calc``)과 저장소(``db.repositories``)를 조합해 하나의 업무 흐름
(예: 항차 CII 추정, 시나리오 비교, 연간 시뮬레이션)을 완성한다. 계산 순서·규칙,
fallback 정책 결정, 상태 전환 검증 등이 여기 위치한다. HTTP(``api``)에는
의존하지 않는다.

계층 규칙은 TECH_SPEC §16 참조.
"""
