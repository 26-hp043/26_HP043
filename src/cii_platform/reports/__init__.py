"""리포트 생성 (PRD §25, #361).

## 한 데이터에서 두 포맷을 만든다

``PRD §25.4``가 *"두 포맷은 같은 데이터에서 각각 렌더링한다. PDF용 수치를 별도로
계산하면 포맷마다 값이 갈린다"* 를 요구한다. 그래서 이 패키지는 세 층이다.

============  ==========================================================
 모듈          역할
============  ==========================================================
``document``  포맷 중립 **문서 모델** — 제목·섹션·표. 수치는 여기서 확정된다
``csv``       문서 모델 → CSV (injection 방어 · UTF-8 BOM)
``html``      문서 모델 → HTML
``pdf``       HTML → PDF (WeasyPrint). **지연 import**
============  ==========================================================

렌더러는 문서 모델을 **읽기만** 한다. 값을 만들지 않으므로 포맷 사이에서 갈릴 수
없다.

## 왜 PDF만 지연 import인가

WeasyPrint는 시스템 라이브러리(Pango)를 필요로 한다. 모듈 최상단에서 import하면
그 라이브러리가 없는 환경에서 **앱 전체가 뜨지 않고** CSV 내보내기까지 막힌다.
지연 import는 PDF 요청 하나만 실패시키고, 그 실패에 설치 안내를 담는다.
"""
