# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Virtual environment
.venv/
venv/

# Environment
.env

# Node / frontend
node_modules/

# Testing
.coverage
htmlcov/

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# AI agent tooling
.omo/

# DB 백업 덤프 (#827) — 운영 데이터 전부(계정·비밀번호 해시 포함)가 들어 있다.
backups/

# 개발 서버 로그 (#429) — 인증 메일 링크가 여기 찍힌다.
# 콘솔 메일 백엔드는 본문을 그대로 남기므로 커밋하지 않는다.
logs/

# vitest 파일 완결성 가드의 산출물 (#828) — 실행마다 새로 만들고 지운다.
frontend/.vitest-report.json

# 지도 자산 (#763 · #1144) — 타일 80 MB + 글리프 12 MB.
# `scripts/fetch_basemap.sh`가 내려받는다. git에 넣으면 clone마다 따라온다.
# 없으면 화면이 개략도로 떨어지므로 저장소에 없어도 동작한다.
frontend/public/basemap/
/basemap/
