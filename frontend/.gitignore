# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# `frontend/.env`는 `VITE_USE_API` 기본값 하나만 담고 있었고 `#542`가 그 변수를
# 없애면서 파일도 함께 지웠다. 루트 `.gitignore`의 `.env` 무시가 그대로 적용된다.
