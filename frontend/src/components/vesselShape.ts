/*
 * 선박 실루엣 경로 — 한 벌만 둔다.
 *
 * 종전에는 `features/fleet/`에 있었다. 선박 관리 화면이 같은 실루엣을 쓰게 되면서
 * **기능 하나가 다른 기능도 쓰는 자산을 소유하는** 모양이 됐다. `.card` 규격이
 * 대시보드 파일에 있다가 카드 제목이 네 갈래로 갈렸던 것과 같은 형태라 미리 옮긴다.
 */
/**
 * 선박 실루엣 경로 — 24×24 그리드 (`#701`).
 *
 * **한 벌만 둔다.** 등급 분포 픽토그램(`VesselGlyph`) · 선박 카드 마크(`VesselMark`) ·
 * 위치 개략도 마커(`PositionChart`)가 같은 모양을 써야 대시보드가 하나의 언어로
 * 읽힌다. 경로를 각자 갖고 있으면 한쪽만 고쳐지는 날이 온다 — `§15.1`이 패턴 자산을
 * 한 벌로 묶어 둔 것과 같은 이유다.
 *
 * 조각이 셋인 것은 채움과 무늬를 **같은 형태로 두 번** 그려야 하기 때문이다
 * (`PositionChart`가 점에 쓰던 방식). 배열로 두면 반복이 한 줄로 끝난다.
 */
export const VESSEL_PATHS: readonly string[] = [
  /** 선체 */
  'M2.5 13.8 H21.5 L18.9 19.4 A1.4 1.4 0 0 1 17.6 20.2 H6.4 A1.4 1.4 0 0 1 5.1 19.4 Z',
  /** 선실 */
  'M8.2 7.4 H14.4 V13.8 H8.2 Z',
  /** 연돌 */
  'M15.6 5.2 H17.6 V13.8 H15.6 Z',
]

/** 경로가 그려진 그리드 한 변. 다른 좌표계에 놓을 때 배율의 분모다. */
export const VESSEL_GRID = 24
