/**
 * 기능② 화면 고지 문구 — `PRD §6.3`.
 *
 * `demoProvider.ts`에 있던 것을 옮겼다 (#542). 데모 데이터와 무관하게 **실 API
 * 결과에도 붙는 문구**인데 demo provider 파일에 있어서, 데모 폐기와 함께 사라질
 * 뻔했다. 문구의 출처는 `PRD §6.3` 표이며 그 행을 그대로 복사한다.
 */

/** `PRD §6.3` — 「자동 결정 금지」 행의 문구를 그대로 복사했다. */
export const NO_AUTO_DECISION_NOTICE =
  '시스템은 시나리오별 수치만 비교하며, 최종 운항 판단은 사용자에게 있습니다.'

/** `PRD §6.3` — 「추정값 사용」 행. */
export const ESTIMATE_NOTICE = '일부 값은 사용자 입력 또는 모델 추정값입니다.'
