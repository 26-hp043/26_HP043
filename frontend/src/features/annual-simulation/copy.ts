/**
 * 기능③ 화면 문구 (#157).
 *
 * ## 왜 문구를 모듈로 빼는가
 *
 * **이 이슈의 가장 큰 위험이 「근거 없는 표현」이었다.** 실제 누적 데이터도 Monte Carlo
 * 결과도 없는 상태에서 「연말 예상 등급」 같은 문구를 띄우면 **없는 근거를 있는 것처럼
 * 보이게 한다.** 당시(`#157`)에는 그랬고, 그 근거는 `#749`에서 걷혔다 — 아래 표 참고.
 *
 * 문구가 JSX에 흩어져 있으면 금지를 테스트로 확인할 수 없다. 여기 모아 두면
 * `copy.test.ts`가 전수 검사한다.
 */

/**
 * 화면에 쓰면 안 되는 표현.
 *
 * | 금지 | 이유 |
 * |---|---|
 * | `추천` | `PRD §6.3` 「자동 결정 금지」 · `§11.2` 「추천 시나리오를 표시하지 않는다」 |
 *
 * ## `연말`·`예상 등급`·`누적 기준 예상`을 금지 목록에서 뺐다 (#749)
 *
 * 셋 다 **근거가 사라졌고, 남겨 두면 정본이 시키는 표기를 막는다.**
 *
 * | 낱말 | 종전 근거 | 지금 |
 * |---|---|---|
 * | `연말` | 기능③ 엔진이 없다(`#63`이 `2026.10`) | `#63`이 2026-08-17 종료. `PRD §12.7` 출력 표가 「연말 예상 CII」·「연말 예상 등급」을 결정론 결과로 둔다 |
 * | `예상 등급` | 등급은 「참고 등급」이다(`#136`) | `#136`은 **기능①** 결과 화면 이슈였고, 근거도 「기능③과 누적 데이터가 **없으므로**」였다 — 둘 다 생겼다. 기능①의 「참고 등급」은 `PRD §10.4`가 따로 정한다 |
 * | `누적 기준 예상` | 실제 누적 데이터가 없다 | YTD 엔진(`#353`)이 있다 |
 *
 * 그리고 `PRD COR-2`가 기능①·③의 연중 화면에 **「현재 누적 기준 예상 등급」 또는
 * 「연말 예상 등급」으로 표기하라**고 처방한다 — 세 낱말이 전부 그 처방 안에 있다.
 * 실시간 CII 화면(`RealtimeCiiView.tsx`)은 이미 그 표기를 쓰고, 기능③의 경고
 * `NO_REMAINING_VOYAGES`(「… 연말 예상 등급을 산출했습니다」)도 그 표기다.
 *
 * 금지를 푼 것이지 쓰라는 것은 아니다 — 이 화면의 등급 라벨(`projectedRatingLabel`)은
 * 그대로 두었다.
 *
 * ## `P(D` 를 금지 목록에서 뺐다 (#442)
 *
 * v1은 「`P(D∪E)`의 계산 정의가 `PRD`에 없다(`#170` ⑶)」를 근거로 금지했다. **그 근거가
 * 해소됐다** — `#339`(2026-08-14)가 `PRD §12.5`에 정의를 넣었고(`P(D∪E) = P(D) + P(E)`,
 * 여사건 조건 포함) `#170`은 CLOSED다. 그리고 `DESIGN_SYSTEM §2.5 (a)`가 **`⚠ P(D/E) 28%`
 * 표기를 규정**하므로, 금지를 유지하면 정본이 시키는 표기를 쓸 수 없다.
 *
 * 실제 표기는 `annualRules.riskFlag`가 만든다(문구가 아니라 값에서 파생되는 표시).
 */
export const FORBIDDEN_PHRASES = ['추천'] as const

/** 화면 문구. 여기 있는 값만 화면에 쓴다. */
export const ANNUAL_COPY = {
  /*
   * 화면 제목 아래 한 문장. **제목이 아니다** — 제목은 `screens.ts`가 소유하고
   * `PageHeader`가 그린다(사이드바가 부르는 이름과 같아야 한다).
   *
   * 종전 값은 `연간 CII 시뮬레이션`이었다. 그것은 **두 번째 이름**이라 소제목으로
   * 읽혔고, 다른 화면의 부제(설명 문장)와 성격이 달랐다.
   *
   * **문구를 여기 두는 이유가 있다.** 위 `FORBIDDEN_PHRASES`가 `ANNUAL_COPY`를
   * 전수 검사하므로, 이 자리에 두면 금지 표현이 부제로 새어 나가는 것을 테스트가
   * 막는다. 초안 「연말 등급 도달 확률」이 당시 금지 목록에 걸렸다 — 「연말」은
   * `#749`에서 금지가 풀렸지만 부제를 여기 두는 이유는 그대로다.
   */
  lead: '목표 등급 달성 확률을 반복 시뮬레이션으로 추정합니다.',

  /** 실제 계산 결과가 아님을 화면에서 구분 가능하게 한다(`#157` 완료 기준). */
  sampleBadge: '예시 데이터',
  sampleNotice:
    '이 화면은 고정된 예시 데이터로 만든 목업입니다. 실제 계산 결과가 아닙니다.',

  /**
   * `DESIGN_SYSTEM §11` — 전면 추정 화면이므로 개별 표기 대신 화면 단위 고지로 갈음한다.
   * 그 고지는 **추정 성격과 기준 시각**을 담아야 한다(§11 「전면 추정 화면의 범위」).
   *
   * 종전 문장 「…산출한 예측값입니다. 실측이 아닙니다.」는 기준 시각이 없었고, 「예측값」이
   * 하단 면책 배너(`PRD §6.3` 「참고용 예측값입니다」)와 같은 말이었다 (#1578). 추정의
   * **전제**를 말하고 기준 시각을 잇는다 — CII 예측(`VoyageCiiResult`)의 고지와 같은 틀이다.
   */
  estimateNotice: '이 결과의 수치는 모두 잔여 계획을 전제로 한 추정값입니다.',
  /** `{time}`은 응답 `meta.as_of`(`API_SPEC §6.1` · `#816`) — 집계에 실제로 쓴 시각 */
  estimateAsOf: '기준 시각은 {time}입니다.',

  /* ── 실행 입력 ─────────────────────────────────────────────────────── */
  runTitle: '실행 조건',
  /*
   * 대상 선박 (#1553). 선박은 상단바 전역 선택을 따르는데 **이름이 입력에도 결과에도
   * 없어** 결과 카드만 보고는 어느 배의 숫자인지 알 수 없었다.
   */
  targetVesselLabel: '대상 선박',
  targetVesselHint: '상단에서 바꿉니다.',
  targetVesselNone: '선택되지 않음',
  targetVesselLoading: '불러오는 중…',
  /** 목록에 이름이 없다 — 실패했거나, 목록에 없는 배를 주소로 골랐다. id를 내보이지 않는다 */
  targetVesselUnknown: '이름을 불러오지 못한 선박',
  /** 결과 머리 한 줄의 앞머리 — 뒤에 「선박 · 2026년 · 목표 등급 C」가 붙는다 */
  resultConditionsLabel: '이 결과의 조건',
  targetRatingLabel: '목표 등급',
  /** `PRD §12.8` — E는 목록에 두지 않는다. 「달성」이 의미를 잃는다 */
  targetRatingHint: 'A~D 중에서 고릅니다.',
  runsLabel: '반복 횟수',
  runsHint: '1,000~10,000회. 많을수록 분포가 안정되고 오래 걸립니다.',
  /*
   * 반복 횟수 검증 문구 (#1096 ⑴). 종전에는 `step={1000}`이 붙어 **2,500 같은 값이
   * 브라우저 기본 툴팁으로만 막혔고 화면 오류 자리는 비어 있었다** — 서버는
   * `ge=1000`이라 2,500을 받는다. 화면이 서버와 같은 규칙으로 검증하고 그 결과를
   * 이 자리에 적는다.
   */
  runsNotInteger: '반복 횟수는 정수로 입력해 주세요.',
  runsBelowMin: '반복 횟수는 1,000 이상이어야 합니다.',
  runsAboveMax: '반복 횟수는 10,000 이하여야 합니다.',
  seedLabel: 'seed (선택)',
  /*
   * 고급 설정 접기 (#1418). 반복 횟수·seed·실적 보정·대체 연료는 모두 기본값이 있어 비워도
   * 실행된다. 접힌 채로 기본값이 아닌 값이 있으면 요약에 그 수를 적는다 — 보이지 않는 칸이
   * 결과를 바꾸고 있다는 것을 접은 쪽이 말해야 한다.
   */
  advancedTitle: '고급 설정',
  advancedDefault: '기본값으로 실행',
  // 앞에 바꾼 칸 수가 붙는다 — 「2개 바꿈」. 문구는 문자열로만 둔다(`copy.test.ts`가 전수 검사한다).
  advancedChangedSuffix: '개 바꿈',
  // 칸 안(placeholder)에 들어가는 문장이라 짧게 (#1418 화면 확인). 낭독에도 같은 문장이 남는다.
  seedHint: '비우면 서버가 정함 · 같은 seed면 같은 결과',
  submit: '시뮬레이션 실행',
  submitting: '실행 중입니다…',

  /* ── 결정론 (PRD §12.3) ────────────────────────────────────────────── */
  deterministicTitle: '결정론 예측',
  deterministicCaption:
    '누적 실적과 잔여 계획을 그대로 더해 계산합니다. 같은 입력이면 항상 같은 값입니다.',
  projectedCiiLabel: '예측 누적 CII',
  projectedRatingLabel: '참고 등급',
  completedLabel: '집계된 실적 항차',
  remainingLabel: '남은 계획 항차',

  /* ── 확률 (PRD §12.4 · §12.5) ──────────────────────────────────────── */
  probabilityTitle: '등급 확률 분포',
  probabilityCaption: '잔여 계획의 변동을 반복 추출해 얻은 분포입니다.',
  targetSuccessLabel: '목표 달성 확률',
  targetSuccessHint: '목표 등급 이상을 받을 확률입니다.',
  riskLabel: '위험도',
  spreadTitle: '분포 요약',
  p10Label: '하위 10%',
  p50Label: '중앙값',
  p90Label: '상위 10%',
  meanLabel: '평균',

  /* ── 민감도 (PRD §12.6) ────────────────────────────────────────────── */
  sensitivityTitle: '민감도',
  columnVariable: '변수',
  columnProjectedCii: '예측 CII',
  columnRatingChange: '등급 변화',
  columnProbabilityChange: '달성 확률 변화',
  /*
   * ⚠️ **잔여 계획이 없으면 민감도 표 전체가 무의미하다** (`#756` · 2026-09-13 화면 실측).
   *
   * 지렛대는 **잔여 계획 항차를 움직여** 결과를 다시 낸다. 그 항차가 0건이면 무엇을
   * 바꿔도 확정 실적만 남아 **여섯 행이 전부 기준과 같은 값**이 된다. 그때 거리 행
   * 설명만 띄우면 **나머지 행은 의미가 있는 것처럼 읽힌다.**
   *
   * 응답에 `NO_REMAINING_VOYAGES` 경고가 이미 실려 있다 — 화면이 **이미 아는 사실**을
   * 이 자리에서 말하지 않고 있었을 뿐이다. `#630`(속도 건너뜀)과 같은 처리다.
   */
  sensitivityNoRemainingNote:
    '예정된 잔여 항차가 없어 모든 변수의 효과가 0으로 나옵니다. 이 표는 남은 계획을 바꿔 보는 것이므로, 항차를 등록하면 값이 달라집니다.',

  /*
   * 거리 행이 기준과 같아 보이는 이유 (`PRD §12.6` 각주 · #756). 말하지 않으면 「효과
   * 없음」으로 읽힌다 — 기능②의 같은 성질에 붙인 문구(`ScenarioComparison.tsx`, #739)와
   * 같은 설명이다.
   *
   * ⚠️ **「거의 변하지 않습니다」로 적지 않는다.** 그것은 **조건부** 사실이다 — 잔여
   * 계획이 지금까지의 실적과 **같은 강도**로 돌 때만 값이 같고(그때는 정확히 같다),
   * 실적이 계획에서 벌어지면 이 행은 실제로 움직인다. 무조건으로 적으면 움직이는 날
   * 화면이 사실과 다른 말을 한다.
   */
  distanceNote:
    '거리 ±5%는 연료도 같은 비율로 함께 바꿔 계산합니다. 그래서 이 행은 거리 자체가 아니라, 남은 계획이 지금까지의 실적과 얼마나 다른 강도로 도는지를 보여 줍니다 — 강도가 같으면 값이 그대로이고, 실적이 계획과 벌어질수록 이 행도 움직입니다.',

  /* ── 필요 감축량 (PRD §12.3.1 · #433) ───────────────────────────── */
  reductionTitle: '목표까지 줄여야 하는 양',
  /* `UIFLOW 2-3` 「함대 단위로 보기」 — `2-10` 진입점 (#513). */
  fleetLink: '함대 단위로 보기',

  /*
   * ⚠️ **「무엇을 고정했는지」를 말한다.** 부등식의 미지수가 둘(잔여 연료·잔여 거리)이라
   * 무엇을 고정하느냐가 곧 산출물을 정한다(`PRD §12.3.1`). 거리를 고정했다는 사실을
   * 적지 않으면 「항차를 줄여도 되는 것 아닌가」로 읽힌다 — 그 경우 값이 달라진다.
   */
  reductionCaption:
    '남은 계획의 거리는 그대로 두고, 연료만 줄이는 경우입니다. 항차를 취소하는 경우는 아래 민감도 표의 「잔여 항차 1개 취소」가 보여 줍니다.',

  reductionTargetLabel: '목표 등급',
  reductionBoundaryLabel: '목표 등급 경계',
  reductionCutLabel: '줄여야 하는 CO₂',
  reductionCutFuelLabel: '연료로 환산하면',

  /* 이미 목표 안에 있을 때. 0을 그냥 보이면 「계산이 안 됐나」로 읽힌다. */
  reductionNoneNeeded: '지금 계획대로 가면 목표 등급 안에 듭니다. 줄일 것이 없습니다.',

  /*
   * ⚠️ **잔여 계획이 없는 것**과 **줄일 것이 없는 것**은 다르다. 둘을 같은 문구로
   * 두면 사용자가 「이미 목표 안이다」로 읽는데, 실제로는 **계산할 대상이 없는 것**이다.
   */
  reductionNoPlan:
    '남은 계획 항차가 없어 줄일 대상이 없습니다. 항차를 등록하면 여기에 필요한 감축량이 나옵니다.',

  /*
   * ⚠️ **잔여 계획을 전부 없애도 목표에 닿지 못하는 경우.** 이때 「n톤 줄이세요」는
   * 거짓이 된다 — 확정 실적만으로 이미 넘겼기 때문이다. 그 사실을 말하지 않으면
   * 사용자가 달성 가능한 줄 알고 계획을 짠다.
   */
  reductionUnreachable:
    '남은 계획을 모두 없애도 이 목표에는 닿지 않습니다. 이미 확정된 실적만으로 목표 등급의 경계를 넘었습니다.',

  /* ── 실적 보정계수 (PRD §12.2.1 · #363) ──────────────────────────── */
  feedbackToggle: '지난 항차 실적으로 남은 계획 연료 보정',
  /*
   * 켜기 전에 **무엇이 바뀌는지**를 말한다. 거리는 그대로라는 사실을 적지 않으면
   * 「남은 항차가 늘어나나」로 읽힌다.
   */
  feedbackToggleHint:
    '확정 항차에서 계획 대비 실제로 쓴 연료의 비율을 구해 남은 계획의 연료에 곱합니다. 거리는 그대로입니다.',
  /* ── 대체 연료 지렛대 (PRD §12.6 · #756 ⑴ — 질량 유지) ─────────── */
  alternativeFuelLabel: '대체 연료 민감도',
  alternativeFuelHint:
    '남은 계획 전체의 배출계수를 이 연료로 바꿔 연말 값을 다시 냅니다. 연료량은 그대로입니다.',
  alternativeFuelNone: '사용 안 함',
  feedbackTitle: '실적 보정계수',
  feedbackCaption: '확정 항차의 연료 강도(연료 ÷ 거리)를 계획과 실적으로 나눈 비율입니다.',
  feedbackFactorLabel: '계획 대비 실적',
  feedbackSampleLabel: '계산에 쓴 확정 항차',
  feedbackApplied: '이번 실행은 남은 계획 연료에 이 비율을 곱해 계산했습니다.',
  /* 켜지 않은 경우 — 값은 보이되 결과에 반영되지 않았음을 밝힌다. */
  feedbackNotApplied: '이번 실행에는 반영하지 않았습니다. 실행 조건에서 켜면 남은 계획 연료에 곱합니다.',
  /*
   * ⚠️ **계산할 수 없는 것**과 **효과가 없는 것(1.0)**은 다르다. 표본이 모자랄 때
   * 1.0이나 빈칸으로 두면 「계획대로 쓰고 있다」로 읽힌다.
   */
  feedbackUnavailable:
    '계획·실적 연료와 거리가 모두 있는 확정 항차가 모자라 비율을 구하지 않았습니다. 확정 항차가 쌓이면 계산합니다.',
  feedbackUnavailableValue: '계산하지 않음',
  feedbackMinSampleHint: '필요한 최소 항차',

  /* ── 재현성 (TECH_SPEC §5.2 · §11) ─────────────────────────────────── */
  reproTitle: '재현 정보',
  reproCaption: '같은 조건으로 다시 실행할 때 필요한 값입니다.',
  snapshotLabel: '데이터 스냅샷',
  snapshotHint: '실행 시점의 항차 데이터를 따로 보관합니다.',
  runIdLabel: '계산 이력',
  /* 스냅샷·계산 이력 식별자와 항차 사본을 접는다 (#1418). seed 줄과 재현 버튼은 밖에 둔다. */
  reproDetailsToggle: '계산 근거 보기',
  /* 이 실행에 쓴 항차 (`API_SPEC §6.3` · #992) — 펼칠 때 불러온다. */
  snapshotVoyagesToggle: '이 실행에 쓴 항차 보기',
  snapshotVoyagesLoading: '항차를 불러오는 중입니다…',
  snapshotVoyagesCaption: '실행 시점에 보관한 사본입니다. 그 뒤에 항차를 고쳐도 이 목록은 바뀌지 않습니다.',
  snapshotVoyagesErrorSubject: '이 실행에 쓴 항차',
  // `PRD §12.4.3` 「결과 재현 버튼」의 원문이다(#776). 버튼 문구를 바꾸면 정본과 갈린다.
  reproduceButton: '이 seed로 다시 실행',
  reproducing: '같은 조건으로 다시 계산하는 중입니다…',
  reproduceSuccess: '원본과 같은 결과가 나왔습니다. 같은 스냅샷·seed·규정 파라미터로 다시 계산해 확인했습니다.',
  // 제목은 `ErrorState`가 「{동작}에 실패했습니다」로 짓는다(`PRD §6.4` · 2026-09-11 확정 B).
  reproduceErrorAction: '재현',
  reproduceErrorFallback: '같은 결과를 재현하지 못했습니다.',

  loading: '시뮬레이션을 실행하는 중입니다…',
  empty: '실행 조건을 고르고 실행해 주세요.',
  /*
   * 실행 **전**에 막히는 세 상태 (#1096 ⑸). 종전에는 오류 상태로 넣어 「시뮬레이션에
   * 실패했습니다」 제목 아래 나왔다 — 실행한 적이 없으니 실패가 아니다. 안내로 낸다.
   * 「없음」의 종류가 다르다: 선박은 **아직 고르지 않은 것**, 연도 목록은 **아직 오지
   * 않은 것**과 **불러오지 못한 것**.
   */
  needVessel: '상단에서 선박을 먼저 선택해 주세요.',
  yearsPending: '규제연도 목록을 불러오는 중입니다. 목록이 오면 실행할 수 있습니다.',
  yearsUnavailable:
    '규제연도 목록을 불러오지 못해 실행할 수 없습니다. 잠시 후 다시 시도해 주세요.',
  errorAction: '시뮬레이션',
  errorFallback: '시뮬레이션을 실행하지 못했습니다.',
} as const
