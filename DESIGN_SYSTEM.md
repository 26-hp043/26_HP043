# DESIGN_SYSTEM — BlueLog

| 항목 | 내용 |
|---|---|
| 문서명 | DESIGN_SYSTEM.md |
| 버전 | v2.20 |
| 작성 | **rlatnals4114** (디자인) — 본문 §0~§16 · **2026-09-10 확정 12건**(`§16` 항목 1·2·3·5·8·13 종결, `§5` 표기, `§12` 아이콘 세트, `§15` `--brand-*`)의 **결정자** · **2026-09-11 확정**(`§5` 정의·오버레이·표기 · `§8` 슬라이더 · `§8.3` · `§4.2` 완결성 · `§2.1`·`§15` 정정)의 **결정자**. 개발은 그 결정을 반영했을 뿐 값·규격을 정하지 않았다 |
| 상태 | **v2.20 — §8 텍스트 버튼 높이 〔제안〕 → 〔확정〕.** 쓰는 자리가 넷이 되자 이웃이 제각각이라 값 하나로 정할 수 없었다 — 상단바 40 / 32, 항차 카드 24 / 26. **같은 줄의 컨트롤과 같다 · 혼자면 32px · 24px 밑 금지**로 정했다. (`rlatnals4114`) <br><br> **v2.19 — §14 「채움 컨트롤의 면」에 테두리 갈래 확정.** 다크 채움 Primary가 주변과 `2.35`~`2.78`인데, 면을 띄우면 그 위 글자가 `4.5` 밑으로 내려가 **한 면으로는 두 기준을 함께 넘을 수 없었다.** 경계를 1px 테두리(`--semantic-primary`)가 맡는다. (`rlatnals4114`) <br><br> **v2.18 — §2.5 (b) 🔒 위험도 단계별 색 · 아이콘 색 확정.** 「Warning · Danger만 쓴다」까지만 있고 **어느 단계가 어느 색인지가 없어** 화면마다 갈렸다 — 기능①·시나리오 비교는 HIGH Warning / CRITICAL Danger, 기능②는 전부 Danger. HIGH = Warning · CRITICAL = Danger, 아이콘은 Warning 하나로 확정했다. (`rlatnals4114`) <br><br> **v2.17 — §3 🔒 「영문 약어 병기」의 범위와 예외 확정.** 한/EN 토글(`#1215`) 뒤에도 규칙이 「약어 병기」 한 줄뿐이라, 화면에 붙어 있던 일반 영단어 47곳(`#1426`)이 규칙 위반인지 아닌지를 읽을 수 없었다. 약어는 라벨 안에 쓰고, 같은 뜻 영단어는 나란히 적지 않으며, 위험도 4단계만 `risk_level` 값과 대조하는 열쇠로 병기한다. (`rlatnals4114`) <br><br> **v2.16 — §7.1 🔒 외곽 여백 24 · 산술 `24+240+24+1608+24=1920` · §7.2 🔒 사이드바 아이콘 허용.** 산술이 외곽 여백 0을 전제해 같은 문서 `§6`(24)·구현(24)·Figma(옛 값 1680·32)와 네 갈래로 어긋나 있었다. 사이드바는 「7개 이하라 아이콘 없이」였으나 항목이 9개가 됐고 구현은 아이콘을 쓴다. (`rlatnals4114`) <br><br> **v2.15 — §14 「비활성 컨트롤은 「왜」를 함께 낸다」에 예외 「누르면 사유를 내는 버튼」 확정.** 선박 상세의 실시간 CII 입구를 페이지 머리로 올리자, 곁에 늘 그려 둔 사유가 **상자를 세 줄로 키우거나 버튼과 따로 놀았다.** 조건 셋을 갖춘 자리에 한해 누르면 말풍선으로 사유를 낸다. (`rlatnals4114`) <br><br> **v2.14 — §14 「경계와 장식을 가른다」 확정.** `1.4.11`의 `3:1`을 **어디에 거는지**가 없어 같은 자리가 세 번 열렸다(`#829` ⑶ 폼 · `#1170` ⑴ 배지 · `#1202`). 갈래를 넷으로 갈라 컨트롤의 경계·채움면에만 걸고, 장식 띠와 비활성은 대상에서 뺐다. 범위는 **토큰 이름이 아니라 역할**로 잡는다. (`rlatnals4114`) <br><br> **v2.13 — §12 아이콘 규격값의 출처를 Figma로 일원화.** 조항이 `strokeWidth={1.5}`를 **문자 그대로 지시**하고 있어, 같은 규격이 생성 토큰과 코드에 한 벌씩 있었다 — Figma에서 두께를 바꾸면 커스텀 아이콘만 따라가고 세트 아이콘은 옛 값으로 남는다. 의도(호출부마다 적지 않는다)는 두고 출처만 옮겼고, `§15` 네이밍 목록에 빠져 있던 `--icon-*`을 넣었다. (`rlatnals4114`) <br><br> **v2.12 — §14 「비활성 컨트롤은 「왜」를 함께 낸다」 확정.** `disabled`는 요소를 **초점 순서에서 빼므로**, 버튼이 왜 잠겼는지는커녕 **있다는 사실조차** 낭독에서 사라진다. 속성을 바꾸는 대신 사유를 화면과 낭독 양쪽에 두기로 확정했다 — `aria-disabled`로 옮기면 `:disabled` 규칙 51개가 화면에서 함께 빠진다. 초점에 남길지는 `§16` 항목 20으로 남긴다. (`rlatnals4114`) <br><br> **v2.11 — §8.5 어시스턴트 오버레이 「답이 아닌 것」의 표시 신설.** 버린 답·호출 실패·면책 셋이 **배경·줄무늬·글자색까지 같아** 가를 수 없었다. 버린 답은 줄무늬 + 접두 문구 **채널 둘**로, 면책은 줄무늬를 걷어 신호를 되돌린다. 치수·겹침 순서·문구 소유는 실험 단계라 `§16` 항목 16~18로 남긴다. (`rlatnals4114`) <br><br> **v2.10 — §8.4 폼 필드·필드 오류 신설.** 확정 1-1이 필드를 폼 규격으로 이관했는데 **그 폼 규격이 없어** 화면마다 갈렸다 — `Field` 컴포넌트 4개 · 오류 클래스 16종 · `role="alert"` 누락 5곳. `Field` 하나로 닫고 낭독 배선을 컴포넌트가 강제하게 한다. (`rlatnals4114`) <br><br> **v2.9 — §6/§7.1 gap 충돌 해소.** §6이 패널 간 gap(24)을 소유하고 §7.1은 참조로 전환. 🔒 그리드 표를 `#707` 패널 셸 배치에 맞게 갱신: `240+24+1656=1920`. 기존 값 32는 사이드바 부착 전제였으므로 폐기. (`rlatnals4114`) <br><br> **v2.8 — rlatnals4114 확정 문서(2026-09-11) 반영.** `§5`에 〔확정〕/〔제안〕 정의 · 오버레이 두 값과 「그림자 + 테두리」 규정 · Light 그림자 표기 통일 · `§8` 슬라이더 확정 3줄과 〔제안〕 치수 · **`§8.3` 등급 전이 표기 신설** · `§4.2` 데이터 완결성 조건 · `§2.1`·`§15` Primary Hover 오기 정정 · `§15` 브랜드 판 「테마 불변」 문안 정정. **확정 E(포커스 링)·G(다크 판 비율)는 확정의 기준 커밋 이후 코드가 바뀌어 재확인을 요청했다** <br><br> **v2.7 — 확정 3-2·3-3을 정본에 기재.** 「세로형 로고를 만들지 않는다」와 「1100px 이하에서 판을 접는다」가 **확정인데 어느 절에도 없어** 다시 열릴 자리로 남아 있었다. `§8`에 `BrandLogo` 항목을 신설하고 `§7.2`에 반응형 규칙을 넣었다 <br><br> **v2.6 — rlatnals4114 확정 문서(2026-09-10) 반영.** `§16` 미확정 6항목을 닫고 `§0.2` 제약 6 신설 · `§5` 전 항목 〔확정〕/〔제안〕 표기 + `--shadow-overlay` · `§12` Lucide · `§15` `--brand-*` 채널 신설. **12곳 개정 전부가 결정자의 확정이며 개발 판단이 아니다** <br><br> **v2.5 — §4.2에 용량(DWT·GT) 자릿수·구분자 신설.** 규정이 없어 같은 DWT 값이 선박 관리 목록과 선박 상세에서 다르게 나오고 있었다 <br><br> **v2.4 — §8.2 CII 적용 대상 배지 신설.** 적용 대상 여부를 보여 주는 화면이 선박 등록 결과 한 곳뿐이라, 등록 이후에는 규제 대상이 아닌 선박도 등급·값만 보였다 <br><br> **v2.3 — §4.2에 일수·속력 자릿수 신설.** 규정이 없어 화면이 `231.64 일`을 내고 있었고, 속력은 규정 부재를 이유로 두 곳이 표시를 보류 중이었다 <br><br> **v2.2 — `UIFLOW` 끊긴 참조 정리 · §8.1 신뢰도 배지 표시 임계 신설.** 존재한 적 없는 `UIFLOW` 절을 가리키던 참조 6곳을 정정·삭제하고, `#485` ⑤를 막고 있던 표시 임계를 본 문서로 가져왔다(층위는 `PRD §8.3` 참조) <br><br> **v2.1 — 신규 화면 2종 확정 반영.** 신규 화면 2종 승인에 따라 v2.0의 잠정 항목 3건(심각도 색·사이드바·슬라이더)을 닫고, 라우트 중복 정의를 제거해 `UIFLOW §2`·`§2.2` 참조로 전환 · 심각도 명칭을 `PRD §8.3` 값 우선순위 기준으로 정정 <br><br> **v2.0 — 대시보드 재설계 반영 개정.** 등급 램프를 5색 → **3색 체계**로 전환(§2.4) · 등급 토큰을 등급당 2개 → **4개**로 확장(§15) · **다크 모드 신설**(§2, §15) · §0.2 제약 2를 채널 분리 규칙으로 재정의 · 폰트를 Noto Sans KR로 변경(§3) · 12컬럼 그리드 명문화(§7.1) · §16 항목 8 빈 상태 부분 해소 |
| 최종 수정일 | 2026-09-22 |
| 상위 문서 | `PRD.md` v4.4 — `AGENTS §4.4` 「마지막으로 대조를 마친 판본」 |
| 후속 문서 | 프론트엔드 구현 (#133 · #136) |
| 문서 목적 | 토큰의 **이름·의미·제약**을 확정한다. 값(hex·radius·그림자 수치)은 Figma가 소유하며 §0.2의 제약을 위반할 수 없다 |

중소선사를 위한 CII 예측 및 운항 의사결정 보조 플랫폼 — 디자인 토큰 계약서

---

## 0. 이 문서를 읽는 법

### 0.1 마커 범례

| 마커 | 의미 |
|---|---|
| 🔒 | 확정. 변경하려면 본 문서를 개정해야 함 |
| 💡 | 확정. 값 조정은 가능하나 §0.2의 제약을 위반할 수 없음 |
| ⚠️ | 미확정. §16에 담당·기한 표기 |

### 0.2 소유권과 우선순위

이 문서와 Figma는 서로 다른 것을 소유한다.

| 소유 주체 | 소유 대상 |
|---|---|
| **본 문서** | 토큰 **이름**, 토큰의 **의미**, 그리고 아래 **제약** |
| **Figma** | 위 제약 안에서의 **값** (hex, radius, 그림자 수치 등) |

**제약 (Figma가 위반할 수 없음)**

1. 텍스트 대비 ≥ 4.5:1 (WCAG 2.1 AA) — **Light·Dark 두 모드 모두에서**
2. **등급 색과 시맨틱 색의 분리** — 아래 세 조건을 모두 만족한다
   - 두 체계는 **동일한 hex 값을 공유하지 않는다**
   - 등급 색은 **항상 A~E 문자 또는 등급 축 라벨과 함께** 나타난다
   - 시맨틱 색은 **항상 아이콘 또는 상태 문구와 함께** 나타난다
   - 두 체계를 같은 컴포넌트 안에서 동시에 색으로 쓰지 않는다
3. 색만으로 의미를 전달하지 않는다 — 반드시 문자·패턴·마커 중 하나를 병행한다
4. 시나리오 **계열(series)** 색은 한색(cool) 계열로 한정한다 (§9.3)
5. 모든 컬러 토큰은 **Light·Dark 두 값을 함께** 정의한다. 한쪽만 정의된 컬러 토큰은 허용하지 않는다
   - 테마에 따라 바뀌지 않는 것이 옳은 토큰(브랜드 색 등)은 **두 값을 같게 두되 「테마 불변」을 명시적으로 기재**한다. 누락과 결정을 구분하기 위해서다
6. 문자 토큰의 대비는 **그 토큰이 놓일 수 있는 가장 어두운 표면**을 기준으로 측정한다. 흰 카드 기준값을 근거로 쓰지 않는다
   - `§16` 항목 1이 여태 닫히지 않은 이유가 이것이다. 대안으로 적힌 `#647380`의 `4.86:1`은 **흰 카드 위** 값이었고, 실제로는 `--surface-page` 위 `4.50`(경계) · `--surface-inset` 위 **`4.34` 미달**이었다
   - `tokens.sync.test.ts`가 (모든 표면 × 모든 문자 토큰) 조합으로 검사한다

> **제약 2 개정 이력 (v2.0)** — v1.2까지 「서로 겹치지 않는다」로만 적혀 있었고, 실제로는 E등급과 Danger가 동일한 `#c53030`이어서 문서가 스스로 정한 제약을 위반하고 있었다. 등급 체계가 3색(초록·주황·빨강)으로 바뀌면서 색상군 분리는 불가능해졌고 바람직하지도 않다 — 등급의 좋음·주의·나쁨이 시맨틱과 같은 직관을 쓰는 편이 맞다. 따라서 제약의 목적을 「색상군 분리」가 아니라 「두 정보의 혼동 방지」로 다시 쓰고, **동일 값 금지 + 채널 분리**로 구체화했다.

**충돌 시**: 제약이 우선한다. Figma 값이 제약을 위반하면 값을 수정한다.

**연결 규칙**: Figma Variables 이름은 §15의 CSS 커스텀 프로퍼티 이름과 1:1로 대응한다. 이름이 어긋나면 매 릴리스마다 수동 매핑이 발생하고 그 시점부터 드리프트가 시작된다.

> **정본은 동작과 제약을 적는다. 구현의 속성·클래스·함수 이름을 적지 않는다** [2026-09-12 확정 O].
>
> 위 표가 소유를 「토큰의 **이름**·의미·제약」으로 정의했는데, `§2.4.4`가 React 속성 이름(`showPattern`)을 적고 있었다. **정본이 구현 세부를 이름으로 붙잡으면 코드가 스스로 정리할 수 없게 된다** — 지우면 정본이 깨지고, 두면 쓰지 않는 속성이 남는다. 실제로 그 교착이 `#831` ⑹을 한 달 가까이 열어 두었다.
>
> 토큰 이름은 예외다. 그것은 이 문서가 소유한다고 위 표가 정한 것이다.

### 0.3 상호 참조

본 문서는 아래 문서를 참조하며, 중복 정의하지 않는다.

| 항목 | 소유 문서 |
|---|---|
| 화면 목록·계층·흐름·진입 조건 | `UIFLOW §2` |
| 면책·경고·UX 문구 원문 | `PRD §6.3` |
| 항차 값 우선순위 (실측/대체 판정) | `PRD §8.3` |
| 기능 요구사항·계산식·임계값 | `PRD` |
| 접근성 백로그 | 백엔드 이슈 #68 |

**본 문서는 위 항목을 중복 정의하지 않는다.** 특히 **라우트를 본 문서에 적지 않는다** — 라우트는 구현 소관이며, v2.0에서 §7.2에 라우트 표를 두었던 것은 중복 정의였다. v2.1에서 제거했다.

---

## 1. 개요 · 디자인 원칙

- **제품**: 중소선사용 CII 예측·운항 의사결정 보조 플랫폼
- **원칙**
  1. 가볍고 직관적
  2. 신뢰감 (해운·규제 도메인)
  3. 데이터를 한눈에 — 패널 간 유기적 연동
- **사용자별 톤**
  - 운항관리팀 — 모니터링·시뮬레이션 (정보 밀도 높음)
  - 선장·항해사 — 입력·조회 (입력 오류 방지 우선)
  - 경영진 — 요약 (단일 지표 + 확률 플래그)
- 🔒 Light Mode 기본 · **Dark Mode 지원** (§2.2 · §15)
- 🔒 데스크톱 우선 설계

---

## 2. 컬러

### 2.1 브랜드

| 역할 | 값 | 용도 |
|---|---|---|
| 🔒 Primary · Deep Navy | `#1a365d` | 헤더, 메인 테마, 주요 버튼 |
| 💡 Primary Hover | `#1a3151` (파생 — Primary 82% + `--text-primary`) | 버튼 hover / active. 종전 `#142b4a`는 **오기**였다 — 코드는 `color-mix`로 파생하며 라이트 해석값이 `#1a3151`이다 (2026-09-11 확정 정정 ⑵) |
| 💡 Primary Subtle | `#eaf0f7` | 선택 배경·강조 영역 (navy 틴트) |

### 2.2 중립 (Neutral)

navy를 소량 섞은 쿨톤. **Light·Dark 두 모드를 함께 정의한다 (§0.2 제약 5).**

| 토큰 | Light | Dark | 용도 |
|---|---|---|---|
| bg | `#f4f6f9` | `#0d1520` | 페이지 배경 |
| surface | `#ffffff` | `#16202e` | 패널·카드 |
| surface-2 | `#eef2f6` | `#1e2a3a` | 입력칸·서브 영역 |
| text | `#1a2733` | `#e3e9f0` | 기본 텍스트 |
| text-muted | `#5a6b7b` | `#93a3b5` | 보조 텍스트·라벨 |
| text-faint | `#8998a6` | `#64748b` | 플레이스홀더·비활성 **전용** |
| border | `#dce3ea` | `#2a3646` | 기본 테두리 |
| border-strong | `#c3ced9` | `#3b4a5e` | 강조 구분선·차트 축 |
| border-control | `#6f7c8f` | `#6f7c8f` | 폼 컨트롤 테두리 — **양 모드 같은 값**. 컨트롤 면과 패널 면의 대비가 `1.12`/`1.09`라 테두리가 유일한 경계이고, `1.4.11` 비텍스트 `3:1`이 걸린다(네 면 최소 `3.77`) |
| grid | `#e8edf2` | `#223042` | 차트 그리드선 |

**고도(elevation)는 모드별로 다른 수단으로 표현한다 (§5).** Light는 그림자, Dark는 표면 명도차다. 다크 배경에서 그림자는 보이지 않으므로 Light의 그림자를 그대로 옮기면 카드가 배경에 붙는다.

> ⚠️ `text-faint`는 4.5:1에 미달한다. **플레이스홀더와 비활성 컨트롤에만** 사용하며, 라벨·본문·힌트 텍스트에는 절대 사용하지 않는다. 대안은 §16 참조.

### 2.3 시맨틱 (상태)

**UI 피드백 전용.** CII 등급 표시에 사용 금지 (§0.2 제약 2).

| 역할 | Light | Dark | 용도 |
|---|---|---|---|
| Info | `#2b6cb0` | `#63a0e0` | 안내 배너, 정보 툴팁 |
| Success | `#38a169` | `#4ecb84` | 저장 완료, CII 개선 방향 (§4.3). ⚠️ 라이트는 `1.4.11` 미달 — 아래 각주 |
| Warning | `#b7791f` | `#e0a845` | 주의 배너, 데이터 지연. **문자에는 `-text` 계열을 쓴다** — 아래 각주 |
| Danger | `#e53e3e` | `#f26b6b` | 오류, 저장 실패, 임계 초과. **문자에는 `-text` 계열을 쓴다** — 아래 각주 |

> **[2026-09-12 확정 N] Warning은 `§2.3` 값을 그대로 쓴다 — 빠진 것은 결정이 아니라 파이프라인이었다.**
>
> 값은 이 표에 이미 있었다(Light `#b7791f` · Dark `#e0a845`). 코드가 등급 C를 빌려 쓴 이유는 **`design/tokens/*.json`(Figma 내보내기)에 Warning이 없어** 생성 토큰에 `--semantic-warning`이 내려오지 않았기 때문이다. 생성 세트에는 `primary`·`success`·`danger`·`focus` **넷뿐**이라 **Info와 Warning 둘 다 출처가 없다**(`--color-info`가 `--semantic-primary` 별칭인 것도 같은 이유다).
>
> **순서가 있다.** ⑴ Figma 세트에 `--semantic-warning`을 추가해 **생성 경로로 내려보낸다** ⑵ 그 뒤 `--color-warning`·`--color-warning-text`의 별칭을 교체한다. **코드에 하드코딩하지 않는다** — `§0.2`가 값의 소유를 Figma에 두었다.
>
> ✅ **2026-09-17 완료** (`#1022`). Figma 세트에 `semantic/warning`·`semantic/info`가 들어와 생성 경로로 내려왔고(Light `#b7791f`·`#2b6cb0` / Dark `#e0a845`·`#63a0e0`), 별칭을 `var(--semantic-*)`로 교체했다. 위 문단의 「생성 세트에 넷뿐」은 **그 시점의 기록**이며 지금은 여섯이다. 곁따라 둘이 함께 풀렸다 — ⓐ 라이트 Warning이 **페이지 면 위에서 `1.4.11`을 넘게 됐다**(`#d97b14` 2.86 → `#b7791f` 3.36) ⓑ `§9.5`가 정한 항로선 토큰 `--semantic-info`가 **존재한 적이 없어** 코드가 리터럴로 떨어지던 것이 해소됐다(`#1052` 조사). 라이트 `-text` 분리는 아래 경고대로 **유지한다**.
>
> ⚠️ **별칭을 바꿔도 라이트 `-text` 분리는 그대로 필요하다.** `#b7791f`는 흰 바탕에서 **3.64:1**이라 문자 기준(4.5)에 미달한다. 라이트 `--color-warning-text`는 `#a8590c`를 유지한다. **이 줄이 없으면 다음에 누가 「이제 시맨틱이 생겼으니 `-text`는 지워도 되겠네」로 되돌린다.** 다크는 `#e0a845`가 카드 7.5 · 페이지 8.9로 넉넉해 `--semantic-warning`을 그대로 쓴다.
>
> **면과 문자의 기준이 다르다.** `--color-warning`의 용처는 대부분 줄무늬(`border-left`)와 배경이라 `3:1`이고 `#b7791f`(3.64)가 통과한다. **면 위에 글자가 얹히는 자리는 문자 기준(4.5)이다** — `.annual-sim__badge`가 그 경우이며(흰 글자 위 Warning 면) 확정 값으로도 **3.64로 미달**이다. 별도 판단 대상으로 남긴다.

> **[2026-09-17 정정 · `#1022`] 라이트 Success·Danger를 생성 세트 값으로 맞춘다.**
>
> 이 표의 라이트 두 값이 **낡은 사본**이었다 — Success `#2f855a` · Danger `#c53030`으로 적혀 있었으나 Figma와 코드는 `#38a169` · `#e53e3e`다. `§0.2`가 값의 소유를 Figma에 두므로 **표를 고친다.** 다크 네 값은 이미 일치했다. 확정 N이 「라이트 네 값 모두 어긋나 있다」고 적어 둔 것의 나머지 둘이며, Warning·Info는 `#1022`가 생성 경로를 열면서 함께 맞춰졌다.
>
> Danger의 종전 표 값 `#c53030`은 사실상 **문자용 `--color-danger-text`(`#c92a2a`)와 같은 색**이었다. 표가 문자용 값을 면 자리에 적고 있어 **표만 보면 대비가 통과하는 것처럼 보였다** — Warning이 겪은 것과 같은 혼동이라 Danger 행에도 `-text` 단서를 붙였다.
>
> ⚠️ **Success는 이 값으로 `1.4.11`을 넘지 못한다 — 별건으로 올린다.** `#38a169`는 카드 `3.25` · 페이지 `3.00` · **inset `2.89`**다. 용처가 좌측 테두리뿐이라 기준은 비텍스트 `3:1`인데, `--color-surface-2` 위에 놓인 `.vessel-management__notice` **한 곳이 미달**이다(`.vessel-registration__result`·`.fr__status`는 카드 위라 `3.25`로 통과). **값을 여기서 덮지 않는다** — `§0.2`상 Figma 소유이고, `#829 ⑶`(입력칸 테두리)·아웃라인 버튼 테두리와 **같은 유형**(카드에서 통과하고 inset에서 무너지는 경계색)이라 묶어서 다룬다. Danger `#e53e3e`는 같은 기준에서 `3.67`로 통과한다.

#### 2.3.1 데이터 점검 심각도 (2-11 데이터 점검) 🔒

**색은 본 절만 차용한다.** 등급 램프에서 가져오지 않는다 — §2.5의 「4단계 전용 색 램프를 만들지 않는다」와 동일한 취지다.

| 심각도 | 색 | 의미 | 근거 |
|---|---|---|---|
| 대체 계산 | Danger | 실적 대신 계획값으로 계산됨 | `PRD §8.3` |
| 계산 불가 | Danger | 대체할 계획값조차 없음 | `PRD §17.3` |
| 이상치 | Warning | 계산됐으나 신뢰도 낮음 | 신설 |
| 실적 확정 전 | 중립 (`text-muted`) | `COMPLETED`에서 `CONFIRMED`로 미전이 | `PRD §8.1`·`§8.1.1` |

> **명칭 정정 (#1532)** — 종전 「실적 미입력」. `PRD §8.1`이 `COMPLETED`를 「실적 입력 완료, 미확정」으로 정의하므로 실적은 들어가 있다 — 이름이 정반대 상태를 가리켰다. 근거 인용도 `PRD §8.4`(재계산 정책)에서 상태 전이(`§8.1`·`§8.1.1`)로 고쳤다. 색·순서는 그대로다.

> **명칭 정정 (v2.1)** — v2.0은 「계산 제외」로 적었으나 `PRD §12.3`은 결측 항차를 제외하지 않고 **`§8.3` 우선순위에 따라 계획값으로 대체**한다. 「대체 계산」이 정확한 명칭이며, 화면에서 사라진 것보다 **실측인 척하는 추정값**이 더 위험하므로 Danger를 배정한다.

> **「대체 계산」과 「계산 불가」가 같은 Danger인 이유** — 둘 다 「이 등급을 그대로 믿으면 안 된다」는 같은 결론으로 이어진다. 구분은 색이 아니라 **그룹 헤더 텍스트**가 담당한다 (§14 보조 채널).

> ⚠️ **[2026-09-12] 이 표와 코드가 어긋나 있다 — 라이트 네 값이 모두 다르다.** Info `#2b6cb0` ↔ 코드 `#1a365d`(primary 별칭) · Success `#2f855a` ↔ `#38a169` · Warning `#b7791f` ↔ `#d97b14`(C fill 별칭) · Danger `#c53030` ↔ `#e53e3e`. 그래서 **아래 「어떤 값도 일치하지 않는다」 선언은 실측이 아니다** — 문서가 문서 안의 값끼리 비교해 통과를 선언했다. `§0.2`가 값의 소유를 Figma에 두었으므로 **생성 세트가 현재값이고 이 표가 오래된 사본일 가능성**이 크며, 어느 쪽이 맞는지는 Figma를 봐야 정해진다. 별건으로 분리한다 — 여기에 끌어들이면 시맨틱 전체가 함께 열린다.

§2.4의 등급 색과 위 4색은 **어떤 값도 일치하지 않는다** (§0.2 제약 2). 가장 가까운 쌍은 Danger `#c53030` ↔ E fill `#c42a2a`, Warning `#b7791f` ↔ C fill `#d97b14`, Success `#2f855a` ↔ A fill `#1e8639`이며 모두 서로 다른 값이다.

### 2.4 CII 등급 램프 (A–E) 🔒

### 2.4.1 3색 체계 원칙 🔒

등급 색은 **3개 색상군**으로 묶고, 군 안에서는 **명도**로 구분한다.

| 색상군 | 등급 | 사용자에게 전달할 것 |
|---|---|---|
| 초록 | A · B | 조치 불필요 |
| 주황 | C | 관찰 |
| 빨강 | D · E | 조치 필요 |

**5색 램프를 쓰지 않는 이유** — A~E를 서로 다른 5색으로 칠하면 함대 목록에서 색이 5갈래로 흩어져 「지금 조치가 필요한 선박이 몇 척인가」가 한눈에 안 들어온다. 3색은 **규제 행동 기준과 일치**하므로 스캔이 빠르다.

**군 안에서 명도로 나누는 이유** — D와 E는 규제상 완전히 다르다. D는 3년 연속이어야 시정조치 대상이고 E는 1년으로 즉시 대상이다. 같은 빨강이되 E를 더 진하게 두어 **C에서 멀어질수록 진해지는** 대칭 구조를 만든다.

```
A(진한 초록) ← B(연한 초록) ← C(주황) → D(연한 빨강) → E(진한 빨강)
```

### 2.4.2 토큰 🔒

등급당 **4개 토큰**을 정의한다. v1.2까지의 「채움색 + 글자색」 2토큰으로는 카드·배지·테두리를 구성할 수 없어 확장했다.

| 토큰 | 의미 |
|---|---|
| `fill` | 지도 마커·차트 선 등 **면 채움** |
| `bg` | 배지·카드의 **연한 배경** |
| `text` | `bg` 위에 올라가는 **글자색** |
| `border` | `bg`와 짝을 이루는 **테두리** |

**Light**

| 등급 | fill | bg | text | border |
|---|---|---|---|---|
| A | `#1e8639` | `#cdebd6` | `#0e4f22` | `#7cc894` |
| B | `#57b570` | `#e9f6ec` | `#2f7043` | `#b8e0c4` |
| C | `#d97b14` | `#fdebd8` | `#7a3f06` | `#f2c289` |
| D | `#dc5b5b` | `#fce9e9` | `#8a2f2f` | `#efb4b4` |
| E | `#c42a2a` | `#f9d5d5` | `#6b1414` | `#e08585` |
| none | `#9b9a93` | `#f2f2ef` | `#6b6a64` | `#c9c7be` |

**Dark**

| 등급 | fill | bg | text | border |
|---|---|---|---|---|
| A | `#43be68` | `#14351f` | `#7be39a` | `#2e7048` |
| B | `#2f8f52` | `#0f2417` | `#56b074` | `#1f4b32` |
| C | `#d98c2a` | `#33220c` | `#f0b268` | `#7a5218` |
| D | `#c95f5f` | `#2e1818` | `#e89191` | `#6b3232` |
| E | `#e83a3a` | `#421212` | `#ff7a7a` | `#9e2c2c` |
| none | `#6b7686` | `#161a21` | `#6b7686` | `#39424f` |

`text`/`bg` 조합은 두 모드 모두 **6:1 이상**으로 AA를 상회한다.

> **`fill`의 대비 한계** — Dark에서 `none`(`#6b7686`)과 E(`#e83a3a`)의 페이지 배경 대비는 4.5:1에 미달한다. `fill`은 **마커·선·밴드 등 도형 전용**이며 그 위에 글자를 올리지 않는다. 마커 라벨은 배경 위 `text`로 따로 그린다. 도형 대비 기준 3:1은 전 등급 충족한다.

### 2.4.3 등급 없음 (`none`) 🔒

CII를 산출할 수 없는 상태를 표현한다 — 항차 0건, 필수 필드 결측 등.

- 글자는 **em dash `—`**를 쓴다. `N/A`나 하이픈이 아니다
- 테두리는 **점선(dashed)** 이다. C가 주황으로 옮겨가 무채색이 비었지만, 점선을 유지해 「값이 없음」을 형태로도 전달한다
- 산출 불가 상태에서 `A`~`E` 중 하나를 임의로 표시하지 않는다
- **`--cii-none-bg`는 고유 색을 갖지 않는다** 〔**확정** 2026-09-10〕. 중립 표면(`--surface-inset`)과 **같은 값**이다
  - 「none 등급」은 **등급이 없다는 뜻**이지 고유한 색을 가진 등급이 아니다. 셸 색이 다시 바뀌어도 어긋나지 않는다
  - 종전 라이트 값 `#f2f2ef`는 **종전 웜 페이지색**이라 `§2.2` 쿨톤 페이지 위에서 웜하게 떴다. 다크는 이미 같은 값이었고 라이트만 어긋나 있었다
  - 값은 **생성 원본**(`design/tokens/*.json`)에서 고친다. `tokens.generated.css`를 직접 고치면 다음 빌드에 되돌아간다

### 2.4.4 패턴 🔒

| 등급 | 패턴 |
|---|---|
| A | 없음 (solid) |
| B | 45° 사선 ╱ |
| C | 도트 |
| D | 135° 사선 ╲ |
| E | 크로스해치 |

**패턴 적용 대상**: 지도 마커, 차트 등급 밴드, 등급 확률 스택 바 (§10.2) — **등급 문자가 함께 놓이지 않는 모든 자리.**
**패턴 미적용**: 등급 배지 — A~E 문자가 항상 함께 놓이므로 보조 채널이 이미 충족된다 (§14). 11px 배지에 패턴을 겹치면 판독만 나빠진다.
**패턴 미적용**: 차트 선 — 마커 모양이 보조 채널로 작동한다 (§9.3).

> **3색 체계에서 패턴은 옵션이 아니다.** 초록·주황·빨강 세 색상군은 적록색맹에서 모두 황갈색 계열로 수렴하며, 5색 체계보다 오히려 취약하다.
>
> **패턴을 끄는 예외를 두지 않는다** [2026-09-12 확정 O · `#831`]. 등급 문자가 색면 **안에** 놓이는 자리는 위 「패턴 미적용」이 이미 정하고 있다(등급 배지 · 차트 선). 그 밖에서 **색면이 문자 없이 등급을 말하는 자리**에서는 끄지 않는다.
>
> 종전 문장은 「`showPattern`의 기본값은 `true`이고 끄는 쪽이 예외다」였다. **조건이 적히지 않은 예외 조항은 다음 사람이 자기 판단으로 끄는 근거**가 된다 — 실제로 그 속성을 넘기는 호출부는 하나도 없었고, 지울 수도 없었다(정본이 이름으로 붙잡고 있었다).

> **C·D 패턴 교체 (v2.0)** — v1.2는 C=촘촘한 사선, D=도트였다. 3색 체계에서 C는 단독 색상군이므로 가장 이질적인 패턴(도트)을 배정하고, 같은 색상군인 D·E에 사선과 격자를 나누어 군 내 구분을 강화했다.

### 2.5 위험도 표기 🔒

**위험도 표기는 산출 방식에 따라 나눈다.**

#### (a) 확률 기반 화면 (기능③ 등)

등급 확률 분포에서 파생되는 값을 **수치 + 아이콘**으로 표기한다. 예: `⚠ P(D∪E) 28%`

| 조건 | 표기 | 색 |
|---|---|---|
| P(D∪E) < 20% | `P(D/E) 12%` | text-muted |
| 20% ≤ P(D∪E) < 40% | `⚠ P(D/E) 28%` | Warning |
| P(D∪E) ≥ 40% | `⚠ P(D/E) 47%` | Danger |

> ⚠️ 임계값 20% / 40%는 초안값이다. 실운항 데이터 확보 후 재조정한다 (§16).

#### (b) 결정론 계산 화면 (기능①·②)

확률 분포가 없으므로 다음을 따른다.

- 주 표기는 **여유율 수치**로 한다. **대상 등급을 함께 표기**한다 — 예: `D 등급까지 7.2%`
  - 「다음 등급까지」는 개선·악화 방향이 문구만으로 갈리지 않는다. 필드(`next_worse_boundary_margin`)는 악화 방향이고, 등급 배지가 현재 등급을 보이므로 대상 등급을 적으면 방향이 자명해진다.
  - **여유율이 0보다 크고 0.1% 미만이면** `0.0%`로 적지 않고 「0.1% 미만」으로 표기한다. 경계 근처에서 `0.0%`는 이미 등급이 넘어간 것처럼 읽힌다. **0 이하에는 이 예외를 적용하지 않는다** — 경계에 도달했거나 이미 넘은 상태다. 0 이하 값의 표시 방식은 해당 결과 상태와 API 계약을 따른다.
  - **등급 E는 여유율 자리에 「해당 없음 — 최하위 등급」을 적는다.** 악화 방향 경계가 없어 여유율이 정의되지 않으며, `PRD §9.4.1`에서 E는 여유율과 무관하게 CRITICAL이다.
    - 계산 계층은 이 상태를 값 없음으로 표현한다(`select_next_worse_boundary`가 E에 `None` 반환 — `#40`).
    - **API 응답은 `null`로 확정됐다 (#171)** — Layer 1 수치 필드에 비수치 문자열을 섞지 않고, 표시 의무는 이 문구로 이행한다(`PRD §9.2` 단서 · `API_SPEC §4.1` nullable 표기).
  - 자릿수는 §4.2 비율 규칙(백분율 1자리)을 따른다.
- `risk_level` 4단계는 **텍스트 라벨 + 아이콘**으로 보조 표기한다.
  - 라벨은 **「낮음 LOW · 보통 MEDIUM · 높음 HIGH · 심각 CRITICAL」** — §14의 「한국어 라벨 + 영문 약어 병기」를 따른다. 한국어를 앞에 두어 좁은 폭에서 잘려도 의미가 남게 한다.
  - **이 라벨은 `§3` 「같은 뜻 영단어를 나란히 적지 않는다」의 예외다** (`#1452`). 뒤의 영문은 번역이 아니라 **API·PDF 보고서의 `risk_level` 값과 같은 글자**라, 화면 · 보고서 · API 응답을 서로 대조하는 열쇠다. 그래서 **언어 모드와 무관하게** 같은 표기를 쓴다 — 한국어 모드에서 걷으면 대조가 끊기고, 영어 모드에서 한국어를 걷으면 보고서(한국어 문서)와 화면이 어긋난다.
  - 아이콘은 **HIGH · CRITICAL 두 단계에만** 동일한 경고 글리프를 붙인다. **단계별로 다른 아이콘을 정의하지 않는다** — 4개의 서로 다른 아이콘은 채널만 색에서 형태로 바꾼 4단계 시각 체계라 아래 금지 조항의 취지가 무너진다. 아이콘은 「주의가 필요하다」만 전달하고 정도는 라벨이 담당한다.
  - **아이콘 옆에는 라벨이 항상 함께 놓이므로 아이콘에 `aria-hidden="true"`를 적용한다.** §12는 「텍스트 라벨 **또는** `aria-label` 병기」를 요구하며 여기서는 라벨 쪽으로 충족한다. 아이콘에도 `aria-label`을 붙이면 스크린리더가 「높음 HIGH 주의 필요」처럼 중복해 읽는다. **라벨 없이 아이콘만 두는 배치는 허용하지 않으며**, 불가피한 경우에만 `role="img"` + `aria-label="주의 필요"`를 붙인다.
  - 위 (a)와 본 항의 `⚠`는 **임시 글리프**다. 필요한 글리프가 하나뿐이므로 아이콘 세트(§16 항목 5)가 확정되면 그 세트의 warning 아이콘으로 교체하며, 최종 형태는 §12 규격을 따른다.
- **4단계 전용 색 램프를 만들지 않는다.** 색은 §2.3 시맨틱(Warning · Danger)만 차용하며, 등급 램프와 겹치는 색 체계를 정의하지 않는다.
  - **단계별 색** — LOW · MEDIUM은 본문색, **HIGH는 Warning, CRITICAL은 Danger**다. 라벨은 문자이므로 `-text` 토큰(`--color-warning-text` · `--color-danger-text`)을 쓴다(§2.3 · §0.2 제약 1). (a)의 「text-muted → Warning → Danger」와 같은 구조다.
  - **아이콘은 단계와 무관하게 Warning 하나**다 — CRITICAL 줄에서도 아이콘은 Warning, 라벨은 Danger다. 아이콘은 「주의가 필요하다」만 전달하고 정도는 라벨이 담당한다는 위 원칙을 색에도 적용한 것이다.
  - 기능① · ② · 시나리오 비교가 같은 규칙을 따른다. 종전에는 기능②만 HIGH · CRITICAL · 아이콘을 모두 Danger로 두었다 — 그 사유(Warning 문자 토큰이 다크에서 등급 C 색으로 풀린다)가 `#1022`로 사라졌다. (2026-09-22 확정, `rlatnals4114`)

두 방식 모두 **등급 램프와 시각적으로 구분**되어야 한다.

> **판정 기준의 소재** — (a)는 `PRD §9.4.2`, (b)는 `PRD §9.4.1`이 임계값 표를 소유한다. 이 절은 그 값을 **화면에 어떻게 보이는가**만 정한다(`AGENTS §3.2.2`).

---

## 3. 타이포그래피

- **폰트**: **Noto Sans KR** (한글 우선) → fallback `system-ui, "Malgun Gothic", sans-serif`
  - v1.2까지 Pretendard였으나 팀 환경 확보 문제로 교체했다. 라이선스(OFL)와 굵기 범위는 동등하다
  - 식별자용 mono는 **Noto Sans Mono** — IMO 번호·좌표 **전용**이며 표 안의 계산값에는 쓰지 않는다. 한 표에 두 서체가 섞이면 어색하다
- **숫자**: `font-variant-numeric: tabular-nums` — 표·수치 전역 적용
- **자간**: Noto Sans KR은 기본 자간이 넓어 밀도 높은 화면에서 헐거워 보인다. 아래 값으로 조인다

| 토큰 | 크기 | 굵기 | 행간(px) | 자간 | 용도 |
|---|---|---|---|---|---|
| `display` | 32 | 500 | 40 | -0.02em | KPI 수치, 카운트다운 |
| `title` | 20 | 500 | 28 | -0.02em | 페이지 제목 |
| `heading` | 16 | 500 | 24 | -0.01em | 카드 제목, 선명 |
| `body` | 14 | 400 | 20 | -0.01em | 본문, 테이블 |
| `label` | 13 | 400 | 18 | -0.01em | 필드 라벨 |
| `caption` | 12 | 400 | 16 | -0.01em | 보조 설명, 단위 |
| `micro` | 11 | 400 | 14 | 0 | 신선도 배지, 차트 축 라벨 |

- **굵기는 400·500만 쓴다.** v1.2의 700은 대시보드에서 무겁게 읽혀 500으로 낮췄다
- `micro`(11px)에는 자간을 조이지 않는다 — 이미 작아 판독이 나빠진다
- 대문자 라벨 자간 `+0.04em`
- 🔒 한국어 기본 + 영문 약어 병기 (PRD §4)
  - **약어**는 업계가 원어로 쓰는 표기다 — CII · IMO · DWT · GT · HFO · MGO 등. 한국어 라벨 **안에** 쓰거나(「CII 예측」 · 「IMO 번호」 · 「중유 (HFO)」) 단위 칩으로 둔다(`DWT` · `GT` · `nm`).
  - 한국어 라벨과 **같은 뜻의 영단어를 나란히 적지 않는다** — 「항해거리 Distance」 · 「대시보드 Dashboard」 ✗. 영어 사용자는 한/EN 토글(`#1215`)이 맡는다. 번역 사전이 없는 폼 라벨의 영문 보조는 **영어 모드에서만** 보인다(`#1426` · `useShowsLabelEn`).
  - **예외는 위험도 4단계 하나다**(`§2.5 (b)`) — 영문이 `risk_level` 값과 같은 글자라 대조의 열쇠이기 때문이다. 새 예외를 두려면 같은 조건(**영문이 계약 값 그대로**이고 화면 밖 문서와 대조된다)을 갖춰 이 목록에 적는다.

  > **v2.17 확정 (`#1452`)** — 종전 규칙은 이 한 줄뿐이라 「영문 약어」의 범위가 없었다. 화면에 붙어 있던 영문 47곳을 열어 보니 **순수 약어는 한 건도 없었고**(`#1426`), 그 표기가 규칙을 따른 것인지 어긴 것인지를 정본만으로 가를 수 없었다. 한/EN 토글 뒤에도 병기를 **유지**하되, 무엇이 병기이고 무엇이 아닌지를 적었다.

> **v1.2 레벨명과의 대응** — Display→`display`, H2→`title`, H3→`heading`, 본문→`body`, 라벨→`label`, 마이크로→`caption`·`micro`. 마이크로가 11–12px 범위였던 것을 두 토큰으로 분리했다.

---

## 4. 숫자 · 단위 포맷 🔒

### 4.1 CII 값

- **소수점 3자리 고정** — `5.421` (자릿수 가변 금지, 정렬 붕괴)
- **절사가 아니라 반올림한다.** 절사하면 `4.9827`이 `4.982`로 나가 실제보다 낮게 보이고, 등급 경계 부근에서 잘못된 등급으로 읽힐 수 있다.
- **내부에는 API 원본 문자열을 그대로 보관하고, 반올림은 표시 시점에만 적용한다** (`PRD §9.3` — 내부 계산값에 표시 반올림값을 다시 쓰지 않는다). 예: `"4.982400"` → 화면 `4.982`
- `required_cii`도 **동일하게 3자리**로 한다. attained와 나란히 비교되므로 자릿수가 다르면 정렬 취지가 깨진다.
- **천단위 구분자 미적용** — CII는 통상 한 자릿수
- **단위 표기 🔒** — **선종의 capacity 축에서 파생시킨다.** 고정 문자열로 박지 않는다.
  - `DWT` 축(8종) → 지표명 `AER` → **`gCO₂/(DWT·nm)`**
  - `GT` 축(4종) → 지표명 `cgDIST` → **`gCO₂/(GT·nm)`**
  - 선종별 축은 `PRD §3.3.3`이 소유한다. **표기 형식**(대문자 · 괄호 · 가운뎃점)만 본 절 소관이다(`AGENTS §3.2.2`)
- **표기 형식의 근거 🔒**
  - **대문자 `DWT`·`GT`** — IMO 원문 표기다
  - **분모를 괄호로 묶는다** — 괄호가 없으면 `gCO₂/DWT`에 `nm`을 곱한 것으로 읽힌다. 분모 전체가 `DWT × nm`임을 드러낸다
  - **`t`를 쓰지 않는다** — 재화중량톤수(실을 수 있는 최대 무게)와 `t`(실제로 실은 무게)는 다른 값이다
  - `nm`은 유지한다. 나노미터와 기호가 겹치나 해운 실무 표기다
- 화면 축약 시 단위 문자열 전체를 유지한다. **`g` 단독 표기 금지**
- **지표명(`AER`·`cgDIST`)을 값 옆에 병기하지 않는다 🔒** — 단위 문자열에 이미 `DWT`·`GT`가 들어 있어 지표가 무엇인지 드러난다. 병기하면 같은 정보를 두 번 싣고, 결과 카드 3열(attained · required · ratio) 우측 정렬에서 폭을 더 쓴다.

> **고정 문자열을 금지하는 이유** — MVP는 `BULK_CARRIER` 단독이라 화면에 `AER`·`DWT`만 나온다. 그러나 `dwt`를 박아 두면 **선종이 늘 때 크루즈선에 `gCO₂/(DWT·nm)`이 표시된다.** 그 선종은 GT 축이라 지표명(`cgDIST`)도 분모도 다르다. **화면이 깨지지 않고 내용만 틀리므로 발견이 늦다.**

### 4.2 물리량

**소수 자릿수 🔒** — 항목별로 고정한다.

| 항목 | 소수 자릿수 | 예 | 근거 |
|---|---:|---|---|
| 연료 소모량 | 1 | `80.0 t` | 2자리는 10kg 단위인데 사용자 입력이 그 정밀도로 들어오지 않는다 |
| 항해거리 | 0 | `1,000 nm` | 0.1 nm ≈ 185 m로 항해 거리에서 의미를 갖지 않는다 |
| CO₂ 배출량 | 1 | `249.1 tCO₂` | 연료 × CF로 산출되는 파생값이라 입력보다 자릿수가 많을 수 없다 |
| 시간 (hour) | 1 | `83.3 h` | `PRD §9.3` 채택 — 대상 필드는 `PRD §7.5 VoyageScenario.duration_hours` |
| 일수 (day) | 0 | `232 일` | 0.1일 ≈ 2.4시간으로 「며칠 남았나」에 의미를 더하지 않는다. 소수가 필요한 구간은 위 「시간」이 소유한다 |
| 평균 속력 | 1 | `14.2 kn` | 0.01 kn은 항해일지·noon report가 담는 정밀도가 아니다. 감속 운항 판단은 0.1 kn 단위로 이뤄진다 |
| 용량 (DWT·GT) | 0 | `50,000` · `25,000` | 0.01 t = 10 kg으로 수천~수만 톤 급 선박 제원에서 의미를 갖지 않는다 — 항해거리와 같은 논리다. **원본은 보존된다**(아래 「반올림」) — CII 분모에는 API 원본 값이 쓰인다 |
| 확률 | 백분율 1 | `28.4%` | 아래 「비율」과 같은 규칙 |

> **`§4.1`의 CII 3자리는 예외다.** 등급 경계 판정에 직접 쓰이는 값이라 자릿수를 고정한 것이며, **다른 항목에 확대 적용하지 않는다.**

**단위 표기 🔒** — 항목별로 고정한다. (#164)

| 항목 | 단위 | 근거 |
|---|---|---|
| 연료 소모량 | `t` | `t`는 tonne(1,000 kg)의 SI 기호다. **`ton`은 쓰지 않는다** — short ton(907 kg)·long ton(1,016 kg)과 표기가 겹쳐 국제 규제 맥락에서 모호하다 |
| 일일 연료소모량 | `t/일` | **질량이 아니라 질량유량이다** — `DB_SCHEMA`가 `reference_daily_foc_ton`을 「기준 일일 연료소모량 (`ton/day`)」으로 정의한다. `t`로 적으면 차원을 잃어 **총량인지 일당인지가 라벨에만 남는다.** 바로 위 `tCO₂`와 같은 이유다(*「둘 다 `t`면 무엇의 질량인지 알 수 없다」*). **위 두 단위에서 파생시킨다** — `t`나 `일`이 바뀌면 함께 따라간다. 자릿수는 「연료 소모량」 행(1자리)을 따른다 |
| CO₂ 배출량 | `tCO₂` | 같은 화면에 연료 질량과 CO₂ 질량이 나란히 놓인다(`PRD §9.2` 필수 출력). **둘 다 `t`면 무엇의 질량인지 구분되지 않는다.** `PRD §3.4.2`(연료 CF 표 헤더 `CF, tCO₂/tFuel`)·`§9.4`(단위표)가 이미 쓰는 표기와도 맞는다 |
| 항해거리 | `nm` | `§4.1` 참조 — 나노미터와 기호가 겹치나 해운 실무 표기다 |
| 시간 | `h` | — |
| 일수 | `일` | **이 표에서 유일하게 문장 안에 섞여 나온다** — 「D등급까지 3일」. 나머지와 맞춰 `d`로 적으면 그 문장이 읽힐 수 없게 된다 |
| 평균 속력 | `kn` | — |

> **CII 단위는 이 표에 없다.** 선종의 capacity 축에서 파생되므로 상수가 아니다 — `§4.1` 「단위 표기 🔒」를 따른다.

> **화면에 리터럴로 박지 않는다 🔒** — 구현은 `src/display/format.ts`의 `DISPLAY_UNITS` 상수를 참조한다. 단위 표기는 회신에 따라 바뀔 수 있는데, 리터럴이 화면마다 흩어져 있으면 교체 시 일부가 남고 **그 누락은 화면이 깨지지 않고 내용만 틀리므로 발견이 늦다.** `§4.1`이 CII 단위에 고정 문자열을 금지한 것과 같은 이유이며, 여기서는 값이 상수라는 점만 다르다.

> **시간과 일수는 대상 필드가 다르다.** `duration_hours`는 `PRD §7.5 VoyageScenario`(기능②) 필드이므로 기능① 결과 화면에는 나타나지 않는다.
>
> **일수는 다르다 — 이미 두 화면이 보이고 있다.** 연말 예상의 「경과 / 잔여」(`assumptions.elapsed_days`·`remaining_days`)와 대시보드의 「D등급까지 n일」이다. v2.2까지 이 각주는 일(day) 값을 *「연료 추정식의 중간값이며 표시 대상이 아니다」*라고 적고 있었으나, `PRD §11.2` `duration_days`만 본 서술이었다. **그 사이 다른 경로의 일수가 화면에 올라왔고, 규정이 없어 소수 2자리(`231.64 일`)로 나가고 있었다.** 자릿수 표에 행을 넣어 닫는다 (#592).

> **용량 단위는 이 표에 없다 (#633).** `DWT`·`GT`는 **선종의 capacity 축에 따라 갈리므로 상수가 아니다** — CII 단위가 같은 이유로 빠져 있는 것과 같다(`§4.1` 🔒). 축 판정은 서버가 하고(`API_SPEC §2.7` `capacity_basis`), 화면은 그 값으로 라벨을 고른다. **화면이 선종에서 유추하지 않는다** — 선종이 늘 때 서버와 갈린다.
>
> **용량에 자릿수 규정이 없어 같은 값이 화면마다 달랐다 (#633).** 선박 관리 목록은 `DWT 6,405.77`, 선박 상세 제원은 `6405.77`을 냈다 — 한쪽은 포맷을 거치고 한쪽은 서버 문자열을 그대로 그렸는데, **어느 쪽이 맞는지 정한 규정이 없었다.** `#592`(일수·속력)와 같은 형태의 공백이며 같은 방식으로 닫는다. 소수 자릿수를 **고정**하는 것은 이 표의 머리글이 「항목별로 고정한다」이기 때문이다 — 값에 따라 자릿수가 달라지면(`50,000`과 `6,405.77`이 섞이면) 표에서 소수부 정렬이 무너진다.

> **`PRD §9.3` 정합화 완료 (#185)** — 연료 소모량(2자리 → 1자리) · CO₂ 배출량(2자리 → 1자리) · 항해거리(1자리 → 0자리) 3건의 상충이 있었으나, `PRD §9.3` 「화면 표시」 열이 본 절 참조로 전환돼 해소됐다. 화면 표시 자릿수는 `AGENTS §3.2.2`의 소관 구분에 따라 본 절을 따른다(`#159` · PR `#162`). **계산·판정·저장값에는 영향이 없다**(아래 「반올림」). CII·시간·확률은 애초에 두 문서가 일치했다. 연료·CO₂의 **단위 표기**는 위 「단위 표기 🔒」 표로 확정했다(#164) — 연료 `t` · CO₂ `tCO₂`.

**반올림 🔒** — 모든 수치는 **표시 시점에 반올림**한다. **절사하지 않으며, 내부에는 API 원본 값을 그대로 보관한다.** 표시 반올림은 **등급 판정·계산·저장값에 영향을 주지 않는다.** (`§4.1`이 CII에 대해 정한 규칙을 전 항목으로 올린 것)

**천단위 구분자 🔒**

| | 항목 |
|---|---|
| 적용 | 연료 소모량(t) · 항해거리(nm) · CO₂ 배출량(t) · **용량(DWT·GT)** — 예: `12,480 t` · `38,215 nm` · `50,000 DWT` |
| **미적용** | **CII · 비율 · 확률 · 일수 · 속력** |

> CII는 통상 1000을 넘지 않아 구분자가 등장하지 않으며, 적용하면 `§4.1`이 자릿수 고정으로 확보한 소수부 정렬을 방해한다. **일수는 연간 최대 366, 속력은 두 자릿수라 구분자가 등장할 수 없다** — 적용 여부가 드러나지 않는 값이므로 목록에 명시한다. 명시적으로 제외하지 않으면 구현에서 일괄 적용될 여지가 있다.

**비율 🔒** — 기준 대비 비율(`ratio_to_required` 등)은 CII 값과 성격이 다른 **무차원 값**이므로 §4.1의 3자리 규칙을 적용하지 않는다.

- **백분율 1자리** — `98.8%`
- 소수 비율을 그대로 쓰지 않는다. `0.98758`에 §4.1을 적용하면 `0.988`이 되어 의미가 전달되지 않는다.
- API는 소수 문자열(`"0.98758"`)로 내려온다. **백분율 환산과 반올림은 표시 시점에만** 적용한다(§4.1과 동일).
- **데이터 완결성(`UIFLOW 2-11`)** — 지표가 **비율**이면 이 규칙(백분율 1자리 · `94.2%`)으로 닫히고 **행을 추가하지 않는다.** **점수**로 정의될 때만 자릿수 표에 행을 추가한다(`#592`·`#633`과 같은 방식). 지표 정의는 PO 소관이다 — 같은 줄의 심각도 3칸(건수)과 성질이 달라 보이는 것이 맞다. 건수에 단위를 붙이거나 완결성을 정수로 깎아 맞추지 않는다(머리글은 「**항목별로** 고정한다」이지 「한 줄은 통일한다」가 아니다) (2026-09-11 확정 I)

### 4.3 증감 표기 ⚠️→🔒

**CII는 낮을수록 좋다.** 일반 대시보드 관습(증가=긍정)과 반대이므로 아래를 강제한다.

| 변화 | 화살표 | 색 |
|---|---|---|
| CII 감소 (개선) | ▼ | Success `#2f855a` |
| CII 증가 (악화) | ▲ | Danger `#c53030` |

- 부호는 항상 명시: `-0.183` / `+0.047`
- 화살표와 부호를 함께 표기한다 (색 단독 의존 금지)
- 연료·CO₂ 배출량 등 "낮을수록 좋은" 다른 지표도 동일 규칙을 따른다

---

## 5. 도형 · 형태 언어

> **이 절의 모든 값에는 〔확정〕/〔제안〕을 명시한다.** 「제안」으로 남기는 값은 **담당자와 기한을 함께** 적는다.
>
> - 〔**확정**〕 — 값이 정해졌다. 바꾸려면 Figma 개정과 문서 갱신을 함께 거친다. **코드가 다르면 코드가 틀린 것이다**
> - 〔**제안**〕 — 아직 정해지지 않았다. **담당과 기한을 함께 적는다**
>
> 값의 소유는 `§0.2`대로 Figma에 있다. 이 정의가 분명히 하는 것은 **코드는 어느 쪽에서도 값을 위임받은 적이 없다**는 점이다 — 다크 그림자(`§16` 항목 2)도 포커스 링도 코드가 스스로 값을 정한 경우였다 (`rlatnals4114` 2026-09-11 확정 F).
>
> 이 규칙이 없던 동안 `§16` 항목 2(다크 그림자)가 닫히지 않았다 — 값이 「제안값」으로만 적혀 있어 **문서와 코드가 갈렸을 때 어느 쪽이 맞는지 판정할 근거가 없었다.** 판정하지 못하는 규격은 규격이 아니다.

- **모서리 (radius)** 〔**확정**〕: 모달 `16px` · 카드/패널 `12px` · 입력/버튼 `8px` · **등급 배지 `4px`** · 칩/pill `999px`
  - v1.2의 「작은 태그 6px」을 **배지 4px**로 조정했다. 11px 배지에서 6px는 과하게 둥글어 문자 판독을 방해한다
  - `lg` 크기 등급 배지(20px 문자)만 예외로 `8px`을 쓴다
  - ⚠️ **`--radius-modal`은 사용처가 0이다** — 모달이 아직 없다. 확정하되 **첫 모달에서 16px이 실물에 맞는지 한 번은 눈으로 확인한다** (2026-09-11 확정 D 단서)
- **테두리** 〔**확정**〕: 기본 `1px solid border`
- **그림자 (elevation) — Light 전용** 〔**확정**〕
  - Lv1 카드 — `0 1px 2px rgb(26 54 93 / 5%), 0 4px 16px rgb(26 54 93 / 6%)`
  - Lv2 팝오버/모달 — `0 4px 8px rgb(26 54 93 / 8%), 0 12px 32px rgb(26 54 93 / 12%)`
    - ⚠️ **`--shadow-lv2`는 현재 사용처가 0이다** (2026-09-12 확정 L ⑵ · `--radius-modal`과 같은 형태의 단서). **지우지 않는다** — `§5`가 고도를 `lv1`·`lv2`·`overlay` **3단**으로 정의하는데, 안 쓴다고 지우면 다음에 중간 고도가 필요할 때 누군가 `lv1`이나 `overlay`를 끌어다 쓴다
  - 표기는 코드(`tokens.css`)와 같은 `rgb(… / %)`로 둔다 — 종전 `rgba(…,.05)`는 **값이 같은데 표기가 달라** diff로 일치 여부가 보이지 않았다 (2026-09-11 확정 D)
- **고도 — Dark** 〔**확정**〕: 그림자를 쓰지 않고 **표면 명도차**로 대체한다 (§2.2). Dark에서 `--shadow-lv1`·`--shadow-lv2`는 `none`이다
  - 어두운 배경 위의 검은 그림자는 거의 보이지 않고, 보이는 만큼은 **얼룩으로** 보인다
- **오버레이 그림자 `--shadow-overlay`** 〔**확정**〕: 모달·드롭다운·토스트 등 **떠 있는 면 전용**. Dark에서도 `none`이 아니다
  - Light — `0 4px 12px rgb(26 54 93 / 10%), 0 16px 40px rgb(26 54 93 / 14%)` · Lv2보다 **한 단 넓고 진하게**. 오버레이가 Lv2보다 높은 고도이므로 진해지는 것이 맞다
  - Dark — `0 8px 24px rgb(0 0 0 / 32%), 0 24px 64px rgb(0 0 0 / 24%)`
  - Lv1·Lv2의 관례를 따른다 — 라이트는 네이비 틴트·넓은 층이 진함, 다크는 검정·가까운 층이 진함
  - 오버레이는 **무엇 위에 뜰지 모르는 면**이라 바로 아래 배경과 명도가 비슷해질 수 있고, 표면색만으로 분리되지 않는다
  - **오버레이 면은 그림자와 테두리를 함께 쓴다. 그림자만으로 분리하지 않는다** — 다크에서는 검은 그림자가 거의 보이지 않아 **실제 분리는 테두리와 표면색이 담당한다.** 테두리는 토큰이 아니라 컴포넌트가 가지므로, 새 오버레이가 그림자 토큰만 가져다 쓰면 **다크에서 분리가 사라지고 라이트에서는 멀쩡해 보인다**
  - 이 값들은 개발이 채운 값을 확정자가 그대로 채택했다(`#930`). ⚠️ 설명은 「한 단 넓고 **옅게**」가 아니다 — 그렇게 적히면 나중에 그 문장을 근거로 값을 낮춘다
- **포커스 링** 〔**확정**〕: Light·Dark 모두 `3px solid <focus>` + `outline-offset: 2px` — **알파를 넣지 않는다**
  - 〔**확정** 2026-09-18 ⓐ · `#1167`〕 **`outline`으로 그린다. `box-shadow`로 그리지 않는다.** 종전 토큰은 `box-shadow` 값(`0 0 0 3px …`)이었는데 **11개 규칙이 그것을 `outline:`에 넣고 있었다** — `outline` 단축은 `width | style | color`만 받으므로 값이 넷이면 **파싱에 실패해 선언이 통째로 버려진다.** 그 결과 **20개 선택자에 포커스 링이 아예 없었다**(`§14` 「키보드 포커스 가시화」 위반). 화면은 정상으로 보이고 **키보드로 다니는 사람에게만 드러난다**
    - `outline`이 나은 이유가 둘 더 있다 — ⑴ 요소가 이미 가진 고도 그림자를 **덮지 않는다**(`box-shadow`로 링을 그리면 포커스 순간 `--shadow-lv1`이 사라진다) ⑵ 시스템 고대비 모드가 쓰는 축과 같다
  - 〔**확정** 2026-09-18 ⓐ · `#1167`〕 **`outline-offset: 2px`를 함께 준다.** 라이트에서 `<focus>`와 `<primary>`가 **둘 다 `#1a365d`**라, Primary 채움 버튼에서는 링과 버튼 면의 경계가 `1.00`이 되어 **「버튼이 3px 커졌다」로 읽힌다.** 링 바깥은 카드 대비 `12.14`라 규격 위반은 아니지만(아래 기준 해석 참조 — `2.4.11`은 2.2 항목이다) 뚜렷한 링은 아니다. `2px` 틈이 그 사이에 면색을 넣어 **링을 링으로 만든다** — 색을 바꾸지 않으므로 Figma 세트를 거치지 않는다
  - Deep Navy는 다크 배경에서 묻히므로 링 색을 밝은 파랑으로 분리한다
  - v2.5까지 `.25`·`.35` 알파가 적혀 있었고, 그 한 줄이 **8개 면 조합을 전부 3:1 미만으로** 떨어뜨렸다(`#829` ⑴). 색 자체는 옳았고 반투명이 배경과 섞인 것이 원인이다
  - 〔**재확인** 2026-09-12 확정 L〕 **9/11 확정 E(45% 알파)는 폐기한다.** 45%는 라이트 2.50/2.45 · 다크 2.03/2.10으로 **네 면 모두 3:1 미만**이고, 불투명은 12.14/11.21 · 4.82/5.70으로 네 면 모두 여유가 있다
    - 기준 해석도 정리됐다 — 3:1이 `2.4.11`(WCAG 2.2)만의 것이 아니다. **`1.4.11 Non-text Contrast`(2.1 AA)가 「구성요소와 *상태*를 식별하는 시각 정보」를 대상으로 하고 포커스는 상태**다. 2.2가 `2.4.11`을 신설한 것은 대비를 처음 도입해서가 아니라 크기·면적 요건을 더해 시험 가능하게 만들기 위해서다
    - ⚠️ 이 문서 안의 CSS 예시 블록이 v2.5의 알파 값을 그대로 갖고 있었다(`rgba(26,54,93,.25)`·`rgba(91,143,212,.35)`). **본문은 불투명, 예시는 반투명**이라 예시를 복사하면 되살아난다 — 코드(`tokens.css:348`)와 같은 형태로 고쳤다
    - ⚠️ **별건**: 링이 Primary 버튼 위에서는 보이지 않는다(`--semantic-focus`와 `--semantic-primary`가 라이트에서 같은 `#1a365d`, 다크 1.28). `1.4.11`의 「adjacent color**s**」에는 링이 감싸는 요소 자체도 들어간다 — 안쪽에 표면색 띠를 더하는 해법이 제시됐고 별도 판단 대상이다
  - 고대비 모드(`forced-colors`)는 `box-shadow`를 그리지 않으므로 `outline` 폴백을 함께 둔다

---

## 6. 여백 · 간격

- **스케일**: 4 · 8 · 12 · 16 · 24 · 32 · 48 (4/8 그리드)
- 카드 내부 패딩 16–20 · 패널 간 gap **24** · 페이지 외곽 여백 **24**

> 🔒 **패널 간 gap 24는 사이드바↔본문 간격을 포함한다.** `#707`(패널 셸) 이후 그 자리가 패널 간 gap과 동일한 성격이므로 이 절이 소유한다. `§7.1`은 이 값을 참조한다 — 별도로 값을 정의하지 않는다.

---

## 7. 레이아웃 · 반응형 🔒

### 7.1 폭 정책

화면 유형에 따라 분기한다.

| 화면 유형 | 최대 폭 | 좌우 패딩 |
|---|---|---|
| 대시보드, 차트, 시나리오 비교 | full-bleed (max 1920) | 패널 간 gap → §6 (24) |
| 폼, 설정, 상세 조회 | 1440 | 24 |

차트가 주인공인 화면에서 1440으로 묶으면 1920 모니터에서 좌우 240px씩 낭비된다.

**그리드 🔒**

| 항목 | 값 |
|---|---|
| 기본 프레임 | 1920 × 1080 |
| 좌측 사이드바 | 240 (확장) / 64 (축소) |
| 패널 간 gap | 24 (→ §6) |
| 콘텐츠 영역 | 1608 |
| 외곽 여백 | 24 (→ §6) |
| 컬럼 | 12 · Stretch |
| 거터 | 24 |

> **산술** 🔒: `24 + 240 + 24 + 1608 + 24 = 1920` (외곽 · 사이드바 · gap · 콘텐츠 · 외곽) — `#707` 패널 셸 배치.
>
> **v2.16 정정 (`#1450`)** — v2.9의 `240 + 24 + 1656 = 1920`은 **외곽 여백 0**을 전제했는데, 같은 문서 `§6`은 외곽 여백을 24로 적고 구현도 24였다. 패널이 떠 있는 셸이라 뷰포트 끝에 닿으면 radius·그림자가 잘려 보이므로 **24가 맞다.** 산술과 외곽 여백 행을 `§6`에 맞췄고, Figma 변수 `grid/content-width` · `grid/margin`도 같은 값(1608 · 24)으로 고쳤다 — 두 변수는 `#707` 이전 값(1680 · 32)에 멈춰 있었다. 1920 미만 뷰포트에서는 콘텐츠 영역이 줄어들 뿐 외곽 여백 · gap은 그대로다.

**주·부 영역 분할은 7:5로 고정한다.** 대시보드(지도:리스트), 선박 상세(차트:항해 패널), 감축 계획(선박 목록:비용) 모두 이 비율을 쓴다. 화면마다 다른 비율을 쓸 이유가 없고, 통일하면 컴포넌트 폭 가정이 하나로 유지된다.

> **7:5는 두 단일 때의 비율이다** (`#1451`) — 주 영역이 품어야 하는 내용(예: 감축 계획의 열 6개 표)이 7/12 칸에 들어가지 않는 폭에서는 **화면이 스스로 1단으로 접는다.** 비율을 바꾸거나 표만 전폭으로 빼는 예외는 두지 않는다. 전환점은 화면마다 그 내용의 최소 폭에서 계산한다 — 감축 계획은 `1365px` 이하(`FleetReduction.css`), 나머지는 `1100px` 이하다.

### 7.2 구조 🔒

- **브랜드와 주 네비게이션은 좌측 사이드바에 둔다.** 상단바는 우측 정렬 유틸리티 영역으로, 전역 컨텍스트(선박·항차) · 알림 · 계정만 배치한다. **네비게이션 항목을 상단바에 두지 않는다.** (UIFLOW §2)
- 상단바 유틸리티 배치 순서(좌→우): **선박 · 항차 · 알림 · 계정**
- 사이드바 항목과 순서는 **`UIFLOW §2.2.1`이 소유한다.** 본 문서는 순서를 복제하지 않고 표시 규칙만 정의한다. (`§2.2`는 화면 ↔ 계층 매핑이고, 사이드바 순서 표는 그 안의 `§2.2.1`이다 — `#1341`)
  - 미구현 항목은 숨기지 않고 **비활성 상태로 노출**한다
  - 활성 항목은 `--color-primary-subtle` 배경 + `--color-text`, 비활성은 `--color-text-faint`
  - **항목마다 아이콘 + 텍스트를 쓴다.** 아이콘은 라벨을 대신하지 않고 곁들인다 — 확장(240) 상태에서 라벨을 지우지 않으며, 아이콘은 장식이라 낭독에서 뺀다(`aria-hidden`). 축소(64) 상태에서는 아이콘만 남으므로 **항목마다 서로 다른 아이콘**을 둔다. 규격은 `§12`
  - **v2.16 정정 (`#1450` · `#1341`)** — 종전 문구는 「아이콘 없이 텍스트만 쓴다. 항목이 7개 이하라 아이콘이 판독에 기여하지 않는다」였다. `#513`으로 항목이 **9개**가 되며 근거가 먼저 무너졌고, 구현은 이미 아이콘을 쓰고 있었다. 항목이 늘수록 첫 글자만으로 훑기 어려워지고, 축소 사이드바는 아이콘 없이 그릴 수 없다

> **v2.1 정정** — v2.0은 이 자리에 사이드바 6항목과 **라우트 표**를 두었다. 라우트는 구현 소관이고 `UIFLOW`는 화면 ID로 흐름을 기술하므로, 어느 정본도 뒷받침하지 않는 중복 정의였다. 실제로 v2.0 표의 라우트 4개가 작성 하루 만에 실제 경로와 어긋났다. 중복 정의가 곧바로 드리프트를 만든 사례다.

> **계층 구조와의 관계** — `UIFLOW §2`의 3계층(선대·선박·항차)은 **데이터 범위**를 뜻하며 사이드바 항목과 1:1로 대응하지 않는다. 항차 계층 화면은 사이드바에 노출하지 않고 선박 계층에서 진입한다.
- 사이드바 **폭은 토큰으로 분리**하고 클래스 토글로 접히도록 구조를 잡는다(접힘 동작 자체는 MVP 범위 밖).
- 메인은 패널/카드 그리드
- 🔒 패널 간 유기적 데이터 연동 — 선박 선택 시 대시보드·추정 패널 동시 갱신
- 브레이크포인트: 데스크톱 기본. 태블릿/모바일은 graceful degrade (MVP 우선순위 낮음)
- **인증 화면의 브랜드 판은 `1100px` 이하에서 숨기지 않고 접는다** 🔒 [#747 3-3] — 판을 제거하지 말고 **로고 + 한 줄을 카드 위에 남긴다.**
  - 그 판이 `UIFLOW §0`의 **「서비스 소개」를 충족시키는 자리**다. 통째로 숨기면 **좁은 화면에서 `§0` 요구가 충족되지 않는다**
  - 심사·시연이 모바일에서 열리면 그 화면에는 서비스 소개가 아예 없게 된다
  - 접는 것은 **소개 본문뿐**이고 로고·태그라인은 남는다

---

## 8. 컴포넌트

- **버튼**: Primary(navy 채움 / 흰 글자 · 테두리 `--color-primary-solid-border` — `§14` · `#1296`) · Secondary(아웃라인) · Danger / 높이 40 · radius 8 · hover = Primary Hover
- **텍스트 버튼** 🆕 [#1266]: 배경도 테두리도 없이 글자만 둔다 — 투명 배경 · `--text-primary` · hover `--surface-inset` · radius는 컨트롤 값 · 포커스 링은 §5. **주 동작이 아닌 자리**에 쓴다
  - 쓰는 곳은 현재 상단바 둘 — 계정 트리거와 로그아웃. 그 자리에 Primary 채움을 두면 **구현된 9개 화면 전부에서 대비가 가장 높은 요소**가 되어 그 화면의 주 동작(`계산하기`·`비교하기`·`시뮬레이션 실행`)과 같은 무게로 경쟁한다
  - **선박 상세 항차 카드의 「이 항차 취소」** (#1551) — 카드마다 반복되므로 외곽선으로 두면 카드의 주 버튼(다음 단계 하나)과 같은 무게로 경쟁한다. 취소는 되돌릴 수 없지만 항차 상태 전환이고 데이터를 지우지 않는다. **hover 면은 `--surface-card`** 다 — 항차 카드 자체가 `--surface-inset`이라 규격 hover가 보이지 않는다
  - **선박 상세 항차 카드의 「확정 되돌리기」** (#1598) — 오류 정정에만 쓰는 뒤로 가는 전환이라 주 버튼 틀(「{상태}로」)에 두지 않는다. 취소와 같이 **카드 안 확인 줄**(경고 스트라이프 · 무엇이 달라지는지 · 동사 실행 버튼 + 「그만두기」)을 거친다 — 되돌릴 수 없거나 감사 기록이 남는 동작을 텍스트 버튼에 둘 때의 조건이다
  - **Secondary(아웃라인)로 대신하지 않는다.** `--color-border-strong`이 `1.4.11`에 미달이라(라이트 1.60 · 다크 1.57 · 기준 3.0) 경계색이 교체되기 전에는 아웃라인이 §14를 통과하지 못한다. 텍스트 버튼은 경계가 없어 **그 의존이 없다**
  - **Danger의 자리를 대신하지 않는다.** 되돌릴 수 없는 동작은 여전히 Danger다 — 로그아웃은 다시 로그인하면 되돌아오므로 여기 해당하지 않는다
  - 〔**확정**〕 **높이는 같은 줄에 놓인 컨트롤과 같다** 〔2026-09-22 · `#1297` · `rlatnals4114`〕 — 혼자 놓이면 `--target-icon-button`(32px)이고, **어느 경우에도 24px 밑으로 내려가지 않는다**(`2.5.8` 최소 타깃). 값 하나가 아니라 규칙으로 정한 것은 쓰는 자리가 넷이 되면서 이웃이 제각각이기 때문이다 — 상단바(로그아웃 · 계정 트리거)는 둘 다 `--target-icon-button` 32px, 항차 카드(취소 · 확정 되돌리기 · 「그만두기」)는 옆 카드 버튼과 같은 상자(위아래 `--spacing-xs` + 1px 테두리 — 텍스트 버튼은 투명 테두리)로 26px이다. 종전 실측(폰트 적재 후 렌더링)은 계정 트리거 40 · 로그아웃 32 · 카드 텍스트 버튼 24 · 이웃 카드 버튼 26이었다. 회귀는 `textButtonHeight.test.ts`가 **값이 아니라 같다는 것**으로 잠근다
- **입력·셀렉트**: surface-2 배경 · border · radius 8 · 라벨 상단 · 포커스 링
- **카드/패널**: surface · border · radius 12 · 그림자 Lv1 · 패딩 16–20
- **칩/배지**: pill · 시맨틱 색 · 점 인디케이터
- **등급 배지 (`GradeBadge`)**: `bg` 배경 + `border` 테두리 + `text` 문자. **Variant = `grade`(a·b·c·d·e·none) × `size`(xs·sm·lg) = 18개**
  - 크기별 규격 — `lg` 20px/패딩 4·12/radius 8 · `sm` 13px/패딩 3·8/radius 4 · `xs` 11px/패딩 2·6/radius 4
  - **오토 레이아웃 필수** — `—`가 `A`보다 넓어 고정 크기로 만들면 `none`에서 문자가 잘린다
  - **패턴을 넣지 않는다** (§2.4.4). 등급 문자가 보조 채널을 이미 충족한다
  - **문자를 뺀 variant를 만들지 않는다** — 문자가 없으면 §14 위반이 된다
- **규제 플래그 (`RegulatoryFlag`)**: 등급 배지와 **별도 컴포넌트**로 배지 옆에 놓는다. D 3년 연속·E 1년차 등 규제 트리거는 등급과 별개 정보다. 같은 D등급이라도 1년차와 3년차는 배지가 동일해야 하고 플래그만 달라야 한다
- **CII 적용 대상 배지 (`ApplicabilityBadge`)** 🆕 [#653]: 그 선박이 **애초에 규제 대상인가**를 알린다. `RegulatoryFlag`와 나란히 놓이되 **또 다른 축**이다 — 플래그는 「규제 의무가 걸렸다」, 이 배지는 「적용 범위 안인가」를 말한다. **표시 조건·자리는 §8.2가 정한다**
- **브랜드 로고 (`BrandLogo`)** 🔒 [#747 3-2]: **가로형 한 벌만 둔다.** 밝은 배경용·어두운 배경용 두 파일을 테마에 따라 고르며, 어두운 색면(인증 브랜드 판 등) 위에서는 **테마와 무관하게 어두운 배경용**을 쓴다
  - **세로형 락업은 제작하지 않는다.** 화면 하나 때문에 세로형을 만들면 **락업이 둘이 되고 어디에 무엇을 쓰는지 규칙이 없어진다**
  - 필요해지면 여백·최소 크기 규정을 포함한 **브랜드 에셋 이슈로 별도 처리**한다
  - 크기는 쓰는 쪽이 정한다 — 한 파일을 CSS `filter`로 반전하지 않는다. 네이비와 액센트가 함께 뒤집혀 브랜드가 깨진다
- **등급 스케일 바**: A–E 구간 + 현재 위치 마커. 등급 배지와 분리된 별도 컴포넌트다
- **슬라이더** 🔒: 채움은 **시맨틱 Primary 단색**을 쓴다 — 토큰은 **`--color-primary-solid`**다(`--color-primary` 아님). 채움은 **면**이고, `--color-primary`를 그대로 면으로 쓰면 다크에서 깨진다(`tokens.css` 「Primary 채움면」 주석). 등급 색으로 칠하지 않는다
  - 채움 길이가 이미 조정량을 뜻하므로, 색에 등급까지 실으면 한 요소가 두 의미를 진다
  - 붉은 슬라이더는 「위험·오류」로 읽히지만 실제로는 사용자가 방금 한 조작 결과다
  - 등급은 같은 행의 `GradeBadge`가 문자와 색으로 이미 전달한다
  - 비활성 상태(조정 불가)는 트랙 `--color-surface-2` + 썸 `--color-border-strong`. 사유 텍스트를 함께 표기한다
  - 〔**확정**〕 **키보드 이동 단위 = 그 값의 `§4.2` 자릿수** — 숫자를 새로 정하지 않고 규칙으로 둔다. 속력이면 `§4.2`가 이미 「0.1 kn」으로 근거까지 적었다. ←→ 1단위 · PageUp/PageDown 10단위 · Home/End 최소·최대(ARIA 슬라이더 표준 패턴)
  - 〔**확정**〕 **값 표시 자릿수 = `§4.2`**
  - 〔**확정**〕 **썸 포커스 = `§5` 포커스 링** — `§14` 「키보드 포커스 가시화」 대상이다
  - 〔**확정**〕 **값 표시는 행 우측 고정 위치** 〔2026-09-17 ⓑ〕
    - ⚠️ **값 표시를 썸 위 툴팁으로 두지 않는다** — 선박별 행에 들어가는 슬라이더라 툴팁이 위아래 행을 덮고, 값이 좌우로 움직이면 **열 정렬이 무너진다**(`§4.2`가 용량 자릿수를 고정한 이유와 같다)
  - 〔**확정**〕 **트랙 두께와 썸 크기는 브라우저 기본을 쓴다** 〔2026-09-17 ⓑ · `#1052`〕 — 치수를 지정하지 않고 `accent-color`(= `--color-primary-solid`) 한 줄로 채움 규격을 지킨다
    - 2026-09-11 확정 K는 이 셋을 〔제안〕으로 두고 *「구현이 0건인 상태에서 치수를 확정하면 **검증 없이 굳는다**」*고 적었으며 기한을 「`UIFLOW 2-10` 범위 복원 시점」으로 달았다. **그 시점이 지났다** — `#513`이 2026-09-13에 `2-10`을 범위 안으로 옮겼고 구현이 있다. 같은 줄의 「`2-10`은 구현 대상이 아니며」도 그때부터 사실이 아니었다
    - 그 구현이 확정분 **넷을 이미 모두 지킨다** — 채움 색 · 값 위치 · 썸 포커스 · 키보드 단위. 출발점으로 적어 둔 트랙 `4px` · 썸 `24px`은 폐기한다
    - ⚠️ **네이티브 위젯을 버리지 않는다.** 트랙·썸을 직접 그리려면 `-webkit-appearance: none`이 필요하고 **그 순간 `accent-color`가 칠하지 않는다.** WebKit에는 `::-moz-range-progress`에 해당하는 의사요소가 없어 **채움을 값 기반 그라데이션으로 직접 계산**해야 한다 — 치수 둘을 얻고 채움 구현의 단순함과 브라우저별 동작의 일관성을 잃는다
  - 키보드 단위 · 값 자릿수 · 썸 포커스는 `rlatnals4114`의 2026-09-11 **확정 K**이고, 치수 둘과 값 표시 위치는 2026-09-17 **확정 ⓑ**다 (`#513` · `#485` 항목 6)
- **신뢰도 배지 (`DataConfidenceBadge`)** 🆕: 등급이 실측이 아닌 값으로 계산됐음을 알린다. `GradeBadge` 옆에 놓되 **등급 색을 쓰지 않는다** — 시맨틱 Warning/Danger를 쓴다. **표시 임계는 §8.1이 정한다**
- **테이블**: 시나리오 비교용 · 행 hover · 수치 tabular-nums · 숫자 우측 정렬
- **경고/토스트·면책 배너**: 상태색 좌측 스트라이프 (좌측 테두리만 있을 경우 radius 0)

### 8.1 신뢰도 배지 표시 임계 🔒 [#583]

`DataConfidenceBadge`를 **언제 붙이는가**를 정한다. 「언제 붙는가」가 이 컴포넌트의 본체다 — 정해 두지 않으면 구현이 임계를 지어내고, **화면이 깨지지 않으므로 정본과 갈린 사실이 늦게 발견된다.**

**층위는 `PRD §8.3` 값 우선순위를 그대로 따른다. 본 문서에 옮겨 적지 않는다.**

```text
CONFIRMED actual              ← 실측
COMPLETED actual              ← 실측
────────────────────────────────  배지를 붙이는 경계
IN_PROGRESS latest estimate
PLANNED adopted scenario
PLANNED initial plan
SAMPLE default
```

- **컷은 `IN_PROGRESS latest estimate` 이하 전부**다. `COMPLETED actual`까지는 실측이고 그 아래는 전부 추정이라 경계가 자연스럽다.
- **예외 — `COMPLETED`인데 `actual_fuel_ton`이 NULL인 항차에는 붙인다.** `PRD §8.3 [ORACLE-C-4B]`가 그 경우 계획값을 임시 실적으로 대입하게 하므로, 층위상 `COMPLETED actual`이지만 **값의 출처는 실측이 아니다.** 같은 상황에 `API_SPEC`의 `COMPLETED_NO_FUEL` warning이 이미 나가고 있어 배지와 문구가 같은 사실을 가리킨다.
- 시각 표현은 §8 본문이 정한다 — `GradeBadge` 옆, 시맨틱 Warning/Danger, **등급 색을 쓰지 않는다.**

> **판정 기준을 복제하지 않는 이유.** 두 곳에 같은 규칙이 생기면 어긋났을 때 **어느 쪽이 맞는지부터 판단해야 한다.** `#463`이 사이드바 라우트를 본 문서와 `UIFLOW` 양쪽에 두었다가 v2.0 표의 라우트 4개가 어긋난 것이 같은 형태이고, 그래서 v2.1이 그 표를 지웠다.

> **종전에는 임계를 `UIFLOW`의 절 하나에 위임하고 있었다.** 그런데 `UIFLOW`는 전 이력에서 최상위 절이 `0`·`1`·`2`뿐이라 **그 절이 존재한 적이 없었고**, `#485` ⑤가 착수 불가로 멈춰 있었다. 임계는 값의 출처를 판정하는 규칙이라 화면 흐름이 아니라 **표시 규칙**이며, `AGENTS §3.2` 소관상 본 문서가 가져오는 것이 맞다 (#583).

### 8.2 CII 적용 대상 배지 🔒 [#653]

`ApplicabilityBadge`를 **언제·어디에 붙이는가**를 정한다. §8.1과 같은 이유로 조건을 정본에 둔다 — 정해 두지 않으면 구현이 자리를 지어내고, 화면이 깨지지 않으므로 어긋난 사실이 늦게 발견된다.

**판정 자체는 본 문서가 소유하지 않는다.** 서버가 `is_cii_applicable_hint`로 내리며(`API_SPEC §2.3`), 임계값(GT 5,000)의 정본은 IMO 원문이다. 본 문서는 **그 판정을 화면에 어떻게 보이는가**만 정한다 (`AGENTS §3.2.3`).

#### 3상태

| 상태 | 조건 | 배지 |
|---|---|---|
| 해당 | `is_cii_applicable_hint = true` | **그리지 않는다** |
| 미해당 | `false` + `gross_tonnage` 있음 | `규제 대상 아님` |
| 판정 불가 | `false` + `gross_tonnage` 없음 | `GT 미입력` |

- **해당일 때 배지를 그리지 않는다.** 정상 상태에 배지를 붙이면 목록 스무 줄이 전부 배지로 덮여 **정작 예외인 선박이 묻힌다.** `RegulatoryFlags`가 빈 배열에서 아무것도 그리지 않는 것과 같은 판단이다.
- **「미해당」과 「판정 불가」를 합치지 않는다.** 같은 말을 하면 **총톤수를 넣지 않은 사용자가 「이 배는 규제 대상이 아니다」로 읽는다.** 두 상태는 사용자가 할 일이 다르다 — 전자는 확인, 후자는 입력이다.

#### 붙이는 자리

**선박을 식별하는 자리마다** 붙인다. 한 화면에만 두면 그 화면을 지나친 사용자는 알 수 없다.

| 화면 | 자리 |
|---|---|
| 대시보드(선대) `UIFLOW 2-4` | 선박 카드 — `RegulatoryFlags` 옆 |
| 선박 목록 | 선박명·IMO 행 |
| 선박 상세 | 제목(선박명) 옆 |

계산 **결과** 화면(기능①·②·실시간 CII·리포트)은 배지 대신 `API_SPEC §1.6` warning(`NON_CII_VESSEL` · `CII_APPLICABILITY_UNKNOWN`)으로 같은 사실을 전달한다 — 그 화면들에는 이미 경고 채널이 있고, 배지를 겹쳐 두면 같은 사실이 두 번 나온다.

#### 시각 표현

- **등급 색(`--cii-*`)을 쓰지 않는다.** `GradeBadge` 옆에 놓이므로 등급 색을 쓰면 두 배지가 같은 축을 말하는 것처럼 읽힌다.
- **시맨틱 Warning을 쓴다 — Danger가 아니다.** Danger는 `RegulatoryFlag`가 쓰며 **규제 의무가 발생한** 상태다. 이 배지는 의무가 아니라 적용 범위 정보이므로 색이 같으면 두 상태가 같은 심각도로 읽힌다.
- 문자색은 `--color-warning-text`다. `--color-warning`은 `--cii-c-fill`의 별칭이라 라이트에서 문자 대비가 **3.09:1**이며 §0.2 제약 1(4.5:1)에 못 미친다.
- 규격은 `RegulatoryFlag`와 같다 — 13px / 패딩 3·8 / radius 4 / 테두리형. 같은 자리에 나란히 놓이는데 높이가 다르면 목록의 행 정렬이 흔들린다.
- **짧은 라벨 + 전체 문구.** 배지에는 위 표의 짧은 라벨만 적고, 전체 문구는 `title`·`aria-label`로 보낸다(§12 · §14). 목록에서 여러 척을 훑을 때 한 줄이 두 줄로 접히면 표가 무너진다.

> **전체 문구는 본 문서가 소유하지 않는다.** 「미해당」은 `PRD §6.3`「공식 적용 대상 아님」 행, 「판정 불가」는 `API_SPEC §1.6` `CII_APPLICABILITY_UNKNOWN` 행의 사용자 메시지를 그대로 쓴다 — §13이 정한 위임을 따른다.

### 8.3 등급 전이 표기 🔒 [#513]

`C → B`처럼 **조정 전후의 등급**을 한 자리에 보일 때의 배지 조합 규칙이다. `§8.1`·`§8.2`와 같은 성격이라 `§2.4`(색)가 아니라 여기에 둔다. **`rlatnals4114`의 2026-09-11 확정 J**이며, 쓰는 화면(`UIFLOW 2-10`)이 1차 심사 범위 밖이라 **범위 복원 시 착수를 앞당기기 위한 선결정**이다.

- **새 컴포넌트를 만들지 않는다.** `GradeBadge` 두 개를 나열하고 사이에 **연결자**를 둔다
- **연결자는 중립색이다.** 등급 색도 시맨틱 색도 쓰지 않는다 — 두 배지가 이미 등급 색을 쓰므로 연결자를 「개선이니 Success 초록」으로 칠하면 한 컴포넌트 안에 등급 색과 시맨틱 색이 동시에 들어가 **`§0.2` 제약 2 위반**이다. `DataConfidenceBadge`(`§8.1`)·`ApplicabilityBadge`(`§8.2`)가 「`GradeBadge` 옆이므로 등급 색을 쓰지 않는다」로 같은 판단을 했다
- **개선·악화를 색으로 말하지 않는다.** `C`와 `B`라는 문자가 이미 말하고, 비교 표에서는 「조정 전 / 조정 후」 행 라벨이 방향을 말한다
- ⚠️ **`§4.3`(▼▲ + 부호)을 끌어오지 않는다.** 그것은 수치 증감용이고 등급은 수치가 아니다. 붙이면 등급 배지 옆에 시맨틱 색이 다시 들어온다
- **변화가 없으면 전이를 그리지 않고 배지 하나만 둔다.** `§8.2`의 「해당일 때 그리지 않는다」와 같은 계열이다 — 바뀌지 않은 것에 화살표를 그리면 바뀐 행과 구분되지 않는다


### 8.4 폼 필드 · 필드 오류 🔒 [#936]

2026-09-10 확정 1-1이 에러 층위를 **페이지 / 영역 / 필드** 셋으로 나누고 필드를 폼 규격으로 이관했다. `#694`(`PR #927`)가 앞의 둘을 `ErrorState`로 닫았고, 이 절이 남은 하나를 닫는다.

- **`Field` 컴포넌트 하나로 닫는다.** 화면마다 두지 않는다
  - 신설 시점에 `Field`가 **넷** 있었고(`VoyageCiiForm`·`VesselRegistration`·`VoyagePanel`·`AuthShell`), 앞의 둘은 **접두어를 빼면 완전히 동일**했다. 필드 오류 클래스는 **16종**이었다 — `§16` 항목 8이 짚은 「클래스 이름 24종」이 필드 층위에서 그대로 재현된 것이다
  - 컴포넌트가 라벨·힌트·오류·`aria` 배선을 **함께** 들고 있어야 한다. 배선만 호출부에 남기면 46곳 중 39곳처럼 연결이 빠진다
- **오류는 입력칸 아래에 둔다.** 라벨 → 입력칸 → 힌트 → 오류 순이다
- **면을 만들지 않는다 — 문자색만 쓴다.** `--color-danger-text` · `--font-size-micro`
  - 페이지·영역(확정 1-3)은 「중립 배경 + 테두리」를 갖지만 **필드는 배경도 테두리도 두지 않는다.** 입력칸이 이미 `aria-invalid`로 테두리를 바꾸므로, 오류 문구까지 면을 가지면 한 자리에 면이 둘이 된다
  - 아이콘을 붙이지 않는다. `§2.5`의 경고 아이콘은 **위험도 표기**의 것이고 검증 실패의 것이 아니다
- **재시도 버튼을 두지 않는다** 〔**확정** 2026-09-10 1-2〕. 고칠 대상이 화면에 있으므로 다시 시도할 것이 없다
- **라벨은 감출 수 있으나 지우지 않는다** — 칸이 하나뿐이고 placeholder가 같은 말을 하는 자리에서는 라벨을 **화면에서만** 감춰도 된다(`Field`의 `labelHidden`, 전역 `.sr-only` — `#831 ⑸`)
  - **지우는 것과 다르다**: 지우면 낭독에서 이름이 사라진다. 자리를 아끼는 것이지 이름을 없애는 것이 아니다
  - 감출 때는 `labelEn`·`unit`도 함께 감춰지므로 **보여야 할 것이 있으면 쓰지 않는다**
  - 화면이 제 나름의 감춤 클래스를 만들지 않는다 — `.sr-only`는 전역 유틸리티가 소유한다(`#831 ⑸`)
- **낭독 배선은 컴포넌트가 강제한다**
  - 오류에 `role="alert"` — 없으면 검증 실패가 낭독되지 않는다
  - 입력칸에 `aria-invalid` — 색만으로 말하지 않는다(`§14`)
  - 오류·힌트를 `aria-describedby`로 잇는다. 둘 다 있으면 **오류를 먼저** 적는다
- **서버 검증(422)과 클라이언트 검증을 같은 자리·같은 모양으로 보인다.** 사용자에게는 둘 다 「이 칸이 틀렸다」이고, 출처는 고치는 방법을 바꾸지 않는다. 422 문구는 `API_SPEC §1.3.2`가 한국어로 내려보낸다(`#900`)
- **여러 칸이 틀려도 요약을 두지 않는다.** 각 칸 아래의 오류가 곧 목록이고, 요약을 얹으면 같은 문장이 두 번 나온다
  - 예외는 **칸에 귀속되지 않는 오류**다(「선박을 선택해 주세요」처럼 폼 전체 조건). 그것만 폼 상단에 한 줄로 둔다
- **문구는 본 문서가 소유하지 않는다** — `PRD §6.3`·`§6.4`에 위임한다(`§13` 규율)

### 8.5 어시스턴트 오버레이 — 「답이 아닌 것」의 표시 🔒 [#1051]

`PRD §20 O-12`가 **MAY · 실험**이라 이 절은 **치수·문구·겹침 순서를 정하지 않는다**(`§16` 항목 16·17). 정하는 것은 **접근성 규칙 하나** — 답이 아닌 것이 답으로 읽히지 않게 하는 채널이다. **기능이 실험이라고 `§14`가 면제되지 않는다.**

- **「답이 아닌 것」이 셋이다** — **버린 답**(서버가 답을 만들고 폐기 · `API_SPEC §15.2`) · **호출 실패**(답을 받지 못함) · **면책**(답에 늘 따라붙는 단서). 셋을 같은 모양으로 두지 않는다
- **버린 답은 채널 둘로 표시한다** 〔**확정** 2026-09-17 ⓐ〕 — ⑴ 좌측 줄무늬 ⑵ **접두 문구**
  - `§14`가 색 단독 구분을 금지한다. 줄무늬 하나로는 모자라고, **접두 문구는 낭독에 실리는 유일한 채널**이다
  - **색 이름을 이 절에 적지 않는다.** 줄무늬가 어느 토큰을 쓰는지는 값의 문제이고 Figma가 소유한다(`§0.2`) — 규격은 「줄무늬 + 문구」라는 **채널**로 적는다. 그래야 값이 바뀌어도 규격이 산다
  - 접두는 서버가 준 폐기 사유를 **대체하지 않고 앞에 붙는다**(`API_SPEC §15.2`)
- **면책에는 줄무늬를 쓰지 않는다** 〔**확정** 2026-09-17 ⓓ〕
  - 면책은 답이 있을 때마다 **상시** 붙는다. 경고 줄무늬를 거기 두면 경고가 늘 떠 있어 뜻이 닳는다
  - 종전에는 버린 답과 **배경·줄무늬·글자색 셋이 모두 같아** 사용자가 둘을 가를 수 없었다. 코드 주석이 *「같은 모양으로 두면 「답을 받았다」고 읽는다」*고 적어 두고도 **면책과 같은 모양이 된 것을 보지 못했다**
  - 면책의 **원문은 서버가 소유한다**(`API_SPEC §15.5`). 본 절이 정하는 것은 표시 방법뿐이다
- **호출 실패는 줄무늬를 함께 쓰되 접두 문구를 붙이지 않는다**
  - 둘 다 「답이 아니다」이므로 줄무늬는 같다. 갈리는 것은 문구이며, 실패 문구는 그 자체로 완결된 문장이라(「챗봇을 사용할 수 없습니다.」) 접두를 붙이면 겹말이 된다
  - ⚠️ **모델에서도 갈라 둔다.** 종전에는 실패도 「버린 답」 플래그를 써서 **타입 주석의 정의와 코드가 어긋나** 있었다. 접두 문구를 넣자 겹말로 드러났다 — 표시를 정하면 모델의 어긋남도 함께 드러난다
- **실패를 화면 단위 에러로 올리지 않는다** — 말풍선으로 둔다(`PRD §16.2` 장애 격리). 던지면 대시보드가 통째로 오류 화면이 되고, 그것이 곧 격리 실패다
- **문구 원문은 본 문서가 소유하지 않는다** — `PRD §6.3`·`§6.4`에 위임한다(`§13` 규율). 접두 문구가 갈 자리는 `§16` 항목 17이 다룬다

---

## 9. 차트 규약 🔒

이 제품의 핵심 화면이므로 토큰을 명시적으로 고정한다.

### 9.1 축 · 그리드

| 요소 | 값 |
|---|---|
| 그리드선 | `#e8edf2`, 1px, 가로만 |
| 축선 | `#c3ced9`, 1px |
| 축 라벨 | text-muted, 12px, tabular-nums |
| 세로 그리드선 | 사용하지 않음 (시간축) |

### 9.2 실적 vs 예측

| 구간 | 선 스타일 | 색 |
|---|---|---|
| 실적 `CII_ytd` | 실선 2px | `#1a2733` (text) |
| 예측 `CII_proj` | 점선 `6 4`, 2px | 시나리오 계열색 (§9.3) |

- 실적/예측 경계에 세로 구분선 1px + "오늘" 라벨을 둔다
- 실적 구간은 시나리오와 무관하므로 단일 선으로 공유한다

### 9.3 시나리오 계열 (최대 3개)

등급 램프가 녹–황–주–적 대역을 점유하므로, 계열색은 **한색 계열로 한정**한다.

| 슬롯 | 색 | 마커 | 흰 배경 대비 |
|---|---|---|---|
| S1 (기준) | `#1a365d` | 원형 r=5 | 11.2:1 |
| S2 | `#2b6cb0` | 사각 10×10 | 5.4:1 |
| S3 | `#6b46c1` | 삼각 | 6.4:1 |

- 4개 이상은 지원하지 않는다. 필요 시 표로 전환한다
- 마커는 색과 독립된 보조 채널이므로 색각이상 대응으로 충분하다 (§14)
- 범례는 캔버스 밖 상단에 배치하고, 색 사각형 + 마커 + 시나리오명을 함께 표기한다

> **적용 범위 🔒 (v2.0)** — 본 절은 차트의 **계열(series)**, 즉 선과 영역에 한정한다. 슬라이더·토글·칩 등 **컨트롤은 대상이 아니며**, 컨트롤은 시맨틱 Primary를 쓴다 (§8).

### 9.4 등급 경계선

| 항목 | 값 |
|---|---|
| 색 | `#c3ced9` (border-strong) — **등급색 사용 금지** |
| 스타일 | 1px dashed `3 3` |
| 라벨 위치 | 차트 우측 바깥, 구간 중앙에 등급 문자 |
| 구간 배경 | **조건부** — 아래 단서 참조 |

> **구간 배경 조건부 허용 🔒 (v2.0)** — v1.2는 무조건 금지였다. 원래 취지는 **불확실성 밴드(§10.1 팬 차트)와 겹치면 판독이 불가능**해지는 것을 막는 데 있었다. 따라서 다음으로 나눈다.
> - **불확실성 밴드가 있는 차트** — 구간 배경을 칠하지 않는다. 기존 규정 그대로다
> - **결정론 추이 차트** (실적선 + 단일 예측선) — 등급 구간을 **불투명도 0.16 이하**로 칠할 수 있다. CII 값 자체는 의미가 없고 등급 경계 대비 위치가 의미이므로, 배경 밴드가 있으면 「지금 어느 구간에 있고 어디로 가는가」가 즉시 읽힌다
> - 구간을 칠하는 경우 **좌측 축에 등급 문자를 반드시 병기**한다 (§0.2 제약 2·3)
> - 밴드 높이는 균등 분할이 아니라 **`d1`~`d4` 실제 경계값에 비례**해야 한다. 실제 경계는 균등하지 않다

### 9.5 지도 위 등급 표시 🔒 (#763)

선대 대시보드의 지도(`UIFLOW 2-4`)는 **배경을 우리가 그리지 않는다** — 타일이 색을 정한다. 그래서 등급 표시 규약을 여기 따로 둔다.

| 항목 | 값 |
|---|---|
| 마커 모양 | **배 모양**(`components/vesselShape`) — 개략도·선박 카드와 같다 |
| 채움 | `--cii-{등급}-fill`. 등급이 없으면 `--cii-none-fill` |
| 등급 표시 | **문자 배지를 마커에 붙인다**(A~E). 등급이 없으면 `—` |
| 주의 대상 | 배지 테두리 2px `--semantic-danger` |
| 항로선 | `--semantic-info` · 2px · **파선** `2 1.5` |

> ⚠️ **무늬(`§15.1` 등급 패턴)를 쓰지 않는다 — 문자가 그 자리를 대신한다.** 지도 마커는 DOM 요소라 SVG `<pattern>` 정의를 가져올 수 없고, 22px 마커에서 무늬는 어차피 읽히지 않는다. `§14`가 금지하는 것은 **색 단독**이며, 등급 문자가 **항상** 함께 놓이므로 조건은 충족된다(`§15.1`이 「패턴 없는 A」를 허용하는 근거와 같다).

> **범례를 두지 않는다** 〔**확정** 2026-09-18 · `#1052` ⓥ〕. 마커마다 등급 문자가 붙고, 같은 화면 위쪽 「등급 분포」가 색↔문자 대응을 **개수와 함께** 보여 준다 — 범례를 더하면 같은 대응이 화면에 세 번 적힌다.
>
> 스스로 설명되지 않는 표식은 **주의 대상의 굵은 테두리** 하나뿐이므로 그림 아래 한 문장이 받는다. 파선은 그 문장이 이미 설명하고 있었다.
>
> **개략도(`PositionChart`)도 같은 문장을 쓴다** — 자산 유무로 두 그림이 갈리는데 읽는 법이 다르면 사용자는 다른 그림으로 읽는다.

> **대시보드 지도 카드의 자리·치수·문구는 현행대로 확정** 〔**확정** 2026-09-22 · `#1052` ⑴ · `rlatnals4114`〕 — 왼쪽 열 첫 카드 · 제목 「선박 위치」 · 메타 「사용자 입력 기준」 · 지도 영역 **8 : 5** · 그림 아래 읽는 법 한 문장(위 「범례를 두지 않는다」가 받는 문장) · 출처 표기는 지도 오른쪽 아래 접힌 「© OpenStreetMap」. **자산이 없어 개략도로 내려갈 때의 안내는 작은 보조 문단**(`--font-size-micro` · `--text-secondary`)이다 — 개략도도 위치 관계와 등급을 그대로 전하므로 경고가 아니다.

> **자산 분기에 참여하는 화면은 대시보드와 항로 비교다** 〔**확정** 2026-09-19 · `#1264` · `#1265`〕. 선박 상세(`UIFLOW 2-8`)의 「현재 위치」는 **자산 유무와 무관하게 개략도**를 쓴다.
>
> **두 화면의 폴백이 다르다.** 대시보드는 자산이 없으면 개략도로 떨어지지만, 항로 비교(`UIFLOW 2-2`)는 **아무것도 그리지 않는다** — 개략도(`PositionChart`)는 선박 기반이라 항로 하나를 담을 그림이 아니다. 그 화면의 좌표는 **선택 입력**이라 지도가 없는 상태가 이미 정상 경로이고, 빈 지도나 오류를 내면 **정상 상태가 고장으로 읽힌다.**
>
> 카드 폭이 480이고 그리는 대상이 **한 척**이라, 그 크기의 타일 지도는 배 하나와 둘레 바다만 비춘다 — 배경이 주는 맥락이 거의 없는데 지도 인스턴스 비용만 든다. `minSpan`을 넓혀 둔 것이 단일 선박에 맞춘 조정이다.
>
> ⚠️ **같은 기기에서 두 화면이 다르게 보인다.** 위 「개략도도 같은 문장을 쓴다」가 그 간극을 받는다 — 읽는 법이 같으면 두 그림은 같은 것의 두 배율로 읽힌다. **어느 한쪽이 고장났다고 읽고 되돌리지 말 것**: 갈리는 축은 자산이 아니라 **대상 수**(선대 ↔ 한 척)다.

> **항로선을 파선으로 두는 이유**도 같다 — 지도 배경에는 도로·경계선이 이미 실선으로 깔려 있어, 실선으로 그으면 **배경의 일부로 읽힌다.**

> **마커에 그림자를 한 겹 둔다**(`drop-shadow` 1px `--color-text`) 〔**정정** 2026-09-17 · `#1052`〕. 밝은 바다 위에서 밝은 등급색(A·B)이 배경에 묻힌다.
>
> ⚠️ **종전 문안의 `--surface-default`는 존재한 적이 없는 이름이었다.** `FleetMap.css`가 규격대로 그것을 썼고, 없는 토큰을 가리키는 선언은 **오류가 아니라 무효**라 **그림자가 한 번도 그려지지 않았다.** 막으려던 현상이 그대로 일어나고 있었다.
>
> **밝은 색으로는 이 목적을 이룰 수 없다.** 지도 타일은 테마를 따르지 않고 원래 밝다 — 흰 후광은 바다 위 `1.60` · 육지 위 `1.15`로 아무것도 하지 못한다. 어두운 쪽이라야 떨어진다(`--color-text` 라이트 기준 `10.87` · `15.19`). 후광 없이 잰 마커 채움은 **E를 빼고 전부 `3:1` 미달**이다(A `2.89` · B `1.59` · C `1.93` · D `2.30` · none `1.76`, 바다 기준).
>
> 배지 배경도 같은 이름을 쓰고 있었으나 그쪽은 **불투명한 면**이 필요해 `--color-surface`로 간다 — 한 이름이 서로 다른 두 일을 하고 있었다.

---

## 10. 불확실성 표현 🔒

Monte Carlo 산출물은 단일값이 아니라 분포다. 동일 데이터를 세 층위로 축약하며, 화면별로 하나씩 배치한다.

### 10.1 팬 차트 (운항관리팀 · 추정 패널)

| 항목 | 값 |
|---|---|
| 밴드 층 수 | **1겹** — P10–P90 |
| 밴드 색 | `rgba(26, 54, 93, .12)` — 등급색·계열색 사용 금지 |
| 중앙선 | **P50 (중앙값)**, 점선 `6 4` |
| 중앙선 라벨 | `"중앙값(P50)"` 명시 — 평균으로 오독 방지 |

> CII 분포는 우측 꼬리가 길다. 평균을 쓰면 실제보다 나쁘게 보이므로 P50을 사용한다.

**시나리오 비교 시 (A안)**: 선택된 1개 시나리오만 밴드를 표시하고, 나머지 2개는 중앙선만 그린다. 밴드 3겹은 판독이 불가능하다.

### 10.2 등급 확률 스택 바 (선장·항해사 · 상세 조회)

- 팬 차트 우측 끝(연말 시점) 분포를 등급 경계로 잘라낸 단면
- 가로 스택 바 1개. 각 구간은 등급 채움색 + §2.4 패턴
- 구간 폭 ≥ 8% 일 때만 내부에 `등급문자 nn%` 표기, 미만은 툴팁으로
- 예: `A 2% / B 15% / C 61% / D 20% / E 2%`

### 10.3 단일 확률 플래그 (경영진 요약 · 알림)

- §2.5 **(a)** 의 `P(D∪E)` 표기 규칙을 그대로 사용 (확률 기반 화면)
- 임계 초과 시에만 색이 붙는다

---

## 11. 데이터 출처 · 추정값 표기 🔒

면책 문구만으로는 방어되지 않는다. **화면상 어떤 숫자가 실측이고 어떤 것이 추정인지 시각적으로 구분**한다.

| 데이터 성격 | 표기 |
|---|---|
| 실측 | 기본 스타일 |
| 추정·보간 | **점선 밑줄** + `ⓘ` 아이콘 + 툴팁(출처 · 수집시각) |
| 외부 데이터 취득 실패 후 대체값 | 위와 동일 + 툴팁에 실패 사유 명시 |

- 툴팁 필수 항목: 데이터 출처명, 수집 시각(KST), 추정 방식
- 표 안에서도 동일하게 적용한다 (셀 단위)
- 항차 간 항내 소모량 등 구조적 갭이 있는 항목은 반드시 추정 표기 대상이다

**전면 추정 화면의 범위** — 화면의 **모든** 수치가 추정·보간값인 경우, 개별 표기를 **화면 단위 고지로 갈음할 수 있다.**

이때 고지에는 **추정 성격과 기준 시각을 포함**한다. **기준 시각이 무엇을 가리키는지는 `PRD`·`API_SPEC` 계약을 따른다** — 본 절은 표기 여부만 정한다.

**외부 데이터 출처가 있는 화면에서는 고지 또는 툴팁에 외부 데이터 출처명을 포함한다. 외부 데이터 출처가 없는 화면에서는 출처명 필드를 강제하지 않는다.** 실측과 추정이 혼재하는 화면에서는 개별 표기 규칙을 그대로 적용한다.

> 기능① 결과 화면이 이 경우다 — 표시 수치가 전부 사용자 입력 기반 추정이라 개별 점선 밑줄이 구분 정보를 전달하지 못한다. **외부 출처가 없으므로 별도 출처명 필드를 강제하지 않는다** — 「출처: 사용자 입력」은 배너 본문이 이미 전달하는 내용이라 정보량이 없다.

---

## 12. 아이콘

- **세트 — Lucide** 〔**확정** 2026-09-10〕. MIT 라이선스이며 **별도 표기 의무가 없다**
  - 약 1,500개로 커버리지가 가장 넓다. Heroicons가 stroke 1.5 기본값으로 규격과 정확히 맞으나 약 300개로는 부족하다
  - Lucide 기본 `strokeWidth`는 **2**라 규격과 다르다. **호출부마다 적지 않는다** — `components/Icon.tsx`가 강제한다 〔**개정** 2026-09-18 · `#1174`〕
    - **값은 코드가 갖지 않는다.** 두께도 크기도 생성 토큰(`--icon-stroke` · `--icon-inline` · `--icon-default` · `--icon-large`)에서 오며 `Icon.css`가 먹인다 — CSS 선언이 Lucide가 붙이는 SVG 표현 속성을 덮는다(Chromium 실측: 속성 `stroke-width="2"` + `var(--icon-stroke)` → 계산값 `1.5px`, 속성 `width="20"` + `inline-size: 16px` → `16px`)
    - 종전 문안의 `strokeWidth={1.5}`는 **같은 규격값을 코드에 한 벌 더 둔 것**이라 `§15`(값의 소유는 Figma)와 어긋나 있었다. 조항의 의도(호출부마다 적지 않는다)는 그대로이고 **출처만** 옮겼다 — `#164`가 단위 리터럴을 금지한 것과 같은 자리다
    - 호출부는 크기를 **이름**으로 고른다 — `size="inline" | "default" | "large"`. 숫자를 받으면 토큰에 없는 치수가 새로 생긴다. 이름 셋과 토큰 셋이 같은지는 `icon.sync.test.ts`가 잠근다
- 라인 아이콘, stroke 두께 1.5–1.6, 24px 그리드
- 단색 — text-muted 또는 accent
- 의미 전달 아이콘에는 반드시 텍스트 라벨 또는 `aria-label` 병기
  - `role` 없는 요소의 `aria-label`은 **낭독되지 않는다.** 의미를 전달하는 아이콘에는 `role="img"`를 함께 준다 (`#829` ⑸b)
- **커스텀 아이콘** — 세트에 없는 아이콘(항차·CII 등 해운 도메인)은 위 규격에 맞춰 직접 그리고, **세트 아이콘과 같은 파일·같은 방식으로 관리**한다
  - 이 규칙이 없으면 `§16` 항목 8이 겪은 「클래스 이름 24종」이 아이콘에서 그대로 재현된다
  - 등급 무늬·선박 마크·차트 도형은 **아이콘이 아니라 도메인 그래픽**이므로 이 규칙의 대상이 아니다

---

## 13. UX 라이팅 · 면책

문구 원문은 **PRD §6**에 있다 — 고지·결정 문구는 `§6.3`, 로딩·빈 상태·에러의 상태 문구는 `§6.4`. **본 문서는 위치만 규정하며 문구를 중복 정의하지 않는다.** (2026-09-11 확정 C — 종전 「§6.3」은 상태 문구가 들어갈 자리가 없어, 에러 기본 제목과 빈 상태 문구가 어느 정본에도 없었다)

- 🔒 모든 결과 화면 하단에 면책 문구 상시 노출
  > 참고용 예측값입니다. 규제 제출용 공식 결과가 아닙니다.
- 외부 데이터 / 추정값 / 기상 취득 실패 / 공식 대상 아님 / 자동 결정 금지 문구 → PRD §6.3 표 그대로 사용
- **하단 고지는 배너 한 칸이다** (#1578 · 디자인 결정 2026-09-21) — 면책 배너 옆에 `§6.3`의 다른 행(예: 「추정값 사용」)을 함께 둘 때는 **같은 배너 안 둘째 문장**으로 잇고, 배너 아래에 따로 문단을 세우지 않는다. 배너가 이미 말한 사실(「공식 결과가 아닙니다」)은 화면 고유 안내에서 되풀이하지 않는다 — 그 안내는 화면에만 있는 사실(예: 보고서의 「내부 보고용」 · `PRD §25.1`)만 말한다
- **실패 표시의 층위**(`§16` 항목 8) — **폼 컨트롤 안의 실패는 영역 실패의 한 줄(compact)이다.** 셀렉트 자리에 「선박 목록을 불러오지 못했습니다」를 두는 것은 입력값 검증이 아니라 **선택지 로드 실패**이므로 필드 층위(C)가 아니다 (2026-09-11 확정 C ⑷)

---

## 14. 접근성

- 목표: **WCAG 2.1 AA** (백엔드 이슈 #68 연계)
- 텍스트 대비 ≥ 4.5:1 — §2.4 등급 램프 전 항목 검증 완료
- **색만으로 의미 전달 금지**. 채널을 반드시 이중화한다.

| 대상 | 색 외 보조 채널 |
|---|---|
| CII 등급 — 배지 | **등급 문자** (§2.4.4) |
| CII 등급 — 지도 마커·차트 밴드·스택 바 | **패턴** (§2.4.4) |
| 시나리오 계열 | 마커 모양 (§9.3) |
| 증감 방향 | 화살표 + 부호 (§4.3) |
| 위험도 | 확률 화면은 수치 + `⚠` 아이콘, 결정론 화면은 여유율 수치 + 텍스트 라벨 (§2.5) |
| 데이터 점검 심각도 | 텍스트 라벨 + 그룹 헤더 (§2.3) |

- **등급 표시는 문자 또는 패턴 중 하나를 반드시 병행한다.** 둘 다 없는 등급 표시는 구현 금지.
  - v1.2는 「패턴 병행 필수」로만 적혀 있었으나, 등급 배지는 A~E 문자가 항상 놓이므로 문자로 충족된다. 11px 배지에 패턴을 겹치면 판독만 나빠진다
  - 반대로 **문자가 놓이지 않는 자리 — 지도 마커, 차트 등급 밴드, 스택 바 — 에서는 패턴이 필수다**
- 적록색맹 대응: 3색 체계(§2.4.1)는 초록·주황·빨강 세 색상군이 모두 황갈색 계열로 수렴하여 **5색 체계보다 취약하다.** 위 병행 규칙을 예외 없이 적용한다
- 대비 검증은 **Light·Dark 두 모드 모두**에서 수행한다 (§0.2 제약 1)
- 키보드 포커스 가시화 — §5 포커스 링. 다크 모드는 별도 링 색을 쓴다
- **프로그램적 초점 대상에는 링을 그리지 않는다** 〔**확정** 2026-09-19 · `#1283`〕 — `tabindex="-1"`로 **Tab 순서 밖**에 둔 채 스크립트만 초점을 주는 요소가 대상이다. 지금은 둘 — 화면 전환 시 초점을 받는 `<main>`, 열릴 때 초점을 받는 오버레이 패널
  - `2.4.7`은 **키보드로 도달하는** 요소의 초점 가시화를 요구하므로 해당하지 않는다. 오히려 본문만 한 컨테이너를 두른 링은 **실제 초점 위치를 가린다** — 화면을 옮길 때마다 콘텐츠 전체가 테두리에 감싸였다
  - ⚠️ **초점 이동 자체는 유지한다.** `2.4.3`이 요구하고 `#829` ⑸c가 넣은 것이다 — SPA는 문서를 다시 읽지 않아, 초점을 옮기지 않으면 스크린 리더 입장에서 **아무 일도 일어나지 않은 것과 구분되지 않는다.** 지우는 것은 **표시뿐**이다
  - **한 요소를 지목하지 않는다.** `§16` 항목 17이 겹침 순서에서 적은 것과 같은 구조다 — 한 자리만 근거를 얻으면 다음 대상에서 같은 자리가 다시 열린다
  - 오버레이 **안에서** 초점 표시가 필요하면 **패널이 아니라 안쪽 요소**가 받는다
- **경계와 장식을 가른다 — `1.4.11`의 `3:1`은 「식별에 필요한」 시각 정보에만 건다** 〔**확정** 2026-09-18 · `#1202`〕

| 갈래 | 예 | `3:1` |
|---|---|---|
| **컨트롤의 유일한 경계** | 아웃라인 버튼 · 폼 컨트롤 · 「등급 없음」 배지 — 제 면이 얹힌 면과 같아 **테두리가 없으면 자리를 알 수 없다** | **필수** |
| **채움 컨트롤의 면** | Primary 버튼 — 식별을 **면**이 진다 | **필수** (면 대 주변) — **면이 못 넘으면 테두리가 맡는다** 〔**확정** 2026-09-22 · `#1296`〕 |
| **위치를 읽는 기준틀** | 개략도(`PositionChart`)의 해안선 — 배 위치를 읽는 좌표계 자체다 | **필수** (윤곽선. 채움은 옅게 둔다) |
| 장식 · 보강 | 안내문 좌측 띠 · 표 머리행 구분선 · 카드 hover 강조 | **대상 아님** — 의미는 문장·위치가 전달한다(위 이중 채널 규칙) |
| 비활성 | `:disabled`의 면·테두리 | **대상 아님** — `1.4.11`이 비활성을 명시적으로 제외 |

  - **다크 채움 Primary는 테두리가 경계를 맡는다** 〔**확정** 2026-09-22 · `#1296` · `rlatnals4114`〕. 다크 채움면(`#385c8d`)은 주변과 `2.35`~`2.78`인데, 면을 띄우면 그 위 글자(`5.60`)가 내려간다 — 카드 대비 `3:1`을 넘기는 순간 글자는 `4.40` 이하다. **밝은 글자를 쓰는 한 면 하나로는 두 기준을 함께 넘을 수 없다.** 면과 글자는 두고, 1px 테두리를 `--color-primary-solid-border`(= `--semantic-primary`)로 그린다 — 다크 `#4a7cc0`은 네 면에서 `3.39`~`4.44`, 라이트는 채움면과 같은 값이라 화면이 바뀌지 않는다. hover에서도 테두리는 그대로다(hover 면은 주변과 더 붙는다). 고르지 않은 것: 다크에서 밝은 면 + 어두운 글자(버튼 모양이 바뀌고 Figma 값 둘이 새로 필요) · 다크에서 아웃라인(주 버튼과 보조 버튼의 무게 차가 사라진다)
  - **안내문의 좌측 띠가 이 판정이 선 자리다.** `.vessel-management__notice`의 Success 띠(`2.89`)와 `.fr__notice`·`.fr__status`의 띠(`1.42`)는 **같은 모양**인데 앞엣것만 문제로 잡혀 있었다 — 갈래를 안 갈랐기 때문이다. 띠를 `3:1`로 올리려면 Figma의 Success 라이트 값을 바꿔야 하는데, **띠는 보조 채널이라 그럴 근거가 없다**. CSS 주석 자신이 *「색만으로 의미를 전달하지 않는다 — 문장이 무엇이 일어나는지 적는다」*고 밝히고 있다
  - **범위는 토큰 이름이 아니라 역할로 잡는다.** `#829` ⑶(폼) · `#1170` ⑴(배지)이 같은 자리를 두 번 열었고, `#1202`가 `--color-border-strong`으로 범위를 잡았다가 **더 나쁜 `--border-default`(라이트 최소 `1.15`) 아홉 곳을 놓쳤다**
  - 회귀는 `tokens.sync.test.ts`가 막는다 — `cursor: pointer`이고 **테두리색 ≠ 배경색**인 규칙의 테두리 토큰은 네 면에서 `3:1`. 예외 목록을 두지 않는다

- **비활성 컨트롤은 「왜」를 함께 낸다** 〔**확정** 2026-09-18 · `#1170` ⑵〕
  - **`disabled` 속성을 유지한다.** `aria-disabled`로 바꾸면 초점은 남지만 저장소의 `:disabled` CSS 규칙 **51개**가 그대로 화면에서 빠진다(`[aria-disabled]` 규칙은 **0개**) — 초점을 얻는 대가로 **비활성임이 보이지 않게 된다**. 사이드바 비활성 항목·상단바 종 버튼이 이미 같은 판단이다(`§16` 항목 10)
  - 대신 **사유를 화면과 낭독 양쪽에 둔다.** 사유 문구를 버튼 곁에 그리고, 그 요소의 `id`를 버튼의 `aria-describedby`로 잇는다. 그리지 않으면 **버튼이 있다는 사실과 이유가 통째로 사라진다** — `disabled`가 초점 순서에서 요소를 빼기 때문이다

| 갈래 | 예 | 사유 표기 |
|---|---|---|
| **일시적 진행 중** | `busy` · `saving` · `submitting` · `…status === 'running'` | **필요 없다** — 버튼 글자가 바뀌고(「저장 중…」) 몇 초 뒤 돌아온다 |
| **선행 조건 미충족** | `stale` · `noVessel` · `blocker !== null` · `pricesInvalid` | **필수** |

  - **예외** — 바로 곁의 칸이 이미 원인을 말하는 자리(빈 입력칸, 고를 것이 없는 선택칸, 자기 칸에 오류가 붙은 필드)는 잇지 않는다. 같은 말이 두 번 읽힌다
  - **예외 — 누르면 사유를 내는 버튼** 〔**확정** 2026-09-20 · `#1415`〕 — 선행 조건이 미충족이어도 `disabled`로 두지 않고, **누르면 사유와 여는 방법을 말풍선으로** 낸다. 지금은 한 곳 — 선박 상세 머리의 「진행 중 항차의 실시간 CII 보기」(진행 중 항차 없음)
    - 쓰는 조건은 셋이다. ⑴ 그 버튼이 **그 기능의 유일한 입구**이고, 사유를 곁에 늘 그려 두면 자리(페이지 머리 등)가 무너진다 ⑵ 말풍선이 **왜 못 여는지와 무엇을 하면 열리는지**를 둘 다 말한다 ⑶ 버튼은 `aria-expanded`·`aria-controls`로 말풍선과 잇고, 말풍선은 `role="status"`로 낭독되며, 다시 누르기 · `Escape`(초점을 버튼으로) · 바깥 누르기로 닫힌다
    - 겉모양은 누를 수 있는 상태와 **같다.** `#588`이 막은 「있다고 읽고 눌렀는데 아무 일도 없었다」는 **누른 자리에서 답이 나오므로** 생기지 않는다
    - 말풍선 면은 `§5` 오버레이 규격(그림자 + 테두리)이고, 겹침 순서는 드롭다운과 같은 `1`이다 — `§16` 항목 17이 정해지면 따른다
    - 이 형태가 두 곳 이상으로 늘면 공용 컴포넌트로 뽑는다. 그때까지는 `vessel-detail/VesselDetail.tsx`의 `NoVoyageDrill` 하나다
  - 회귀는 `frontend/src/a11yWiring.test.ts`가 막는다 — 선행 조건으로 비활성이 되는 버튼은 **등재돼 있어야** 하고, 등재되지 않은 것이 새로 생기면 실패한다
- 한국어 라벨 + 영문 약어 병기 — 범위와 예외는 `§3` 🔒(`#1452`)

---

## 15. 토큰 구현 규약

- 모든 값은 CSS 커스텀 프로퍼티로 U0(공통 셸)에서 구현한다
- 컴포넌트는 토큰만 참조한다. **하드코딩 hex 금지**
- 네이밍: `--color-*`, `--cii-*`, `--brand-*`, `--radius-*`, `--space-*`, `--chart-*`, `--tracking-*`, `--icon-*`
  - `--icon-*`은 v2.13에서 목록에 들어왔다 — 토큰은 있었으나 **이름이 이 목록에 없어** 세트 아이콘이 값을 코드로 들고 있는 것이 규약 위반으로 읽히지 않았다 (`#1174`)
- **`--brand-*`는 장식용 브랜드 색이다** 〔**신설** 2026-09-10〕. `--color-primary`(UI 기본색)와 **다른 축**이다
  - 이 채널이 없어 로그인 브랜드 판이 막혀 있었다. 빈자리는 마케팅 페이지·메일 템플릿·로딩 화면·파비콘 배경에서 계속 다시 나타난다
  - **값은 `--brand-base`가 소유한다** 〔**확정** 2026-09-17 ⓐ · `#933`〕 — Figma `BlueLog Color`의 `brand/base`(`#16305C`)를 받는다. 종전의 `primary` 계열 별칭을 끝낸다
    - **「Figma 값 대조」의 대조 상대는 로고 자산이었다.** `public/brand/bluelog-logo-light.svg`의 딥네이비가 `#16305C`로 `--color-primary`(`#1A365D`)와 **다르다** — 브랜드 색이 따로 없다던 상태의 실체가 이것이다. 이제 두 축이 이름이 아니라 **값으로** 갈린다
    - **`--brand-base`는 모드 둘에서 같은 값이다** — 「브랜드 색상(hue)은 테마 불변」을 토큰 자체가 지킨다. 종전에는 별칭이 가리키던 `--semantic-primary`가 다크에서 `#4A7CC0`으로 덮여 **한 겹 아래에서 갈라지고 있었다**
    - 그라데이션의 끝값(`--brand-gradient-to`)은 `--color-primary-hover`와 **같은 식으로 파생**한다(82% + `--text-primary`). Figma에 `brand/hover`를 만들지 않는다 — 아래 「다크 값은 … 새 색을 만들지 않는다」가 파생을 이미 허용한다
  - **브랜드 색상(hue)은 테마 불변이다. 브랜드 판의 표면색은 다른 표면과 같이 테마를 따른다.** 단, 다크 값은 라이트 브랜드 색에서 파생하며 새 색을 만들지 않는다 (2026-09-11 확정 정정 ⑴)
    - 종전 문안은 「두 값을 같게 두고 **테마 불변**을 명시한다」였다. 색의 **정체성**과 판이라는 **표면**을 뭉뚱그린 것이 원인이다 — 다른 표면이 전부 테마를 따르는데 이 판만 고정하면 다크에서 흰 글자가 기준에 못 미치거나 판이 배경에 붙거나 둘 중 하나가 반드시 난다
    - **다크 판 배경의 도출 규칙** 〔**개정** 2026-09-12 확정 M · `#933`〕 — 다크 판 배경은 라이트 브랜드 색을 `--surface-page` 쪽으로 내려 얻는다. **판 위에서 가장 약한 글자**(현재 `--auth-brand-muted`)가 그라데이션의 **가장 밝은 끝**에서 `4.5:1` 이상이면 된다. **판과 페이지의 분리는 `--brand-panel-edge`가 맡는다** — 면 대비로 분리하지 않는다.
      - 종전 문안은 「흰 글자 ≥ 4.5 · 판↔페이지 ≥ 3을 동시에 만족하는 비율」이었다. **두 전제가 틀렸다.** ⑴ 판 위 글자는 흰색이 아니다(`--color-on-primary`가 다크에서 `--text-primary` = `#e6e9ee`) — 흰색으로 재면 같은 면에서 5.80이 나오지만 실제 글자는 4.74다. **화면이 쓰지 않는 색으로 기준을 세운 것이다.** ⑵ 「판↔페이지 ≥ 3」은 「판이 페이지에서 떨어져 보여야 한다」의 **대리 지표**였는데, `#941`이 진짜 경계선(`--brand-panel-edge`)을 세워 조건이 해제됐다(현행 60%에서 그 선은 페이지 대비 약 6.9:1)
      - **현행 `60%`·`45%`를 유지한다.** 확정 G(`80%`·`68%`)는 폐기한다
      - ⚠️ **비율은 그대로이나 곱해지는 대상이 2026-09-17 확정 ⓐ로 바뀌었다** — `--semantic-primary`(다크 `#4A7CC0`)에서 `--brand-base`(테마 불변)로. 확정 M의 문안 「**라이트 브랜드 색**을 `--surface-page` 쪽으로 내려 얻는다」가 **이 배선에서 비로소 참이 된다**. 확정 M이 인용한 6.63 · 4.77 · 6.9는 종전 배선의 값이다(재실측 6.61 · 4.75 · 6.85)
      - **실측 (2026-09-17 · `#16305C` 배선 후)** — 판 `#132440` · 주 문자 `12.74` · 보조 문자 `8.30` · 경계선 `5.15`. 판정 기준(가장 약한 글자 ≥ 4.5)을 통과하며, 보조 문자가 `4.75 → 8.30`으로 여유가 커져 위 구조적 경고는 발동하지 않는다
      - ⚠️ 판↔페이지 **면 대비는 `2.34 → 1.22`로 떨어진다.** 확정 M이 「분리는 `--brand-panel-edge`가 맡는다 — 면 대비로 분리하지 않는다」로 조건을 옮겼으므로 규격상 맞으나, **분리를 그 선이 혼자 진다**(페이지 대비 `5.15`)
      - 제약이 **셋에서 하나**로 줄었다 — 가장 약한 글자가 통과하면 나머지는 자동으로 통과하므로, 판 위 글자가 늘어도 이 한 줄로 판정된다
      - ⚠️ **구조적 경고**: 이 항목이 두 번 열린 원인은 **위계를 알파로 만든 것**이다. 알파 글자의 대비는 판 색에 종속되므로 판을 건드릴 때마다 글자가 함께 열린다. **세 번째로 열리면 보조 문자를 불투명 토큰으로 바꾼다** — 지금은 아니다(현행이 통과하고 있고, 여기서 또 손대면 정리한 것이 다시 흔들린다)
  - 시맨틱(`--color-info` 등)을 장식에 쓰지 않는다. `§2.3`이 「UI 피드백 전용」으로 못박았고, 그것이 `#748`과 같은 유형의 위반이다
- **등급 토큰은 `--cii-*` 접두어를 쓴다.** v1.2의 `--color-grade-*`에서 변경했다. 시맨틱(`--color-*`)과 접두어를 나누어야 「이 빨강이 E등급인지 오류인지」가 코드에서도 구분된다 (§0.2 제약 2)
- **Figma Variables는 두 컬렉션으로 나눈다** — `CII Grade`(`cii/*`)와 `Semantic`(`color/*`). 값이 겹치지 않더라도 컬렉션이 같으면 한쪽만 교체할 때 실수가 난다
- **다크 모드는 `[data-theme="dark"]`로 오버라이드한다.** Figma에서는 동일 컬렉션의 두 번째 Mode로 대응한다. 토큰 **이름은 모드와 무관하게 하나**이며, 모드별로 다른 이름을 만들지 않는다

```css
/* ============================================================
   BlueLog design tokens
   Figma Variables와 1:1 대응 (§0.2 연결 규칙).
   값 수정 시 Figma와 본 파일을 함께 갱신한다.
   ============================================================ */

:root {
  /* ---------- Brand ---------- */
  --color-primary: #1a365d;
  --color-primary-hover: #1a3151;  /* 파생값 — 코드는 Primary 82% + text-primary의 color-mix. 종전 #142b4a는 오기 (§2.1) */
  --color-primary-subtle: #eaf0f7;

  /* ---------- Neutral ---------- */
  --color-bg: #f4f6f9;
  --color-surface: #ffffff;
  --color-surface-2: #eef2f6;
  --color-text: #1a2733;
  --color-text-muted: #5a6b7b;
  --color-text-faint: #8998a6;
  --color-border: #dce3ea;
  --color-border-strong: #c3ced9;
  --color-border-control: #6f7c8f;
  --color-grid: #e8edf2;

  /* ---------- Semantic — UI 피드백 전용 ---------- */
  --color-info: #2b6cb0;
  --color-success: #2f855a;
  --color-warning: #b7791f;
  --color-danger: #c53030;

  /* ---------- CII grade — 등급당 4토큰 (§2.4.2) ---------- */
  --cii-a-fill: #1e8639;  --cii-a-bg: #cdebd6;  --cii-a-text: #0e4f22;  --cii-a-border: #7cc894;
  --cii-b-fill: #57b570;  --cii-b-bg: #e9f6ec;  --cii-b-text: #2f7043;  --cii-b-border: #b8e0c4;
  --cii-c-fill: #d97b14;  --cii-c-bg: #fdebd8;  --cii-c-text: #7a3f06;  --cii-c-border: #f2c289;
  --cii-d-fill: #dc5b5b;  --cii-d-bg: #fce9e9;  --cii-d-text: #8a2f2f;  --cii-d-border: #efb4b4;
  --cii-e-fill: #c42a2a;  --cii-e-bg: #f9d5d5;  --cii-e-text: #6b1414;  --cii-e-border: #e08585;

  /* 산출 불가 — border는 dashed로 렌더 (§2.4.3) */
  --cii-none-fill: #9b9a93;  --cii-none-bg: #eef2f6;  /* = --surface-inset (§2.4.3) */
  --cii-none-text: #6b6a64;  --cii-none-border: #c9c7be;

  /* ---------- Chart ---------- */
  --chart-series-1: #1a365d;
  --chart-series-2: #2b6cb0;
  --chart-series-3: #6b46c1;
  --chart-actual: #1a2733;
  --chart-band: rgba(26, 54, 93, .12);
  --chart-axis: #c3ced9;
  --chart-grid: #e8edf2;
  --chart-boundary: #c3ced9;
  --chart-grade-band-opacity: .16;   /* §9.4 구간 배경 상한 */

  /* ---------- Radius ---------- */
  --radius-modal: 16px;
  --radius-card: 12px;
  --radius-control: 8px;
  --radius-badge: 4px;
  --radius-pill: 999px;

  /* ---------- Space ---------- */
  --space-4: 4px;   --space-8: 8px;   --space-12: 12px;
  --space-16: 16px; --space-24: 24px; --space-32: 32px; --space-48: 48px;

  /* ---------- Elevation — Light ---------- */
  --shadow-lv1: 0 1px 2px rgb(26 54 93 / 5%), 0 4px 16px rgb(26 54 93 / 6%);
  --shadow-lv2: 0 4px 8px rgb(26 54 93 / 8%), 0 12px 32px rgb(26 54 93 / 12%);
  --shadow-overlay: 0 4px 12px rgb(26 54 93 / 10%), 0 16px 40px rgb(26 54 93 / 14%);
  --focus-outline: 3px solid var(--semantic-focus);   /* 불투명 · `outline`으로 그린다 (§5 · 확정 L · 2026-09-18 ⓐ) */

  /* ---------- Layout ---------- */
  --layout-max-wide: 1920px;
  --layout-max-form: 1440px;
  --layout-sidebar: 240px;
  --layout-sidebar-collapsed: 64px;
  --grid-columns: 12;
  --grid-gutter: 24px;
  --grid-margin: 24px;     /* 외곽 여백 (§6 · §7.1) */
  --split-primary: 7;      /* 지도·차트 영역 (§7.1) */
  --split-secondary: 5;    /* 리스트·패널 영역 */

  /* ---------- Typography ---------- */
  --font-sans: "Noto Sans KR", system-ui, "Malgun Gothic", sans-serif;
  --font-mono: "Noto Sans Mono", ui-monospace, monospace;

  --tracking-tight: -0.02em;   /* display · title */
  --tracking-snug: -0.01em;    /* heading ~ caption */
  --tracking-none: 0;          /* micro */

  --font-display: 500 32px/40px var(--font-sans);
  --font-title:   500 20px/28px var(--font-sans);
  --font-heading: 500 16px/24px var(--font-sans);
  --font-body:    400 14px/20px var(--font-sans);
  --font-label:   400 13px/18px var(--font-sans);
  --font-caption: 400 12px/16px var(--font-sans);
  --font-micro:   400 11px/14px var(--font-sans);
}

/* ============================================================
   Dark — 이름은 동일하고 값만 교체한다 (§15 네이밍)
   ============================================================ */

[data-theme="dark"] {
  color-scheme: dark;

  --color-primary: #4a7cc0;
  --color-primary-hover: #5d8dd0;
  --color-primary-subtle: #1a2a3f;

  --color-bg: #0d1520;
  --color-surface: #16202e;
  --color-surface-2: #1e2a3a;
  --color-text: #e3e9f0;
  --color-text-muted: #93a3b5;
  --color-text-faint: #64748b;
  --color-border: #2a3646;
  --color-border-strong: #3b4a5e;
  --color-border-control: #6f7c8f;  /* Light와 같은 값 */
  --color-grid: #223042;

  --color-info: #63a0e0;
  --color-success: #4ecb84;
  --color-warning: #e0a845;
  --color-danger: #f26b6b;

  --cii-a-fill: #43be68;  --cii-a-bg: #14351f;  --cii-a-text: #7be39a;  --cii-a-border: #2e7048;
  --cii-b-fill: #2f8f52;  --cii-b-bg: #0f2417;  --cii-b-text: #56b074;  --cii-b-border: #1f4b32;
  --cii-c-fill: #d98c2a;  --cii-c-bg: #33220c;  --cii-c-text: #f0b268;  --cii-c-border: #7a5218;
  --cii-d-fill: #c95f5f;  --cii-d-bg: #2e1818;  --cii-d-text: #e89191;  --cii-d-border: #6b3232;
  --cii-e-fill: #e83a3a;  --cii-e-bg: #421212;  --cii-e-text: #ff7a7a;  --cii-e-border: #9e2c2c;

  --cii-none-fill: #6b7686;  --cii-none-bg: #161a21;
  --cii-none-text: #6b7686;  --cii-none-border: #39424f;

  --chart-series-1: #7ba7e0;
  --chart-series-2: #63a0e0;
  --chart-series-3: #a78bfa;
  --chart-actual: #e3e9f0;
  --chart-band: rgba(123, 167, 224, .18);
  --chart-axis: #3b4a5e;
  --chart-grid: #223042;
  --chart-boundary: #3b4a5e;

  /* 다크에서는 그림자가 보이지 않으므로 표면 명도차로 대체한다 (§5) */
  --shadow-lv1: none;
  --shadow-lv2: none;
  --shadow-overlay: 0 8px 24px rgb(0 0 0 / 32%), 0 24px 64px rgb(0 0 0 / 24%);
  --focus-outline: 3px solid var(--semantic-focus);   /* 다크도 같다 — 링 색만 생성 토큰에서 갈린다 */
}

/* 숫자 정렬 — CII 값의 자릿수가 바뀔 때 표가 흔들리지 않게 한다 (§3) */
body, table, input, button {
  font-family: var(--font-sans);
  font-variant-numeric: tabular-nums;
}

/* IMO 번호·좌표 전용 (§3) */
.identifier { font-family: var(--font-mono); }
```

### 15.1 등급 패턴 (SVG defs)

지도 마커·차트 등급 밴드·스택 바에서 공유한다 (§2.4.4). **등급 배지에는 적용하지 않는다.**

**공통 규격 🔒**

| 항목 | 값 |
|---|---|
| 타일 | 64 × 64 |
| 패턴 간격 | 4px |
| 선 굵기 | 사선 1.5 · 격자 1.2 · 점 r 0.9 |
| 선·점 색 | 해당 등급의 `bg` 토큰 |
| 바탕 색 | 해당 등급의 `fill` 토큰 |

**타일은 확대·축소하지 않고 잘라서 쓴다.** 20px 마커에 쓸 때 64px 타일을 20px로 줄이면 간격이 1.25px로 뭉개진다. 20×20 영역만 마스크로 잘라낸다.

**웹 구현 (SVG `<defs>`)**

```html
<svg width="0" height="0" aria-hidden="true">
  <defs>
    <pattern id="grade-b" width="4" height="4" patternTransform="rotate(45)"
             patternUnits="userSpaceOnUse">
      <line x1="0" y1="0" x2="0" y2="4" stroke="var(--cii-b-bg)" stroke-width="1.5"/>
    </pattern>
    <pattern id="grade-c" width="4" height="4" patternUnits="userSpaceOnUse">
      <circle cx="2" cy="2" r="0.9" fill="var(--cii-c-bg)"/>
    </pattern>
    <pattern id="grade-d" width="4" height="4" patternTransform="rotate(135)"
             patternUnits="userSpaceOnUse">
      <line x1="0" y1="0" x2="0" y2="4" stroke="var(--cii-d-bg)" stroke-width="1.5"/>
    </pattern>
    <pattern id="grade-e" width="4" height="4" patternUnits="userSpaceOnUse">
      <line x1="0" y1="0" x2="4" y2="0" stroke="var(--cii-e-bg)" stroke-width="1.2"/>
      <line x1="0" y1="0" x2="0" y2="4" stroke="var(--cii-e-bg)" stroke-width="1.2"/>
    </pattern>
  </defs>
</svg>
```

A등급은 패턴 없음(solid)이므로 정의하지 않는다.

> **Figma 자산은 `<pattern>`을 쓰지 않는다 🔒 (v2.0)** — Figma는 SVG `<pattern>` 요소를 읽지 못하고 붙여넣으면 단색으로 뭉개진다. Figma용 패턴 자산은 **선분 좌표를 전부 전개한 SVG**로 별도 관리하며, 위 웹 구현과 규격(타일 64·간격 4·굵기)이 동일해야 한다. 두 자산이 어긋나면 디자인과 구현이 다른 무늬를 그리게 된다.

> **v1.2 대비 변경** — ① C와 D의 패턴을 교체했다(§2.4.4 단서). ② 간격을 6px·4px 혼용에서 **4px 통일**로 바꿨다. ③ 하드코딩 hex를 `bg` 토큰 참조로 바꿔 다크 모드에서 자동 대응되게 했다. ④ `opacity`를 제거했다 — 반투명은 배경 색에 따라 결과가 달라져 두 모드에서 일관되지 않는다.

---

## 16. 미확정 목록

> **2026-09-10에 닫힌 항목(1 · 2 · 3 · 5 · 8 · 13)의 판정은 전부 `rlatnals4114`가 내렸다.**
> 개발은 그 확정 문서를 반영했을 뿐이다 — 각 비고의 「닫힘 (2026-09-10)」은 **개발이 정한 것이 아니다.**
> 예외는 항목 1 비고의 `--border-faint` 미생성 한 건으로, 그것만 **개발 판단**이다(`PR #928`).

| # | 항목 | 담당 | 비고 |
|---|---|---|---|
| ~~1~~ | ~~`text-faint` 처리~~ | — | **닫힘 (2026-09-10)** — **토큰 분리로 해소.** ⓐ·ⓑ 둘 다 성립하지 않았다: 저장소에 `::placeholder` 선언이 **0건**이고, ⓑ의 `4.86:1`은 **흰 카드 위** 값이라 `--surface-inset` 위에서는 `4.34`로 미달이다. `--text-faint`를 **폐기**하고 용도별로 쪼갰다 — `--color-text-disabled`(비활성 · WCAG 1.4.3이 대비 요구에서 제외) · `--color-surface-muted`(면적 · 비텍스트 3:1). **중립 계조 재정립은 하지 않는다**(항목 3을 닫기 위해 범위를 좁혔다). ⚠️ **확정 목록의 세 번째 이름 `--border-faint`는 만들지 않았다** — 아이콘 stroke·테두리 용도인데 그 자리가 저장소에 **0곳**이고, 같은 날 `#831`이 참조 0건 토큰 10개를 걷어낸 참이었다. **쓰는 자리가 생길 때 3:1 기준으로 함께 넣는다.** `tokens.sync.test.ts`가 「선언과 사용이 함께 간다」를 잠근다 — 소비처 없이 되살리면 실패한다 |
| ~~2~~ | ~~radius / 그림자 최종 수치~~ | — | **닫힘 (2026-09-10)** — **정본(`§5`)이 정답.** 다크 `--shadow-lv1`·`--shadow-lv2`는 `none`이고, 코드에 있던 검은 그림자를 제거했다. 떠 있는 면을 위해 `--shadow-overlay`를 신설했다. `§5`의 모든 값에 〔확정〕/〔제안〕 표기를 넣었다 — **판정하지 못하는 규격은 규격이 아니다** <br><br> **2026-09-11 재확인** — radius·테두리·Light 그림자를 확정자가 〔확정〕으로 판정했다(`#932` — 개발이 먼저 〔확정〕으로 적어 둔 것을 확정자가 확인). 〔확정〕/〔제안〕의 정의도 `§5` 머리에 들어가 **`§5`에 〔제안〕으로 남은 값이 없다.** ~~⚠️ 포커스 링만 확정(E · 45% 알파)이 `#829`(`PR #923`)가 알파를 뺀 뒤의 코드와 달라 재확인 중이다~~ → **2026-09-12 확정 L로 종결** — 45%는 네 면 모두 3:1 미만이라 **폐기**하고 불투명을 유지한다(`§5` 재확인 항목). `§5`에 〔제안〕도 재확인 대기도 남지 않았다 |
| ~~3~~ | ~~중립색 팔레트 미세 조정~~ | — | **닫힘 (2026-09-10)** — 범위를 **「라이트 셸 색상 확정」으로 한정**해 닫는다. `#707`(`PR #708`)이 라이트 셸을 `§2.2` 쿨톤으로 되돌린 것은 정본을 고친 것이 아니라 **정본이 적어 둔 값을 구현이 따르게 한 것**이다. 함께 `--cii-none-bg`를 중립 표면(`surface/inset`)에 맞췄다 — 종전 `#f2f2ef`는 웜 페이지색이라 쿨톤 위에서 떴다. ⚠️ **중립 문자 계조 재정립은 별건으로 남는다 — 항목 19가 받는다**(`#1169`). 종전에는 `#68`로 미뤄 두었으나 그 이슈는 **접근성 테스트**가 범위이고 `a11y.test.tsx` 12건으로 덮여 **2026-09-11에 닫혔다** |
| ~~4~~ | ~~**Noto Sans KR** 서브셋 self-host 여부~~ | — | **닫힘 (2026-09-11 · PR #939)** — **self-host로 확정됐다.** `scripts/build_fonts.py`가 한글 전체(11,172음절) 서브셋을 굵기 400·500 두 벌로 만들고(`frontend/public/fonts/` · `OFL.txt` 동봉), `frontend/src/styles/fonts.css`의 `@font-face` 4건이 그 파일들을 가리킨다. `#925`의 해결이다. mono는 IMO 번호·좌표 전용 라틴·숫자 서브셋이다. CDN은 쓰지 않는다(`#791` — 인터넷 없는 경로가 전제) |
| ~~5~~ | ~~아이콘 세트 선정~~ | — | **닫힘 (2026-09-10)** — **Lucide** (MIT · 표기 의무 없음). `§12`에 세트와 커스텀 아이콘 관리 규칙을 명시했다. 기본 `strokeWidth`가 2라 `components/Icon.tsx`가 1.5를 강제한다. ⚠️ **아이콘을 쓸 수 있게 된 화면 훑기는 별건** |
| ~~6~~ | ~~사이드바 vs 상단바 최종 구조~~ | — | **닫힘 (2026-08-04)** — 좌측 사이드바로 확정. §7.2에 반영 |
| 7 | P(D∪E) 임계값 20% / 40% | PO | **〔조건부 대기〕** 실운항 데이터를 확보한 뒤 PO가 확정. 현재 §2.5의 20% / 40%는 **초안값**(⚠️ 표기)이며, 확정 전까지 코드는 이 값을 그대로 사용한다 |
| ~~8~~ | ~~로딩 / 빈 상태 / 에러 상태 패턴~~ | — | **닫힘 (2026-09-10)** — 빈 상태 2종은 v2.0에서, 로딩은 `#613`(`PR #614`)에서 확정됐고 **에러가 마지막이었다.** 층위는 **셋**이다: **A 페이지 실패**(재시도 필수) · **B 영역 실패**(목록 로드 + **계산 실패** — 크기는 `size` 속성) · **C 필드 검증**(폼 규격 소관). CSV 행 오류 표는 **상태 표시가 아니라 결과 데이터**라 대상 밖이다. 재시도 문구는 **「다시 시도」 하나**이며 컴포넌트가 인자로 받지 않는다 — 문구가 갈리는 것이 클래스 24종이 생긴 경로다. 색은 **면이 중립, 아이콘·문구만 위험색**이다(`§0.2` 제약 2·3). `--cii-*`를 실패 표현에 쓰지 않으며 `errorPattern.test.ts`가 막는다 |
| 9 | 경영진 요약 PDF·인쇄 스타일 | 디자이너 | **〔조건부 대기〕** 두 갈래이며 조건이 다르다. ⑴ **흑백 인쇄에서 등급 패턴(§14 보조 채널)이 작동하는지 검증** — `PRD §25`가 리포트를 PDF·CSV로 이미 산출하므로(`SCR-010` · MUST) **지금 검증할 대상이 있다.** 디자이너 검증 후 확정 ⑵ **「경영진 요약」이라는 별도 문서의 필요 여부** — `PRD §25`의 리포트는 항차 완료(`§25.2`)·연간 실적(`§25.3`) 둘뿐이고 경영진 요약은 그 목록에 없다. **범위 판정은 `PRD §5`가 소유하므로**(`AGENTS §3.2`) 본 문서가 정하지 않는다 |
| 10 | 알림 우선순위 체계 | PO | **〔조건부 대기〕** PO가 §2.5 임계값과 연동하여 알림 우선순위 체계를 확정한 뒤. <br><br> ⚠️ **알림 기능 자체가 없다** — 상단바 종 버튼은 이 항목이 정해질 때까지 `disabled` + 「준비 중」으로 둔다(`#771` ⑽ · **개발 임시안**, 사이드바 비활성 항목과 같은 방식). 종전에는 `onClick` 없는 살아 있는 버튼이 `aria-label` 「읽지 않음 없음」을 달고 있었다. `§7.2`의 배치는 그대로다 |
| 11 | 다크 모드 중립 팔레트 미세 조정 | 디자이너 | **〔조건부 대기〕** 디자이너가 야간·저조도 실제 관제 환경에서 검증을 마친 뒤 확정. §2.2 Dark 열은 Light를 기준으로 파생한 초안이다. 검증 결과가 현행과 다를 경우 §2.2 갱신 → 코드 반영 순서를 따른다 |
| 12 | 데이터 점검 이상치 판정 임계값 | PO + 도메인 | 연료 소모율 변동폭·속력 상한 등. 본 문서는 **표기 방식만** 소유하며 임계값은 PRD가 소유한다 |
| ~~13~~ | ~~등급 밴드 높이의 경계값 연동~~ | — | **닫힘 (2026-09-10)** — 이미 구현돼 있다. `GradeScaleBar`가 `d1`~`d4` 실제 경계로 밴드를 나눈다 |
| 14 | AIS 신선도 임계값 표기 | PO + 개발 | **〔조건부 대기〕** AIS 기능이 PRD 범위에 들어온 뒤. **현재 PRD §5.2 OUT OF SCOPE** — 임계값을 정할 근거가 없다. 본 문서는 배지 색(§2.3 Warning·Danger)만 정하고 임계값은 소유하지 않는다 |
| 15 | 신설 UX 문구 2종 | PO | ⓑ 만료 딥링크 컨텍스트 바 ⓒ 신뢰도 배지. **문구는 `PRD §6.3`이 소유하므로 PRD 개정이 선행되어야 한다** <br><br> ~~ⓐ `UIFLOW 2-10` 결정론 계산 안내~~ — **삭제 (2026-09-10)**: 그 화면이 `#513`에서 1차 심사 범위 밖으로 확정돼 전제가 사라졌다 → **해소 (2026-09-13)**: `2-10`이 범위 안으로 옮겨지며(`#513` · `PRD` v4.12) 문구가 `PRD §6.3` 「함대 감축 계획 — 결정론 안내」로 신설됐다. 위 삭제 사유는 그날의 기록이다 (`#1678`) |
| 16 | 어시스턴트 오버레이 치수 | 디자이너 | **〔조건부 대기〕** `PRD §20 O-12`가 **MAY · 실험**이라 지금은 규정하지 않는다 — 패널 폭 `380` · 최대 높이 `560` · 대화 영역 최소 높이 `120` · 말풍선 최대 폭 `90%` 넷은 **개발이 정했고 코드가 갖는다**. 셋은 `--space-*`로 떨어지지 않는 생짜 픽셀이라 확정하려면 **새 토큰이 필요하고, 그러면 `§0.2`상 Figma 세트를 거친다.** 실험이 정식 기능으로 승격될 때 함께 본다. **표시 규칙(`§8.5`)은 이와 별개로 지금 확정됐다** — 접근성은 실험 여부로 면제되지 않는다 (`#1051` 2절) |
| 17 | 겹침 순서(`z-index`) 층위 표 | 개발 + 디자이너 | **〔대기〕** 정본에 겹침 순서 규정이 **아예 없다** — `§5`는 고도를 *그림자*로만 말한다. 저장소의 실제 층은 넷뿐이며(`100` 본문 바로가기 · `40` 어시스턴트 오버레이 · `10` 상단바 · `1` 드롭다운·툴팁) **순서 자체는 맞으나 근거가 없다.** 챗봇 절만 만들고 이 자리를 비우면 어시스턴트의 `40`만 근거를 얻고 **다음 오버레이(모달·토스트)에서 같은 자리가 다시 열린다** — `§8.4`가 「규격이 침묵한 자리를 화면이 메운다」로 겪은 구조다. 챗봇 밖에도 적용되므로 `§8.5`가 아니라 `§5` 소관이다 (`#1051` 4절) |
| 18 | 어시스턴트 문구의 소유 | PO | **〔대기〕** `§13`이 UX 문구를 `PRD §6.3`·`§6.4`에 위임하는데, 어시스턴트 문구 여섯(열기 버튼 · 패널 제목 · 「실험」 태그 · 안내문 · placeholder · 보내기 · 대기 · **버린 답 접두**)은 **어느 정본에도 없고 컴포넌트가 들고 있다.** 정하지 않으면 두 문서가 같은 문장을 갖게 된다. 면책 **원문은 대상이 아니다** — 서버가 소유한다(`API_SPEC §15.5`) (`#1051` 5절) |
| 19 | 중립 계조의 웜/쿨 정합 | 디자이너 | **〔대기〕** `#1169`. 라이트 중립 계조는 **웜**(`#1a1a18` → `#5f5e5a` → `#8b8a83`)인데 다크는 **쿨**(`#e6e9ee` → `#9ba5b4` → `#6b7686`)이고, 표면은 **양쪽 다 쿨**이다(라이트 `--surface-inset` `#eef2f6`). `#707`이 `--cii-none-bg`에서 *「웜 페이지색이라 쿨톤 위에서 떴다」*며 고친 것과 같은 자리다. **항목 3이 `#68`로 미뤄 둔 몫을 여기가 받는다** — `#68`은 접근성 테스트가 범위이고 이미 닫혔다. 항목 11(다크 모드 중립 팔레트)과 **짝**이며 라이트·다크를 한자리에서 본다 <br><br> ✅ **`--color-border-control`은 여기서 떨어져 나갔다**(`#829` ⑶ · `#1169` · **2026-09-18 확정 O**). 컨트롤 면과 패널 면의 대비가 라이트 `1.12` · 다크 `1.09`라 **테두리가 유일한 경계**이고 `1.4.11`의 비텍스트 `3:1`이 걸리는데, 종전 값이 문자 토큰(`--text-muted`) 별칭이라 **라이트 여유가 `0.08`뿐**이었다(`3.08` 대 기준 `3.00`). 원인은 값이 아니라 **면적·경계 채널에 자기 값이 없는 것**이었고, Figma가 `border/control`을 신설해 끊었다 — 라이트·다크 **공통** `#6f7c8f`, 네 면 최소 **`3.77`**(라이트 inset `3.77` / card `4.24` · 다크 inset `4.12` / card `3.77`). **이 항목이 라이트 중립을 어떻게 손보든 이 토큰은 따라 움직이지 않는다** — `tokens.sync.test.ts`가 문자 토큰 참조를 막는다. 항목 19 자체는 문자 계조(`--text-*`)의 웜/쿨이 남아 **열려 있다** |
| 20 | 비활성 컨트롤을 초점에 남길지 | 디자이너 + 개발 | **〔대기〕** `#1170` ⑵. `§14`가 **사유 표기**를 확정해 「버튼이 왜 잠겼는지 모른다」는 닫혔으나, **버튼이 있다는 사실 자체는 여전히 초점 순서 밖**이다 — `disabled`가 요소를 빼기 때문이다. 이를 풀려면 **선행 조건 미충족 24곳**(일시적 진행 중 33곳은 대상이 아니다)을 `aria-disabled`로 옮겨야 하고, 그것은 표기 변경이 아니라 **동작 변경**이다: 핸들러 가드 24개 · `[aria-disabled]` 시각 규칙 신설(현재 `0`개 · `:disabled`는 51개) · 기존 검사 갱신이 함께 와야 한다. `#829` ⑸(f)가 `PR #923`에서 보류된 이유가 같다(배선 보강과 성격이 다르다) <br><br> ⚠️ **범위가 「이슈 하나 ≈ 하루」(`AGENTS §6`)를 넘는다** — 나눌 단위를 함께 정한다 |

---

**작성 기준**: 본 문서의 🔒·💡 항목은 §0.2의 제약을 만족함이 검증되었다. Figma 반영 시 §15의 토큰명을 Variables 이름으로 그대로 사용할 것.

---

## 변경 이력

> 변경 시점에 직접 기록한다. squash merge로 브랜치 커밋 해시가 재작성되므로 커밋 열에는 **PR 번호**를 적는다.

| 날짜 | 커밋 | 변경 요약 |
|---|---|---|
| 2026-07-29 | `#141` | v1.0 최초 작성 (rlatnals4114 작성분 반영) |
| 2026-07-29 | `#142` | 헤더에 작성 행 추가 (rlatnals4114 명시) |
| 2026-08-04 | `#175` | 디자인 확인 요청 1~5번 회신 반영 — §2.5 위험도 표기를 (a)확률/(b)결정론으로 분리 · §4.1 반올림·required 자릿수 명시 · §4.2 비율 항 신설 · §7.2 셸 구조 개정 · §11 전면 추정 화면 범위 조항 · §16 항목 6 닫힘 · §16 항목 8 임시안 기록 |
| 2026-08-05 | `#184` | 디자인 확인 요청 20·21·22번 회신 반영 — §4.2에 자릿수 5개·반올림 공통 조항·천단위 적용/제외·CII 3자리 예외 근거 신설(`PRD §9.3` 상충 3건 각주 포함) · §2.5 (b)에 여유율 문구·0.1% 미만 예외·등급 E 표기·라벨·아이콘 보강 · §11에 외부 출처 없는 화면 단서 추가 (#164) |
| 2026-08-07 | `#196` | §4.1 단위 표기를 capacity 축 파생으로 확정 — `gCO₂/t·nm` → `gCO₂/(DWT·nm)`·`gCO₂/(GT·nm)`, 표기 형식 근거 4항, 고정 문자열 금지, 지표명 병기 금지 (디자인 확인 28번 ⑴·⑵ · #163) |
| 2026-08-14 | `#338` | §2.5 등급 E 여유율 항의 「미결」 표기를 `null` 확정으로 갱신 — API 계약(`API_SPEC §4.1` nullable)·표시 의무(이 문구)로 이행 (#171) |
| 2026-08-14 | `#340` | §4.2 상충 각주를 해소 상태로 갱신 — `PRD §9.3`이 본 절 참조로 전환됨(#185). 단위 표기는 #164 확정 대기로 명시 |
| 2026-08-15 | `#388` | **v1.2 — §4.2 「단위 표기 🔒」 신설.** 연료 `t`(SI 기호, `ton`은 short/long ton과 혼동) · CO₂ `tCO₂`(같은 화면에 두 질량이 병기되어 `t`만으로는 구분 불가) · 거리 `nm` · 시간 `h` · 속력 `kn` 확정 · 화면 리터럴 금지 조항(`format.ts`의 `DISPLAY_UNITS` 참조) · 자릿수 표 예시를 확정 표기로 갱신 (#164) |
| 2026-08-15 | `#396` | §4.2 「화면에 리터럴로 박지 않는다 🔒」가 가리키는 경로를 `src/display/format.ts`로 갱신 — 표시 규약 모듈이 feature 종속에서 공용 경로로 이전됨 (#392) |
| 2026-08-16 | `#463` | **v2.0 — 대시보드 재설계 반영 전면 개정.** ① §2.4 등급 램프를 5색 → **3색 체계**(A·B 초록 / C 주황 / D·E 빨강, 군 내 명도 구분)로 전환하고 등급당 토큰을 2개 → **4개**(fill·bg·text·border)로 확장 · `none`(산출 불가) 신설 ② **다크 모드 신설** — §2.2·§2.3·§2.4·§5·§15에 Light/Dark 이중 정의, 고도는 Light 그림자 / Dark 명도차로 분리 ③ §0.2 제약 2를 **채널 분리 규칙**으로 재정의 (v1.2의 E==Danger 자기모순 해소) · 제약 5 신설 ④ §3 폰트를 Pretendard → **Noto Sans KR**, 굵기 700 → 500, 자간 조임값 신설 ⑤ §7.1 **12컬럼 그리드·7:5 분할** 명문화 · §7.2 사이드바를 6항목으로 재편(감축 계획·데이터 점검 신설) ⑥ §8 `GradeBadge` 18 variant 규격 · `RegulatoryFlag` 분리 · 슬라이더 Primary 단색 ⑦ §9.3 적용 범위를 계열(series)로 한정 · §9.4 구간 배경 **조건부 허용** ⑧ §14 보조 채널을 「문자 **또는** 패턴」으로 재정의 ⑨ §15 토큰 접두어 `--color-grade-*` → `--cii-*` · §15.1 패턴 간격 4px 통일 · Figma용 `<pattern>` 금지 조항 ⑩ §16 항목 4·8 갱신, 11~14 신설 |
| 2026-08-16 | `#463` | **v2.1 — `UIFLOW 참조 정리.** ① §0.3 상호 참조를 소유 문서 표로 확장하고 **라우트를 본 문서에 적지 않는다**를 명문화 ② §2.3.1 데이터 점검 심각도를 신설하고 명칭을 「계산 제외」 → **「대체 계산」**으로 정정 (`PRD §12.3`·`§8.3` — 결측은 제외가 아니라 계획값 대체) ③ §7.2에서 사이드바 라우트 표를 **삭제**하고 `UIFLOW §2.2` 참조로 전환 — v2.0 표의 라우트 4개가 이미 어긋나 있었다 ④ §8 슬라이더 조항 확정(🔒) 및 `DataConfidenceBadge` 신설 ⑤ §16 항목 15 신설 |
| 2026-08-21 | `#604` | **v2.2 — `UIFLOW` 끊긴 참조 정리 · §8.1 신설.** 본 문서가 **존재한 적도 없는 `UIFLOW` 절**을 가리키고 있었다 — `§1.1`·`§2.2`·`§3`·`§4.3`·`§4.5`·`§9-3`·`§9-4`. `UIFLOW`는 전 이력에서 최상위 절이 `0`·`1`·`2`뿐이라 **재번호 드리프트가 아니라 처음부터 없는 절을 가리켜 쓴 것**이고, 7종 전부 한 커밋(`59a0805`, PR `#463`)에서 들어왔다. 제목이 `Update`이고 본문이 비어 `#465`가 PR 템플릿을 만들게 한 그 커밋이며, **내용은 리뷰될 표면이 없어 그대로 통과했다.** ① 8행 `§3`→`§2.2` · 441행 `§1.1`→`§2` — `UIFLOW`가 하위 절에 번호를 붙여(v2.5) `§2.2`가 실재하게 됐다 ② 466·855·858행 참조 삭제 — 세 문장 모두 소유자를 본문에 이미 적고 있어 자족한다 ③ **§8.1 신뢰도 배지 표시 임계 신설** — 종전 위임 대상이 없어 `#485` ⑤가 착수 불가였다. 층위는 `PRD §8.3`을 참조하고 복제하지 않으며, 컷은 `IN_PROGRESS latest estimate` 이하 전부다. `COMPLETED`+`actual_fuel_ton` NULL(`[ORACLE-C-4B]`)은 층위상 실측이나 값의 출처가 아니므로 붙인다 ④ `tests/test_doc_cross_refs.py`가 재발을 CI에서 막는다 (#583) |
| 2026-08-21 | `#592` | **v2.3 — §4.2 자릿수 표에 「일수(day) 0자리」·「평균 속력 1자리」 신설.** 표에 행이 없어 **화면과 보고서가 각자 정하고 있었다** — 실시간 CII의 「경과 / 잔여」가 `231.64 일`로 나가는데 같은 블록의 거리·연료는 `§4.2`를 따르고 있었고, 속력은 `services/report.py`가 `_UNSPECIFIED_DIGITS`로, `VoyagePanel`(#610)이 **열 자체를 빼는 것으로** 규정 부재를 표시하고 있었다. ① 자릿수 2행 신설 ② 단위 표기 표에 「일수 `일`」 신설 — 이 표에서 유일하게 문장 안에 섞여 나오는 단위라 `d`를 쓰지 않는다 ③ 「시간 항목은 …」 각주 정정 — `PRD §11.2 duration_days`만 보고 *「표시 대상이 아니다」*라고 적었으나 **다른 경로의 일수가 그 사이 화면에 올라와 있었다** ④ 천단위 구분자 「미적용」에 일수·속력 추가 (#592) |
| 2026-08-22 | `#653` | **v2.4 — §8.2 CII 적용 대상 배지 신설 · §8 컴포넌트 목록에 `ApplicabilityBadge` 추가.** `PRD.md:161`이 *「화면에는 「공식 CII 적용 대상이 아닐 수 있음」을 표시해야 한다」*고 요구하는데, 그 표시가 **선박 등록 결과 화면 한 곳에만** 있었다 — 등급과 값은 어디서나 보이는데 그 값이 규제상 무의미할 수 있다는 사실만 보이지 않았다. ① 3상태(해당·미해당·판정 불가)와 **해당일 때 그리지 않는다**는 규칙 ② 붙이는 자리 3곳(대시보드·목록·상세)과, 계산 결과 화면은 배지 대신 `API_SPEC §1.6` warning으로 전달한다는 경계 ③ 시맨틱 **Warning**을 쓴다 — Danger는 `RegulatoryFlag`(규제 의무 발생)의 것이고, 이 배지는 의무가 아니라 적용 범위 정보다 ④ 문자색은 `--color-warning-text`(`--color-warning`은 라이트 3.09:1로 §0.2 제약 1 미달) ⑤ 전체 문구는 본 문서가 소유하지 않는다 — `PRD §6.3`·`API_SPEC §1.6`에 위임(§13 규율) (#653) |
| 2026-08-22 | `#633` | **v2.5 — §4.2 자릿수 표에 「용량 (DWT·GT) 0자리」 신설 · 천단위 구분자 「적용」에 용량 추가.** 표에 행이 없어 **같은 값이 화면마다 달랐다** — 선박 관리 목록은 `DWT 6,405.77`(포맷 경유), 선박 상세 제원은 `6405.77`(서버 문자열 그대로)을 냈고 `Spec`에는 포맷 지점 자체가 없었다. `#592`(일수·속력)와 같은 형태의 공백이며 같은 방식으로 닫는다. ① 자릿수 **0** — 0.01 t = 10 kg으로 수천~수만 톤 급 제원에서 의미를 갖지 않는다(항해거리 0자리와 같은 논리). 가변 자릿수를 택하지 않은 것은 이 표의 머리글이 「항목별로 고정한다」이기 때문이다 ② 구분자 **적용** — 5만 톤 급 값에 구분자가 없으면 자릿수를 세어야 읽힌다. 보고서(`#584`)도 이미 쓴다 ③ **단위 표기 표에는 넣지 않는다** — `DWT`·`GT`는 선종의 capacity 축에서 갈리므로 상수가 아니다(CII 단위와 같은 사정) ④ 표시 반올림이라 **CII 분모에는 원본이 쓰인다**(「반올림 🔒」) (#633) |
| 2026-08-23 | `#359`→`#589` | **헤더 「상위 문서」를 `PRD` v3.2 → v4.4로 갱신 · `AGENTS §4.4` 각주 신설 · 제목을 `DESIGN_SYSTEM — BlueLog`로 (`§4.5`).** 숫자만 올린 것이 아니라 **v3.2 → v4.4 사이의 변경을 실제로 대조했다** — v4.0 관리 중심 전환은 본 문서 **v2.0이 「대시보드 재설계 반영 전면 개정」으로 이미 반영**했고, v4.2가 신설한 `§3.3.8` 실시간 CII는 `§8.1` 신뢰도 배지가 `PRD §8.3`을 참조하는 형태로 들어와 있다. **v4.4만 미반영이었고 그 내용이 제품명 정정이었다** — 제목이 `§4.5`(「문서 제목은 `문서명 — BlueLog`」)를 어기고 있었다. 부제는 `§4.5`대로 설명구로 남겼다. ⚠️ **`PRD v4.3`이 신설한 `SCR-011`(인증 화면)에 대응하는 시각 규약이 본 문서에 없다** — `§7` 레이아웃 일반 규약으로 덮이는 범위이며 `#608`이 그 자리를 다룬다. `§4.3`상 헤더·각주 정정이라 버전은 올리지 않는다 (#589) |
| 2026-09-10 | `#926` | **v2.6 — rlatnals4114 확정 문서 반영(1).** ⚠️ **개정 8곳 전부가 `rlatnals4114`의 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `§16` 항목 1(`text-faint` → 토큰 분리) · 2(다크 그림자는 정본 `none`이 정답) · 3(중립 팔레트를 「라이트 셸 색상 확정」으로 한정) · 5(아이콘 세트 **Lucide**) · 13을 닫았다. 함께 `§0.2` 제약 5 보강 · **제약 6 신설**(문자 대비는 가장 어두운 표면 기준) · `§5` 전 항목에 〔확정〕/〔제안〕 표기와 `--shadow-overlay` 신설 · `§12` 아이콘 세트·커스텀 규칙 · `§15` `--brand-*` 채널 신설 · `§16` 항목 15ⓐ 삭제 · 항목 4에 `#925` 경고. 개발이 판단한 것은 **정본이 적어 둔 두 선택지가 둘 다 성립하지 않음을 실측으로 보인 것**뿐이다 (#747) |
| 2026-09-10 | `#927` | **rlatnals4114 확정 문서 반영(2).** ⚠️ **에러 규격은 `rlatnals4114`가 정했다.** `§16` 항목 8을 닫는다 — 층위 **3개**(페이지/영역/필드) · 재시도 문구 **「다시 시도」** 단일 · **중립 배경 + 테두리, 위험색은 아이콘·문구에만** · 전역 클래스를 더 얹지 않음. 필드 층위는 폼 규격으로, CSS 구조 개편은 별건으로 이관한다는 범위 결정도 확정자의 것이다 (#694) |
| 2026-09-10 | `#928` | **개발 판단 — 확정 문서 소관이 아니다.** `§16` 항목 1 비고에 `--border-faint` **미생성**을 확정으로 등재했다. 확정 문서 2-1이 이름을 부른 토큰 셋 중 하나인데 소비처가 **0곳**이고 같은 날 `#831`이 참조 0건 토큰 10개를 걷어낸 참이라 만들지 않았다. 쓰는 자리가 생길 때 3:1 기준으로 함께 넣는다 (#747 후속) |
| 2026-09-10 | `#934` | **v2.7 — `§8`에 `BrandLogo` 항목 신설 · `§7.2`에 브랜드 판 반응형 규칙 추가.** ⚠️ **둘 다 `rlatnals4114`의 2026-09-10 확정(3-2 · 3-3)이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 확정인데 **정본 어디에도 없어** 같은 질문이 다시 열릴 자리로 남아 있었다 — `§16`은 미확정만 담으므로 넣을 수 없었고, 저장소 전체에서 「로고」를 언급하는 절이 **0건**이었다. `§16` 항목 15ⓐ가 같은 유형의 선례다(범위 밖으로 확정된 항목이 미확정 목록에 남아 계속 열린 것처럼 읽혔다). 구현은 `#608`(`PR #937`)이 이미 그렇게 하고 있으며 가드도 있다 (#934) |
| 2026-09-11 | `#930` · `#932` · `#933` | **v2.8 — rlatnals4114 확정 문서(2026-09-11) 반영.** ⚠️ **개정 전부가 `rlatnals4114`의 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). ⑴ `§5` 머리에 〔확정〕(「코드가 다르면 코드가 틀린 것」)·〔제안〕(「담당과 기한을 함께」) **정의**(확정 F) ⑵ `§5` 오버레이 그림자 두 값 등재 · 설명 「Lv2보다 한 단 넓고 **진하게**」(`#930` 본문의 「옅게」는 실제 값과 반대였다) · **「오버레이 면은 그림자와 테두리를 함께 쓴다」** 규정(확정 A) ⑶ Light 그림자 표기를 코드와 같은 `rgb(… / %)`로 · `--radius-modal` 사용처 0 단서(확정 D) ⑷ `§8` 슬라이더 — 채움 `--color-primary-solid` · 이동 단위와 값 자릿수는 `§4.2` 연동 · 썸 포커스는 `§5` 링 · 치수 셋은 〔제안〕(확정 K) ⑸ **`§8.3` 등급 전이 표기 신설** — 배지 2개 + 중립 연결자 · 변화 없으면 배지 하나(확정 J) ⑹ `§4.2` 데이터 완결성은 비율이면 기존 규칙, 점수일 때만 행 추가(확정 I) ⑺ `§2.1`·`§15` Primary Hover `#142b4a` → **`#1a3151`**(파생값 · 확정 정정 ⑵) ⑻ `§15` 「테마 불변」 → **색상은 불변, 판 표면은 테마를 따른다**(확정 정정 ⑴) ⑼ `§16` 항목 2에 재확인 기록. **적용하지 않은 것 둘** — 확정 E(포커스 링 45% 알파)와 G(다크 판 `80%`·`68%`)는 확정의 기준 커밋(`ef4bcf4`) **이후** 코드가 바뀌었다: 링은 `#829`가 알파를 빼 불투명이 됐고(45%는 네 면 모두 3:1 미만), 판에는 **보조 문자**(주 문자 78% 알파)가 생겨 G의 값에서 `3.56`·`4.24`로 `4.5:1` 미달이다. 「어긋나면 임의로 조정하지 말고 이슈를 걸라」(2026-09-10 확정 문서)에 따라 둘 다 현행을 두고 재확인을 요청했다(`#933`). I·J·K는 1차 심사 범위 밖 화면(`2-10`·`2-11`)의 선결정이다(`#513`) |
| 2026-09-11 | `#931` | **§13 문구 위치를 `PRD §6`으로 확대 · 폼 컨트롤 안 실패의 층위 한 줄.** ⚠️ **`rlatnals4114`의 2026-09-11 확정(C)이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 종전 「문구 원문은 PRD §6.3에 있다」는 상태 문구가 들어갈 자리가 없어 에러 기본 제목·빈 상태 문구가 **어느 정본에도 없었다** — `PRD §6.4`가 신설됐다. 한 줄 정정·소규모 추가라 `AGENTS §4.3`에 따라 버전은 올리지 않는다 (#931) |
| 2026-09-11 | `#771` | **개발 판단 — 확정 문서 소관이 아니다.** `§16` 항목 10 비고에 상단바 종 버튼의 임시 처리(`disabled` + 「준비 중」)를 적었다. 알림 체계가 없는데 종 버튼이 살아 있어 시연에서 누르면 아무 일도 없었고 `aria-label`은 「읽지 않음 없음」으로 사실과 다른 상태를 말했다(08-29 코멘트). `§7.2` 배치는 바꾸지 않는다. 비고 보강이라 `AGENTS §4.3`상 버전은 올리지 않는다 (#771 ⑽ 부분) |
| 2026-09-12 | `#831` | **§2.4.4 「끄는 쪽이 예외」 문장 삭제 → 「패턴을 끄는 예외를 두지 않는다」 · §0.2에 「정본은 구현의 속성·클래스·함수 이름을 적지 않는다」 신설**(2026-09-12 디자인 확정 O). 종전 문장이 React 속성 이름(`showPattern`)을 붙잡고 있어 **코드가 스스로 정리할 수 없었다** — 그 속성을 넘기는 호출부는 하나도 없는데, 지우면 정본이 가리키는 것이 사라지고 두면 쓰지 않는 속성이 남는 교착이었다. 패턴을 쓰지 않는 자리는 「패턴 미적용」이 이미 이름으로 정하고 있다(등급 배지 · 차트 선). `AGENTS §4.3` 「문장 정정·항목 추가」라 버전은 올리지 않는다 (#831) |
| 2026-09-12 | `#829` · `#933` · `#748` | **2026-09-12 디자인 확정 L·M·N 반영.** ⑴ **L — 포커스 링은 불투명 유지**(9/11 확정 E 폐기). 본문은 이미 맞았으나 **같은 문서의 CSS 예시가 v2.5의 알파 값을 갖고 있어** 복사하면 되살아나는 상태였다 — 코드와 같은 형태로 고쳤다. `1.4.11`(2.1 AA)이 포커스를 「상태」로 덮는다는 해석도 함께 적었다 ⑵ **M — 다크 판은 현행 `60%`·`45%` 유지, 도출 규칙 교체.** 확정 G의 전제 둘이 틀렸다: 판 위 글자가 흰색이 아니고(`#e6e9ee`), 「판↔페이지 ≥ 3」은 `#941`이 경계선을 세워 해제된 대리 지표였다. 제약이 셋에서 **하나**(가장 약한 글자 ≥ 4.5)로 줄었다 ⑶ **N — Warning은 `§2.3` 값 그대로**(`#b7791f`·`#e0a845`). 빠진 것은 결정이 아니라 **파이프라인**이었다(Figma 세트에 Warning이 없어 생성 토큰이 안 내려왔다). 라이트 `-text` 분리는 별칭 교체 뒤에도 유지한다(`#b7791f`는 흰 바탕 3.64). ⚠️ `§2.3` 표와 코드가 **라이트 네 값 모두 어긋나** 있어 「어떤 값도 일치하지 않는다」 선언이 실측이 아님을 함께 적었다. `AGENTS §4.3` 「값 정정·각주 보강」이라 버전은 올리지 않는다 |
| 2026-09-12 | `#763` | **v2.9 — §9.5 「지도 위 등급 표시」 신설.** 지도는 **배경을 우리가 그리지 않아**(타일이 색을 정한다) 기존 차트 규약이 그대로 적용되지 않는다. ⚠️ **무늬 대신 등급 문자 배지**를 쓴다 — 지도 마커는 DOM 요소라 SVG `<pattern>`을 가져올 수 없고 22px에서 무늬는 읽히지 않는다. `§14`가 금지하는 것은 **색 단독**이고 문자가 항상 함께 놓이므로 조건은 충족된다(`§15.1`의 「패턴 없는 A」와 같은 근거). 항로선을 **파선**으로 둔 것은 배경에 도로·경계선이 이미 실선으로 깔려 있어 실선이 배경의 일부로 읽히기 때문이다. 절 신설이라 `AGENTS §4.3`에 따라 버전을 올린다 (#763) |
| 2026-09-15 | `#746` | **v2.9 — §6/§7.1 gap 충돌 해소 · 🔒 그리드 표 재작성.** `#707`(패널 셸) 이후 §7.1의 「좌우 패딩 32」와 §6의 「패널 간 gap 16–24」가 같은 자리를 다른 값으로 정하고 있었다. ① **§6이 소유절**: 패널 간 gap 24로 확정(범위 16–24 상한). §7.1 대시보드 행의 32를 삭제하고 §6 참조로 전환. ② **🔒 그리드 표 갱신**: `240+1680=1920`(사이드바 부착 전제)을 `240+24+1656=1920`(패널 셸 배치)으로 대체. 「좌우 패딩 32」 행을 「패널 간 gap 24(→§6)」와 「외곽 여백 0」으로 분리. `#707`·`#709`의 「범위 밖 — 확인해서 적어 둔다」 동일 항목은 `AGENTS §6.1`에 따라 후속 이슈로 이관. ③ frontend/src/styles/ 영향 확인은 별건 이슈로 분리. (`rlatnals4114`) |
| 2026-09-15 | `#771` | **개발 정리 — 확정 문서 소관이 아니다.** §16 미확정 목록 5건에 상태 명시. 항목 7(P(D∪E) 임계값) · 9(경영진 요약 PDF) · 10(알림 체계) · 11(다크 모드 팔레트) · 14(AIS 신선도)에 **〔조건부 대기〕** 상태와 확정 조건을 명시했다. §16 표에 조건 없이 남아 있는 항목이 없어졌다 |
| 2026-09-16 | `#858` | **§4.2 단위 표기 🔒 표에 「일일 연료소모량」(`t/일`) 행 신설.** 행이 없어 **같은 필드가 화면마다 `t`와 `t/일`로 갈려** 있었다(`#592`가 입력만 통일하고 정본에 등재하지 못한 채 남긴 상태 — `ScenarioComparison.tsx`가 그 사실을 주석으로 남겨 두고 있었다). ⚠️ **어느 쪽도 정본과 맞지 않았다** — `§4.2`의 「연료 소모량 `t`」는 **질량**이고 이 값은 **질량유량**이라(`DB_SCHEMA`: `ton/day`) 표에 해당 항목이 아예 없었다. 표기 형식은 본 문서 소관이나 **물리량의 정의는 `DB_SCHEMA`·`API_SPEC`이 소유하므로**(`AGENTS §3.2`) 그 정의에 단위를 맞춘다. 구현은 `t`와 `일`을 조합해 파생시키며 리터럴을 새로 박지 않는다(`§4.2` 「리터럴 금지 🔒」 — `VesselManagement`의 `(t)` 하드코딩도 함께 걷어냈다) |
| 2026-09-16 | `#936` | **v2.10 — `§8.4` 폼 필드·필드 오류 신설.** ⚠️ **`rlatnals4114`의 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 2026-09-10 확정 1-1이 층위를 셋으로 나누며 필드를 폼 규격으로 이관했는데 **그 폼 규격이 신설된 적이 없어** 각 화면이 알아서 처리하고 있었다 — `Field` 컴포넌트 **4개**(앞의 둘은 접두어를 빼면 동일) · 오류 클래스 **16종** · HTML 요소 3종 · `role="alert"` 누락 **5곳** · `aria-describedby` 연결 **7/46곳**. `§16` 항목 8의 「클래스 이름 24종」이 필드 층위에서 재현된 것이다. ⚠️ **시각 표현은 이미 일치했다** — 13개 클래스 전부 `--color-danger-text`·면 없음이라, 갈린 것은 **구조와 접근성**이었다. 요약을 두지 않는 것과 서버·클라이언트를 같게 보이는 것이 확정자의 판단이다. 절 신설이라 `AGENTS §4.3`에 따라 버전을 올린다 (#936) |
| 2026-09-17 | `#936` | **`§8.4`에 「라벨은 감출 수 있으나 지우지 않는다」 추가.** ⚠️ **`rlatnals4114`의 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `§8.4`는 라벨의 **자리**만 정하고 보임 여부를 말하지 않아, 어시스턴트 질문칸처럼 라벨을 감춰야 하는 자리에서 **화면이 알아서 처리했다** — `.assistant__label`이 전역 `.sr-only`(`#831 ⑸`)를 그대로 베낀 국지 복제였다. 규격이 침묵한 자리를 화면이 메우는 것이 `Field` 4개·오류 클래스 16종이 된 경로이므로 등재한다. 감추되 **지우지 않는 것**이 확정자의 판단이다 — 지우면 낭독에서 이름이 사라진다. 소규모 행 추가라 `AGENTS §4.3`상 버전은 올리지 않는다 (#936) |
| 2026-09-17 | `#1022` | **`§2.3` 확정 N 각주에 완료 기록 — Warning·Info가 생성 경로로 내려왔다.** ⚠️ **`rlatnals4114`가 Figma 세트에 넣은 것이며 개발이 값을 정하지 않았다**(`AGENTS §7.3`). 확정 N이 적어 둔 순서 ⑴ Figma → ⑵ 별칭 교체를 그대로 밟았다. 각주의 「생성 세트에 `primary`·`success`·`danger`·`focus` 넷뿐」은 **그 시점의 기록**이 되어 그렇게 표기한다. ⚠️ **`§9.5` 🔒가 정한 항로선 토큰 `--semantic-info`가 존재한 적이 없었다** — 코드가 규격대로 읽고 언제나 빈 문자열을 받아 리터럴 `#1f6feb`가 실제 색이었고, 잠긴 규격이 지켜지지 않는데 아무것도 실패하지 않았다(`#1052` 조사). 이번에 토큰이 실재하게 되어 해소됐고, **같은 부류를 잡는 가드**(정본이 적은 토큰 이름이 CSS에 정의돼 있다)를 함께 세웠다. 그 가드가 `§9.5`의 나머지 하나 `--surface-default`를 미제로 들고 있다 — 마커 그림자가 그려지지 않는다. 각주 보강이라 `AGENTS §4.3`상 버전(v2.10)은 올리지 않는다 (#1022) |
| 2026-09-17 | `#1052` | **`§9.5` 마커 그림자 토큰 정정 — `--surface-default` → `--color-text`.** ⚠️ **`rlatnals4114`의 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 종전 문안이 가리킨 이름은 **존재한 적이 없고**, 없는 토큰을 가리키는 선언은 오류가 아니라 무효라 **그림자가 한 번도 그려지지 않았다** — 잠긴 규격이 막으려던 현상이 그대로 일어나고 있었다. ⚠️ **가장 가까워 보이는 `--color-surface`는 답이 아니다**: 지도 타일은 테마를 따르지 않고 원래 밝아, 흰 후광은 바다 위 `1.60`·육지 위 `1.15`로 목적을 이루지 못한다(실측). 어두운 쪽이라야 떨어진다. 같은 이름을 쓰던 **배지 배경은 불투명한 면이 필요해 `--color-surface`로 간다** — 한 이름이 두 일을 하고 있었다. 이름 정정이라 `AGENTS §4.3`상 버전(v2.10)은 올리지 않는다 (#1052) |
| 2026-09-17 | `#933` | **`§15` 브랜드 색을 `--brand-base`로 확정 — 「primary 계열 별칭」 종료.** ⚠️ **`rlatnals4114`의 2026-09-17 확정 ⓐ이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `§15`가 *「시안 색 적용은 별건(Figma 값 대조가 선행)」*으로 미뤄 둔 그 대조를 마쳤다 — **대조 상대는 로고 자산이었고**, `public/brand/bluelog-logo-light.svg`의 딥네이비 `#16305C`가 `--color-primary`(`#1A365D`)와 달랐다. 그 값을 Figma `brand/base`로 들여 `--brand-gradient-*`가 받게 했다. ⑴ 브랜드 축이 이름이 아니라 **값으로** 갈렸다 ⑵ `--brand-base`가 모드 둘에서 같아 「색상은 테마 불변」을 토큰이 직접 지킨다 — 종전에는 별칭이 가리키던 `--semantic-primary`가 다크에서 `#4A7CC0`으로 덮여 한 겹 아래에서 갈라지고 있었다 ⑶ **확정 M의 문안이 비로소 참이 된다** — 「라이트 브랜드 색을 `--surface-page` 쪽으로 내려 얻는다」가 테마 불변 토큰에서만 성립한다. 비율 `60%`·`45%`는 그대로이나 곱해지는 대상이 바뀌어 다크 판이 `#32517C` → `#132440`으로 어두워진다. 실측 주 문자 `12.74` · 보조 문자 `8.30` · 경계선 `5.15`로 판정 기준을 통과하며, 보조 문자 여유가 `4.75 → 8.30`으로 커져 구조적 경고는 발동하지 않는다. ⚠️ 판↔페이지 면 대비는 `2.34 → 1.22`로 떨어지나 확정 M이 분리를 경계선으로 옮긴 뒤라 규격상 맞다. `AGENTS §4.3`상 값 정정이라 버전은 올리지 않는다 (#933) |
| 2026-09-17 | `#933` | **`#933`을 붙잡고 있던 「재확인」 표기 두 곳 정리 — 이슈 종결 정리.** `§16` 항목 2 비고가 *「포커스 링만 … 재확인 중이다」*로 남아 있었으나 **2026-09-12 확정 L이 이미 폐기로 종결**했고 `§5`에 그 기록이 있다. 함께 `AuthShell.css`의 *「다크 값은 임시 … `#933` 재확인 대기」* 주석도 걷었다 — 확정 M·ⓐ로 값의 출처와 판정 기준이 모두 정해졌다. 그 주석이 적고 있던 **「흰 글자 `8.07:1`」은 확정 M이 틀린 전제로 지적한 바로 그 표현**이라(판 위 글자는 `--color-on-primary`) 현행 판정값 `8.30`으로 바꿨다. 닫힌 이슈를 가리키는 「대기」 표기가 남으면 다음 사람이 같은 자리를 다시 연다 — `#1052`가 실재하지 않는 토큰 참조 열 곳에서 겪은 것과 같은 형태다. `AGENTS §4.3` 「각주 보강」이라 버전은 올리지 않는다 (#933) |
| 2026-09-17 | `#1022` | **`§2.3` 라이트 Success·Danger를 생성 세트 값으로 정정 — 표가 낡은 사본이었다.** Success `#2f855a` → **`#38a169`** · Danger `#c53030` → **`#e53e3e`**. `§0.2`가 값의 소유를 Figma에 두므로 어긋나면 표가 틀린 것이다. 확정 N이 「라이트 네 값 모두 어긋나 있다」고 적어 둔 것의 나머지 둘이며, Warning·Info는 `#1022`가 생성 경로를 연 `PR #1195`에서 이미 맞춰졌다. 종전 Danger 표 값 `#c53030`이 **문자용 `--color-danger-text`(`#c92a2a`)와 사실상 같은 색**이라 표만 보면 대비가 통과하는 것처럼 보였다 — Warning과 같은 혼동이라 Danger 행에도 `-text` 단서를 붙였다. ⚠️ **정정으로 드러난 것 하나** — Success `#38a169`는 카드 `3.25` · 페이지 `3.00` · **inset `2.89`**로 `1.4.11` 비텍스트 `3:1`에 미달하며, `--color-surface-2` 위의 `.vessel-management__notice` 한 곳이 실제 위반이다. `§0.2`상 값은 Figma 소유라 덮지 않고, `#829 ⑶`·아웃라인 버튼 테두리와 **같은 유형**이라 별건으로 묶는다. `AGENTS §4.3` 「값 정정」이라 버전은 올리지 않는다 (#1022) |
| 2026-09-17 | `#1051` | **v2.11 — `§8.5` 어시스턴트 오버레이 「답이 아닌 것」의 표시 신설 · `§16` 항목 16~18 신설.** ⚠️ **`rlatnals4114`의 2026-09-17 확정 ⓐ·ⓓ이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 오버레이에 **답이 아닌 것이 셋**(버린 답 · 호출 실패 · 면책)인데 **배경·줄무늬·글자색이 모두 같아** 사용자가 가를 수 없었다 — 코드 주석은 *「같은 모양으로 두면 「답을 받았다」고 읽는다」*고 적어 두고도 면책과 같은 모양이 된 것을 보지 못했다. ⑴ **버린 답은 채널 둘**(줄무늬 + 접두 문구) — `§14`가 색 단독을 금지하고, **접두 문구는 낭독에 실리는 유일한 채널**이다(확정 ⓐ) ⑵ **면책은 줄무늬를 쓰지 않는다** — 답이 있을 때마다 상시 붙어 경고가 늘 떠 있게 되고 뜻이 닳는다(확정 ⓓ) ⑶ **호출 실패는 줄무늬만 공유하고 접두를 붙이지 않는다** — 실패 문구가 이미 완결된 문장이라 겹말이 된다. 이 과정에서 **모델의 어긋남이 드러났다**: 실패도 「버린 답」 플래그를 쓰고 있어 타입 주석의 정의(`API_SPEC §15.2`)와 코드가 갈려 있었고, 접두를 넣자 「답을 드리지 못했습니다 — 챗봇을 사용할 수 없습니다.」라는 겹말로 나타났다 ⑷ **색 이름을 절에 적지 않는다** — 값은 Figma 소유(`§0.2`)라 「줄무늬 + 문구」라는 **채널**로 적어야 값이 바뀌어도 규격이 산다. `PRD §20 O-12`가 MAY·실험이므로 **치수·겹침 순서·문구 소유는 정하지 않고** `§16` 항목 16~18로 남긴다 — 접근성 규칙만 실험 여부와 무관하게 확정한다. `AGENTS §4.3` 「절 신설」이라 버전을 올린다 (#1051) |
| 2026-09-18 | `#1171` ⑵ | **`§16` 항목 4 마감 — Noto Sans KR self-host 완료.** 항목 4의 「지금은 폰트가 아예 없다 — `@font-face` 0건」 경고가 사실이 아니게 된 지 6일 만에 등재했다. 실측: `frontend/src/styles/fonts.css` `@font-face` **4건** · `frontend/public/fonts/`에 woff2 4파일 + `OFL.txt` · 생성 스크립트 `scripts/build_fonts.py`(한글 11,172음절 · 굵기 400·500 · mono는 라틴·숫자). PR #939(`#925`)가 넣었다. 행 상태 갱신이라 `AGENTS §4.3`상 버전(v2.10)은 올리지 않는다 (#1171) |
| 2026-09-17 | `#1052` | **`§8` 슬라이더 치수 셋을 〔제안〕 → 〔확정〕으로.** ⚠️ **`rlatnals4114`의 2026-09-17 확정 ⓑ이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 확정 K가 셋을 〔제안〕으로 둔 이유는 *「구현이 0건인 상태에서 치수를 확정하면 검증 없이 굳는다」*였고 기한은 「`UIFLOW 2-10` 범위 복원 시점」이었다. **그 시점이 지났다** — `#513`이 2026-09-13에 옮겼고 `FleetReduction.tsx`에 슬라이더가 있다. 같은 줄의 *「`2-10`은 구현 대상이 아니며」*도 그때부터 사실이 아니었다. ⑴ **값 표시는 행 우측 고정**으로 확정(이미 `<output>`이 그렇다) ⑵ **트랙 두께·썸 크기는 브라우저 기본**으로 확정하고 출발점 값(트랙 `4px` · 썸 `24px`)은 폐기한다 — 치수를 직접 그리려면 `-webkit-appearance: none`이 필요하고 **그 순간 `accent-color`가 칠하지 않아 확정 K의 채움 규격이 깨진다.** WebKit에는 `::-moz-range-progress`에 해당하는 의사요소가 없어 채움을 값 기반 그라데이션으로 직접 계산해야 한다. 현행이 확정분 넷(채움 색·값 위치·썸 포커스·키보드 단위)을 **이미 모두 지키므로** 치수 둘을 얻고 잃는 것이 더 크다는 판단이다. 되돌리지 않도록 `FleetReduction.css`에 근거를 적었다. `AGENTS §4.3` 「값 정정」이라 버전은 올리지 않는다 (#1052) |
| 2026-09-18 | `#1167` | **`§5` 포커스 링을 `box-shadow` → `outline` + `outline-offset: 2px`로.** ⚠️ **`rlatnals4114`의 2026-09-18 확정 ⓐ이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). ⑴ **20개 선택자에 포커스 링이 아예 없었다.** 토큰 `--focus-ring`이 `box-shadow` 값(`0 0 0 3px …`)인데 11개 규칙이 그것을 `outline:`에 넣고 있었고, `outline` 단축은 `width \| style \| color`만 받으므로 **파싱에 실패해 선언이 통째로 버려진다**(Chromium 실측 `outline: 0px / none`). `§14` 「키보드 포커스 가시화」 위반이며 **화면은 정상으로 보이고 키보드 사용자에게만 드러나는** 유형이다 — `#1052`의 죽은 토큰 참조와 같은 성질 ⑵ 토큰을 `outline` 형태로 바꾸고 이름도 **`--focus-outline`으로** 바꿨다. 잘못 쓰면(=`box-shadow`에 넣으면) 즉시 드러나게 하려는 것이다. 전 저장소 **43곳**(box-shadow 32 · 깨진 outline 11)을 `outline`으로 통일했다 ⑶ **`outline-offset: 2px` 신설** — 이슈가 제기한 건이다. 라이트에서 `--semantic-focus`와 `--semantic-primary`가 둘 다 `#1a365d`라 Primary 채움 버튼에서 링과 면의 경계가 `1.00`이 되어 「버튼이 커졌다」로 읽혔다. **링은 `box-shadow` 바깥에 그려지므로 버튼 면 위에 얹히지 않으며**(이슈 본문의 전제 정정) 카드 대비 `12.14`로 규격 위반은 아니나, `2px` 틈이 링을 뚜렷하게 만든다. 색을 바꾸지 않아 Figma를 거치지 않는다. 되돌림 방지 가드 4건을 `tokens.sync.test.ts`에 넣었다. `AGENTS §4.3` 「항목 추가」라 버전은 올리지 않는다 (#1167) |
| 2026-09-18 | `#1169` | **`§16` 항목 19 신설 — 「중립 계조의 웜/쿨 정합」(디자이너).** 항목 3이 *「중립 문자 계조 재정립(`#68`)은 별건으로 남는다」*며 미뤄 둔 몫이 **갈 곳을 잃고 있었다** — `#68`은 「접근성 테스트(WCAG 2.1 AA)」가 범위이고 `a11y.test.tsx` 12건으로 덮여 **2026-09-11에 닫혔다**. 닫힌 이슈를 가리키는 유예가 **정본 1곳·코드 2곳**(`tokens.css` 항목 1 주석 · 폼 컨트롤 경계 주석)에 남아 있었고, 그 셋을 항목 19로 갈아끼웠다. `TEST_PLAN` 변경 이력의 `#68` 언급은 **그날의 기록이라 고치지 않는다**(`AGENTS §4.3`). 항목 19는 항목 11(다크 모드 중립 팔레트)과 짝이며, `#829` ⑶이 「임시」로 둔 `--color-border-control`이 여기에 걸린다 — 라이트 여유가 `0.08`뿐인 원인은 값이 아니라 **면적·경계 채널에 자기 값이 없는 것**이고, Figma `border/control` 신설로 끊는다(후보 `#6f7c8f` · 네 면 최소 `3.77`). **값의 확정은 `§0.2`대로 Figma 소관이며 이 PR은 값을 바꾸지 않는다.** 버전은 올리지 않는다 — 절 신설이 아니라 `§16` 행 추가와 참조 정정이다(`AGENTS §4.3`). 함께 머리표 최종 수정일을 `2026-09-15` → `2026-09-18`로 실측 갱신했다 (#1169) |
| 2026-09-18 | `#1169` | **`border/control` 확정 반영 — 폼 컨트롤 경계가 자기 값을 갖는다.** ⚠️ **`rlatnals4114`의 2026-09-18 확정 O이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `#829` ⑶이 「임시」로 둔 `--color-border-control`은 `3:1`을 **넘기면서도** 문제였다 — 기존 토큰 중 양 테마·양면을 모두 넘기는 것이 `--text-muted`뿐이라 **문자 계조를 빌려 썼고**, 그 탓에 라이트 여유가 `0.08`뿐(`3.08` 대 `3.00`)이고 웜(`#8b8a83`)이 쿨 표면(`#eef2f6`) 위에 놓였다. Figma가 `border/control`을 신설해 끊었다 — 라이트·다크 **공통** `#6f7c8f`, 네 면 최소 **`3.77`**(라이트 inset `3.77` / card `4.24` · 다크 inset `4.12` / card `3.77`)로 여유가 약 10배가 됐다. `§2.2` 중립 표와 `§15` Light·Dark 예시에 등재하고, `§16` 항목 19의 ⚠️를 ✅로 바꿨다 — **항목 19는 문자 계조의 웜/쿨이 남아 계속 열려 있으나 이 토큰은 더 이상 거기에 걸려 있지 않다**. 가드는 값이 아니라 **출처**를 잠근다: 참조 사슬에 `--text-*`가 끼면 실패한다 — 대비 검사만으로는 임시 별칭을 잡지 못했던 것이 이번 건의 교훈이다. 버전은 올리지 않는다 — 절 신설이 아니라 표 행 추가와 확정값 반영이다(`AGENTS §4.3`) (#1169) |
| 2026-09-18 | `#1170` | **v2.12 — `§14` 「비활성 컨트롤은 「왜」를 함께 낸다」 확정 · `§16` 항목 20 신설 · `§2.4` 등급 없음 배지 테두리 정정.** ⚠️ **`rlatnals4114`의 2026-09-18 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `#829`를 닫으며 판단을 받지 못한 둘을 받았다. ⑴ **「등급 없음」 배지 테두리가 등급 축에 남아 있었다** — `#829` ⑷가 같은 배지의 *문자*를 중립으로 옮기며 **두 줄 아래 `border`는 그대로 두었다**. 그 배지는 등급 문자를 그리지 않으므로(`—` 하나) `§0.2` 제약 2상 등급 채널이 아니고, `--cii-none-border`는 대비도 미달이었다(배지면 위 라이트 `1.51` · 다크 `1.72`). `#1168`이 `.vessel--risk`에서 걷어낸 것과 같은 결함이다. `--color-border-control`로 옮겼다 — **이 배지가 놓인 상황이 폼 컨트롤과 같다**(배지면과 카드 면의 대비 `1.12`라 테두리가 유일한 경계). 실측 라이트 `3.77` / `4.24` · 다크 `4.12` / `3.77`. ⑵ **`aria-disabled` 전환은 하지 않는다.** 이슈는 「버튼 하나」로 적었으나 실제는 **58곳**이고, 성질이 갈린다 — 일시적 진행 중 **33** · 선행 조건 미충족 **24** · 미구현 **1**. 그리고 저장소에 `:disabled` CSS 규칙이 **51개**인데 `[aria-disabled]`는 **0개**라, 속성만 바꾸면 **비활성 표시가 51곳에서 조용히 사라진다**. 대신 사유를 화면과 낭독 양쪽에 두기로 확정하고, 초점 문제는 항목 20으로 남겼다. 버전을 올린다 — 확정 반영과 규정 신설이다(`AGENTS §4.3`) (#1170) |
| 2026-09-18 | `#1174` | **v2.13 — `§12` 아이콘 조항 개정 · `§15` 네이밍에 `--icon-*` 추가.** ⚠️ **`rlatnals4114`의 2026-09-18 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `§12`가 *「`strokeWidth={1.5}`를 지정해 규격을 맞춘다」*고 **문자 그대로 하드코딩을 지시**하고 있었다. 조항의 의도(「호출부마다 적지 않는다 — `Icon.tsx`가 강제한다」)는 옳고 지켜지고 있었으나 **값의 출처**가 `§15`와 어긋났다 — 같은 규격이 생성 토큰(`--icon-stroke` 등 넷)과 코드에 한 벌씩 있어, Figma에서 `icon/stroke`를 `1.6`으로 바꾸면 **토큰을 읽는 커스텀 아이콘만 굵어지고 세트 아이콘은 그대로**였다. 지금 깨지지 않은 이유는 두 값이 **우연히 같았기** 때문이라 오래 남을 자리였다(`#620`의 경로). 의도는 두고 출처만 옮겼다 — `Icon.css`가 네 토큰을 먹이고 `Icon.tsx`는 **이름만** 고른다(`size="inline" \| "default" \| "large"`). CSS 선언이 SVG 표현 속성을 덮는 것은 Chromium으로 실측했다. 함께 `§15` 네이밍 목록에 `--icon-*`이 빠져 있던 것을 넣었다 — **이름이 목록에 없어** 이 위반이 규약 위반으로 읽히지 않고 있었다. 버전을 올린다 — 조항 개정이다(`AGENTS §4.3`) (#1174) [`#1324` — 이 행 안 인라인 코드의 이스케이프 없는 `\|`가 표 열 구분자로 읽혀 표가 깨지고 있어 `\|`로 이스케이프했다. 내용·판단은 바꾸지 않은 표시 형식 정정이라 버전·최종 수정일은 올리지 않는다] |
| 2026-09-18 | `#1202` | **v2.14 — `§14` 「경계와 장식을 가른다」 확정.** ⚠️ **`rlatnals4114`의 2026-09-18 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `1.4.11`의 `3:1`을 **어디에 거는지**가 정본에 없어 같은 자리가 세 번 열렸다 — `#829` ⑶(폼 컨트롤) · `#1170` ⑴(「등급 없음」 배지) · 이번. 갈래를 넷으로 갈랐다: **컨트롤의 유일한 경계**와 **채움 컨트롤의 면**은 필수, **장식·보강**(안내 띠 · 표 머리행 구분선 · 카드 hover)과 **비활성**은 대상 밖. ⑴ 범위를 **역할로** 잡아 아웃라인 컨트롤 **18곳**을 `--color-border-control`로 옮겼다 — 이슈가 `--color-border-strong`(19선언/13파일)으로 범위를 잡았으나 그 방식은 **더 나쁜 `--border-default` 아홉 곳(라이트 최소 `1.15`)을 놓친다**. 표 머리행·비활성 채움·장식 띠는 건드리지 않아 화면은 그대로다. 곁따라 hover 셋이 기준보다 약해지는 것을 다른 아웃라인 버튼과 같은 표현으로 맞췄다. ⑵ **Success 값은 손대지 않는다** — 문제의 띠(`2.89`)는 `.fr__notice`·`.fr__status`의 띠(`1.42`)와 같은 모양의 보조 채널이고, 의미는 문장이 전달한다. 가드는 역할로 잠근다(`cursor: pointer` + 테두리색 ≠ 배경색 → 네 면 `3:1`, 예외 목록 없음). ⚠️ **가드가 새로 드러낸 것** — 다크의 `.assistant__send`(채움 Primary)가 면 대 주변 `2.12`~`2.35`다. 고치려면 `#829` ⑴이 문자 `5.60`을 위해 세운 파생을 되물어야 하므로 **별건으로 남긴다**. 버전을 올린다 — 규정 신설이다(`AGENTS §4.3`) (#1202) |
| 2026-09-18 | `#985` | **`§16` 항목 19·20의 행 순서 정정.** `#1170`이 항목 20을 항목 18 뒤에 끼워 넣으면서 **먼저 있던 항목 19보다 위에** 놓였다 — 표가 `18 · 20 · 19`로 읽혔다. 번호 순으로 되돌린다. 내용은 그대로이고 버전은 올리지 않는다 — 오기 정정이다(`AGENTS §4.3`) (#985) |
| 2026-09-18 | `#985` | **`§14` 갈래 표에 「위치를 읽는 기준틀」 한 행 추가 · 개략도 해안선 대비 정정.** 베이스맵이 없을 때 뜨는 `PositionChart`가 **육지면 대 바다 `1.15` · 윤곽 대 육지면 `1.23`**(라이트)이라 셋이 서로 갈리지 않았다 — 시연 피드백의 「밋밋하다」가 가리키던 자리다. 「배경이라 옅게」의 의도는 맞았으나 **옅은 것과 안 보이는 것을 가르지 못했다**. 채움은 그대로 두고 **윤곽만** `--color-border-control`로 올린다(라이트 바다 `3.77`/육지면 `3.27` · 다크 `4.12`/`3.09`) — 면을 올리면 마커 윤곽이 바다색이라 육지 위의 배가 사라진다. 해도(海圖)가 옅은 면 + 뚜렷한 해안선으로 그리는 방식과 같다. 굵기도 `0.5` → `0.7`로 올렸다(뷰박스 배율 1.62~2.19배라 종전에는 화면에서 `0.81~1.09px`였고 1px 아래에서 흐려져 대비 이득이 되돌아간다). 갈래 표에 행을 더한 것이라 버전은 올리지 않는다(`AGENTS §4.3`) (#985) |
| 2026-09-18 | `#1052` ⓥ | **`§9.5`에 「범례를 두지 않는다」 확정 · 개략도 위험 테두리의 축 정정.** ⚠️ **`rlatnals4114`의 2026-09-18 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 지도에 범례가 **0건**이었다. 다만 마커마다 등급 문자가 붙고 위쪽 「등급 분포」가 색↔문자 대응을 개수와 함께 보여 주므로, 범례는 **같은 대응의 세 번째 사본**이 된다. 스스로 설명되지 않는 표식은 **굵은 테두리 하나**뿐이라 캡션 한 문장이 받는다. 개략도도 같은 문장을 쓴다 — 자산 유무로 두 그림이 갈리는데 읽는 법이 다르면 다른 그림으로 읽힌다. <br><br> ⚠️ **곁따라 드러난 것** — 개략도의 `.position-chart__dot--risk`가 `--cii-e-fill`을 쓰고 있었다. 이 클래스가 붙는 조건은 등급이 아니라 `isAtRisk()`이고 사유가 둘이라(`E_THIS_YEAR` · `D_THREE_YEARS`) **`D`가 찍힌 배에 E의 색**이 붙었다. `#1168`(선박 카드) · `#1170` ⓥ(등급 없음 배지)에 이어 **세 번째**다 — 이름으로 한 곳을 찍는 가드는 그 한 곳만 지킨다는 것이 드러났으므로, 가드를 저장소 전체의 **위험 선택자**(`.vessel--risk` 꼴) 전부로 넓혔다(검출 3건 · 위반 0). ⚠️ 이름을 본문에 **토큰처럼** 적으면 `#1022` 가드가 정의 없는 토큰으로 읽는다 — 선택자는 선택자 꼴로 적는다. 대비는 양쪽 다 3:1을 넘어 **값이 아니라 축**을 고친 것이다. 행 추가라 버전은 올리지 않는다(`AGENTS §4.3`) (#1052) |
| 2026-09-19 | `#1267` | **`§8`에 텍스트 버튼 변형 등재.** 버튼 규격이 Primary·Secondary·Danger 셋뿐인데 **그 밖의 변형을 쓰는 곳이 둘**이었다 — 상단바 계정 트리거는 이전부터, 로그아웃이 `#1266`으로 합류했다. 정본이 침묵하는 자리를 화면이 메운 상태이며 `§16` 항목 8이 「클래스 이름 24종」에서 겪은 것과 같은 계열이다. Primary 채움을 그 자리에 두면 **구현된 9개 화면 전부에서 대비가 가장 높은 요소**가 되어 주 동작과 경쟁하고, Secondary는 `--color-border-strong`이 `1.4.11` 미달이라 경계색 교체가 선행돼야 한다 — 텍스트 버튼은 그 의존이 없다. 높이는 두 구현이 갈려 있어 〔제안〕으로 남긴다. 행 추가라 버전은 올리지 않는다(`AGENTS §4.3`) (#1266) |
| 2026-09-19 | `#1271` | **`§9.5`에 「자산 분기에 참여하는 화면은 대시보드뿐」 확정.** ⚠️ **`rlatnals4114`의 2026-09-19 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `#763` ⓑ가 정한 자산 분기는 **대시보드만** 참여하고 선박 상세는 `basemap`을 보지 않는다 — 자산이 있는 같은 기기에서 한쪽은 타일 지도, 한쪽은 개략도가 된다. 두 화면이 `PositionChart`를 공유한다는 사실은 코드 주석에 있었으나 **분기에서 빠진다는 사실은 어디에도 없었다.** `#1052` ⓥ가 「자산 유무로 갈리는 두 그림」의 읽는 법을 맞췄는데, **화면별로 갈리는 세 번째 축**이 남아 있었다. 개략도 고정을 유지하고 근거를 적는다 — 폭 480에 대상 한 척이라 타일 지도가 주는 맥락이 거의 없다. 되돌리는 것을 막기 위해 **갈리는 축이 자산이 아니라 대상 수**임을 함께 적었다. 행 추가라 버전은 올리지 않는다(`AGENTS §4.3`) (#1264) |
| 2026-09-19 | `#1272` | **`§9.5` 「자산 분기 참여 화면」에 항로 비교 추가 · 폴백이 화면마다 다름을 명시.** ⚠️ **`rlatnals4114`의 2026-09-19 확정이다**(`AGENTS §7.3`). `#1264`가 「대시보드뿐」으로 적은 문장이 `#1265`로 항로 비교가 지도를 갖는 순간 **거짓이 된다** — 같은 PR에서 함께 고친다. 함께 드러난 것은 **폴백이 하나가 아니라는 사실**이다. 대시보드는 자산이 없으면 개략도로 떨어지지만 항로 비교는 아무것도 그리지 않는다 — `PositionChart`가 선박 기반이라 항로 하나를 담지 못하고, 그 화면의 좌표는 선택 입력이라 **지도가 없는 것이 이미 정상 경로**다. 적어 두지 않으면 다음 사람이 「폴백이 빠졌다」로 읽고 빈 지도를 채워 넣는다. 행 추가라 버전은 올리지 않는다(`AGENTS §4.3`) (#1265) |
| 2026-09-19 | `#1284` | **`§14`에 「프로그램적 초점 대상에는 링을 그리지 않는다」 확정.** 화면을 옮길 때마다 **본문 전체가 파란 테두리에 감싸였다.** 셋이 겹친 결과다 — `<main>`이 본문 바로가기 착지점이라 `tabindex="-1"`이고(`AppShell.tsx:501`), `#829` ⑸c가 `2.4.3`을 위해 **화면 전환마다** 그리로 초점을 옮기며(`:153`), 전역 `:focus-visible`에 **제외 규칙이 없다**(`global.css:144`). 앞의 둘은 접근성 요구라 고칠 것이 아니고 `a11yWiring.test.ts:142`가 잠근다 — 빠진 것은 셋째 하나다. `2.4.7`은 **키보드로 도달하는** 요소에 걸리므로 Tab 순서 밖 요소는 대상이 아니며, 본문만 한 링은 진짜 초점 위치를 가린다. **초점 이동은 유지하고 표시만 지운다.** `<main>` 하나를 지목하지 않은 것은 `AssistantOverlay`에 같은 `tabindex="-1"`이 이미 있어서다 — `§16` 항목 17이 겹침 순서에서 적은 「한 자리만 근거를 얻으면 다음에서 다시 열린다」와 같은 구조다. 행 추가라 버전은 올리지 않는다(`AGENTS §4.3`) (#1283) |
| 2026-09-20 | `#1390` | §7.2 사이드바 항목의 **소관 절 인용 정정** — `UIFLOW §2.2` → **`§2.2.1`** (`#1341`). `§2.2`는 화면 ↔ 계층 매핑이고 사이드바 순서 표는 그 안의 `§2.2.1`이라, 위임받은 절로 간 사람이 **찾는 표가 없는 자리**에 도착했다. 기존 참조 가드는 **그 절이 실재하는가**만 보므로 `§2.2`도 있어 통과했다 — `tests/test_doc_cross_refs.py`에 소관 절을 직접 보는 함수를 더했다. ⚠️ **「항목이 7개 이하」(실제 9개)는 고치지 않았다** — 본 문서는 디자인 담당 소관 정본(`AGENTS §3.2.2`)이고, 「7개 이하라서 아이콘이 불필요하다」는 **결론이 9개에서도 유지되는지**가 문구 판단이다. 확인 요청을 `#1341` 코멘트로 올렸고 회신 오면 반영한다. `AGENTS §4.3`상 인용 정정이라 버전은 올리지 않는다 (#1341) |
| 2026-09-20 | `#1480` | **v2.15 — `§14` 「비활성 컨트롤은 「왜」를 함께 낸다」에 예외 「누르면 사유를 내는 버튼」 확정.** ⚠️ **`rlatnals4114`의 2026-09-20 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `#1415`가 선박 상세의 실시간 CII 입구를 「현재 상태」 카드에서 페이지 머리로 올리며 `#588`의 「사유와 함께 비활성」을 그대로 옮겼는데, 머리에서는 셋 다 맞지 않았다 — 사유를 상자 안에 두면 상자가 세 줄로 커져 **링크 상태와 모양이 달라졌고**, 점선 테두리는 페이지 바탕에 묻혔고, 흐린 채움 + 곁의 한 줄은 사유와 버튼이 따로 놀았다(화면 확인 세 차례). 조건 셋(유일한 입구 · 왜와 어떻게를 둘 다 · 열고 닫기와 낭독 배선)을 갖춘 자리에 한해 **누르면 말풍선**으로 사유를 낸다. `#588`의 결함(「있다고 읽고 눌렀는데 아무 일도 없었다」)은 누른 자리에서 답이 나오므로 생기지 않는다. 돌연변이 **7/7 검출**. 버전을 올린다 — 규정 신설이다(`AGENTS §4.3`) (#1415) |
| 2026-09-21 | `#1513` | **v2.16 — `§7.1` 🔒 외곽 여백 24 · 산술 `24 + 240 + 24 + 1608 + 24 = 1920` · `§7.2` 🔒 사이드바 아이콘 허용.** ⚠️ **`rlatnals4114`의 2026-09-21 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 같은 폭이 네 갈래였다 — `§7.1` 산술은 외곽 여백 0을 전제한 `240 + 24 + 1656`, `§6`과 구현은 외곽 여백 24, Figma 변수 `grid/content-width` · `grid/margin`은 `#707` 이전 값(1680 · 32). 두 변수는 코드가 읽지 않아 틀려도 드러나지 않았다. 패널이 떠 있는 셸이라 뷰포트 끝에 닿으면 radius·그림자가 잘리므로 **24로 확정**하고 산술·표·`§15` 예시·Figma 변수를 모두 맞췄다. `§7.2` 「아이콘 없이 텍스트만 — 항목이 7개 이하라」는 `#513`으로 항목이 9개가 되며 근거가 무너졌고 구현은 이미 아이콘을 쓴다 — **아이콘 + 텍스트**로 바꾸고 축소(64)를 위해 항목마다 다른 아이콘을 두게 했다(`#1341`). 셸의 radius·그림자는 바꾸지 않았다. `frontend/src/styles/grid.sync.test.ts`가 Figma 변수의 산술과 `§7.1` 표·산술 문장을 대조한다. ⚠️ **이 행은 `#1513`이 머지된 뒤 `#1451` PR이 싣는다** (#1450) |
| 2026-09-21 | `#1514` | **`§7.1`에 「7:5는 두 단일 때의 비율이다」 한 줄.** ⚠️ **`rlatnals4114`의 2026-09-21 결정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 함대 감축 계획의 가로 스크롤은 7:5 자체가 아니라 **다른 화면에서 온 1단 전환점(1100)** 때문이었다 — 1920에서 7/12 칸은 약 890px로 넉넉하고, 스크롤은 약 1100~1350 구간에서만 났다. 예외(표 전폭 + 요약 띠)는 두지 않고, 주 영역의 내용이 7/12 칸에 들어가지 않는 폭에서는 화면이 스스로 1단으로 접는다고 적었다. 전환점은 그 내용의 최소 폭에서 계산한다 — 감축 계획 1365px(`FleetReduction.css` · `layout.sync.test.ts`). 기존 규칙의 **적용 범위**를 적은 한 줄이라 버전은 올리지 않는다(`AGENTS §4.3`) (#1451) |
| 2026-09-21 | `#1521` | **v2.17 — `§3` 🔒 「영문 약어 병기」의 범위와 예외 확정.** ⚠️ **`rlatnals4114`의 2026-09-21 결정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 한/EN 토글(`#1215`) 뒤에 병기를 유지할지가 열려 있었다. **유지**로 정하고, 종전 한 줄(「한국어 기본 + 영문 약어 병기」)에 ① 약어의 범위(CII · IMO · DWT · GT · HFO — 라벨 안 또는 단위 칩) ② 같은 뜻 영단어를 나란히 적지 않는다(`#1426`이 걷은 47곳 · 폼 라벨 영문 보조는 영어 모드에서만) ③ 예외는 위험도 4단계 하나를 붙였다. `§2.5 (b)`에 예외의 이유(영문 = `risk_level` 값 · 화면·보고서·API 대조의 열쇠 · 언어 모드 무관)를, `§14`에 `§3` 참조를 적었다. 종전 문구는 코드 주석 네 곳이 인용하므로 지우지 않고 그 아래에 덧붙였다. 코드 변경 없음 ⚠️ **이 행은 `#1521`이 머지된 뒤 `#1538` PR이 싣는다** (#1452) |
| 2026-09-21 | `#1552` | **`§8` 텍스트 버튼 「쓰는 곳」에 항차 카드의 「이 항차 취소」 추가** (`#1551`). 카드마다 반복되는 취소를 외곽선으로 두면 카드의 주 버튼(다음 단계 하나)과 같은 무게로 경쟁한다. 취소는 항차 상태 전환이고 데이터를 지우지 않으므로 Danger 자리가 아니다. **hover 면은 `--surface-card`** 로 적었다 — 항차 카드 자체가 `--surface-inset`이라 규격 hover가 보이지 않는다. 쓰는 곳 추가라 `AGENTS §4.3`상 버전은 올리지 않는다 (`rlatnals4114`) (#1551) |
| 2026-09-22 | `#1606` | **§2.3.1 🔒 심각도 표의 「실적 미입력」 → 「실적 확정 전」 · 근거 `§8.4` → `§8.1`·`§8.1.1`** — 🔒 절 개정. 디자인 담당(`rlatnals4114`)이 `#1532`로 제기하고 2026-09-21 사용자가 결정(결정요청 v6 `D-29`)했다. 화면 표시 문구는 `#1604` |
| 2026-09-22 | `#1612` | **`§8` 텍스트 버튼 「쓰는 곳」에 항차 카드의 「확정 되돌리기」 추가** (`#1598`) — 뒤로 가는 전환이라 주 버튼 틀에 두지 않고, 취소와 같이 카드 안 확인 줄을 거친다. 모달이 아닌 이유는 `§16` 항목 17(겹침 순서) 미결. `§4.3`상 소규모 행 추가라 버전은 올리지 않는다 (#1598) |
| 2026-09-22 | `#1676` | **v2.18 — `§2.5 (b)` 🔒 위험도 단계별 색 · 아이콘 색 확정.** ⚠️ **`rlatnals4114`의 2026-09-22 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). 절은 「색은 §2.3 시맨틱(Warning · Danger)만 차용한다」까지만 적고 **단계와 색의 대응을 정하지 않아** 화면이 갈려 있었다 — 기능①(`VoyageCiiResult`)·시나리오 비교는 HIGH `--color-warning-text` / CRITICAL `--color-danger-text` / 아이콘 Warning, 기능②(`RealtimeCiiView`)는 셋 다 Danger. 기능②가 갈라 둔 사유(*「Warning 문자 토큰이 다크에서 `--cii-c-fill`로 풀린다」*)는 `#1022`(PR `#1195`)로 별칭이 사라져 더 이상 성립하지 않았다. **HIGH = Warning · CRITICAL = Danger(문자는 `-text`) · 아이콘은 단계와 무관하게 Warning 하나**로 확정 — (a)의 3단 구조와 같다. 기능②를 맞췄고 `riskColor.test.ts`가 세 화면을 함께 잡는다. 등급 C 색과 다크 Warning의 구분 실측은 `#1022` 「제약 2」 작업 소관이다. 확정 반영이라 버전을 올린다(`AGENTS §4.3`) (#1674) |
| 2026-09-22 | `#1677` | **`§9.5`에 대시보드 지도 카드의 자리·치수·문구 확정 각주** (`#1052` ⑴) — `rlatnals4114`의 2026-09-22 확정. 왼쪽 열 첫 카드 · 8 : 5 · 읽는 법 한 문장 · 접힌 출처 표기 · 자산 없을 때 안내는 작은 보조 문단(경고가 아니다). 현행을 확정한 것이라 화면은 바뀌지 않는다. `§4.3`상 각주 추가라 버전은 올리지 않는다 (#1052) |
| 2026-09-22 | `#1679` | **`§16` 항목 15 ⓐ에 해소 기록** — 「범위 밖이라 삭제」로 남아 있었으나 `2-10`은 2026-09-13 범위 안으로 옮겨졌고 문구는 `PRD §6.3`에 있다. 삭제 기록은 두고 해소를 덧붙였다. `§4.3`상 정정이라 버전은 올리지 않는다 (#1678) |
| 2026-09-22 | `#1680` | **v2.19 — `§14` 「채움 컨트롤의 면」에 「면이 못 넘으면 테두리가 맡는다」 확정 · `§8` 버튼 규격에 테두리 토큰.** ⚠️ **`rlatnals4114`의 2026-09-22 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `#1202`가 드러낸 다크 `.assistant__send` 면 대 주변 `2.12`~`2.35`의 해소다. 실측: 다크 채움면 `#385c8d`는 카드 `2.35` · 인셋 `2.56` · 페이지 `2.78`이고, 파생을 85% · 90%로 띄우면 카드 `2.99` · `3.22`가 되는 대신 글자가 `4.40` · `4.08`로 떨어진다 — `#829` ⑴이 세운 파생은 그대로 둔다. 새 토큰 `--color-primary-solid-border`(= `--semantic-primary`: 다크 `#4a7cc0` 네 면 `3.39`~`4.44` · 라이트는 채움면과 같다)로 **채움 Primary 컨트롤 5자리**(어시스턴트 보내기 · 함대 감축 계획 · 실시간 CII 실적 입력 · 선박 상세 입구 · 항차 카드 주 버튼)의 테두리를 그린다. 새 색이 아니라 Figma 작업이 없다. 가드는 역할로 잠근다(채움면 + 테두리 선언 또는 `cursor: pointer` → 이 토큰 · 예외 목록 없음). 버전을 올린다 — 확정 규정 개정이다(`AGENTS §4.3`) (#1296) |
| 2026-09-22 | `#___` | **v2.20 — `§8` 텍스트 버튼 높이 〔제안〕 → 〔확정〕.** ⚠️ **`rlatnals4114`의 2026-09-22 확정이며 개발이 정한 것이 아니다**(`AGENTS §7.3`). `#1267`이 「구현이 둘뿐이라 굳히지 않는다」로 남긴 칸인데, 그 뒤 항차 카드의 취소(`#1551`)·확정 되돌리기(`#1598`)가 들어와 쓰는 자리가 넷이 됐고 **이웃이 제각각**이었다. 폰트를 적재해 렌더링한 실측 — 상단바 계정 트리거 **40** · 로그아웃 **32**, 항차 카드 텍스트 버튼 **24** · 같은 줄 카드 버튼 **26**. 값 하나로는 두 자리를 함께 맞출 수 없어 **규칙으로** 정했다: 같은 줄 컨트롤과 같다 · 혼자면 `--target-icon-button` · 24px 밑 금지. 계정 트리거는 위아래 여백을 걷고 `--target-icon-button`을, 카드 텍스트 버튼은 투명 테두리 1px을 준다. 색·hover·포커스 링은 손대지 않았다(`#1266` 확정). 버전을 올린다 — 확정 반영이다(`AGENTS §4.3`) (#1297) |
